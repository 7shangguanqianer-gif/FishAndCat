# N3-P4：改动面基线、用户导览、报告草稿、canonical H 锁与 Claude 四组清单复核

## 0. 总裁决

1. 指定 13 类正则已全仓登记：48 个文件、326 个去重 `file:line` 位置；完整三分类见 `N3_P4_指定grep分类登记_0712.md`。
2. canonical 的新 H 单 seed 回归锁落点已精确确认：`docs/canonical_assumptions.md:41` 应从 `7.6202` 改为 **`8.6232`**；测试真相是 `sim/tests/test_sim.py:174-192` 的 `m["exp_t"]`，历史 and 锁 `:194-205` 的 `7.6202` 必须保留。
3. 三份用户导览均有残留。最严重的不是旧数字本身，而是“PLC 运行时查表”“报告正文/封面继续用旧 H”“3 seeds”“23/24 项”和“同一算法数字孪生”这些现在时指令。
4. 五份 `报告草稿_*.md` 已全数复核：B1/B2/B10 主稿有报告级错配；B6/B7/B11 的吞吐边界多数准确，但把 8.76 叫 AC500 真跑；B3/B4/B8、B5/B9/B14、C3/C5/B14 基本可保留，但不得从测试表外推“存量冻结”或 PLC H 闭环。
5. Claude 的“四组闭环”框架正确，但项目清单仍漏 QA 卡、A5、旧 HTML、生成器内其他绑定串、实际 DOCX 的 45 项旧数、桌面面板的 D2 代理误称，以及三页 HMI 尚未在线长跑的状态。
6. Claude 的“无需改”三项只能接受两项：`plc/04_FB_Warehouse.st:910-915` 与 `sim/lifecycle_sim.py:3` 可保留；**`plc/README_PLC.md:40-44` 不能整体列为无需改**，`:43-44` 的“存量冻结/画面元素锁定”均与当前实现不符。

## 1. canonical H 锁：精确落点与四口径隔离

### 1.1 当前错误落点

`docs/canonical_assumptions.md:38-41` 同时有两类旧值：

- `:40` 仍写 and 历史头条 `7.42±0.42/66.2%/74.3%` 与对照 `5.33/73.5%`；
- `:41` 把 `test_h_caliber_headline_lock` 错写成 `7.6202`。

### 1.2 测试真相

- 新 sum H 锁方法：`sim/tests/test_sim.py:174-192`；
- 权重：batch `lookup_weights(120,"skew","batch")=(0.05,0.6)`：`:177-186`；
- 运行：`ACCEL=True`、`rule="sum"`、AWRA-LS：`:181-188`；
- **锁变量/落点**：`m["exp_t"]` 在 `:189` 断言 **8.6232**；
- 历史 and 锁：`test_legacy_and_h_lock` 在 `:194-205`，`m["exp_t"]==7.6202` 位于 `:204`，保留不改。

当前 `sim/out/headline_numbers.csv` 的 AWRA 行又独立确认：`H_exp_mean=8.402`、`H_exp_std=0.477`、`H_energy_mean=14344.6`、`H_heavy_tier=0.567`、`legacy_exp_mean=6.443`、`viol_sum=0`。因此 canonical 建议改成：

> sim H 设计口径（梯形加减速+场景自适应，5 seeds）头条 8.40±0.48s/62.0%/67.3%/0.57；单 seed 回归锁 `test_h_caliber_headline_lock=8.6232`。匀速+固定对照均值 6.443。历史 and 单 seed 锁 `test_legacy_and_h_lock=7.6202`。

不要把四个值混成一个：8.40（5-seed 均值）、8.6232（sum 单 seed 锁）、8.757（`ACCEL=True+static` sim 代理）、6.443（匀速+static 5-seed PLC 默认开关代理），全部是 sim 数值。

## 2. 三份用户导览残留

### 2.1 `项目通俗导览.md`【需系统改，不是只换数字】

阻塞项：

- `:59` 把 δ=0 说成当前严格等同 near；当前 sum sensitivity 仍差 0.4128s，只能说历史 F2 场景重合/当前显著趋近。
- `:62-64` 说“运行时查表切换”，PLC 实际没有消费者。
- `:74-75` 把 AWRA-LS 叫“上界”；它是启发式批量参考，不是数学上界。
- `:79` 写 3 个随机批次，真实 headline 是 5 seeds：`sim/headline.py:24,53-60`。
- `:83-90` 的 8.40/62.0/67.3/0.57 数字本身准确，但必须标“sim H 设计口径”。
- `:104` 的 9.9% 是距已找到可达点，不是理论下界；`:386` 仍问“离理论天花板不到9%”，数值/对象双错。
- `:105` RL 仍写旧 8.2%，当前 F18 是 6.3%。
- `:150-151,373` 把 Python/ST 说成镜像互证，超过静态匀速黄金向量覆盖。
- `:162` 年化数必须标 sim 情景换算；`:163,171` 的 23/24 项均改为 59，并注明 AB 仿真。
- `:167-168` “不再需要写新代码”已被后续 A2-A4/C1-C6 推翻，属于过期状态判断。
- `:234` “当时7.42/5.33，官方8.40/6.44”是准确历史叙事，可保留。
- `:283` 仍用 8.8%；当前 sum 是 9.9%。
- `:293-296,384,419` 仍把 7.42/5.33 当现在 H/对照/头条，须改 8.40/6.44 并标 sim。
- `:298-303` 宣称 9 维都有切换开关且所有组合方向一致，验证面不足；改成“已测试口径/已报告组合”。
- `:409-417` 把 H 当工程运行档且称三档同一负载；`realism.py` 各策略 arrivals 实际不同，排队指标不能这样说。

### 2.2 `差异化战略_如何超过别人.md`【状态页严重过期】

- `:48-50` 计划输出微秒耗时/CPU 占用，三时钟已证不可测；当前只能用任务完成周期。
- `:99,126` 仍是 and 旧 8.8%，当前 9.9%。
- `:104-117` 把周期重排和自适应权重都列为“已完成”，并称 PLC 在线查表；PLC 没有夜排调度状态机，也没权重消费者。
- `:145` 称六杠杆/A1-A3 全部落地，和当前 D1/D2、HMI 长跑待验冲突。
- `:161` 的数字孪生包装需收窄到“核心 ST/静态匀速向量”。
- `:165` RL 仍是旧 `7.82 vs 7.23/8.2%`，当前 sum 对照为 6.3% gap。

### 2.3 `实验发现.md`【历史保留可行，但局部标签与错误实现句必须修】

- 文件顶部 `:4-6` 已明确 F1-F17 是 and 历史、F18 是 sum 当前，因此旧 F8/F10/F12/F13/F14/F17 的数表可归历史证据，不宜删除。
- 但深链阅读仍会误导：`:120` 说旧 268 是“报告正文用这组”；`:154` 说旧年化是“封面数字”；`:209-218` 标题仍是 3.6/8.5、密度120/240/330；`:226-242` 又给旧 7.42 报告句；`:275-280` 给旧三档和旧 P50/P95。这些段首要加醒目的“历史 and 快照，现行见 F18”。
- `:221` 不是历史数字问题，而是事实错误：PLC 不会“场景在线判档即查表切换”，必须改。
- 当前 F18 `:22` 的 3.51%/7.27% 是当前 sim 输出事实，但 P2 已发现在线高密度选优忽略 failed；须加“当前表的在线高密度可行性待修，batch 120 头条不受影响”。

三份 MD 改完后必须通过 `tools/make_user_docs.py` 再生同名 DOCX；现有 DOCX 均为 7/10 旧快照。

## 3. 五份报告草稿全复核

### 3.1 `报告草稿_B1B2B10_0711pm.md`【最高优先】

- `:34-45,95-106` 把 H/吞吐/年化列成“本项目头条”但不标 sim。
- `:140-149` 把 59/0 扩大成“仿真结论已在目标控制器闭环验证”；D4 致命错配。
- `:146` 三档“完整闭环/同一管线可比”还受 realism arrivals 问题限制。
- `:163-164` 运维功能 59/0 可保留，但不能由此推出画面锁或存量冻结。
- `:259-266` 把 sim lex 98.5%与 PLC 硬实时工程最优形态绑定；A5 没测出 7 策略并行必超时。
- `:274-277,343` 的 sim CI 限定准确，可保留。
- `:349-350` 明令与错误 v4-1 同步，是必须删除的制度化错配。

### 3.2 `报告草稿_B3B4B8_0712.md`【基本通过，有一个禁止外推点】

- T56/T46/T48/T47/T54/T55 的测试表 `:68-72` 与 ST 一致，当前 N_CASES=59 的说明 `:82-84` 准确。
- 但这些测试只证明检修位禁新入、目标位禁交换、权限回滚；没有测试“局部搜索不把存量从检修源位搬走”。不得从 TC-01~03 外推“存量冻结”。

### 3.3 `报告草稿_B5B9B14_0711pm.md`【通过】

- `:9-10,309` 明确排除 D1/D2，不把引用材料当实现证据；指定 grep 命中均可保留。

### 3.4 `报告草稿_B6B7B11_0712.md`【吞吐段好，D2 代理称谓错】

- `:45-56,66,71` 对 230 的定义/非 AC500/非档3边界写得准确，可直接作为最终报告模板。
- `:58,127` 却把 8.76/60.4/67.2 叫“AC500 静态权重真跑/实现档”；按 N2 已采纳裁决，改为“`xUseAccel=TRUE+static` 的 sim PLC-可实现代理”，且说明 PLC 默认 `xUseAccel=FALSE`。
- `:95-124` Factory I/O 属计划/成熟度展望，未完成前不得写成现有 L2 闭环。

### 3.5 `报告草稿_C3C5B14_0712.md`【通过】

- `:13,21` 明确 kWh/heavy_tier 是 sim 内部代理，且不引用 98.5；边界正确。
- `:33` 明确 δ 自适应待拍板并只讲固定0.15，也正确。

## 4. 生成器和二进制：除已知行外的新绑定

### 4.1 `sim/build_report.py`

除已知摘要 `:386-394` 与自适应段 `:479-485` 外，新增需改：

- `:453-456`：把 δ=0 严格写成 near 占位集合完全重合；当前 sum sensitivity 不支持严格恒等。
- `:489-495`：双命令 29.1% 用“实测”但实际是 sim 百周期统计。
- `:499-515`：H 表未标 sim，且“仅绝对数平移”过强。
- `:523-525`：把距理论下界和距可达点的含义混近，需避免“理论天花板”话术。
- `:580-609`：档2称工程主口径，排队 P50/P95 又按不同 arrivals 横比；“全部拟真档与口径组合一致”无全组合证据。
- `:625-645`：AB 仿真任务周期可保留，但“仿真与真机同值”应降为逻辑周期预期；真机仍待测。
- `:647-653`：Python/ST 同一算法、加减速/双命令双端对齐、报告数字互背书、任一口径变化红灯均超过覆盖。
- `:674-677`：自适应 PLC 同源落地和周期重排闭环是两处未实现能力。
- `:687-688`：称在线评分微秒级，AB 三时钟实际测不出微秒。
- `:694-698`：结论把 sim KPI、AC500 落地、全口径不变性和每个数字一键复现重新焊死。
- `:711-725`：附录称全部组合已验证，并把自适应权重和真机列成主口径；需改成“sim 设计/待验证”。

### 4.2 `sim/build_ppt.py`

P3 已逐页列出；生成器关键落点为：

- PLC 自适应表：`:94-99,184-188`；
- δ=0 严格退化：`:101-104,180`；
- H 3 seeds：`:115-120`；
- 9.9% 却写不到9%/理论天花板：`:122-125`；
- 全口径只平移：`:129-135`；
- 24/23 项双重旧数：`:155-160,200-203`；
- “同一算法两实现/现场互证”：`:163-168`；
- sim 年化/吞吐未标边界：`:108-113,170-173,200-203`。

### 4.3 其他生成/算法元数据

- `sim/headline.py:6,67` 仍写 `and`，执行代码已是 `sum`：`:43,45`。
- `sim/analytic_bounds.py:147` 图标题仍写 `and`，其运行规则是 sum（P2 已确认）。
- `sim/adaptive_weights.py:6` docstring 虚写 PLC 在线查表；`:9` 的选优目标也漏 failed，不能只改文案。
- `sim/build_report.py:234-250` 重算增益同样只取 `exp_mean`，会继承 failed 选优问题。

### 4.4 当前二进制并非“源改即自动好”

- 实际 `验证报告_draft.docx`（7/10）摘要仍把 H/230/AC500 全链绑定，并写 **45 项**；见 `N3_P4_DOCX文本抽取_0712.md:8-15`。当前生成器若重跑会动态读 59，但旧二进制不会自行变化。
- 实际 `答辩PPT_draft.pptx` 同时写 24 和 23；见 `N3_P3_PPTX文本抽取_0712.md:18,24`。
- 因此验收必须包含：修源→重生 DOCX/PPTX→全文抽取 grep→逐页渲染 QA，而非只看生成器 diff。

## 5. Claude 四组闭环清单复核

| 组 | 裁决 | N3 新增/修正 |
|---|---|---|
| 1 真相源 | 框架正确，仍漏项 | canonical C/E、现状、技术总库之外，还应纳入根 README、`README_PLC` 能力边界、`headline.py/adaptive_weights.py` 模块元数据；canonical 还要写 PLC 默认 `xUseAccel=FALSE`。 |
| 2 报告与演示文本 | 原清单明显不足 | 不只是 v4-1：步骤3/6/10/11/12、v4-3/v4-4 都需改；另加 A5、QA 卡、五份报告草稿、旧可视化 HTML、三份导览。 |
| 3 生成器与二进制 | 正确但需扩面 | 加 actual DOCX 的45旧数、actual PPT 的23/24冲突、用户层3份 DOCX；P2 的 failed/arrivals 方法问题若不修，重生仍会输出方法上过强的表述。 |
| 4 渲染与审计 | 正确但验收条件不足 | 桌面面板还把8.76称AC500真跑、三页施工称已完成；旧审计需 superseded；最终 grep要同时核文本源、DOCX抽取、PPTX抽取，并以三页在线长跑结果决定演示是否可标“已验”。 |

### 桌面面板新增实况

`C:/Users/86177/Desktop/智储优控_作战面板.html` 已加入 D1/D2/D4 风险框，但仍有二次错配：

- `:78-83,133`：81.6称“真实 attain”、8.76/60.4称 AC500 真跑/实现值；
- `:83`：仍称方案甲只改8处纯文本、1.5-2h，和已采纳半天~1天/四组闭环冲突；
- `:101,107,127,173`：98.5 仍作为“检测器选择成绩”强展示，即使加警告也易误读；
- `:121`：称 Python 算法逐行翻译且结果逐位一致，覆盖过强；
- `:127,177`：20.6s/10ms 写得像物理 AC500；应标 AB 仿真实测+真机标称预估；
- `:163`：称三页施工完成；当前在线长跑仍待验证：`docs/画面三页制施工蓝图_0711.md:142-147`。

## 6. “无需改”清单独立裁决

### 6.1 `plc/04_FB_Warehouse.st:910-915`——接受，无需改

注释准确区分：sim `select_strategy(holistic)` 被 ST 化；lex/speed 需要 PLC 缺失的 TOB/CB；98.5 是 sim lex。可保留。

### 6.2 `sim/lifecycle_sim.py:3`——接受，是 false-positive

该行明确标题为“全生命周期仿真”的 δ 闭环验证，后文 `:5-7` 也限定为仿真实验，没有声称 PLC 闭环。保留。模型方法限制（失败丢弃/高压 CRN 分叉/尾部未结算）应在报告结论处限定，但不需要删“闭环验证”这个 sim 模块标题。

### 6.3 `plc/README_PLC.md:40-44`——拒绝整体列为无需改

- `:40-42` 的检测器边界与人工覆盖说明基本准确；
- `:42-43` 称检修语义“禁新入库、存量冻结可查询”，但局部搜索重定位 `plc/04_FB_Warehouse.st:285-327` 可把存量从检修源位搬走；
- `:43-44` 称权限是“画面元素锁定+ST回滚双闸”，但当前 P3 生成器只创建直接绑定输入和提示文字，没有 `Deactivated` 权限表达式：`tools/ab_scripting/build_three_pages_0713.py:431-432,451-462`。

因此应拆分：保留 `:40-42`，修改 `:43-44` 为“检修位禁新入；存量查询可用，但局部搜索源位冻结待补”“当前为 ST 回滚守卫；HMI 输入锁未绑定”。同文件 `:36-37` 的“报告数字与 PLC 演示互背书”也应同步收窄。

## 7. 最终验收基线（供 Claude 直接执行）

1. 四口径不混：8.40 / 8.6232 / 8.757 / 6.443 各带定义；7.6202 只作 legacy and 锁。
2. 任何 62.0/67.3/0.57/230/年化/F17 均标 sim 具体模块；不得称 PLC 实测。
3. 任何 98.5 均标 sim lex；不称理论上限，不和 `AWRA_HOLISTIC` 演示输出绑定。
4. PLC 能力只宣称核心 ST 状态机、公式与 59/0 静态匀速黄金向量；AB 仿真实测和物理 AC500 真机分开。
5. 权重表只说“常量已生成，消费者未接”；管理员手改另一组静态值也不叫自适应。
6. 演示按 P3 新顺序重写；三页在线长跑完成前不得写“已验收”。
7. 修 MD/Python 后重生 3 份用户 DOCX、验证报告 DOCX、答辩 PPTX；桌面面板同步。
8. 最终对源码文本+DOCX抽取+PPTX抽取重跑指定 grep；仅允许清楚限定的 sim 事实、历史证据和否定边界。
