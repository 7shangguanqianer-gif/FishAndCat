# 给 Claude Code 的同步提示词：S3 三轮收敛与五方案

请进入 `F:\abb_wh_work`，先只读恢复上下文，不要立即改代码。

## 必读顺序

1. `research/0716/S3重构决策记录_0716.md`
2. `research/0716/S3重构12问一页规格_0716.md`
3. `research/0716/S3五套差异化设计方案_0716.md`
4. `_通信/codex_out/0716_S3重构第3轮与12问收敛回执.md`
5. `research/0716/run_manifest.json`
6. `.claude/handoffs/2026-07-16-014130-s3-task17-and-next-design-research.md` 的最新 addendum

## 当前状态

- 用户已完成三轮 12 问；现在只允许“五方案选型”，尚未授权实现。
- `l4_showcase/src/样张_S3定稿.html` 和候选 HTML 均未因本阶段修改。
- 推荐但未替用户拍板：A“货物决策链”为主壳，吸收 B 的容量书签、D 的 linked 2D/3D；C 作为独立算法对比模式；E 作为以后可选答辩模式。
- 54 格 Factory I/O 仍是独立证据页任务，不得嵌入 S3 或与 20×20 KPI 合并。

## 必须继承的新事实

1. 20×20 官方主线是 133 个预占格 + 267 个算法可管理格。正确双计数是：管理货物 `0/267→267/267`；总占用 `133/400→400/400`。
2. `400/400` 只能称静态容量终点。当前 mixed cycle 先入库后出库，满库后无空位承接下一件；不得说“满库仍可连续一进一出”。
3. 当前 S3 仍是 120 件库存 + 100 paired IN/OUT、单 seed 代表回放，不是 267 件或满库证据。
4. 0→满必须新建 `fill-only` runner 与独立 `s3-fill-comparison-v1` schema；禁止裁剪/拼接旧 trace。
5. 267 是槽位上限，不是任意货流或策略已验证能力。SCORE/AUTO 的 267 密度 online 权重入口存在 fail-closed 风险；必须先冻结 profile/seed/货序/权重/归一化并证明 267/267、0 fail、0 viol。
6. AWRA-LS 使用离线批量 Rank→Assign→局部搜索信息集，只能列为离线批量参考；在线公平主对比必须同信息集、同外生流。
7. 对比默认按相同 `event_id/inbound_gid` 设置 barrier；每个 lane 同时有自己的 2D 与 3D，保留各自 SIM 时钟，失败后禁止自动换算法/货物/顺序。
8. 当前 S3 没有路径 m、kg·m 能耗或吞吐字段。`travel_dual_s` 是时间，三维轨迹不是路径 KPI；字段未导出前 UI 不得显示。
9. 失败域现有吞吐可能因固定计划操作数虚高；存在 fail/viol 或完成数不齐时必须 N/A，或基于真实完成操作数重新定义。
10. `lap_optimal.py` 对 `n>m` 未检查完整分配；修复前不得用作超容量 oracle。
11. “完美摆放比例”无定义。只用入库成功率、违规数、受限 VBS attainment 或同实例真实 LAP gap。
12. Python/ST accel=true 评分归一化尚未证明同源；S3 只能标 sim，不得称控制器同算法验证。
13. Q12 新手教学暂定 60 秒、点击“下一步”，但核心页面完成后必须再正式问用户；当前不开发。

## 你现在可以做的事

- 对上述四份新文档做只读复核，以 `接受 / 部分接受 / 拒绝` 给出裁决；任何异议必须带 `file:line` 和可执行替代。
- 检查五方案是否真的结构不同、是否有口径遗漏；不要生成 HTML 样张。
- 将你自己的复核写入新回执，不覆盖 Codex 文件；如无用户进一步授权，到此停止。

## 你现在不能做的事

- 不改正式/候选 S3 HTML。
- 不改 sim、PLC、canonical、报告/PPT。
- 不启动 AB、Factory I/O，不跑 `tools/ab_scripting`。
- 不做 Git 写。
- 不把 54 格、20×20、S1、S3 或 PLC/FIO 证据混为一体。

## 用户以后选定方案后的正确起点

先修数据与验证门（LAP 完整分配、fill-only schema、267 可行性证书、候选审计、共享输入 hash、旧回归隔离），再做低保真信息架构和截图验收；不能反过来先画完整前端后补证据。
