# 0715 S3 A+C 事件流首版回执

## 状态

- 已按用户拍板采用 A+C：单一档3事件流 + 算法证据幕 + 连续多货物3D作业。
- 开发文件：`l4_showcase/src/样张_S3_三方向候选.html`；尚未覆盖 `样张_S3定稿.html`。
- 原候选已备份：`l4_showcase/src/样张_S3_三方向候选.html.bak_before_AC_0715`。

## 数据链

- 新增 `l4_showcase/tools/generate_s3_tier3_trace.py`。
- 复用 `warehouse_sim.py / mixed_ops.py / realism.py` 当前实现，生成100个混合流双命令周期。
- 事件束：`l4_showcase/out/s3_tier3_trace.{json,js}`。
- 同一 gid 贯穿 arrival → queue → dequeue → inbound cargo → store slot；同一 outbound gid 贯穿 inventory → retrieve → carrier → I/O。
- 当前结果与 `realism.run_tier3('awra')` 对齐：P50=71.0s、P95=667.0s、故障1、失败0、违规0、最大队列7。
- 演示使用真实连续窗口 C08–C15，包含 C11 的真实设备故障；完整 trace 仍保留100周期。窗口由12周期收敛为8周期，避免机械步骤在有限时长内过快切换。

## 画面链

- 新增 `l4_showcase/src/s3_ac_runtime.js`：事件总线同时驱动3D、实体队列、20×20图、速度图、队列图、任务字幕、候选Top3和KPI。
- 第一幕：AWRA-LS 被收敛为“货物排序→约束选位→局部搜索→结果复查”四幕静态证据，每幕明确说明目的，并注明“算法计算，不是机械动作”。
- 第二幕：在线 score；显示400→预占→占用→可行的筛选漏斗、Top3候选、明确“已选”位、选中原因和效率/稳定/能耗/预留四项分解。
- 双命令轨迹三段：载货入库 / 空载联程 / 载货回口。
- 七个机械步骤均新增“STEP n/7 + 本步作用”，故障维修明确为第4步联程中的中断与断点续行；相位条强化当前步骤边框。
- 单摄像头按完整货架、I/O、队列和设备统一取景；无额外视角。按用户反馈将取景距离由1.20倍增至1.34倍，并向右构图。解析投影验收（aspect=1.15、跟随偏移全范围）：X NDC [-0.647,0.805]、Y [-0.812,0.715]，未裁切且右侧留有动作空间。
- 货物采用 ABB 工程语义重/中/轻 `#17365D/#4A6FA5/#9DBBDD`，另加深色/绿色轮廓保证浅色货物与背景区分。

## 验证

执行：

```text
python l4_showcase/tools/generate_s3_tier3_trace.py
python l4_showcase/tools/test_s3_tier3_trace.py
node --check l4_showcase/src/s3_ac_runtime.js
```

结果：3/3 tests PASS；100 jobs；所有周期库存120；队首 gid=当周期 inbound gid；出库 gid 与其货位一致；Top1候选=真实 store slot；阶段时间连续；trace指标与 realism 引擎一致。

第二轮静态验收：`node --check` PASS；HTML新增钩子完整且无重复 id；演示循环约97.28s（算法18s + 8周期机械作业78.58s + 重置0.7s）；相机在常见宽高比1.15/1.22/1.35及完整跟随偏移范围内均未裁切。

## 过程纠错

- 初版故障插入把故障前运动段排到了维修后；已修为运动前段 → 600s维修 → 运动后段，并新增连续性断言。
- `inventory_before` 初版在 `pos_of.pop()` 后遗漏待出库货物；已把 rack 中尚未取出的 outbound gid 补回快照，并逐周期断言。
- 固定目标底板和固定货架空洞已移除，目标位改为随事件动态移动。

## 边界

- 本事件束严格跟随当前 `realism.py` 的 G1=15s 常数装卸，不在本批擅自改写为 canonical U2 三档装卸；U2统一仍是后续独立口径任务。
- 自动浏览器无法重新载入本地 `file://` 页面，因此本批完成源代码、事件、身份、时序、HTML依赖和相机数学验收；最终视觉截图待用户刷新当前页后继续纠偏。
