# 块2 独立验证任务书(0707;交给独立 claude,复制下方提示词即可)

> 用途:把下面「提示词」整段复制给另一个 Claude(本地开在 F:\abb_wh_work),
> 让它独立、批判性地复核块2 成果——不盲信本项目已有的自我评价。

---

## 提示词(复制这一段)

你是一个独立的代码审查员。请全面、批判性地验证 `F:\abb_wh_work` 项目「块2 PLC 工程质量」的成果。**不要盲信项目里已有的结论和自我评价——你的任务是独立核实,找出遗漏、错误、假绿、隐患。发现问题比确认无误更有价值。**

### 项目背景
- 2026 ABB杯「智储优控」赛道一:20列×20层=400库位立体仓储优化。
- 算法 AWRA-LS(Rank 排序→Assign 逐件评分→Local Search 局部搜索),两套实现:Python 仿真 `sim/` + ABB AC500 PLC 结构化文本 ST `plc/`。
- 块2 声称:算法移植进 PLC、写了《ST编写标准》、边界用例扩到 41、参数溯源;并用 ScriptEngine 管线 `tools/ab_scripting/` 实测 AB 里 `iPassed=41/iFailed=0`、编译 0 错。
- 真相源:`docs/canonical_assumptions.md`(建模口径)、`docs/技术月总纲_0706.md`(进度)。

### 逐项独立验证(别信结论,自己核实)

**1. sim↔ST 一致性是否真成立**
读 `docs/T2.1_一致性审计_gap表_0707.md`(声称两套 AWRA-LS「数值等价」)。跑 `python sim/consistency_probe.py`,核对它报告的 G1/G2 触发数与「聚合零差异」是否属实。读 `plc/06_PRG_Test.st` 的 T25 端到端护栏:`aAwraExpect` 是 sim 导出的终局布局,T25 逐位比对——这个比对逻辑对吗?`aGoods` 索引==Id 的假设成立吗?**批判点:「G2 触发但是等价解、聚合零差异」这个论证站得住吗?会不会掩盖真实分歧?**

**2. 边界用例 T26-T41 有没有假绿**
读 `plc/06_PRG_Test.st` 的 T26-T41。**手算验证**几个:T30(承重边界 y=10 限重 80,断言 79.9 可行/80.1 不可行)、T35(优先级单调)、T41(FB_GoodsInput 三分支)——期望值算对了吗?**批判点:T33 被改过(除零保护后 score 可负,断言从 ≥0 改为验有限值)——这修正对吗?其他用例有没有类似的「假设错误」?有没有断言恒真的假测试?边界值留 0.1 间隙避单精度——够稳吗?**

**3. FB_GoodsInput 抽取是否行为等价**
`git log --oneline` 找到 FB_GoodsInput 抽取的提交,`git show` 看 PRG_Main 抽取前后的 CmdAddGood 分支 diff。读 `plc/04_FB_Warehouse.st` 的 FB_GoodsInput。**批判点:抽取后行为真等价吗?边沿检测(xAdd 上升沿)移进 FB 内部,PRG_Main 每周期调用它——非上升沿时输出是否安全(不误更新可视化/不重复入表)?超重货「入表但 iErrId=3」的行为对吗?**

**4. ScriptEngine 管线 iPassed=41/0 是否可复现**
前提:AB 别开着(独占工程)。跑 `powershell -File tools\ab_scripting\ab_sync.ps1`(约 5 分钟,无头启动 AB→同步 36 对象→编译→在线跑 PRG_Test)。看是否真 `iPassed=41/iFailed=0`、编译 0 错。**批判点:那个「1 warning」是什么?在 AB GUI 打开工程看消息窗口的具体 warning 内容(ScriptEngine API 查不到,只能 GUI 看)——无害还是隐藏问题?**

**5. 追溯矩阵是否名副其实**
读 `docs/ST编写标准_0707.md` 的「评分细则×POU×测试追溯矩阵」(9 条)。**逐条核实**:每条评分细则声称的对应 POU 真实现了那个功能吗,还是只是名字挂靠?特别是细则1(重量/频次/体积因素+最优路径规划)、细则8(统计信息含路径规划输出:起点/终点/长度/时间)——真有完整实现吗?

**6. 参数溯源表引用是否真实**
读 `docs/参数溯源表_0707.md`。**抽查** 1-2 个:Mecalux 0.818(声称已核对原文「66/54 m/min 空/满载」)、ACS880/AC500 手册编号——打开 URL 核对,是真的这么说,还是编的?

### 输出
一份批判性验证报告:①确认无误的项 ②存疑/需进一步核实的项 ③**发现的错误/假绿/隐患(最重要)**。每个发现给出核实过程(读了什么、跑了什么、算了什么),不要泛泛而评。

---

## 给用户的说明

- 让它跑 `ab_sync.ps1` 前**确认 AB 没开着**(管线要独占工程文件),否则它跑不了会报锁冲突。
- 它若发现真问题(尤其边界用例假绿、FB_GoodsInput 行为不等价、追溯矩阵挂靠),把它的报告发我,我核实并修。
- 它查不到的(那个 1 warning 的具体内容)——你在 AB GUI 消息窗口能一眼看到,顺手告诉我。
