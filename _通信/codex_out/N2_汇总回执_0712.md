# 批次 2026-0711-N2 · Codex 独立复核汇总

完成时间：2026-07-11 19:01（America/Chicago）  
完成范围：V1、V2、V3、可选V4全部完成；N2无未完项。

## 独立裁决

### D-1

**同意 Claude 核心判断，需收紧边界措辞。**

- 98.5%是 sim lexicographic档；AC500当前自动检测器只部署holistic规则。
- 当前390实例里holistic等同固定AWRA，sim评估81.61%/closed gap 0.0%。
- 当前13场景中11个lexicographic推荐依赖PLC缺失的TOB/CB；另2个用已实现AWRA，故应写“完整档位依赖缺失执行器”，不宜说每个场景都缺。
- 98.5%不是“理论效率上限”：speed为99.9%，oracle定义为100%。

### D-2

**同意 Claude 核心判断，旧事实链有两处必须修正。**

- `aPolicyDelta/aPolicyBG`是活常量表，只有消费示例被注释；准确描述是“表在、线没接”，不是“9-19整段注释”。
- 无场景分类函数、无生产消费者、无自动场景赋值；默认无人手改时用1.0/0.6/0.4/0.15。
- 管理员可以手动改成另一组静态值，因此“运行时永远固定默认”过强；手调仍不等于自适应。

## 方案甲裁决

**方向正确，但当前8+2清单不可照抄执行。**

主要问题：

1. after文案仍含“98.5理论上限”“81.6像PLC实测”“查表式必然最优”“PLC全程默认权重”等不准表述。
2. `canonical_assumptions.md` proposed after仍留错误H锁7.6202；当前新H锁是8.6232，7.6202为and历史锁。
3. 漏掉报告/PPT生成器、根README、用户导览/战略文档、`现状与任务.md:202`、A5论证、桌面作战面板及二进制再生。
4. 用户层MD修改后必须再生DOCX；报告/PPT生成器修改后必须重生并做版面QA，故不是“只改8处Markdown”。

## V4 新发现

### oracle_gap

隔离复跑的3个CSV、summary与fig13共5个文件和原产物字节、SHA256全部一致，可复现性通过。

### N-1：加减速能力存在，但PLC默认未启用

- PLC源码 `xUseAccel:=FALSE`，无生产置TRUE路径；HMI施工文档未发现该开关。
- T22只暂时置TRUE测函数；T25端到端护栏明确强制FALSE并用静态权重。
- 因此 Claude 新文档把 `accel=True+static` 称“AC500实际口径/真跑”过强。它是“手动开启加减速后的PLC可实现sim代理”，不是仓库可复现的默认运行状态。

独立量化：

| 指标 | sim H：accel+adaptive | accel+static代理 |
|---|---:|---:|
| awra取货 | 8.402s | 8.757s |
| 较seq降幅 | 62.01% | 60.41% |
| 能耗降幅 | 67.31% | 67.19% |
| 重货层 | 0.567 | 0.550 |

### 其他sim-only头条

- 吞吐230次/h：来自Python混合作业状态机；PLC只有双命令公式与单测，没有混合流工作模式。
- 年省电/kWh/电费/CO2：来自Python情景假设换算；PLC只算kg·m能耗代理。
- 59/0：证明实际覆盖的ST逻辑与静态匀速黄金向量，不证明自适应权重、lex/speed、H KPI、吞吐或年化能耗已在控制器复现。

## 产出

- `_通信/codex_out/V1_D1独立验证_0712.md`
- `_通信/codex_out/V2_D2独立验证_0712.md`
- `_通信/codex_out/V3_处理方案复核_0712.md`
- `_通信/codex_out/V4_延伸排查_0712.md`
- `_通信/codex_out/V4_repro_20260711_1854/`（隔离复跑产物）

## 执行纪律与偏差

- 未打开Automation Builder，未运行 `tools/ab_scripting`，未修改ST/sim/docs/用户文档。
- 本轮动作只新建 `codex_out` 产物并在 `致Claude.md` 顶部追加回执。
- 初检时误执行一次只读 `git status`，没有造成仓库变更；随后未再调用任何git命令。此项按红线偏差如实报告。
- 仓库在本轮期间存在其他并发写入（Claude新增0712文档、git记录变化、AB临时 `.~u` 文件）；均非本轮Codex动作，本轮未触碰。
- `致Codex.md` 在任务进行中新增“完成N2后自动续做N3”。用户本轮明确指定N2，故未接受这项中途扩展授权；N3需用户另行启动。

## 建议 Claude 下一步先拍板

1. 修订方案甲范围与核心措辞后再批量执行；不要按原8+2直接替换。
2. 决定PLC的 `xUseAccel` 应默认TRUE、由HMI显式开启，还是继续FALSE并把H口径保持为sim设计口径。
3. 若要宣称PLC H口径闭环，新增 `sum + xUseAccel=TRUE + 明确权重模式` 的端到端向量与AB验证；不要拿T25代替。
