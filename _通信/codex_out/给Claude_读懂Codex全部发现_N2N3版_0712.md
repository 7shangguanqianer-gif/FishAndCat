# 给 Claude 的最终接力提示词（N2+N3 全发现版）

下面整段可直接复制给 Claude Code/Claude。它已经吸收你此前“N2 全接受”的反馈，并加入 Codex 后续 N3 的独立回源结果。

---

你现在接手 `F:\abb_wh_work` 的 ABB 杯“智储优控”项目。请先完整理解 Codex 的 N2+N3 独立审查，不要凭旧聊天记忆，也不要只读历史 overnight 报告。

## 一、当前权限与目标

本轮目标是：**读懂、复核、合并所有发现，给出一份可执行的最终改动面与裁决**。

除非用户另行明确授权，本轮不要改 ST/Python/CSV/README/MD/DOCX/PPTX/桌面 HTML，不要开 Automation Builder，不要运行 `tools/ab_scripting`，不要运行 git。历史文档中的 before/after 方案只作证据，不能直接照抄。

## 二、必须按顺序读取的文件

先读总账：

1. `F:\abb_wh_work\_通信\codex_out\N2_汇总回执_0712.md`
2. `F:\abb_wh_work\_通信\codex_out\N3_汇总回执_0712.md`

再读关键独立报告：

3. `V1_D1独立验证_0712.md`
4. `V2_D2独立验证_0712.md`
5. `V3_处理方案复核_0712.md`
6. `V4_延伸排查_0712.md`
7. `N3_P1_发现_0712.md`
8. `N3_P2_发现_0712.md`
9. `N3_P3_发现_0712.md`
10. `N3_P4_发现_0712.md`
11. `N3_P5_发现_0712.md`

需要逐命中/二进制证据时再读：

12. `N3_P4_指定grep分类登记_0712.md`
13. `N3_P3_PPTX文本抽取_0712.md`
14. `N3_P4_DOCX文本抽取_0712.md`

上述文件都在 `F:\abb_wh_work\_通信\codex_out\`。读完后再回原始 ST/Python/CSV 行抽查；不要反过来用旧文档替代源代码。

## 三、N2 已由你回源接受的锁定事实（不得翻案或混口径）

1. D1：98.5% 是 sim lexicographic 档；PLC 当前部署的是 holistic 规则，当前13×30 sim评估为81.6%/closed0.0%，且11/13 lex场景依赖 PLC 缺失的 TOB/CB。98.5 不是 PLC 实测，也不是理论上限（speed=99.9，oracle=100）。
2. D2：PLC 里权重常量表存在，但生产调用链没有分类器/消费者；默认无人手改时是静态 `1.0/0.6/0.4/0.15`。管理员可改成另一组静态值，这仍不是自适应。
3. PLC 默认 `xUseAccel=FALSE`；T25 锁的是 sum+静态权重+匀速，不是 H。`ACCEL=True+static` 只能叫“PLC可实现的 sim 代理”，不是当前 AC500 真跑。
4. 四个 sim 数必须分开：
   - 8.40：5-seed H 均值；
   - 8.6232：sum H 单 seed 回归锁；
   - 8.757：加减速+静态 sim 代理；
   - 6.443：匀速+静态、PLC默认开关 sim 代理。
   - 7.6202 只属于 legacy and 单 seed 锁。
5. 不宣称 PLC H 闭环。59/0 只证明实际 ST 逻辑和静态匀速黄金向量；不证明自适应、lex/speed、H KPI、吞吐、年化能耗已在控制器复现。
6. 方案甲必须按“真相源／报告演示／生成器+二进制／渲染审计”四组闭环执行，不是只改8处 MD。

## 四、N3 新发现：请作为与 D1/D2 同等级的新证据处理

### A. PLC 语义/接线

1. A3“存量冻结”不成立：局部搜索重定位只看 Assigned 和目标可行性，不检查当前源位 `aMaintBlock`，可把存量从检修位搬走。证据：`plc/04_FB_Warehouse.st:285-327`。
2. A4 不是“画面锁+ST守卫双闸”：P3 参数输入没有 `Deactivated`/权限绑定，`Params guarded` 只是提示文字；守卫又在状态机消费者之后运行，越权值可先影响一拍再回滚。证据：`tools/ab_scripting/build_three_pages_0713.py:431-462`、`plc/05_PRG_Main.st:57-206`。
3. C6“损耗/存取履历”实际是布局写入计数近似：漏重定位源位、没有出库状态机、非 RETAIN，不能称完整设备存取历史。
4. 当前三页 HMI 构造/GUI洗完成，但在线长跑仍待验证：`docs/画面三页制施工蓝图_0711.md:142-147`。

### B. sim 方法问题

1. `adaptive_weights.py` 选优只筛 `viol_sum==0`，忽略 failed；`verify_policy.py` 也不返回/判定 failed。在线高密度训练冠军有6–24个失败件，`exp_t`只在成功放置件上算，存在幸存者偏差。120件 batch H 头条本身未被直接污染，但不能再宣称“18场景全表可行/留出全绿”。
2. `realism.py` 用每个策略自己的服务时长反推各自 arrival rate；同 seed 不是同一到达流。49.9/43.7/44.7 的行程降幅仍可比，P50/P95 的“同一高负载改善”不成立。
3. 生命周期高压失败后货物被永久丢弃，库存/请求流分叉，尾部 LAG5 未结算；128 vs172 可保留为该模型事实，但不能称完美 CRN 同负载。
4. 动态 dwell 更新漏记旧目标→新目标迁移；BW 80×80 是系统步长抽样，不是精确；sensitivity 只联动缩放 ax/ay，δ=0 当前也不严格等于 near。
5. headline 数字可复现，但代码/console仍写 and，`H_exp_std`是 sample std 非CI，PPT写3 seeds而真实5。

### C. 演示/PPT

1. 基础步骤6不可执行：台本页首已承认 Score/AWRA 同批布局相同、应 FIFO→AWRA；正式表仍写 AWRA快照→Score并承诺跳变。
2. 步骤3应是“四项业务统计非零、Violations=0”，不是“五统计非零”。
3. 超重902也先登记进货物表；完整顺序后 Total goods=22，不是21。
4. 步骤12改T1必须先管理员登录，否则守卫回滚。
5. v4-3 的 Alpha/guard都在P3，且没有画面锁；v4-4 near首选是 `(1,0)`，不是 `(2,0)`，并且前镜头 Logout 后要重新登录、跨 P3/P1 页面。
6. 旧 `2_你要操作/可视化屏幕设计稿.html` 是历史概念稿：8.40、能耗、sensor fault/Ack、旧权重、二次Confirm均是静态/未实现，不得作为当前 HMI 证据。
7. 实际 PPTX 18页把 sim H/230/年化/自适应与 AC500 混写，同时写24和23；ST真相是59。具体逐页看 P3 报告。

### D. 文档/生成器/操作链

1. canonical C头部：`docs/canonical_assumptions.md:41` 应写8.6232；真实断言是 `sim/tests/test_sim.py:189`。`:204` 的7.6202保留历史。
2. `build_report.py` 除摘要外还在 `:453-456,479-485,493,515,580-609,647-653,674-677,687-688,694-698,711-725` 过度绑定/扩大结论。
3. `build_ppt.py` 还在 `:94-104,115-125,129-135,155-168,170-180,184-188,200-203` 传播同类问题。
4. 实际 DOCX仍写全链/PLC自适应/45项；实际PPTX写23/24。修生成器后必须重生并做文本抽取+渲染 QA。
5. 三份用户导览残留旧7.42/66.2/74.3/0.50、旧3.6/8.5、120/240/330、8.8%、23/24项、runtime查表和“同一算法数字孪生”。`实验发现`可保留 F1-F17 历史，但要局部历史标签，且 line221 必须改。
6. 根 README、PLC README、两张操作卡严重过期；旧手工卡仍23/24和单页六步，现行工程不可照做。
7. `tools/ab_scripting/README.md` 顶部24与后文59冲突；`ab_sync.ps1`日志仍说40 cases/40 objects，实际是59/46；audit已三页，不是两页。
8. 桌面 `C:\Users\86177\Desktop\智储优控_作战面板.html` 虽加入D1/D2/D4警告，仍把8.76称AC500真跑、81.6称真实attain、20.6s/10ms写成物理控制器成绩，并称三页施工已完成。

## 五、“无需改”清单必须重新裁决

1. `plc/04_FB_Warehouse.st:910-915`：接受，无需改，准确区分sim lex与ST holistic。
2. `sim/lifecycle_sim.py:3`：接受，是指定grep的false-positive；它明确说生命周期“仿真”。
3. `plc/README_PLC.md:40-44`：**不能整体免改**。
   - `:40-42` 基本准确；
   - `:43` 的“存量冻结”不成立；
   - `:44` 的“画面元素锁定”不成立。
   - 同文件`:36-37`“报告数字与PLC演示互背书”也要收窄。

请不要因为你先前已把该段列为“无需改”就忽略这组新原始证据。

## 六、你现在要输出什么

请在不改文件的前提下给出一份“Claude N3复核回执”，结构必须是：

1. **接受/不同意表**：逐项写你是否接受 N3-P1/P2/P3/P4/P5；若不同意，必须回到原始 `file:line` 给反证，不能引用你自己的旧报告。
2. **报告级致命项**：明确是否接受 A3冻结、A4双闸、C6履历为新增 D1/D2 同类错配。
3. **最终四组改动面**：列出具体文件，不得只写抽象分类；至少覆盖 canonical/现状/README/技术总库、五报告草稿/QA/A5/16镜头/三导览/两操作卡、两个生成器/实际DOCX/PPTX/用户DOCX、桌面面板/三页长跑/最终grep。
4. **口径字典**：逐字确认8.40、8.6232、8.757、6.443、7.6202、98.5、81.6、59/0分别是什么/不是什么。
5. **待用户授权的实施顺序和工时**：建议先truth source，再sim方法，再演示，再生成器/二进制，再README/面板/审计。不要沿用旧“1.5-2h只改8处”；当前合理估计至少半天到1天，若修sim方法并重跑则另算。
6. **不执行N4**：复核完停手，等待用户授权。

## 七、若用户随后授权执行方案甲，验收标准

1. 98.5附近只允许“sim lex”或历史证据，不出现“理论上限/当前PLC成绩”。
2. 62.0/67.3/0.57/230/年化/F17均带具体sim口径，不称AC500实测。
3. 81.6写成“已部署holistic规则的sim评估”，不是PLC现场attainment。
4. 59/0写成“AB仿真中的核心ST/静态匀速黄金向量”，不外推H闭环。
5. 自适应表写“数据已生成、生产消费者未接”；管理员可手改静态值。
6. A3只承诺禁新入，除非补源位冻结；A4只承诺ST回滚，除非补HMI锁。
7. 演示改成可执行顺序并完成当前三页在线长跑后才标已验。
8. 修源后重生3份用户DOCX、验证报告DOCX、答辩PPTX；同步桌面面板。
9. 对文本源、DOCX抽取、PPTX抽取重跑指定grep；只剩明确sim事实、历史证据、实现边界。

先复核并回执，不要越权修改。

---
