# 0712 口径修正 + N5 + sim 方法 Implementation Plan

> **完成状态（0712最终）**：Codex授权范围内Task 1-14均已完成；细节与验证见`0712_合并修复_总改动对照表.md`、`0712_二进制与渲染QA.md`、`0712_合并修复_验证日志.md`。执行期间Claude并发修改ST，最终又做了一轮源码/证据分离校准；Codex未改可执行ST、未开AB、未做git写。

> **For agentic workers:** 本批按用户红线采用 inline execution；禁止子代理、git 写、Automation Builder 与 `tools/ab_scripting` 运行。

**Goal:** 在不修改 PLC 可执行 ST 的前提下，完成 A/B/C/D 四合一修复、重算受影响 sim 数字、重生 DOCX/PPTX，并留下可审计的原句→改句证据链。

**Architecture:** 先建立纯函数和测试门修复 adaptive/arrival/lifecycle/oracle，再更新真相源、图源和生成器，最后统一重生二进制。所有生成数据一文件一 schema；所有指标携带 sim/seed/统计角色/可比域 provenance。

**Tech Stack:** Python 3、`unittest`、CSV、matplotlib、python-docx、python-pptx、LibreOffice、Poppler、MarkItDown、PowerShell。

---

### Task 1: 建立可回滚基线与变更清单

**Files:**
- Create: `_通信/codex_out/backup_0712_merge/`（仅复制本批会改的文本/二进制）
- Create: `_通信/codex_out/0712_变更前基线.tsv`

- [ ] 记录本批文件的绝对路径、大小、mtime、SHA256；复制到 backup，排除 `plc/AB_Project` 临时/锁文件。
- [ ] 运行现有 Python 测试基线：

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'
python -B -m unittest discover -s sim/tests -v
```

Expected: 当前测试全部通过；记录用例数，不把它称“全量项目回归”。

### Task 2: 先写 sim 方法失败测试

**Files:**
- Create: `sim/tests/test_method_integrity.py`
- Modify: `sim/tests/test_oracle_gap.py`

- [ ] 添加 adaptive 测试：零失败候选存在时选最小 exp；不存在时 `status=INFEASIBLE` 且 best/gain 为空。
- [ ] 添加 verify 测试：`run()` 返回 `fail`，可行策略 `fail==0`。
- [ ] 添加 realism 测试：四策略传入同一 arrivals；`build_arrivals()` 对 seed/reference 确定。
- [ ] 添加 lifecycle 测试：短周期结束后 `pending_end=0`、`inventory_end=n_init`、`flush_count>0`。
- [ ] 添加 oracle 测试：`excess_fail>0` 时 gap/attain 为空；零超额失败的 98.55 锚保持。
- [ ] 添加 CSV schema 测试：新 12 个拆分文件各只有一种列宽、无重复 header。
- [ ] 运行并确认测试先失败：

```powershell
python -B -m unittest sim.tests.test_method_integrity sim.tests.test_oracle_gap -v
```

Expected: FAIL，明确指向尚未实现的新函数/字段。

### Task 3: 修复 oracle 可比域（N5-34）

**Files:**
- Modify: `sim/oracle_gap.py`
- Modify: `sim/tests/test_oracle_gap.py`

- [ ] 抽出可比计算：

```python
def comparable_gap(exp_t, oracle_exp, excess_fail):
    if excess_fail > 0:
        return None, None
    return ((exp_t - oracle_exp) / oracle_exp * 100.0,
            oracle_exp / exp_t * 100.0 if exp_t > 0 else None)
```

- [ ] 聚合只用非空 gap/attain，新增 `comparable_n`、`total_n`；excess_fail 对全部实例求均值。
- [ ] summary/图表对空值显示 `N/A`，98.5/99.9 句补 `excess_fail=0`。
- [ ] 运行 oracle 单测；Expected: PASS，58 个伪 >100% 消失，det_lexicographic 98.55 保持。

### Task 4: 修复 adaptive fail-first 与选择稳定性（N5-13/19）

**Files:**
- Modify: `sim/adaptive_weights.py`
- Modify: `sim/verify_policy.py`
- Modify: `sim/warehouse_sim.py`（仅 Python 状态/注释，不同步写 ST）
- Modify: `sim/tests/test_method_integrity.py`

- [ ] 新增 `select_policy()`，仅在 `viol_sum==0 && fail_sum==0` 内选优；空集返回 `INFEASIBLE/min_fail`。
- [ ] 记录每个 seed 的候选表现与 winner vote，策略表新增 `status/seed_count/winner_votes/winner_rate/selection_stable/min_fail`。
- [ ] `verify_policy.run()` 返回三元组 `(exp_t, viol, fail)`；有效行断言 fail=0，不可行行不执行伪泛化判定。
- [ ] ST 输出改为显式 `--emit-st` 且遇 INFEASIBLE fail closed；本批不传该参数。
- [ ] `warehouse_sim.py` 注释/状态表标出高密 online 不可行，保留当前部署权重仅作历史/待降级，不宣称零失败。
- [ ] 运行 quick 与单测；Expected: 120/skew 可行，高密 online 输出 INFEASIBLE，无 `plc/08_*` mtime 变化。

### Task 5: 修复 realism 共同 arrival（N5-14）

**Files:**
- Modify: `sim/realism.py`
- Modify: `sim/tests/test_method_integrity.py`

- [ ] 新增 `build_arrivals(cycles, seed, reference_pair_s)`。
- [ ] `tier3()` 以 seq reference 计算一次 service mean，生成一次 arrivals，并传给四个 `run_tier3()`。
- [ ] 删除 `run_tier3()` 内按各策略反推 `e_pair` 的路径。
- [ ] 输出 `arrival_reference=seq/reference_pair_s`；运行 quick 测试共同数组，再运行 100 cycles 记录 P50/P95 前后表。

### Task 6: 修复 lifecycle 尾部（N5-15）

**Files:**
- Modify: `sim/lifecycle_sim.py`
- Modify: `sim/tests/test_method_integrity.py`

- [ ] 主循环后 drain pending，只回库不再取货；将 flush 存储时间加入总时间。
- [ ] 返回/CSV 新增 `pending_end/inventory_end/flush_time/flush_count`，final_q 在 flush 后计算。
- [ ] 快速 2-seed/40-cycle 验证，再按原 30-seed/200-cycle 参数重生两组 lifecycle CSV。

### Task 7: 修正 headline、能耗与 CO₂（N5-17/18/21/23/24/25）

**Files:**
- Modify: `sim/headline.py`
- Modify: `sim/energy_report.py`
- Modify: `docs/canonical_assumptions.md`
- Modify: `docs/报告草稿_C3C5B14_0712.md`
- Modify: `sim/tests/test_method_integrity.py`

- [ ] headline 元数据写 `seed_count=5/dispersion=sample_sd/evidence_role=regression_anchor_not_U3`，修 docstring `and→sum`。
- [ ] 保持 8.402388/0.476878/8.6232/7.6202 数值锁；不新增 final test。
- [ ] `CO2_FACTOR=0.5306`，注公告 2025年第47号；`DAILY_OPS` 文案改“500 单操作/日=250 双周期/日”。
- [ ] 将能耗绝对量标为“sim 净机械功情景换算”，把 `546.0→545.4`；重算 CO₂：基线约289.5、AWRA约148.0、年减约141.4 kg。

### Task 8: 拆分六份多段 CSV（N5-33）

**Files:**
- Modify: `sim/energy_report.py`, `sim/realism.py`, `sim/rl_probe.py`, `sim/reslot_cycle.py`, `sim/scan_g2g4.py`
- Modify: `tools/ab_scripting/archive/measure_l4b.py`（只改未来输出路径，不运行）
- Modify: `sim/build_report.py`, `sim/build_ppt.py`
- Create: 新 data/params/summary CSV
- Delete after migration: 6 份旧多段 CSV

- [ ] 每个生成器分别写单 schema 文件；L4 从现有 CSV 无损拆出 data/params。
- [ ] 更新消费者路径并删除专用双段 parser；required inputs 缺失时 exit 1。
- [ ] 全仓搜索旧文件名并逐处迁移；无引用后删除旧多段 CSV。
- [ ] 跑 schema 测试；Expected: 12 新文件矩形，旧多段文件不存在。

### Task 9: 修图源与口径标签（N4-27）

**Files:**
- Modify: `sim/plots.py`, `sim/experiments.py`
- Regenerate: `sim/figs/fig1*`, `fig4*`, `fig5*`, `fig12*`, `fig13*`, `fig16*`

- [ ] 图内 rule 统一当前 `sum`；静态/匀速图明确标 `uniform+static`，H 图明确 `trapezoid+adaptive`、seed 数与角色。
- [ ] adaptive INFEASIBLE 用红色/斜线与文字，不画成正常增益柱。
- [ ] realism P50/P95 图使用共同 arrivals 新结果；oracle N/A 不画 >100%。

### Task 10: 口径文本 A/B/D 全面修正

**Files:**
- Modify: `README.md`, `现状与任务.md`, `docs/canonical_assumptions.md`, `docs/技术方向总库_0711.md`, `plc/README_PLC.md`
- Modify: `docs/演示脚本v3_0711.md` 与提示词点名的报告/QA/执行时间/审计/README 文件
- Modify: `1_给你看/*.md` 中点名文件、`2_你要操作/可视化屏幕设计稿.html`
- Modify comments only: `plc/04_FB_Warehouse.st:59,101`

- [ ] 按旧提示词组1-4和 D 清单逐条替换；历史 F1-F17/legacy 保留并加历史标签。
- [ ] 统一 8.757/8.76 为 Python accel+static sim；98.5/81.6/59/0/230/kWh 均带边界。
- [ ] A3/A4/C6 降级到当前实现；README 计数更新 17 Functions/15 FB/59 tests、三页 505+134+67。
- [ ] 作战面板只审计，不回退；新数字若被引用再最小同步。

### Task 11: 更新报告/PPT 生成器并 fail-fast

**Files:**
- Modify: `sim/build_report.py`, `sim/build_ppt.py`

- [ ] 读取新 CSV schema；缺 required source、`MISS` 或 body_miss>0 时不保存成功产物并 exit 1。
- [ ] 摘要删除“全链实现/AC500实测/全量回归”过强串；写 sim/AB 仿真/PLC 源码边界。
- [ ] 注入新 CO₂、共同 arrival、drain、adaptive INFEASIBLE、oracle comparable coverage、5-seed sample SD。

### Task 12: 重生 sim 数据并记录数字变化

**Files:**
- Regenerate: `sim/out/*.csv`, `sim/out/oracle_gap_summary.txt`, affected figures
- Create: `_通信/codex_out/0712_sim数字变更表.md`

- [ ] 按依赖顺序运行 headline、energy、oracle、realism、lifecycle、adaptive（不 emit ST）与相关图。
- [ ] 重跑 `consistency_probe.py`，用新输出替换旧 log，确认 13×30=390。
- [ ] 对比旧备份与新产物，列所有变化及原因；未变化头条也列“保持”。

### Task 13: 重生 DOCX/PPTX 与文本 QA

**Files:**
- Regenerate: `1_给你看/*.docx`（同名用户文档）
- Regenerate: `sim/out/验证报告_draft.docx`, `sim/out/答辩PPT_draft.pptx`
- Create: `_通信/codex_out/qa_0712/` 文本/PDF/逐页图

- [ ] 运行 `python -B tools/make_user_docs.py`；运行 build_report/build_ppt。
- [ ] 验证 DOCX/PPTX 包结构：

```powershell
python C:\Users\86177\.agents\skills\docx\scripts\office\validate.py <docx>
python C:\Users\86177\.agents\skills\pptx\scripts\office\validate.py <pptx>
python -m markitdown <pptx>
```

- [ ] 用 LibreOffice 转 PDF，再用 Poppler 150dpi 转逐页 JPG；逐页检查溢出、遮挡、乱码、空白页、旧标题。
- [ ] 至少完成一轮“发现问题→修生成器→重生→复验”。用户禁止子代理，因此由当前会话直接检查。

### Task 14: 最终审计与回执

**Files:**
- Create: `_通信/codex_out/0712_总改动对照表.md`
- Update: `_通信/致Claude.md`（顶部追加）

- [ ] 运行 Python 单测、CSV integrity、doc_check、旧口径 grep、DOCX/PPTX 文本 grep。
- [ ] 确认无 PLC 可执行 ST 变化；只有点名注释变化。
- [ ] 输出 A/B/C/D 原句→改句→依据 file:line、数字变化、测试结果、QA 页数、未完成/归 Claude 项。
- [ ] 顶部追加简明回执后停止，不扩 N6、不做 ST/AB/提交包。

## Plan Self-Review

- 规格覆盖：A/B/C/D、N4 图源/假绿/二进制、N5 Python/文档项、DOCX/PPTX QA 均有任务。
- 无 TBD/TODO；关键分支已在设计单值锁定。
- 类型/字段一致：adaptive status、oracle comparable_n、lifecycle flush 字段、CSV 新文件名在生成与消费任务中同名。
- 用户红线优先于 skill 默认：无 commit、无 subagent、无 AB、无 `ab_scripting` 执行。
