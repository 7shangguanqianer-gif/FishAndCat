# S3 多算法 AUTO 测试账本（0715）

> 规则：只追加任务记录；每条写命令、输入、预期、实际、hash、负结果和 critic 裁决。  
> 红线：不开 AB、不跑 `tools/ab_scripting`、不做 git 写。

## Task 1 · 规格与修改前基线

时间：2026-07-15 03:03:04 -05:00  
状态：PASS（仅旧基线）；新功能实现状态仍为 0。

### 设计与独立监督

- 设计：`docs/Codex设计_S3多算法AUTO与货物实验补齐_0715.md`
- 实施计划：`docs/Codex实施计划_S3多算法AUTO_0715.md`
- critic：`gpt-5.6-sol / xhigh / read-only`
- critic 经过四轮：当前基线 No-go → 规格两轮阻断修订 → workload 契约修订 → **Go-for-implementation**。
- critic 的 Go 只代表规格可实施，不代表实验或视觉验收通过。

### 环境

- Python：3.9.13
- Node：v24.16.0

### 修改前 artifact hash

| 文件 | SHA256 |
|---|---|
| `l4_showcase/src/样张_S3_三方向候选.html` | `B49EDC25AB0310A702DA7ADCCD1661773B37AA276193845A84F47E35421145D7` |
| `l4_showcase/src/s3_ac_runtime.js` | `7E4A2903681DC8FFE739F0BC6903265059299DDAD0E7F5F2A459CCD2EA8C0017` |
| `l4_showcase/out/s3_tier3_trace.js` | `5BA8761760A756FD8E2B41F9E8BF19F8F2C40EBED84CF6C5CCEEA443712D3FE1` |

### 基线命令与结果

1. `python sim/core_smoke.py`
   - 预期：现有仿真/口径回归不红。
   - 实际：32 tests，OK；headline 5-seed 锚仍为 8.40±0.48s。
   - 说明：命令会再生 oracle-gap 输出；再生后 `git diff --name-only -- sim/out` 为空。

2. `python l4_showcase/tools/test_s3_tier3_trace.py`
   - 预期：当前单 trace 自洽。
   - 实际：3 tests，OK。
   - 限定：只覆盖 AWRA-LS 初排→score 在线；不得外推为四算法/AUTO。

3. `node --check l4_showcase/src/s3_ac_runtime.js`
   - 实际：exit 0。

### 已知阻断（实施前事实）

- 当前页面只有单 trace；无五模式、三 Tier、三画像切换。
- `strategy_lib` 函数默认仍为 holistic；S3 必须显式传 lexicographic。
- `realism.run_tier3` 尚不支持 CB/TOB/AUTO 事件链。
- exporter 仍手写第二套 Tier 3 循环。
- 出库只有抽象 unload，相同 gid 未经过 OUTFEED/SCANNED/EXIT_MASK。
- 当前 HTML 仍有旧内联循环 + AC runtime 双层接管。
- 最新候选无对应当前 hash 的多视口阶段截图。

## Task 2 · 版本化场景注册表与画像生成器

时间：2026-07-15 03:09-03:16 -05:00  
状态：独立 critic **PASS**。

### 修改面

- 新建 `sim/scenarios/cargo_dynamic_v1.json`：E01~E18、四段互斥 seeds、初始/入库双生成器、请求池、非法入库、Tier 3 扰动和 E15 三个降级字段。
- 新建 `sim/cargo_scenarios.py`：注册表加载/校验、初始货与入库货生成、零依赖 Spearman、画像、合法门和 provenance manifest。
- 新建 `sim/tests/test_cargo_scenarios.py`：7 个测试方法。
- 未修改 `strategy_lib.select_strategy` 默认目标；未修改现有算法或动画。

### 红灯→绿灯证据

1. 先运行 `python -m unittest sim.tests.test_cargo_scenarios -v`。
   - 预期：模块尚不存在，测试失败。
   - 实际：`ModuleNotFoundError: No module named 'cargo_scenarios'`，exit 1。
2. 实现后同命令：7 tests，OK。
3. `python -m py_compile sim/cargo_scenarios.py sim/tests/test_cargo_scenarios.py`：exit 0。
4. `python -m unittest discover -s sim/tests -v`：39 tests（core_smoke 再跑口径），OK。
5. `python sim/core_smoke.py`：39 tests，OK；headline 5-seed 锚仍为 8.40±0.48s。
6. `git diff --name-only -- sim/out sim/figs`：空；回归命令未留下已跟踪输出差异。

### 数据契约实测

- E04~E09 初始货逐属性与 `instance_gen.gen_instance` 现有源场景一致。
- E10/E11/E12 同 seed 重排前后 `weight/freq/volume` 三个边际多重集逐值一致。
- S1 10 seeds + S2 30 seeds，初始/入库各一次，共每场景 80 组：
  - E10 `rho(weight,freq)`：0.999673~0.999884；
  - E11 `rho(weight,freq)`：-0.999882~-0.999691；
  - E12 `rho(weight,volume)`：0.999467~0.999731；
  - 均通过预注册 ±0.75 门。
- E13 初始/入库均在 90.0~99.5kg、0.90~0.99m³，入口合法。
- E14 初始 120 件全合法；100 件入库严格 3 超重+3 超容积。
- E15 同时具备 `extended_pressure=true / canonical_g2=false / headline_eligible=false`。
- E18 注册表锁定 0~32 uniform、33~65 skew、66~98 heavy；路由只在 run 开始选一次，33/66 仅 advisory（workload 行为在 Task 3 单测）。

### 当前文件 SHA256

| 文件 | SHA256 |
|---|---|
| `sim/scenarios/cargo_dynamic_v1.json` | `550EAA4BA50F0BF6AE7DDE402437E02082053A9F5E7F362A11D68279F9987613` |
| `sim/cargo_scenarios.py` | `F757D268FA90D30F05CD412B4960E4E470D551EC6C87507F0ACEC44E0BE1CD11` |
| `sim/tests/test_cargo_scenarios.py` | `01E99622A677185C8DC6B0A52F45E71F476D0122DEA55883BA04EA6EB45B9CB7` |

### 负结果/限定

- 此 Task 只建立货物事实层，尚未证明动态 workload、7+AUTO 策略链、Tier 3 事件或 UI。
- 相关场景是显式 rank-coupled 压力测试，不代表真实业务天然具有接近 ±1 的相关性。
- E14 的 `reject_service_s=0` 是无 canonical 参数的 modelled 占位，禁止称真实门检节拍。

### critic 第一轮裁决与闭环

- `gpt-5.6-sol / xhigh / read-only` 第一轮给出 **CONDITIONAL**，无阻断 Task 3 的算法错误；要求补三项：
  1. E04~E09 inbound 也直接对照现有源参数；
  2. 注册表校验 E14 算术、E18 连续无重叠和 request policy 枚举；
  3. provenance 纳入 `mixed_ops.py` 及 workload 函数 hash。
- 已全部补齐：新增 inbound 对照与三组坏注册表拒绝测试；manifest 现含 `sim/mixed_ops.py` 文件 hash，以及 `build_workload` / `build_scenario_workload` AST 函数段 hash。
- 修订后 `core_smoke` 为 47 tests，OK；等待 critic 第二轮最终裁决。

修订后 SHA256：`cargo_scenarios.py=D61DF3BB...5397FE7`；`test_cargo_scenarios.py=9D004BA9...57F099`。注册表内容未改，仍为 `550EAA4B...87613`。

## Task 3 · 参数化 workload、非法拒收与漂移契约

时间：2026-07-15 03:18-03:28 -05:00  
状态：独立 critic **PASS**。

### 修改面

- 修改 `sim/mixed_ops.py`：保留旧 `build_workload()` 不动；新增 `build_scenario_workload()`。
- 新建 `sim/tests/test_scenario_workload.py`：6 个测试方法。
- 返回统一 `scenario_workload_v1`：周期记录、valid inbound、requests、dispatch mask、E18 advisory、最终库存、inbound/request/workload 三类 hash。

### 红灯→绿灯证据

1. 新测试先行：`python -m unittest sim.tests.test_scenario_workload -v`。
   - 实际首跑：`ImportError: cannot import name 'build_scenario_workload'`，exit 1。
2. 实现后同命令：6 tests，OK。
3. Task 2+3 联合：15 tests，OK。
4. `python -m py_compile ...`：4 个相关 Python 文件 exit 0。
5. `python sim/core_smoke.py`：47 tests，OK；旧 headline 锚未变。
6. `git diff --name-only -- sim/out sim/figs`：空。

### 契约实测

- 旧 `build_workload()` seed=2026 golden SHA256 仍为 `332dad835199fd1f39145fbda2d3e321780b6de687c66b108ea7dd0f1faeac2f`。
- E01~E18 均保持最终合法库存数量等于初始数量，最终 gid 无重复。
- 同一 E02/seed 连续以 7 固定链名+AUTO 八次构造，`workload_hash` 只有 1 个值；函数本身不接收策略或仓位。
- 独立重算 `random.Random(seed+13).choices()`，请求 gid 序列逐项一致；顺序严格为“从当前合法池抽出→移除→加入当期合法 inbound”。
- E14 固定 100 scheduled / 94 valid / 6 rejected / response sample 94 / reject service 0；拒收周期不抽请求、不变库存、非法 gid 不进最终池。
- E18 0~32 / 33~65 / 66~98 三段标签逐周期锁定；33/66 advisory 显式 `advisory_only=true / applied=false / existing_inventory_relayout=false / objective=lexicographic`。

### 当前 SHA256

| 文件 | SHA256 |
|---|---|
| `sim/mixed_ops.py` | `FC3D0CB8E4AFC7096721B1805A7FF4952817D7605F4F53CC83965E0BD115531A` |
| `sim/tests/test_scenario_workload.py` | `C6435AE9F3FDAE0E817EB823D563D655B5FD4E530E5F54D4FE0B39717FDDEBE3` |

### 限定

- 当前只证明负载身份/请求/拒收/漂移 advice 公平；尚未证明 arrivals/noise/fault stream 在策略间同 hash，留 Task 4 唯一事件引擎。
- E18 advisory 只描述下一独立批次建议，不应用、不重排，不能称在线智能换策闭环。

### critic 最终裁决

- 独立监督者实际复跑 Task 2+3 联合 15 tests：OK；测试后 `sim/out`/`sim/figs` 无已跟踪差异。
- **Task 2：PASS；Task 3：PASS。**
- 静态确认 E14 非法分支在 `rng.choices()` 前短路；E18 advisory 不写策略/库存；workload hash 覆盖策略无关输入。
- 留给 Task 4 的条件：每条策略对 `Good` 只读/克隆，并补齐 arrivals/noise/fault stream 共同 hash。

## Task 4 · 唯一 Tier 3 事件引擎与共同随机流

时间：2026-07-15 03:30-03:43 -05:00  
状态：首版曾本地绿但被 critic 阻断；最终修订已获独立 **PASS**（见下方闭环）。

### 修改面

- 修改 `sim/realism.py`：
  - 新增外生 noise factors/noise stream、busy-time fault intervals、canonical stream hash；
  - 新增 scenario-aware `common_arrivals_for_workload()`；
  - `run_tier3()` 成为唯一事件循环，支持 `workload/events/fault_intervals/event_sink/noise_stream/tier3_params`；
  - 默认旧调用不附加字段，保持旧返回字典逐浮点完全一致；
  - event sink 输出连续 phase、库存前后快照和 gid ownership；
  - E14 拒收只出零时长 `GATE_REJECT`，不进服务/响应样本。
- 新建 `sim/tests/test_tier3_event_engine.py`：5 个测试方法。
- `scenario_manifest()` 追加 `sim/realism.py` 文件 hash，以及 `build_fault_intervals/common_arrivals_for_workload/run_tier3` 函数段 hash。

### 红灯→绿灯证据

1. 改引擎前先保存四策略 100-cycle Tier 3 完整浮点字典；新测试首跑：
   - legacy bit-exact 测试已 PASS；
   - 其余 4 项因 `build_fault_intervals/common_arrivals_for_workload/event_sink` 尚不存在而 ERROR；
   - 证明测试不是在实现后追写。
2. 实现后 `python -m unittest sim.tests.test_tier3_event_engine -v`：5 tests，OK。
3. Task 2~4 联合：20 tests，OK。
4. `python sim/core_smoke.py`：52 tests，OK；旧 headline 5-seed 锚仍为 8.40±0.48s。
5. `git diff --name-only -- sim/out sim/figs`：空。

### 关键验收

- 默认旧调用四策略 `seq/near/score/awra` 的 `tot_dual/busy_total/avg_retr/P50/P95/util/fault/fail/viol` 字典逐浮点 `assertEqual`，不是近似比较。
- fault stream 由 `seed+29` 一次预生成 `cycles+1` 个忙时 Exp(MTBF) 间隔；策略分支只消费，不再抽 RNG。
- 同一 E02/seed 的四策略 `workload_hash / arrivals_hash / noise_hash / fault_stream_hash` 四项逐字一致。
- 8-cycle event sink：每周期 phase 首尾闭合、相邻 phase 时间连续、库存前后 120、4 个 ownership 事件，最终 OUTFEED 时刻等于 finish。
- E14：100 scheduled / 94 dispatch / 6 reject / response sample 94 / dual-time sample 94；拒收 service=0、response=None、库存前后值相同且非同一列表别名。
- P50/P95 沿现有约定回归：排序后 `resp[n//2]` 与 `resp[min(n-1,int(n*0.95))]`。

### 当前 SHA256

| 文件 | SHA256 |
|---|---|
| `sim/cargo_scenarios.py` | `DABFF6BDC2D5748463D97F7ED76F3F5E647E81DD96CDDDDCB8D5B289ED44BA08` |
| `sim/mixed_ops.py` | `FC3D0CB8E4AFC7096721B1805A7FF4952817D7605F4F53CC83965E0BD115531A` |
| `sim/realism.py` | `48AE9C5D1537030F22DB514552ACE3AEC7C5BA18F2E60BB8D8476115AF94807E` |
| `sim/tests/test_tier3_event_engine.py` | `20D1569F0B569F4028409F4C9821EE191A7DFC4EAE468F7F7602D8F04AC27FCA` |

### 限定

- 事件引擎目前仍只允许旧四种策略名；CB/TOB/random/AUTO 在 Task 5 接入。
- 此处“恢复段放周期末端”是首版旧结论，**已废止**；最终版按 productive busy-time 的真实 operation crossing 切相位，详见下方修订闭环。
- 事件引擎只把出库货交到 `OUTFEED`；scan/exit curtain/CONSUMED 是展示层生命周期，Task 7/9/11 接上且不得伪造 canonical 门检节拍。

### Task 4 最终修订闭环（supersede 首版限定）

- critic 首轮在测试全绿时仍判 BLOCK：①故障游标把下一 Exp gap 从周期末而非实际 crossing 起算，无法同周期消费第二故障；② events-only hash 漏 `Good.name`。
- 新增先红后绿反例：同一周期两个不同 operation phase 各触发一次故障、MTTR 非零；以及 Good 五字段+cycle/status/dispatch/profile/advisory 逐字段篡改 hash。
- 最终实现把历史无扩展调用隔离为 bit-exact compatibility lane；所有 dynamic 调用使用独立 `productive_busy_acc`，queue/MTTR 不推进故障时钟，同周期可消费多个 gap。
- online placement 首次失败采用 `TERMINATE_NO_FALLBACK`：恢复 outbound 映射、库存不变、无策略偷换、partial P95 不可比。该修复来自真实 S1 首跑 E08/2032 的 `KeyError(10005)`，不是预想测试。
- 最终 critic：**Task 4 PASS**。四个历史 Tier 3 字典仍逐浮点 `assertEqual`；events-only 全量 provenance、phase freeze/resume、库存/ownership 均通过。

## Task 5 · 七固定链与 AUTO 词典序路由

时间：2026-07-15 03:44-04:02 -05:00  
状态：独立 critic **PASS**。

- 七固定链：`random→random / seq→seq / near→near / score→score / awra→score / cb→score / tob→score`。
- AUTO 仅在 run start 以 `objective=lexicographic` 选择一次；E18 周期 33/66 只给下一批 advisory，`strategy_switches=0`。
- 全局 `select_strategy()` 默认仍是 holistic；S3/新实验显式传 lex，未扩大既有作用域。
- S0 直接阈值与生产可达 n 边界均有三点测试；只改 `w_mean/w_heavy` 时 v1 pick 不变，禁止写重量感知路由。
- CB/TOB 初排与 `instance_gen` 同源；动态入库统一 score；初排不可行返回显式 INFEASIBLE，不崩溃、不兜底。
- 同一 cell 七固定+AUTO 的 workload/arrivals/noise/fault 四 hash 逐字一致。

## Task 6 · 风险矩阵、smoke 与 S1 全矩阵

时间：2026-07-15 04:02-04:34 -05:00  
状态：独立 critic **PASS**；S1 证据可供 Task 7 使用，仍只属 sim。

### 真实运行与证据位置

- runner：`sim/cargo_dynamic_matrix.py`；测试：`sim/tests/test_cargo_dynamic_matrix.py`。
- S1：`sim/out/cargo_dynamic_matrix/s1/`，18 场景×10 seeds（2026..2035）×8 模式。
- 实际：180 cells / 1440 detail rows / 144 agg rows；唯一 `(scenario,seed,mode)`=1440，无重复。
- 每个 cell 八模式且四流 hash bundle 唯一；`fairness_pass=False`=0。
- manifest 的 detail/agg/params SHA256 均由主 agent 和 critic 独立复算一致；runner 文件及关键函数 hash、modes/cells/runs 均已锚定。

### 被真实运行抓出的缺陷与修复

1. 首版 stage 共用同名输出，会覆盖 S1/S2/S3 → 改为 `cargo_dynamic_matrix/<stage>/`。
2. manifest 未锚 runner → 加 runner 文件 hash、7 个关键函数 hash、Python/platform、实际 modes/cells/runs。
3. `stage=".."` 可目录逃逸 → strict allowlist `{smoke,s1,s2,s3}`，含 `..`/绝对路径/设备名负测。
4. 两进程首次写有 TOCTOU → 每 stage `O_EXCL` lock，锁内重读 identity，同盘临时构建，manifest 最后替换。
5. 首次全 S1 在 E08/2032 因 placement fail 后继续消费请求，后续 missing gid 崩溃 → fail-first terminal、恢复库存、partial P95 置不可比；新增事件级和矩阵级反例。

### S1 独立审计结论

- 可行 run 但 `fail/viol>0`=0；不可行 run 暴露可比 `resp_p95_s`=0；负 regret=0；aggregate 从 detail 复算不一致=0。
- E14 100/94/6、response sample 94 全对；E15 `extended_pressure=true / canonical_g2=false / headline_eligible=false` 全对。
- E13 八模式×10 seeds 全部 INITIAL_LAYOUT 不可行：90~99.5 kg 货物的可行槽不足 120，是物理可行域为空；AUTO regret=`NA_NO_FIXED_ORACLE`，不得排名。
- AUTO 不是“总最优”：S1 run-start pick 合计 TOB 130、AWRA 20、CB 30；多个场景有显著正 regret。正确口径是“固定阈值路由的受控 regret 测量”。
- E08/E09/E15 的 AUTO regret=0 是 AUTO 与事后 oracle 同选 CB；E15 仍非 headline。
- score policy 固定为 `fixed_default_v1_router_isolation`，明确不是自适应权重证据。

### 当前关键 SHA256（Task 6 封账时）

| 文件 | SHA256 |
|---|---|
| `sim/realism.py` | `9FA25EAEDDB751A2EEDB6536AC46ED3AE2561887C2C25CCCC133E3E860E6D6CE` |
| `sim/strategy_lib.py` | `8F219A767F0A691B4D032E93574ED734493C74D56010631B78470158E6B73FFD` |
| `sim/mixed_ops.py` | `7CA16F3EF833CAE1F77DFC1B5456CF9FA6A81F01A2C8D2FA47A4093E78432E74` |
| `sim/cargo_dynamic_matrix.py` | `3D7212CE520BF9F3A00B0DCB82EF85EA90EEF770CE09E4DE4FC9D863717EA846` |

验证：45 项 Task2~6 专项 PASS；最近完整 `core_smoke` 76 tests PASS；`py_compile` PASS；未开 AB、未跑 `ab_scripting`、未做 git 写。

## Task 7 · 45 trace 与证据隔离发布

时间：2026-07-15 04:34-05:05 -05:00  
状态：独立 `gpt-5.6-sol` critic 经三轮对抗审查后 **PASS**；正式 bundle 已生成并逐文件验收。

### 实现与边界

- 产出 `Tier 1/2/3 × uniform/skew/heavy × seq/near/score/awra/AUTO` 共 45 条；业务状态只消费 `sim.realism.run_tier3(..., event_sink=...)`，exporter 无第二套入库/出库循环。
- 每条均含 100 周期、101 份去重库存快照、连续 phase、七步过程与完整所有权链：`QUEUE→CARRIER_IN→RACK→CARRIER_OUT→OUTFEED→SCANNED→EXIT_MASK→CONSUMED`。扫码/退出后三段明确为 display-only，未计入 canonical performance time。
- 单 seed trace 为代表性回放；S1 多 seed 仅作独立 fixed-score router-isolation 证据。两者 `score_policy` 与 `freq_noise_sigma` 不同，45/45 均带 `s1_matrix_same_parameterization=false` 和 `ui_rule=do_not_compare_or_merge`。
- S1 身份读取无缓存：manifest/agg/detail/params 连续双读同一 bytes，hash 与解析同源；runner 文件及七函数 hash 重算；agg 144=18×8、detail 1440=18×10×8、params 11/11 逐值锁定。
- bundle 开始锁定一次 reference，45 条显式共用；构建后与紧邻发布前各重读一次。真实临时文件 `os.replace` 四类破坏、中途替换、同进程二次生成均有负测。

### 验收证据

- `python -m unittest l4_showcase.tools.test_s3_tier3_trace -v`：12/12 PASS；`py_compile` PASS。
- 正式产物：`l4_showcase/out/s3_trace_manifest.{js,json}` + `l4_showcase/out/s3_traces/*.js`，文件数 45。
- 逐文件复算：payload SHA 45/45、完整文件 SHA 45/45、comparability guard 45/45；组合覆盖无缺失/重复。
- 最大 trace 759,947 bytes（≤819,200）；总 trace 32,101,324 bytes；manifest JS 21,421 bytes（≤102,400）。
- critic 最终结论：未找到可复现阻断反例，允许进入 Task 8；未来若 S1 支持并发重跑，可再与 runner 共享读锁，当前冻结发布流程不阻断。

### 当前 SHA256

| 文件 | SHA256 |
|---|---|
| `l4_showcase/tools/generate_s3_tier3_trace.py` | `ED798D7AA7C1E6EFA3B19C543E1DEA75B02B0CC0181CA46ECB10CEE599DC74DC` |
| `l4_showcase/tools/test_s3_tier3_trace.py` | `15F30F8C9188FF4CC67AED23E026FC39B5115AC3E367BF959DC9F3B5C8032852` |
| `l4_showcase/out/s3_trace_manifest.js` | `45539852CF43BB1A51CC8B8BA61A2D3DAAB5C4D19C21E30FF16EF86AD2A79FCE` |
| `l4_showcase/out/s3_trace_manifest.json` | `B0E416A6C2E3A176E166C191B06D72C57AA5E1E55DE4100235DBF5ABAEE87844` |

### Task 8 发现后对 Task 7 的完整性加固（supersede 上表对应 SHA）

- `file://` 动态 script 不能读取原始文件 bytes；浏览器也不能可靠重算 Python JSON canonical hash（`1.0` 解析后折叠为 JS `1`）。因此保留原 `payload_sha256/file_sha256` 作离线审计，新增 `semantic_runtime_sha256_v1` 作浏览器实际对象内容验证。
- 跨语言编码按 type tag + uint32 长度 + UTF-8 + 对象 key Unicode code point 排序 + finite IEEE-754 binary64；`1`/`1.0` 语义等价，`+0/-0` 可区分；拒绝 NaN/Infinity、循环、函数、非纯 JSON 与超 JS safe integer。
- 正式 bundle 已重生；45/45 同时通过 file SHA、Python payload SHA、semantic runtime SHA。最大 trace 760,043 bytes；manifest JS 25,741 bytes。
- 新 SHA：generator=`C1D8F0BE3E33F467527CD23739F8D4FD37D655FF0F463988CBDE4AC3696758C4`；generator test=`EC51FE88025B9F827DA7BD164BF7375D90AAB5E7CBAAE2315A251CA12B5A1548`；manifest JS=`774FADF525EE47BA0BD7A1DCE7141E779C8696E1065C2C042BE49B3A67322C3C`；manifest JSON=`88B0F4EDB24BB2856704D99FFCF4341A12DAEA3ECAB4178E48B2E4BCFEE51799`。

## Task 8 · file:// trace store、3-entry LRU 与错误恢复

时间：2026-07-15 05:05-05:23 -05:00  
状态：独立 `gpt-5.6-sol` critic 经两轮攻击审查后 **PASS**。

### 实现

- 新建 `l4_showcase/src/s3_trace_store.js`：只通过动态本地 `<script>` 注册，不使用 fetch/CDN；manifest 强制完整 3×3×5、严格本地 entry 路径、三种 hash 格式和 sim/comparability 边界。
- 浏览器对注册后的实际对象重算 `semantic_runtime_sha256_v1`；trace 声明、manifest 声明、重算值三者必须相同，深层库存/phase/stats 任何改动都拒绝。
- LRU 最多 3 条；触碰更新新旧次序；淘汰删除全局注册并调用资源 disposer；destroy 主动 reject inflight、移除 hook/script/引用。
- 错误区显示 Tier/画像/算法、错误码和“重试该组合”；缺文件连续失败不污染 cache，修复路径后可恢复。重试按钮内部捕获 rejection，不制造 unhandled Promise。
- `do_not_compare_or_merge` 在 manifest、trace 校验与 `assertComparisonAllowed()` 三层强制；页面不得把 S1 fixed-score 聚合与当前回放 KPI 合并。
- URL 双门：允许本机 `file:`，以及与页面完全同源的 loopback HTTP(S)；拒绝外网/CDN、协议相对、凭据、UNC、跨端口/跨源。构造后篡改 `baseUrl` 仍在 append 前拒绝。

### 测试

- `node --check l4_showcase/src/s3_trace_store.js`：PASS。
- `node --test l4_showcase/tools/test_s3_trace_store.mjs`：14/14 PASS；其中实际按 `file:` URL 动态执行全部 45 条，LRU 最终仅 3 条、42 次 disposer。
- 攻击覆盖：深层内容保留旧 hash、坏声明 hash、坏 schema、重复注册、脚本互换、非纯 JSON、unsafe number、manifest 缺组合/远程路径、baseUrl 网络绕过、并发 4 条、destroy-inflight、真实空目录连续 3 次缺文件后恢复。
- Python trace 13/13 PASS；正式 45 条三 hash 复算 45/45。

### 当前 SHA256

| 文件 | SHA256 |
|---|---|
| `l4_showcase/src/s3_trace_store.js` | `DE20985D908BC79204A2C45A5239DC7C87AEAAA8347C1760FE07D36ECA5DF4EC` |
| `l4_showcase/tools/test_s3_trace_store.mjs` | `5040F9ED7D387EC410D858B9742D4A4970BDE51FF059C385B3FC396E09AECB49` |

## Task 9 · 单一页面状态机与 45 trace 原子切换

时间：2026-07-15 05:23-06:05 -05:00  
状态：独立 `gpt-5.6-sol` critic 首轮抓到冷启动固定目标后，修复并复验 **PASS**。

### 实现与同源边界

- 候选页删除旧 G016、固定目标、固定库存/排队箱、旧 story timeline、旧 trace 全局与内联 RAF；静态 HTML 只保留货架、I/O、机构、灯光和空的 loading skeleton。
- `s3_ac_runtime.js` 成为唯一 controller、唯一 `requestAnimationFrame`、唯一 `paused` 状态与唯一 `__S3_QA`；45 条业务 trace 全部经 Task 8 Store 动态加载、语义 hash 复算后才能 commit。
- checkpoint 锁定 `(seed, cycleIndex, operation, normalized progress, loop)`；故障拆段统一映射到 underlying operation，因此 T3 `FAULT_RECOVERY` 切到无故障 T1 时仍停在同周期/同作业相位，不回 cycle 0。
- `selection epoch` 实现 latest-only：A→B→C 乱序 resolve 只允许 C commit；旧请求成功或失败都不得覆盖当前 DOM/QA/error host。
- 库存由目标 trace 的 `inventory_before_ref/after_ref` 派生；queue 直接取 `queue_waiting_gids`；gid/grade 取当前 `cargo_catalog`；owner 取 phase/ownership lifecycle。切换时用目标 trace 一次性 reconcile，不复用上一算法库存。
- 出库货不在 I/O 突然消失：继续经过 `OUTFEED→SCANNED→EXIT_MASK→CONSUMED`；后三段明确 presentation-only，不进入 performance KPI。
- 固定镜头不随周期/算法切换；目标框、货物信标、左栏任务、轴位、地图和队列由同一 committed frame 驱动。

### 真实缺陷与闭环

- critic 首轮 **FAIL**：虽然 `TARGET` 已改为 `(0,0)` 占位，targetBox/targetPad 冷启动仍可见，慢加载时会短暂显示伪目标。
- 最终删除 `const TARGET`；两个目标对象创建后立即 `visible=false`，仅 `setTarget(frame)` 在 verified trace commit 后显示。
- 使用浏览器 CDP 暂停 `t3_skew_auto.js` 请求做真实慢加载复验：`targetBoxVisible=false / targetPadVisible=false / qaLoading=true`；放行后正常进入“已校验 · sim trace”。critic 复读当前磁盘并转为 **PASS**。

### 测试证据

- `node --test l4_showcase/tools/test_s3_runtime_contract.mjs`：9/9 PASS。覆盖 45 timeline、700 checkpoint group/trace、fault→no-fault、目标 trace 库存、A/B/C stale、destroy-inflight、20-loop 与 retired-state 静态门。
- 浏览器真实动态加载 45/45：`bad=[]`；第 12 周期 `INBOUND_TRAVEL` checkpoint 在全部组合保持；一次快速连切记录 `staleCount=1` 且无旧 commit/error。
- 45 组合把 queue mesh pool 预热到 6 后，20-loop：scene meshes `231→231`、inventory `120→120`、path `3→3`；控制台 error/warning 为空。
- 联合回归：Store 14/14、trace exporter 13/13、`core_smoke` 77/77 PASS；未开 AB、未跑 `ab_scripting`、未做 git 写。

### 当前 SHA256

| 文件 | SHA256 |
|---|---|
| `l4_showcase/src/样张_S3_三方向候选.html` | `5685EF05BECDAB357CA0EB59C57E9FBFA1457B3799D35319D4B4F30DA59E3661` |
| `l4_showcase/src/s3_ac_runtime.js` | `A0E642B14B59B48EF322D011DC4AC0059D770B764A05139B22E4ACBCA721EB00` |
| `l4_showcase/tools/test_s3_runtime_contract.mjs` | `AC8142F12A29D4FDFF54C6947CDD127D19D8BC01DAC2FDD53237609068C9AD0D` |

## Task 10 · 紧凑左栏、AUTO 理由与当前 trace 指标解释

时间：2026-07-15 06:05-06:34 -05:00  
状态：独立 `gpt-5.6-sol` critic 完整终审 **PASS**；主代理随后发现“CB/TOB 未明示路由额外策略”的规格遗漏，修复后 critic 增量复核再次 **PASS**。

### 实现与口径边界

- 左栏改为同一 DOM 的五区 grid：标题/选择器/当前任务/算法判定+轴位/规则图+洞察栈；不复制 mobile/desktop 组件，不新增 timer、RAF 或业务状态。
- 1366×768 使用 410~470 px 紧凑栏；≥1600×900 使用 600 px 高屏密度版，并把七步横条展开为带名称的纵向流程。两档只通过 CSS 重排同一节点。
- 算法判定显示启动时一次选定、`initial→online` 链、密度/偏斜/初始货量。AUTO 选中 CB/TOB 时明确写 `AUTO 路由额外策略`；手动下拉仍只有 SEQ/NEAR/SCORE/AWRA/AUTO。
- 仅当 `router.online_strategy=score` 时读取 `trace.meta.score_weights.online`。四项显示的是 α/β/γ/δ 权重与 `warehouse_sim.py:249-265` 公式，不是本周期候选分值；能耗项明确为归一化提升做功代理、不是 kWh。SEQ/NEAR 显式说明不调用四项评分函数，不渲染伪评分。
- 当前统计仅取 committed trace 的 `response_p50_s / response_p95_s / utilization / queue_peak_waiting / faults / violations`；固定标注“当前 trace · 单 seed · 不与 S1 合并”，运行时不读取 `stats.multi_seed_source`。
- 每个指标为原生 button，支持 mouseenter/focus/click/Enter/Space，Escape 关闭；`aria-describedby + aria-expanded + role=tooltip` 完整。trace 动态文本均用 `textContent`/节点构造，未拼接进 `innerHTML`。
- 逐帧仍只更新当前阶段、轴位、速度、队列和地图；判定、权重与 trace 统计只在 verified latest-only commit 后更新。

### 浏览器实测

- 1366×768：`workHud clientHeight=scrollHeight=768`，body 1366×768；十个关键 ID 各 1，全部 `bottom≤768`。tooltip 实测矩形 `(88.8,598.9)~(298.8,636.9)`，完整留在视口。
- 1920×1080：`workHud clientHeight=scrollHeight=1080`，body 1920×1080；十个关键 ID 各 1，全部 `bottom≤1080`。高屏不再把方形规则图/速度卡机械拉出巨型空白。
- 页面连续加载 45 组合：`loaded=45 / bad=[] / runtimeErrors=0`；暂停前后 checkpoint 的 seed/cycle/phase/progress 保持，误差小于 `1e-13`。
- 快速 A→B→C：结果 `stale/stale/committed`，最终仅 `t3_heavy_score` 提交，`staleCount=2`；统计和 tooltip 均来自 C。
- SEQ：weight grid=0 且显示“不调用评分函数”；SCORE：权重按钮=4；键盘 Enter 后 `aria-expanded=true`、Escape 后 `false`。浏览器 `dev.logs=[]`。
- AUTO 实测标题=`AUTO 路由额外策略 · TOB`；manual options=`seq,near,score,awra,AUTO`。

### 回归与独立审查

- `node --test l4_showcase/tools/test_s3_runtime_contract.mjs`：10/10 PASS（新增 Task10 静态契约门）。
- Store：14/14 PASS；trace：13/13 PASS；本 Task 首轮联合 `core_smoke`：77/77 PASS。
- critic 建议但不阻断：未来可补纯 DOM 的触摸 click/focus 事件测试；当前真实浏览器 click、Enter、Escape 路径已实测。
- 未开 AB、未跑 `ab_scripting`、未做 git 写。

### 当前 SHA256

| 文件 | SHA256 |
|---|---|
| `l4_showcase/src/样张_S3_三方向候选.html` | `3B6544751C893AE711D53583388CC2179B66622C7C584A0C72BE8BD1547920F2` |
| `l4_showcase/src/s3_ac_runtime.js` | `6E43BBF572BE465B8F5E9EB26330E3FC760BF32482334593FD84423413353E3B` |
| `l4_showcase/tools/test_s3_runtime_contract.mjs` | `86D8F5320D14A7367D65FAFAE219A461F8F274C1D214ACFA49F88ED9EF303493` |

## Task 11 · 3D 物理、固定镜头与可见出库闭环

时间：2026-07-15 06:34-07:40 -05:00  
状态：运动学/几何已由独立 `gpt-5.6-sol` critic 复算 **PASS**；候选页尚未替换定稿页。

### 实现

- 只保留一台固定相机，沿用 formal 运动学坐标系；最终 `pos=[31.923,-35.740,21.597] / look=[11,.5,8] / fov=36`，controls 禁用，不再提供多余视角。
- 货架、I/O、堆垛机、短出口线、蓝色扫码门和退出软帘同框；`OUTFEED→SCANNED→EXIT_MASK→CONSUMED` 由同一 trace owner 链驱动。
- 3D 与 2D 规则图同时显示 `(列+层) mod 3 = 0` 的 133 个斜纹预占位；活动物使用蓝系实色+琥珀轮廓，目标位保留琥珀语义。
- 目标/活动物 DOM 标签在 CSS transform 后按完整矩形钳位；固定相机补光随 camera，同一场景无第二套业务状态或 RAF。
- 新增纯函数 `cargoWorldPosition()`，3D renderer 与 Node 契约测试共用；货物不再在各阶段分别写坐标。

### 红队发现与修复

1. `fov=34` 时货架顶/底硬边距不足；量化后改为 36，保留完整机构和右侧场景占用率。
2. 1600×900 高宽组合首次出现左栏纵向溢出 132 px；新增 900~999 px 紧凑高度布局，实测 `clientHeight=scrollHeight=900`。
3. 原正弦货叉下沉使 STORE 后段和 RETRIEVE 前段出现 AABB 穿透；改为 `handleCargoDrop()` 单调分段，货叉与货物共享同一 drop。
4. critic 抓到 OUTBOUND 终点→SCAN 起点约 0.359 m 跳变；把 `OUTFEED.startY` 对齐 `IO.y`，货物中心偏置改为 `.05`、放取下沉改为 `.055`、出口 z 锁 `.55`。
5. 首轮截图普通 reload 命中旧缓存，仍带旧 `z=.63 / startY=-1.10`；该批判无效，最终流程强制 `Network.setCacheDisabled + Page.reload(ignoreCache=true)`。
6. 次轮截图接口返回 JPEG 字节却沿用 `.png` 后缀；该批再次判无效，最终改用 CDP `Page.captureScreenshot(format=png)`，逐张检查 8-byte PNG 签名与 IHDR 尺寸。

### 验收证据

- Node：`node --test l4_showcase/tools/test_s3_runtime_contract.mjs` = **13/13 PASS**；含 45×100=4,500 个 OUTBOUND→SCAN 边界连续性测试。
- 浏览器：STORE/RETRIEVE 共 202 个点，货物底面与货叉顶面净距恒为 `0.0025 m`；OUTBOUND→SCAN 实测距离 `1.7763568394002505e-15 m`；1600×900 无页面/左栏溢出。
- critic 独立复算：4,500 个 OUTBOUND→SCAN、INBOUND→STORE、RETRIEVE→OUTBOUND 边界最大距离均 0；货架中心与扫码/遮罩锚最大误差分别 `1.78e-15 / 4.44e-16`，结论 PASS。
- 联合回归：`python sim/core_smoke.py` = **77/77 PASS**；未开 AB、未跑 `ab_scripting`、未做 git 写。

### 当前 SHA256

| 文件 | SHA256 |
|---|---|
| `l4_showcase/src/样张_S3_三方向候选.html` | `4608103A4DF6B0A62872D7F314BFF2CA6AEE569657EAAA50E0BF57D91735A310` |
| `l4_showcase/src/s3_ac_runtime.js` | `BA25809B37BD29058EAA44B345BB311FB6E876E05059419A5EEA7FADA0595FCA` |
| `l4_showcase/tools/test_s3_runtime_contract.mjs` | `614D2347F24C8E878941D6C261CE398A1C64741D179E38464147FBD9E082A4BA` |

## Task 12 · 多视口 QA（用户视觉确认前置段）

时间：2026-07-15 07:12-07:40 -05:00  
状态：44 张截图与证据 manifest 已完成；按计划停在“用户视觉确认”门前，尚未启动完整 S1/S2/S3 或视频。

### 证据矩阵

- 四视口：1280×720、1366×768、1600×900、1920×1080。
- 十一工况：初始 AUTO、入口交接、远端高位、放货、取货、回口、扫码、退出遮罩、SEQ 切换、heavy 画像、真实 fault；共 44 张原生 PNG。
- 44/44：PNG 签名正确、像素尺寸匹配、候选页/runtime/test/trace manifest hash 已锚定、新几何缓存门通过、runtime error=0、document/rail overflow=0、可见标签越界=0。
- 四张 contact sheet 与逐图状态、gid/owner/坐标、机构坐标、DOM 边界、SHA256 均存于 `l4_showcase/out/s3_qa_0715/`；清单=`capture_manifest.json`，SHA256=`D907F613C97F74F09ADA7A6556C38C1D6837DC2A847E25A3D5CB1F78177FD25D`。
- 独立 `gpt-5.6-sol` 复算 44 张+4 张 contact 的 SHA/签名/尺寸/源锚，并目检 1600×900 的 03~08、11 原图，结论 **PASS、无阻断项**。非阻断观察：EXIT_MASK 透明帘在静帧中的对比偏弱，但阶段文本、货物 owner 标签、出口位置和现场 `EXIT` 标牌足以判别；本轮不为此改源并使 44 张证据失效。
- critic 同时确认工作区有一份 0714 既有 MP4；它不是本次 Task 12 产物，不得写成“本次已生成视频”，也不得写成“工作区从未有视频”。
- `样张_S3定稿.html` 未改，视频未生成；只有用户明确通过候选视觉后才进入计划中的完整 S1→独立 S2/S3→同镜头视频链。
