# 给 Claude Code 的接管提示词（S3 0716 冻结态）

请接管 `F:\abb_wh_work` 的 S3 三维展示任务，但**不要立即改代码**。

第一步，按顺序完整读取：

1. `F:\abb_wh_work\.claude\handoffs\2026-07-16-080158-s3-0716-freeze-and-claude-code-takeover.md`（当前唯一接管真相源）
2. `F:\abb_wh_work\.claude\handoffs\2026-07-16-014130-s3-task17-and-next-design-research.md`（前序背景）
3. `F:\abb_wh_work\research\0716\S3历次截图反馈恢复矩阵_0716.md`（以第 6—7 节为当前裁决）
4. `F:\abb_wh_work\l4_showcase\implementation-notes.md` 末尾 `0716 11:09` 节
5. `F:\abb_wh_work\_通信\codex_out\0716_S3冻结与ClaudeCode接管完整回执.md`

随后做只读核验：

- `l4_showcase/src/样张_S3定稿.html` SHA256 必须仍为 `b59954cb21ca2959886add1d28cb1a9ce32684bd76c5ff051744762b9a92bedb`；
- active fill release 应为 `d75a06b085f9b1aef9ed`；
- `l4_showcase/out/s3_layout_reviews_0716_v7/` 三份 QA 虽为 20/20，但**只是技术证据，用户已否决其信息架构**。

用户最新裁决必须逐字落实：

- “七步在做什么”不应常驻，应在鼠标移到具体阶段节点时再显示；
- “容量书签”术语不学术且重复；
- 上一轮最大错误是没有先给用户过目大体方案就直接编码。

因此你的本轮任务只有一个：**先提交 2—3 套宏观信息架构方案给用户选择，禁止修改 HTML/CSS/JS。**

每套方案必须包含：

1. 一张文字线框（1280×720 和 1600×900 各说明一次）；
2. 原“容量书签”的 3 个更学术、中文人话的候选名称，并说明推荐名称及使用位置；
3. 阶段节点的 hover / focus / click 显隐规则：默认只显示什么，展开后显示哪些七步目的、SIM 时长和演示时长；
4. 当前作业的唯一显示位置，明确删除顶部/底部中的哪一份；
5. 2D 容量图、3D 仓库、算法 Top3/分项、过程数据的主次关系；
6. 明确删掉的重复文案和模块；
7. 说明如何保留成熟实体 3D、同侧并行 T 形双链、0→267/133→400 的 single-source frame；
8. 风险清单：窄屏地图可读性、浮层遮挡、键盘可达性、自动 QA 假绿。

推荐你把方案控制在 3 套以内，差异应是信息架构而非换颜色。先向用户解释每套方案的取舍并请求选择；**不要生成候选 HTML，不要自行代拍板**。

用户选定后也不要马上编码：先把选定方案写成一页冻结规格（区域、术语、显隐状态、禁止重复项、截图验收、事实边界），再请用户第二次确认。只有二次确认后才进入实现。

实现阶段若获批，必须保留以下事实边界：

- 267 是可管理容量，133+267=400 总占用；
- 7 步来自 4 个原始 trace 阶段的几何/货叉阈值拆分，不是 7 个原生时间戳；
- SCORE 稳定/能耗代理共用“载重×层高”原始因子，仅权重不同；
- 本地证据是 hash-locked reproducibility snapshot，不是签名或抗篡改；
- fill-only 当前只证连续入库 SIM，不证出库闭环、PLC、Factory I/O 或现场运行；
- 临近满仓 Top3 可能少于 3，必须写“最多 3 个”。

红线：不开 AB；不跑 `tools/ab_scripting`；不启动 Factory I/O；不做 Git 写；不覆盖 `样张_S3定稿.html`；不改用户文档/canonical；不用“病例/病历”；不把旧 v1—v6 当当前证据。

在第一条回复中，请先用 5—8 行复述你理解的冻结状态和流程纠错，再给 2—3 套宏观方案。不要先报告“准备修改”。

