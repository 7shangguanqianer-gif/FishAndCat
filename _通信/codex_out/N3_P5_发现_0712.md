# N3-P5：README、操作卡与实际脚本/文件/流程一致性审计

## 0. 总裁决

P5 有新发现，不能判零：

1. 根 README 仍把旧 23 测试、已答完的官方群两问、旧六步可视化卡和旧 and H 数字当现行入口。
2. `plc/README_PLC.md` 的文件/对象计数、L4 操作、三页 HMI、能力边界均混有过期内容；特别是 17 Functions/15 FB/59 tests 被写成 15/12/23。
3. `tools/ab_scripting/README.md` 首段仍说 24 用例，审计仍说“两页”；实际脚本已是 59/0、46 对象、P1/P2/P3 三页。
4. `tools/visu_gen/README.md` 记录的是 7/10 两页 590 元素历史阶段；当前 as-built 是三页 505+134+67=706 元素。文件本身可保留，但必须标 archive/superseded。
5. 两份用户操作卡是最大操作风险：建工程卡在 23/24 间摇摆；可视化卡仍是手搭单页、23/0、六/七步、无管理员权限，且承诺 Score/AWRA 数字差和 δ=0 推荐变化，均不适用于当前工程。
6. Factory I/O 教具 README 的全部路径存在，且明确是 7/14-15 未来课程材料；本轮未发现断链。

全程只读，未运行 `ab_sync.ps1`、未打开 AB、未修改现有操作文档。

## 1. 根 `README.md`

### P5-F1【高】首页导航把历史操作卡当当前入口

- `README.md:26` 指向建工程卡并验收 `iPassed=23`；当前真相源为 `plc/06_PRG_Test.st:16-17` 的 59。
- `:27` 仍要求把 B1/B2 两问发官方群；canonical 已明确“待办清零”，B1/B2 在 `docs/canonical_assumptions.md:57-62` 已答。
- `:30` 仍指向旧六步可视化搭建卡；当前是三页 HMI + 基础12步/v4四镜头，且在线长跑未完成：`tools/ab_scripting/build_three_pages_result.txt:5-10,14-31`、`docs/画面三页制施工蓝图_0711.md:142-147`。
- 建议：首页把两张旧卡标“历史手工建工程/单页 HMI 卡，仅考古”，当前入口改为 `plc/README_PLC.md`、`docs/画面三页制施工蓝图_0711.md`、`docs/演示脚本v3_0711.md`。

### P5-F2【高】首页“活状态/真相源”契约本身已经失效

- `README.md:4-5,19` 称活状态只在 `现状与任务.md §5`；本轮已验证该节同时含旧 7.42/268/23/24 与新 8.40/230/59，不能继续单独承担真相源。
- `:39` 又称 canonical 是口径唯一真相源，但 canonical C 头部也尚未更新 H 锁。
- 建议明确优先级：**测试/当前 CSV/ST > canonical（修正后）> 现状状态段 > 日期化历史文档**；不再让“当前状态文档”和“canonical”互相宣称唯一。

### P5-F3【中】“全部产物可删/一键复现”范围过大

- `README.md:36-37` 把 `sim/out`/`sim/figs` 全部称可再生，并把 `run_all.py` 叫底盘一键复现。
- `sim/run_all.py:19-30` 实际只跑测试、主对比、07 向量、experiments、基础图、headline、doc_check；明确不跑 mixed_ops/analytic_bounds/energy/adaptive/realism/lifecycle 等专项。
- `sim/run_all.py:3-5` 自己却写“一键复现全部仿真产物”，与 `:29-30` 冲突。应改成“底盘产物”；专项产物需清单化命令，不能承诺删后只跑一条命令恢复全部。

### P5-F4【阻塞】数字速记仍是旧 and

- `README.md:47-48` 仍写 H=7.42/66.2/74.3/0.50；见 P4 canonical 裁决。此处同时要换当前 sim 数字和 PLC 默认边界，不能只换数。

## 2. `plc/README_PLC.md`

### P5-F5【高】文件清单/对象数量与当前源码不一致

- `plc/README_PLC.md:13` 写 03 有 15 Functions；实际 `plc/03_Functions.st:10-395` 共 **17** 个（新增 `FC_HeatColor/FC_BlendLight` 等）。
- `:14` 写 04 有 12 FB；实际 `plc/04_FB_Warehouse.st:11-1065` 共 **15** 个。
- `:16` 写 06 有 23 用例；实际 `plc/06_PRG_Test.st:16-17` 是 59。
- 文件表在 `:9-17` 到 07 为止，漏 `08_GVL_WeightPolicy_generated.st`；09 是备用探针、当前不在 `sync_st.py` MAP，也应在“备用/不入工程”栏说明。
- `:5` 仍说官方交付形式待确认；canonical `:61-62` 已答“工程+源码最好都附”。

### P5-F6【高】能力说明超过测试/画面实际

- `:27` 的 59 清单基本准确，但应注明 T25 是 **sum+静态权重+匀速**黄金向量，不是 H。
- `:35` 的“不触发看门狗”证据来自 AB PC 仿真，不是物理 AC500。
- `:36-37` 把“评分/演示 seed 同源”扩大成“报告数字与 PLC 演示互背书”，须收窄。
- `:38` 仍只说 T01-T18，和 59 总数不一致。
- `:43-44` 的存量冻结/画面锁错误已在 P4 独立否决。
- `:62-63` 写单周期200对“微秒级、余量巨大”，但 L4 三时钟无法测出微秒；改成“当前 AB 仿真未跨周期/未触发看门狗，真机裕量待测”。

### P5-F7【高】L4 章节同时给出已证伪操作和终局操作，容易照错

- `:68-78` 仍以“L4怎么跑”指导用户在 PRG 中包夹探针、记录 Avg/Max µs/CPU占比；`:79-82` 紧接着又说明该方法三时钟全灭、周期内 µs 不可测。
- 应把 `:68-78` 移到“历史失败方法，禁止再跑”，现行流程只留任务完成周期数。
- `:85` 引用根目录 `tools/ab_scripting/measure_l4b.py`，实际文件不存在；它在 `tools/ab_scripting/archive/measure_l4b.py`，A5 四组最终脚本则是 `archive/measure_l4c.py`。这是明确断链。
- `:83` “确定性指标，真机同值”过强；可写“状态机逻辑周期数预期同值，物理真机仍待测”。

### P5-F8【高】可视化章节还是旧单页/六步

- `:87-101` 仍把当前入口写成旧 `AB可视化搭建操作卡.md`、单页20×20和六步演示。
- 当前 as-built 是 P1=505、P2=134、P3=67，编译0错：`tools/ab_scripting/build_three_pages_result.txt:5-12`；17个动作件仍需在线长跑：`:14-31` 与 `docs/画面三页制施工蓝图_0711.md:147`。
- 章节应改成三页导航/当前演示脚本，并明确“GUI洗完成；在线长跑待验”。

## 3. `tools/ab_scripting/README.md` 与脚本

### P5-F9【高】同一 README 内 24 与 59 自相矛盾

- `tools/ab_scripting/README.md:3,10` 仍说全套跑24、绿灯24/0；`:17` 又正确写59。
- 实际 gate 是 `tools/ab_scripting/ab_sync.ps1:37-40` 的 59/0，应统一为59。
- `ab_sync.ps1:2` 注释还说40 cases，`:25` 控制台还说40 objects；实际 `sync_st.py:18-64` MAP 为 **46** 对象。脚本虽正确执行，但日志元数据会误导排障。

### P5-F10【中】画面审计/归档计数过期

- README `:21` 说 audit 两页；实际 `audit_pages.py:31-32` 已遍历 `VisuMain/VisuStats/VisuAdmin` 三页。
- README `:22` 写 archive 63 件；只读实数为65。计数不应硬编码，或标“约65”。
- `:44` 用 T24 的 iPassed=24 作为历史“新值铁证”可保留，但建议加“当时里程碑；当前总数59”。

### P5-F11【正面】主要入口和核心文件均存在

- `ab_sync.ps1`、`sync_st.py`、`run_test.py`、`run_probe.ps1`、`audit_pages.py`、`analyze_audit.py` 均存在；README `:17-22` 没有改名断链。
- `sync_st.py:18-64` 的 46对象 MAP 含 08 GVL；`:174-183` 有 NOT_IN_MAP 护栏。
- 由于本批次红线，本轮没有运行这些脚本；这里只确认文件/代码静态一致性。

## 4. `tools/visu_gen/README.md`

### P5-F12【中】内容是准确历史，但标题没有清楚说“已被三页构造替代”

- `tools/visu_gen/README.md:4-5,13` 的 477+113=590、45/0 是 7/10 两页构造历史，不是当前 as-built。
- 当前三页计数是 505+134+67=706：`tools/ab_scripting/build_three_pages_result.txt:5-9`。
- `:19-29` 所列探针/生成器文件均存在；`:36` 的备份文件存在，SHA-256 确以 `185D6D5E…` 开头，路径准确。
- `:38-41` 的“生成→GUI洗→在线长跑”三段律仍准确，且恰好证明当前工程不能只凭“构造完成”写“已验收”。
- 建议文件顶部加：`历史两页生成器档案；当前三页增量施工见 tools/ab_scripting/build_three_pages_0713.py`。

## 5. 两份用户操作卡

### P5-F13【阻塞】`AB建工程操作卡.md` 已不适合现行工程

- `2_你要操作/AB建工程操作卡.md:3-5,186-200` 全程以23为验收；`:256` 又说24，当前是59。
- `:196-197` 用旧 T19/T20/T22/T23 推出“仿真与实现闭环”，覆盖过强。
- 该卡是手工新建早期工程流程，未覆盖新增 08 GVL、17 Functions、15 FB、当前三页 HMI和管线。应标“历史手工 fallback”，若要保留现行版须按 `sync_st.py` MAP 自动生成对象清单，避免人工表再次漂移。

### P5-F14【阻塞】`AB可视化搭建操作卡.md` 的步骤在当前工程会得到错误结果

- `:3-5,49-52,230` 仍以“唯一施工图/23/0”自居；当前三页已建、59/0。
- `:36-52` 要人工重贴5类对象（标题却说4个），已被 ScriptEngine 管线和三页生成器替代。
- `:175-187` 允许直接改权重，却没有管理员登录；当前 `FB_ParamGuard` 会回滚。
- `:200-215` 的六/七步旧脚本有三处已知错误：Score/AWRA 同批不一定数字更小（`:209-210`）；δ=0 不保证推荐位改变/严格退化（`:213`）；报警/测试数仍是23（`:211`）。
- `:230-237` 验收标准因此不可用；当前入口应是 `docs/演示脚本v3_0711.md` 经 P3 修正后的版本，并等待三页在线长跑。

## 6. Factory I/O README【通过】

- `factoryio/lesson2_st/README.md:3-4` 指向的教程与教练任务书存在；`:8-9` 两个 ST 文件存在。
- 文件明确写“7/14-15 开课用”，当前 `factoryio/` 只有教具目录、尚无保存场景，与未来态一致，不构成断链。
- 本轮未验证 Factory I/O 连接或 UI 操作，只确认静态路径/状态。

## 7. 建议的操作文档优先级

1. 当前代码/测试：`plc/*.st` + `sync_st.py` MAP + `N_CASES=59`。
2. 当前三页结构：`build_three_pages_result.txt` + `画面三页制施工蓝图_0711.md`；在线长跑前仍是“构造完成、行为待验”。
3. 当前运行入口：`tools/ab_scripting/README.md` 修正后的59/46/三页版本。
4. 旧建工程卡、旧可视化卡、`tools/visu_gen` 两页 README：全部标历史/fallback，不再从根 README 作为现行入口直达。
