# 0715 S3 · 6h 视觉终验优化账本

开始：2026-07-15 12:31（America/Chicago）  
工作对象：`l4_showcase/src/样张_S3_三方向候选.html`  
目标：在不改变业务真相的前提下，将候选升级为截图可核、交互可验、无已知阻断缺陷的终验级 HTML。  
门控：用户视觉签收前不覆盖 `样张_S3定稿.html`；不开 Automation Builder、不跑 `tools/ab_scripting`、不做 git 写。

## 一、12-task 节奏

### 现在锁定的 Task 1~9

1. 固化真相源、不可改核心参数、可改视觉参数与终验矩阵。
2. 建立当前候选的多视口/多状态视觉基线与缺陷登记表。
3. 重审左栏信息架构、密度、层级与 ABB 操作台一致性。
4. 优化三维总览构图、可调镜头边界与关键动作可见性。
5. 深化货架、堆垛机、出入库设备、地面、材质和多点照明。
6. 校准多周期运动编排、货权连续性、速度和出入口消失逻辑。
7. 完善算法切换、AUTO 路由、同条件对比与货物实验呈现。
8. 完善标签避让、统计定义、键鼠交互、可访问性与异常反馈。
9. 优化离线可靠性、性能、缩放响应和可复现 QA 工具。

### 截图驱动的 Task 10~12

10. 动态任务 A：由中期多视口截图和独立 critic 的最高优先缺陷决定。
11. 动态任务 B：由连续回放、边界状态和低性能环境观察决定。
12. 动态任务 C：最终视觉抛光、全回归、证据封账与交付。

本轮交付目标明确为 HTML，6 小时不再机械切出 2 小时制作视频；视频仍受“用户先确认候选画面”门控。

## 二、Task 1 · 真相冻结

### 2.1 真相源优先级

1. 用户 0715 的最新直接反馈与选择。
2. `docs/Codex设计_S3多算法AUTO与货物实验补齐_0715.md`。
3. `docs/Codex任务_S3与导览重做规格_0714.md` 中仍未被 supersede 的运动学、几何、口径、美术真相。
4. `docs/canonical_assumptions.md`。
5. `tools/anim3d_formal.py` 的运动学/相机/灯光/配色黄金参照。
6. 当前 trace、manifest、运行时与自动测试的可复现事实。

### 2.2 后续指令已覆盖的旧条款

- 0715 设计规格已明确 supersede 0714“单目标静态循环、静态 KPI”的旧范围：当前必须使用真实 45 trace、多周期七步事件流与唯一货权状态机。
- 用户最新要求“提供统一预设镜头，同时可以自己调节镜头位置”，覆盖 0715 规格 §6.1 中“正常 UI 不开放 OrbitControls”的旧条款。现行口径是：**只有一个总览预设；允许受限 Orbit/Pan/Zoom；不增加自动切镜或第二业务视角；一键精确复位。**
- Factory I/O 垂直观察确认单巷道、单排/单货架面。当前双链只表示出入库物理工位，不得解释成第二排货架或第二套逻辑 I/O。

### 2.3 绝对不可改的业务真相

| 维度 | 锁定值/约束 |
|---|---|
| 坐标 | X=列、Z=层高、Y=纵深；`camera.up=(0,0,1)` |
| 结构 | 单巷道、单排 20×20 货位、单堆垛机；逻辑 I/O=(0,0) 左下外侧 |
| 预占位 | `(列+层) mod 3 = 0`，133/400 |
| 物理尺寸 | 单格 1m×1m×1m；货物位于合法格中心 |
| 轴参数 | VX=2.0m/s、AX=0.5m/s²、VZ=0.5m/s、AZ=0.3m/s² |
| 运动学 | 双轴同时启动、独立梯形/三角积分、快轴先到后钳位、慢轴定节拍；不得用样条伪圆滑 |
| 机构 | 桅杆 X 恒等载台 X；货叉只沿 Y 伸缩；不得抽回托板导致货物掉落 |
| 物理链 | `INFEED: ENTRY→LOAD`、`OUTFEED: UNLOAD→SCAN→EXIT` 仅为展示层，均映射同一逻辑 I/O=(0,0) |
| 货权 | `QUEUE→CARRIER_IN→RACK→CARRIER_OUT→OUTFEED→SCANNED→EXIT_MASK→CONSUMED`；任一 gid 同时只有一个 owner |
| 数据 | 45 trace、同 seed/周期/phase/progress 公平切换；页面不手抄 KPI，不改 trace |
| 算法 | 五 UI 模式与 AUTO 固定规则路由；CB/TOB 只作 AUTO 额外结果，不伪装常驻手动模式 |
| 口径 | 全部标 SIM；不宣称 400 位 Factory I/O/PLC 闭环、在线学习、自学习 AI 或“400格数字孪生” |
| 颜色语义 | 红=ABB 品牌细节/报警/退出遮挡；重量档保持蓝系，状态色不得冒充重量类别 |
| 信息架构 | 左侧同源操作台 + 右侧单一三维工作区；不拆成“三维页/数据页” |

### 2.4 可以优化的视觉/表现参数

- 左栏宽度、栅格、模块顺序、间距、字号、折叠层级和说明密度。
- 总览相机位置、焦点、FOV 与受限交互边界，但必须保持一镜同框与物理方向正确。
- 灯位、强度、阴影参数、环境补光、材质粗糙度/金属度、色差与背景层次。
- 地面视觉延展、缝线密度、设备外观细节、护柱/门架尺寸与不影响业务的站间视觉间距。
- 活动物轮廓、轨迹管径、DOM 标签候选位置和指向线。
- 演示时间压缩、默认倍率与速度控件样式，但不得改变 sim 时间或 checkpoint。
- 截图状态、QA-only 机位与证据布局；QA-only 机位不得变成第二个交付镜头预设。

### 2.5 终验矩阵

| 维度 | 硬门 |
|---|---|
| 支持视口 | 1280×720、1366×768、1600×900、1920×1080；1024×640 明确不支持，不偷报通过 |
| 基本布局 | 页面无滚动；左栏无大片空白；3D、遥测、标签、工具、字幕无相互遮挡 |
| 文字 | 关键正文/控件在 1280×720 不小于 9px，核心操作与状态不小于 10px；目标标签≥15px；不依赖缩放才能读懂 |
| 三维主体 | 默认总览下货架与作业区成为明确视觉主体；不得大面积无意义空白、裁切货架、丢失 I/O 或把双链缩成不可辨小点 |
| 五秒法则 | 5 秒内可识别：当前策略/Tier/画像、当前 gid 与动作、AUTO 结果/理由、下一步、SIM 边界 |
| 动画 | 七步连续，多周期无瞬移/双重货权/提前消失/下一货替换；速度可调且业务不变量成立 |
| 物理 | 桅杆/载台同步，货物/托板/门架/地面无碰撞穿模；连续 swept 检查通过 |
| 算法 | 五模式/三 Tier/三画像可切；同检查点切换；AUTO/CB/TOB 话术准确；比较不偷换 seed/状态 |
| 交互 | 镜头拖动、精确复位、速度滑块、暂停/重播、tooltip hover/focus/click、错误恢复均可见可验 |
| 证据 | 每轮截图必须绑定当前 HTML/runtime/test/QA generator/trace manifest 哈希；console error 和 bad response 均为 0 |
| 复核 | 主 agent 目检 + 独立 gpt-5.6-sol critic；任一可复现阻断项未闭合不得封账 |

### 2.6 Task 1 基线

- 当前证据：`l4_showcase/out/s3_task12_qa_0715/capture_manifest.json`，48/48、17/17、6,300 帧与 swept collision 已通过。
- 当前哈希：HTML=`1A6B0C…B2C6C7`；runtime=`84F54A…A1FE8B`；test=`E922D1…4435B`；QA generator=`02301F…D4BC8`；trace manifest=`88B0F4…51799`。
- 基线通过只证明当前功能闭环，不代表视觉已达终验。当前 1280 与 1920 截图仍需按 Task 2 重新做可读性、主体占比和信息层级审查。

Task 1 状态：**PASS（真相冻结完成）**。下一步：Task 2 基线审查。

## 三、Task 2 · 当前视觉基线与缺陷登记

审查输入：`out/s3_task12_qa_0715/` 当前 48 图、1280/1920 友好截图、候选最终 CSS、runtime overlay/QA 实现。主 agent 目检与独立 `gpt-5.6-sol` critic 结论一致：业务/运行时基线通过，但视觉发布门**不通过**。

### P0-1 · 主镜头主体占比不足

- 规格硬门：左栏 31%~34%；右侧三维主体横向占右视口 75%~92%；右侧大面积无信息空白为阻断。
- 当前 CSS：1280 下左栏固定 440px≈34.4%，1920 下上限 560px≈29.2%，两端均越界（`样张_S3_三方向候选.html:331,407-412`）。
- 当前截图：`1280x720_04_retrieve.png`、`1280x720_07_exit_mask.png`、`1920x1080_01_infeed_entry.png`、`1920x1080_04_retrieve.png` 中，货架+I/O 只约占右侧一半；作业机构和双链过小，右/上留白过多。
- 影响：评委第一眼看到的是空白与面板，不是“单货架 + 双物理链 + 当前动作”。
- 修复门：四视口、入场/取货/回程/扫码四态均满足左栏 31%~34%；三维主体 75%~92%；顶部安全边 4%~12%；货架、堆垛机、双链完整同框。

### P1-1 · 双物理链口径在正常总览下不够自解释

- 初始字幕有“仅展示层”（HTML:484），runtime 每帧改写后只剩“共用逻辑 (0,0)”（runtime:892）。
- 正常总览中 ENTRY/LOAD/UNLOAD/SCAN/EXIT 字样太小，双链只能在 QA-only 近景中快速辨认。
- 风险：评委可能误读成两套业务 I/O 或第二条业务通道。
- 修复门：恢复“展示层双物理链；共用逻辑 I/O (0,0)”；默认 1280/1920 总览不读小字幕也能区分 INFEED 与 OUTFEED，但区域标签不能遮挡交接点。

### P1-2 · 目标标签字号与 3D 遮挡契约不合格

- 规格要求目标标签正文≥15px，不能遮目标格、载货台或出口交接点。
- 当前最终样式 `#targetBeacon b=12px`（HTML:208-210），低层目标时黄色标签覆盖目标附近货格。
- 当前自动 QA 只避免标签与标签/HUD 重叠，未把目标格、载台和交接点投影矩形加入障碍。
- 修复门：正文≥15px；四视口×12 状态中目标格、载台、扫码交接点投影区域均不与标签矩形相交；详细信息可移到次行或左栏。

### P2-1 · 720p 微缩字导致功能“存在但不可发现”

- 右下交互说明和倍率端点为 7px；多处左栏次级标签为 6~8px（HTML:224-275,342-404）。
- 当前 1280 截图中，三种鼠标操作、0.25×/2.00×、统计样本说明无法在 100% 缩放可靠辨认。
- 修复门：首要控件/状态≥10px；一级指标标签≥9px；非关键解释改为折叠或 hover/focus，不再靠全面缩字塞满。

### 非阻断但列入后续观察

- 货架与背景整体偏淡；目前不足以单独判失败，但放大主体后需复核格口、占用货物、预占斜纹和机构是否仍有稳定层次。
- 当前功能证据没有显示货权、连续碰撞、复位、SIM 标识或红色语义错误；这些不重复返工，只做回归守门。

### Task 2 裁决

**FAIL（预期失败，问题已精确定位）**。这不是停止条件，而是 Task 3~5 的输入。独立 critic 的三个最高优先级顺序：①构图/左栏宽度；②双链语义；③目标标签与 3D 避让。主 agent 同意，不做无关改动。

## 四、Task 3 · 左栏信息架构与 ABB 操作台可读性

### 进入前计划

- 不同时调镜头，先固定左栏占比和真实可用宽度，避免 Task 4 相机反复返工。
- 保留五秒内必须常显的当前任务、三选择器、AUTO 结果/理由、货位图、队列、统计和七阶段。
- 把“路由画像参数 + 四项候选位权重”降为第二层原生 `<details>`；它们仍可访问，但不再靠 6px 微缩字常驻。
- 对照本地成熟的 `FactoryIO三课过程教练_20260711.html`：白色卡、灰分隔、黑字、单一黄/红状态强调、清晰目标尺寸；同时采用 W3C WCAG 2.2 2.5.8 的 24×24 CSS px 最小目标门。

### 实施

- 左栏统一为 `32.5vw`，四个支持视口都落在锁定的 31%~34% 内；删除 1280 固定 440px 与 1920 上限 560px 造成的两端漂移。
- 将路由细节改为原生 `<details class="decisionAdvanced">`：AUTO 选择结果和理由常显，画像参数/权重按需展开；Esc 关闭并把焦点返回 summary。
- 入口队列 3D 仍显示最多 4 件，左栏只显示前 2 件 +N，减少狭窄列里的 7px 芯片；业务队列和 3D 货权未改。
- 一级文字与控件不再低于 9px：任务 12px、选择器 10.5px、操作按钮 9.5px、规则/统计/阶段 9px、目标标签 15px。
- 选择器/按钮实际高度 26~28px；tooltip 字号提高到 9.5px。
- 运行时字幕恢复并强化为“SIM 单排单巷道 · 展示层双链 … · 共用逻辑 I/O (0,0)”。

### 客观验证

- 1280×720：左栏 416px=32.5%；document 1280×720；rail `clientHeight=scrollHeight=716`；无滚动/溢出。
- 1920×1080：左栏 624px=32.5%；document 1920×1080；rail `clientHeight=scrollHeight=1076`；无滚动/溢出。
- 路由详情真实点击后弹层 `[15.8,318.25,399.4,412.25]` 完整位于左栏；Esc 后 `open=false` 且焦点回到 SUMMARY。
- `node --check s3_ac_runtime.js` PASS；`node --test test_s3_runtime_contract.mjs` **17/17 PASS**。
- 当前证据：`l4_showcase/out/s3_6h_qa_0715/task03/`，含 1280/1920 两张当前哈希截图与 README。
- 截图同时再次确认 Task 4 P0 仍存在：3D 主体约占右视口一半，未偷报关闭。

Task 3 状态：**PASS**。下一步：Task 4 调整总览构图，并新增主体投影占比的机器断言。

### Task 3 独立复核

- `gpt-5.6-sol` critic 对当前磁盘、两张证据图与 17/17 回归独立复核：**PASS**。
- 确认无左栏截断/重叠/口径漂移，原生 `<details>` 没有掩盖一级信息，720p 与 1080p 的 32.5% 左栏均成立。

## 五、Task 4 · 相机、构图与关键动作可见性

### 进入前计划

- 不再靠目测试相机：先把货架、双工位、入口队列三个真实世界区域分别投影，再用屏幕矩形并集计算主体占比与安全边。
- 以 1280×720 和 1920×1080、入场/取货/扫码离场三态为最小截图矩阵。
- 不改 trace、运动学、I/O、单排单巷道或货架容量；保留一个总览预设和受限自由观察。

### 实施

- 相机从 `pos=[37,-43,24.5], look=[12.8,-1.2,8.6], fov=38` 调整为 `pos=[31,-36,21.5], look=[9.8,-1,9], fov=34`。
- runtime 新增 `projectWorldBox()` / `compositionSnapshot()`；明确避免“先把不相连区域 union 成三维 Box3”造成不存在角点与虚假裁切。
- 默认视图新增两个随相机投影的工位标签：INFEED（ENTRY→LOAD）与 OUTFEED（UNLOAD→SCAN→EXIT）；两者进入主动态标签的避障障碍集合。

### 客观验证

- 1280/1920 主体横向占右视口 `78.667%`，门槛 75%~92%；顶部安全边 `4.888%`，门槛 4%~12%。
- 左/右/底安全边分别 `9.078% / 12.255% / 8.139%`；货架、双工位、队列三块投影全部 `inside=true`。
- 1280 三态全部：动态标签与工位/HUD 重叠=0；标签越界=0；碰撞=0；地面越界=0。
- 人工扰动 camera/target 后复位逐值相同；Orbit/Pan/Zoom 仍启用且距离限于 15~56。
- 证据：`l4_showcase/out/s3_6h_qa_0715/task04/` 六张截图 + README。
- `node --check` PASS；Node 契约回归 **17/17 PASS**。

Task 4 当前状态：**主 agent PASS，等待独立 critic 后封账**。下一步并行启动 Task 4 独立复核；主线进入 Task 5 材质、照明、地面与设备细节。

## 六、Task 5 · 材质、照明、地面与设备细节

### 进入前计划

- 保持货架为纯竖直背景平面，不用斜撑和装饰件掩盖几何问题。
- 只优化背景地坪、空/预占/在库格、单桅杆设备、主辅光四层；每层同时过截图与业务回归。

### 实施

- 灰阶层次重做，货物分级改为深蓝/工业蓝/青绿；琥珀只承担目标/活动描边，报警红只承担设备状态。
- ACES exposure 1.08→0.78，降低环境填充并加强定向主光，减少“柜格与阴影同灰、整幅洗白”。
- 地坪改为围绕 `(x=10,y=IO.y=-.75)` 对称，地缝也按巷道中心成对布置；加入低对比外边界。
- 单桅杆补导轨、线缆链、维护横档；驱动柜补门缝/状态条/铭牌；货叉补叉尖。所有细节沿既有 X 同步，不新增业务设备。

### 客观验证

- 同帧右视口图像统计：亮度标准差 +5.1%，平均饱和度 +4.6%，边缘强度 +24.4%，亮度均值下降 5.5%；视觉层次提升不是仅改文案。
- 三态 scene mesh=287；碰撞=0、地面越界=0、货权唯一；标签无越界；构图仍为 78.667% / 顶边 4.888%。
- `node --check` PASS；Node 契约回归 17/17 PASS。
- 证据：`l4_showcase/out/s3_6h_qa_0715/task05/` 三张同预设截图 + README。

Task 5 状态：**主 agent PASS，待独立 critic 串行复核**。下一步：Task 6 多周期运动、货物归属、交接时序与出场闭环。

## 七、Task 6 · 多周期运动、货物归属、交接时序与出场闭环

### 进入前发现

- `CONSUMED` 是 trace 既有事实，但画面没有保持段；遮罩后的货物消失虽逻辑正确，视觉上仍可能被误解为瞬移。
- `#cycleReset` 已存在却从未由 runtime 激活；100 周期整段回放重置会裸露。
- 第一周期的短行程 1× 仅 0.43~0.88 秒，低于可靠观察窗口。

### 实施

- `SCAN_EXIT` 增 0.36 display-s 的 `CONSUMED` 保持；出口标签逐态显示扫码、遮罩、已离场和 gid。
- travel/handle 增加 0.32/0.36 display-s 展示下限；只拉伸画面时间，标记 `presentationScaled`，不改真实轨迹、sim 时钟或 KPI。
- 启用 100 周期整段回环遮罩；遮罩只在 `loop>0 && elapsedLocal<0.28` 时出现。

### 客观验证

- Node 回归升为 **18/18 PASS**；遍历 45×100 周期，共 4,500 个 CONSUMED 帧，货权和 snapshot 全部一致。
- 浏览器 299 个交接边界检查 PASS，碰撞/地面越界/runtime error 全为 0。
- 负结果保留：首轮把下一周期 owner 错锁为 `CARRIER_IN`，47 次实际为合法 `QUEUE`；修正验收模型后全绿，没有为了绿测删掉真实排队阶段。
- 四张证据：`l4_showcase/out/s3_6h_qa_0715/task06/`。

Task 6 状态：**主 agent PASS，待独立 critic 串行复核**。下一步：Task 7 算法切换、AUTO 路由、同场景对比与货物实验表达。

## 八、Task 4 独立驳回与重验（旧结论已被替代）

### 独立复核驳回

- `gpt-5.6-sol` critic 对首轮 Task 4 给出 **REJECT**：旧 `compositionSnapshot()` 无条件计入潜在 4 箱队列包络，空队列截图却报告 78.667%，属于证据口径失真；实际固定主体约 72.3%。
- 另两项有效缺陷：站点标签只做边界 clamp、没有对极端自由镜头验证 HUD 避让；旧六张 PNG 没有与截图生成器和源文件哈希绑定。
- 因此本账本上文 Task 4 的“主 agent PASS”和 78.667% 不再作为验收结论，只保留为失败过程证据。

### 修复

- 构图证据拆为 `fixedSubject`、`visibleSubject`、`maxQueuePressure`：验收只使用当前真实可见的货架、双工位和可见队列实体；潜在满队列包络只作为压力边界单列。
- 默认镜头二次校准为 `pos=[31,-36,21.5] / look=[9.8,-1,9.4] / fov=32.5`。空队列真实可见主体宽度 75.756%，顶部安全边 4.333%，均在 75%~92% / 4%~12% 门内。
- 站点标签和动态标签共用候选位求解器；遥测、规则徽标、控制条、底部说明、两站点标签和锚点呼吸区进入避让，固定 HUD 外扩 8px 安全边。
- 将小于 0.5px² 的设备像素量化误差归零；实际 DOM 矩形仍要求零像素交叠。诊断曾捕获 0.0777px² 的 8px 安全边浮点误差，未隐瞒。
- QA 生成器新增源文件/运行时/测试/生成器/trace manifest 与每张 PNG 的 SHA-256；新增 `CONSUMED` 回执帧、四组极端镜头和最坏帧诊断。

### 重验结果

- Node 契约：**18/18 PASS**。
- `l4_showcase/out/s3_6h_qa_0715/task04_rerun/capture_manifest.json`：4 分辨率 × 13 状态 = **52/52 PASS**。
- 全业务扫描：6,300/6,300 帧标签、构图、货权、碰撞、地面和 runtime error 全绿；四组极端自由镜头标签/HUD 全绿；LOAD_IN/SCAN_EXIT 2,000 点连续碰撞扫描全绿。
- 人工查看 1280×720 的入库交接/存货/CONSUMED 与 1920×1080 存货图：货架、单桅杆、双展示链和关键标签完整入镜，标签没有互相覆盖；1920 左栏统计卡纵向留白列入 Task 10 视觉压缩项，不在 Task 4 偷报完美。

Task 4 重验状态：**主 agent PASS，已交同一独立 critic 二次复核，等待裁决**。

### Task 4 独立二次裁决

- 同一 `gpt-5.6-sol` critic 已对当前磁盘、重验 manifest、52 张 capture 与 4 个 alias 重新核验：**PASS**；P0/P1 均为 0。
- critic 重算当前五项输入哈希及 60 次 PNG/alias 哈希，0 不匹配；确认 `visibleSubject` 只计真实可见实体，空队列时与 `fixedSubject` 相同，满队列只列为 `maxQueuePressure`。
- critic 建议把四个极端自由镜头也输出成独立 PNG；该项转入 Task 12 证据增强，不回退 Task 4 构图。

Task 4 最终状态：**独立 PASS，封账**。

## 九、Task 7 · S1 受控对比、AUTO 路由与货物实验矩阵

### 进入前的真相裁决

- 当前 45 条 S3 trace 明确带有 `s1_matrix_same_parameterization=false` 与 `ui_rule=do_not_compare_or_merge`；因此禁止把 S3 代表性回放拼成算法排行榜或 regret。
- 可用于同场景算法对比的真相源是 `sim/out/cargo_dynamic_matrix/s1/`：18 情景 × 8 模式 × 10 seeds（2026..2035），每格 100 周期，SIM-only、固定评分权重。
- S1 与 S3 允许并列解释但不池化：S1 回答“同一受控条件下各策略如何”，S3 回答“当前代表性事件流怎样运动”。AUTO 的选择是确定性规则阈值、启动时一次选定，不是运行中逐货在线学习。

### 实施

- 新增 `l4_showcase/tools/generate_s1_comparison_evidence.py`，从 S1 aggregate/params/manifest 生成 `l4_showcase/out/s1_comparison_evidence.js` 与 canonical 原文 `s1_comparison_evidence.canonical.json`。
- bundle 固定 144 单元、18 情景、8 模式、10 seeds，并记录 aggregate/params/manifest/generator 四项源哈希及 payload canonical SHA-256。
- 左栏常驻四条迷你比较仅显示当前画像映射的 SEQ/NEAR/SCORE/AWRA-LS；条长与数值均来自 S1，明确标 `10 seeds · SIM · 短条=低 P95`。
- “对照证据”抽屉提供两个正交视图：同情景看八模式；同算法看十八情景。响应 P95、95%CI 半宽、可行数、fail/viol/reject、picked_counts、选择稳定率均保留原始语义。
- E15 强制显示“扩展 Tier 3 压力情景（非 canonical G2、非 H）”；AUTO regret 说明限定为同 `(scenario, seed)` 可行固定策略最小 P95 的事后 oracle，不声称运行前已知。
- 18 情景已经覆盖 uniform/skew/heavy、tiny/sparse/dense、hot-heavy/hot-light/bulky-heavy、near-limit、illegal-mix、扩展压力、噪声、MTBF 和画像漂移；本轮不为“显得丰富”重复造实验。

### 负结果与修正

- 生成器首版在本机 Python 3.9 对 `Path.write_text(..., newline=...)` 报参数不支持，改为 `Path.open(..., newline="\n")` 后生成成功。
- 新契约首跑 18/20：一处是假设 JS `JSON.stringify` 与 Python 对 `0.0/0` 的 canonical 数字序列化完全相同；另一处是 VM 跨 realm 数组原型导致 `deepStrictEqual` 假失败。未删除哈希门，而是新增 canonical 原文字节文件，改为“原文字节哈希 + 解析后与运行 bundle 等值”的双门；跨 realm 统一转为本地数组。

### 客观验证

- Node 契约升为 **20/20 PASS**：144 单元、源哈希、canonical hash、E15 三字段、两轴顺序、S1/S3 禁止混算均有断言。
- `l4_showcase/out/s3_6h_qa_0715/task07/capture_manifest.json`：4 分辨率 × 16 状态 = **64/64 PASS**；每张图绑定 PNG SHA-256。
- 同一 manifest：6300/6300 全周期帧、2000 点 LOAD_IN/SCAN_EXIT 连续扫掠、100 周期队列压力、四极端相机、镜头复位、倍率不改 KPI、hover 释义、页面错误/坏响应均全绿；最小扫掠净空 `0.0624999849 m`。
- 证据主哈希：HTML `5A533108…F32F7`；runtime `E5AC8510…F6687D`；test `D450E7D1…A1A90`；S1 bundle `1BB50E7F…4F4DE`；canonical payload `AA360D4C…FC5F0`。
- 主 agent 目检 `1280x720_14_s1_same_scenario_e02.png`、`1280x720_16_s1_e15_downgraded.png`、`1920x1080_15_s1_same_mode_auto.png`：两轴、E15 降级、SIM/S1/S3 隔离均在首屏可读；抽屉无水平溢出、无遮挡。
- 客观遗留：1920×1080 正常视图的 `TRACE 统计` 因 `1fr` 拉伸产生过大纵向留白；列入 Task 10 信息密度重排，未在 Task 7 偷报视觉完美。

Task 7 状态：**主 agent PASS，交同一独立 `gpt-5.6-sol` critic 复核；主线进入 Task 8**。

## 十、Task 8 · 键盘、释义、错误恢复与可访问性

### 进入前计划

- 不改运动学、trace 或 KPI；只处理焦点、读屏语义、控件目标尺寸、tooltip、加载/错误反馈和 reduced-motion。
- 将对比抽屉按真实 modal 行为补齐：背景不可交互、焦点不逃逸、方向键切 tab、Esc 关闭并回到触发按钮。
- 用不存在的 manifest 组合主动触发失败，再切到有效 trace 恢复；不靠静态 aria 字符串自证。

### 实施

- 对比抽屉增加 `aria-describedby`、tab 的 `aria-controls`/roving `tabindex`、可滚动表格的键盘入口；打开后 18 个背景区域设 `inert`，关闭后完整恢复原 `aria-hidden`。
- 实现首尾 `Tab/Shift+Tab` 环回，`←/→/Home/End` 切换两种对照轴，Esc 关闭并把焦点返回原触发器。
- 加载状态为 polite `role=status`，错误为 assertive `role=alert`；`runtimeControls` 同步 `aria-busy`，错误仍保留“重试该组合”。
- 统计 tooltip 保持 hover/focus 可见、Esc 关闭；range 与所有表头帮助按钮的目标尺寸不低于 24 CSS px。
- `prefers-reduced-motion: reduce` 下关闭状态徽标、回环遮罩和 tooltip 的非必要 transition，不停止用户主动要求的 trace 回放。

### 首轮负结果与修正

- 首轮 QA 正确失败：五个对比表头按钮视觉文字只有约 10.625px 高，最窄“可行/护栏”为 17px，虽有完整 aria 释义仍不满足 24px 目标。修为 `min-width/min-height:24px`，未放宽门。
- tooltip 焦点在 1280 的最后一帧留存到下一 viewport，污染 1366/1600/1920 的前七张图；每个截图状态开始前显式释放旧焦点，仍只在指定 tooltip 状态要求其打开。

### 客观验证

- Node 契约升为 **21/21 PASS**。
- `l4_showcase/out/s3_6h_qa_0715/task08_rerun/capture_manifest.json`：4 分辨率 × 18 状态 = **72/72 PASS**；`accessibilityComplete=true`，其余 6300 帧、碰撞、构图、镜头和网络门未退化。
- 常规界面 21 个可交互目标、对比抽屉 31 个可交互目标：低于 23.5px 的目标均为 0；重复 ID=0。
- 打开抽屉后焦点=`closeComparisonLab`、背景 inert=18；Shift+Tab 从首项环到末项、再 Tab 回首项；方向键后焦点与选中轴均为 `comparisonModeTab`，表格 18 行；Esc 后焦点回 `openComparisonLab`、残留 inert=0。
- 故意请求 `t3_skew_missing` 后 assertive alert 出现且含 1 个重试按钮；加载有效 `t3_skew_near` 后 alert 隐藏、`aria-busy=false`，再恢复 AUTO/skew。
- reduced-motion 下 `cycleReset` 与 `actionBadge` transition 均为 `0s`；hover/focus tooltip 真实可见且 Esc 后 `aria-expanded=false`。
- 目检 `1280x720_18_metric_tooltip_focus.png`：定义框在左栏内完整可读、未遮挡 3D；对比抽屉 1280/1920 仍无截断。随后把 Task 17 截图动作改为真实键盘 Tab 聚焦，最终 Task 12 会以最新 generator 重跑并绑定新哈希。

Task 8 状态：**PASS**。下一步：Task 9 离线资源、性能、响应式与 QA 证据加固。

## 十一、Task 9 · 离线交付、性能稳定性与确定性

### 进入前计划

- 正式支持窗口严格按规格限定为 1280×720、1366×768、1600×900、1920×1080；候选明确 `min-width:1080px`，不把未经设计的手机布局包装成“响应式”。
- 核验 HTML 只加载项目内 six-pack：Three.js、OrbitControls、S3 manifest、S1 evidence、trace store、runtime；运行期 trace 也必须来自同源本地 manifest，禁止 fetch/CDN。
- 以固定 AUTO/skew/STORE_HANDLE 帧执行 1,400 次 seek 压力，前后比较 DOM、draw calls、geometry、texture、program、scene mesh、库存、路径与队列池；另以两次 canvas PNG 字节哈希验证确定性。

### 首轮负结果

- `task09_first/capture_manifest.json`：除性能外所有门通过；draw calls=430，超过 350 预算；压力后 `sceneMeshes 289→295`、`geometries 195→204`、`stockPool 0→1`、`queuePool 1→4`。
- 根因一：120 件在库货物逐件绘制“箱体+绿色描边”，库存本身约占 240 draw calls。
- 根因二：入口队列最多四件，但初始只构造一件；压力帧首次遇到较长队列时懒创建其余模型，形成场景图增长。

### 实施与第二轮负结果

- 将库存改为动态实例绘制：一个绿色外框底片 + heavy/mid/light 三个等级色内片，共四个批次；gid、col/tier、等级色及绿色在库语义均保持。
- 对库存快照加内容签名；只有 gid/货位/等级真正变化时才重建 instance matrix，避免每个 RAF 重复更新 120 件静态库存。
- 队列预建四个展示单元。第二轮 `task09_rerun` 的 draw calls 已降至 194、seek 提升到 155.12 帧/秒，scene mesh/库存/队列池均稳定；但 geometry 仍 `195→204`，因此仍判红。
- 第二根因是四个队列单元虽已预建，初始不可见单元的独立 Box/EdgesGeometry 尚未上传 GPU；首次显示三件时各上传三份，净增九。修为四单元共享唯一托盘、箱体、描边几何与材质，未修改性能阈值。

### 最终客观验证

- Node 契约：**22/22 PASS**。
- `l4_showcase/out/s3_6h_qa_0715/task09_final/capture_manifest.json`：4 分辨率 × 18 状态 = **72/72 PASS**；离线、自包含、可访问性、确定性、构图、相机、碰撞、全帧与页面错误门全部为 true。
- 离线门：所有请求均为本机 `127.0.0.1` 临时静态服务器，外部 request/resource 均为 0；DOMContentLoaded 约 0.90s，ready 1.306s。
- 性能门：draw calls **430→194（-54.9%）**；triangles 16,507；120 RAF 采样约 129.19fps；1,400 次 seek 用时 8.889s（157.50 次/秒）。
- 长跑前后完全恒定：DOM 618、draw calls 194、triangles 16,507、lines 48、geometries 195、textures 8、programs 9、sceneMeshes 179、inventory 120、path 3、stockPool 0、queuePool 4、runtimeErrors 0；GC 后 heap 增量 9.49MB，在 16MB 门内。
- 固定帧两次画布 PNG 字节完全一致，SHA-256 均为 `D5406E68F4B74C7D88496A47F5C30949BABF37BAABFB1F3F9FE6E5668676E423`。
- 目检 1280×720 与 1920×1080 STORE_HANDLE：等级色和绿色在库边框均清晰、目标黄与活动货物蓝仍有优先级，实例化未造成视觉退化；Task 8 的键盘焦点截图也已随最新生成器重生。
- 保留 `task09_first` 与 `task09_rerun` 两份失败 manifest，证明最终通过来自两次真实修复，而非删项或放宽预算。

Task 9 状态：**PASS**。下一步：Task 10 评委阅读路径与 1920 高窗信息密度重排。

## 十二、Task 10 · 评委阅读路径与高窗信息密度

### 进入前缺陷

- 1920×1080 中右侧 `TRACE 统计` 继承 `minmax(0,1fr)`，六个数字被拉成三行巨型空白卡；虽然数值正确，但信息层级失真。
- 不能用新 KPI 填空，也不能把 S1 多 seed 与 S3 单 seed 混成第二数据源。

### 实施

- 1600×900 与 1920×1080 下让六项 trace 统计回到自然高度；1280×720、1366×768 保持原紧凑布局。
- 用剩余高窗空间加入“本屏证据链”，三段均读取当前 trace：①策略（AUTO 路由链/固定策略）；②动作（当前 STEP、gid、货权）；③验收（P95、违规、模型故障，并明确 S3 单 seed / S1 10 seeds 另列）。
- 证据链不含独立计时器、不访问 `stats.multi_seed_source`，不生成新业务状态；只把现有 decision/frame/stats 以评委阅读顺序重排。

### 验证

- Node **22/22 PASS**。
- `l4_showcase/out/s3_6h_qa_0715/task10/capture_manifest.json`：**72/72 PASS**，性能、离线、碰撞、标签、构图、确定性和可访问性均未退化。
- 主 agent 目检 1600/1920 STORE_HANDLE：原大面积统计空白消失，三段证据链形成“为什么选→正在做→如何验”的垂直路径；1280 仍为原六项紧凑统计，没有新增滚动或挤压 3D。

Task 10 状态：**PASS**。

## 十三、Task 11 · 语义纠错、故障边界与视觉统一

### 逐状态审查发现

- `RETRIEVE_HANDLE` 与 `OUTBOUND_TRAVEL` 仍统一写“目标货位”：前者实际是取货源位，后者货物已经离架返航，不应继续显示货架目标。
- trace 含 1 次 sim 模型故障，但既有 18 状态截图没有故障恢复画面，故障显示仅靠统计数字，无法证明边界态可读。
- 活动货物标签继续显示原始 `CARRIER_IN/CARRIER_OUT`，与左台中文层级不统一；非活动入库链写 `DISPLAY`，含义弱。

### 实施

- 入库三段标 `入库目标`；LINK/RETRIEVE 标 `出库源位`；OUTBOUND/SCAN 不再保留已完成的货架信标，slot map 同步取消过期黄格。
- 活动货物标签改为 `载货台·入库/出库` 等中文货权名，同时保留 HEAVY/MID/LIGHT 工业等级码；入库展示链待机改为中文。
- 新增 `seekFault(index)` QA 定位器，只在测试接口中定位真实 `FAULT_RECOVERY` segment；界面显示红色“SIM 故障恢复 / FAULT 01 / 当前动作保持位”，并把该块加入动态标签/HUD 避让。
- 新增 19 号故障状态，四分辨率各一张；常规帧明确要求故障条隐藏。

### 负结果与修正

- 新 Node 断言首跑 22/23：测试作用域漏读 `runtimeSource/htmlSource`，补齐后 23/23；运行时代码未改以迎合该失败。
- 首轮 76 图只有四张 fault 的 `faultStateCorrect` 红；画面无重叠/越界。根因是审计误要求 checkpoint.phase=`FAULT_RECOVERY`，而既有跨 trace checkpoint 规范会投影为底层 `INBOUND_TRAVEL`。保留规范，改由 renderer 的真实 `lastFrame.phaseName/faulted/faultIndex` 验证。

### 最终验证

- Node **23/23 PASS**。
- `l4_showcase/out/s3_6h_qa_0715/task11_final/capture_manifest.json`：4 分辨率 × 19 状态 = **76/76 PASS**；其余全帧/碰撞/性能/确定性/离线/可访问性门全绿。
- 目检 1280 RETRIEVE：黄色标签为“出库源位 01/04”；OUTBOUND：黄色货架信标已消失，仅保留同一出库 gid 与中文货权；1920 fault：红色故障条、左台活动故障=1、动作保持位和目标/货物标签均清楚且互不遮挡。
- `task11_first` 保留为审计口径错误的负证据；未把 checkpoint 语义改坏来换绿灯。

Task 11 状态：**PASS**。下一步：Task 12 极端镜头 PNG、最终全量重跑、输入/输出哈希与独立 critic 终审。

## 十四、Task 12 · 极限镜头、冻结证据与独立终审

### 进入前计划

- 不再扩展业务功能；只冻结最终视觉基线，验证默认/高窗/故障/E15 与自由镜头边界。
- 所有截图必须绑定当前源哈希；除 4×19 正式状态外，将四个极限相机姿态落为真实 PNG 并纳入 manifest。
- 复跑 Node 契约、6300 帧、扫掠碰撞、性能、离线、确定性和可访问性全门；最后交同一独立 `gpt-5.6-sol` critic 进行只读否决式终审。

### 初次终审指出的问题与修正

- 独立 critic 首轮拒绝 Task 7 收尾：左侧 mini 仅显示固定 4/8 策略却未披露范围；对比抽屉两处脚注只有约 7.8px/8.5px；Node test 隐含依赖当前工作目录。
- mini 标题改为 `S1 E02 · 固定 4 / 8`，增加 `非全量 · 10 seeds · SIM`、`AUTO / CB / TOB 见全量` 与 `全量 8` 入口；1280 下改为两行网格，避免标题/按钮拥挤。
- 关键抽屉脚注与证据隔离文字下限提高到 9px；E15 明确保留“扩展 Tier 3、非 canonical G2/非 H、事后 oracle”的降级边界。
- 测试以 `import.meta.url` 推导项目根目录，不再由 cwd 推导。随后从 `C:\Users\86177` 和 `F:\abb_wh_work` 分别复跑，均 24/24。

### 最终重跑与主动视觉否决

- `task12_final` 自动门全绿，但主 agent 人工检查发现 1280 mini 标题/按钮换行仍不够整洁，因此主动否决该批次，修正布局后生成 `task12_final_v2`；未把“自动化绿”当成视觉完成。
- 冻结 manifest：`l4_showcase/out/s3_6h_qa_0715/task12_final_v2/capture_manifest.json`。
- 正式截图 4 分辨率 × 19 状态 = 76/76；四张极限镜头全部保存并绑定哈希；另有四张交付别名，总 PNG 84。
- 17 个总门全部为 true：截图数量/逐图审计、镜头复位与移动、标签重投影、播放不污染 KPI、tooltip、可访问性、离线、自包含、性能、canvas 确定性、极限镜头、6300 帧、扫掠碰撞、页面错误和网络错误。

### 客观结果

- manifest SHA-256：`F71CFF2CF2EFE6D733B7746DB0AD5F554512A3E7EE2667AC6FFEE6D853098049`。
- 当前源文件 11/11、主截图 76/76、别名 4/4、极限镜头 4/4 独立复算哈希一致。
- 性能：renderer calls 194；1400 次 seek 为 152.86 次/秒；RAF 130.04fps；GC 后 heap 增量 8.44MB；前后 renderer/DOM/geometry/mesh/pool 计数不增长。
- 6300 帧：碰撞 0、越界 0、重复 owner 0、构图门失败 0、runtime error 0。
- 扫掠审计：每操作 1000 采样，最小净距约 0.0625；100 周期队列压力无碰撞、无穿地。
- 主 agent 逐张目检 1280 STORE、1920 STORE、E15、FAULT 和四张极限镜头：mini 披露清楚，高窗无左侧空洞，故障红条与普通 checkpoint 可区分，标签/HUD 无冲突；近景极限允许裁掉货架边缘，但作业对象与状态仍可判读。

### 独立终审裁决

- 同一独立 `gpt-5.6-sol` critic 复跑两个 cwd 下的 24/24，重算 manifest、当前源和 84/84 PNG 哈希，并人工抽看默认、高窗、E15、故障与四张极限镜头。
- 最终裁决：**FINAL PASS；P0/P1 阻塞无；非阻塞 P2 无。**

Task 12 状态：**PASS**。12 个任务全部完成；候选冻结，视频继续等待用户确认 HTML 视觉方向后再做。
