# 0715 S3 A 单视角配色与去视角回执

> 状态：待用户刷新终审；未覆盖 `样张_S3定稿.html`。  
> 当前候选：`F:\abb_wh_work\l4_showcase\src\样张_S3_三方向候选.html`  
> SHA256：`F23A6BEB937A0A99E388F44824F07AA09A48E62D021D2ADB474AFEA26083667F`  
> 文件大小：65,340 B。

## 1. 本轮裁决

1. **只保留一个 A 主视角。** 已删除 B/C 风格入口、动作特写次相机、次 WebGLRenderer 及其渲染分支。文件名为历史沿用，不代表页面仍含三方向。
2. **不把 AB 八态色直接当重量档色。** AB 的绿/黄/蓝/红分别表示已占用/推荐/路径/报警；若整组改成这些颜色，会把重/中/轻误读成运行状态。
3. **使用双层语义编码。** 货物主体继续表达重量档；AB 绿色窄边统一表达“已占用”；G016 用 AB 路径蓝；目标黄和报警红保持专用。
4. **无额外装饰。** 静态货物仍是竖直平面，只增加同平面的窄边，不恢复托盘、箱盖或铭牌。

## 2. 配色落点与来源

- AB/PLC 状态色真相源：`plc/03_Functions.st:245-266`；其中已占用 `#43A047`、推荐 `#FFC107`、路径 `#42A5F5`、报警 `#E53935`。
- 正式 S3 原始重量档来源：`tools/anim3d_formal.py:53-58`、`docs/Codex任务_S3与导览重做规格_0714.md:46`。
- 用户反馈后调整的主体色：重 `#0B2E4F`、中 `#0057B8`、轻 `#006A7A`。三色仍是克制的蓝/青工业色域，但与灰色内衬的对比提高。
- 对代表性货架内衬 `#AEB6BF` 的静态对比：
  - 原 heavy/mid/light：5.95 / 2.49 / 1.03。
  - 新 heavy/mid/light：6.75 / 3.35 / 3.06。
- 页面落点：图例 `:126-127,241`；3D 材质 `:256-321`；静态货物双层平面 `:396-397`；G016 与轮廓 `:479-480`；左侧规则图 `:549-551`。

## 3. 单视角删除证明

- `new THREE.WebGLRenderer` 命中数：1。
- `new THREE.PerspectiveCamera` 命中数：1。
- `detailFrame|detailRenderer|detailCamera|detailLook|styleSwitch|?style` 命中数：0。
- B/C 条件样式与 `STYLE==='C'` 命中数：0。
- 主循环只执行 `renderer.render(scene,camera)`，见当前候选 `:636`。

## 4. 回归验证

- `node --check`：PASS。
- 20×20 规则枚举：`(列+层) mod 3 = 0` 共 133 / 400，PASS。
- 运动学不变量未改：`N=20`；目标 `(15,4)`；`VX/VZ=2.0/0.5`；`AX/AZ=0.5/0.3`；两端驻留 `4.0/4.0 s`；`camera.up=(0,0,1)`。
- 1920×1080、右区 70% 的相机数学投影：货架四角 NDC x∈[-0.564,0.650]、y∈[-0.770,0.875]，全部在主视口内；I/O 与输送机端点也在主视口内。
- 禁用词扫描：无“蜂窝”“400 格数字孪生”“AC500 闭环”。
- 红色未用于正常货物；黄色只承担目标/推荐，G016 使用路径蓝并加深色轮廓。

## 5. 尚需用户视觉终审的一项

应用内浏览器安全策略拒绝代理刷新本地 `file://` 页面，因此本轮没有伪称完成最新帧截图验收。请用户在现有 A 标签页按 `Ctrl+R` 后只判断两点：

1. 15 个静态货物是否能从灰色货架中一眼分离，同时能读出绿边=AB 已占用、主体=重/中/轻；
2. 全周期是否始终只出现一个主场景，没有动作特写窗或 B/C 视角入口。

在用户通过前，不覆盖 `F:\abb_wh_work\l4_showcase\src\样张_S3定稿.html`，也不重生视频，避免把未终审视觉写入正式交付。
