# 0712 轨A文档更新设计说明

## 已批准范围

- 更新 `1_给你看` 下三份 Markdown 真相源，并由既有生成器再生对应 DOCX。
- 更新 `sim/build_report.py`，重生 `sim/out/验证报告_draft.docx`。
- 优化桌面 `智储优控_作战面板.html`，修改前保留可回滚备份。
- 仅生成上述 DOCX；PPTX 延后，等待用户另行确认。

## 内容口径

- C12 为 U3 泛化主证据：30 个未参与开发调参的种子（10001–10030），AWRA `8.17±0.67s`、较 seq 降 `61.6%`、30/30 正收益、失败/违规均为 0。
- 5-seed `8.40±0.48s` 继续作为回归锁，不与 C12 混称。
- B10 表述固定为：lex/CB/TOB 执行链已在 AB PC 仿真验证；`98.5%` 是 sim oracle-gap attainment，不是 PLC 实测或理论上限。
- B1 采用 Hausman、Schwarz 与 Graves（1976）作为 unit-load AS/RS 的方法谱系锚；定量说服力由项目内部 30-seed、理论夹逼与 oracle 对标证据链承担。
- §6 只写已实现并已验证的三页画面与交互；C6 称“布局操作留痕/写入热图”，不称“完整损耗履历”。

## 面板信息架构

- 首屏五秒内回答：项目是否可交付、当前唯一动作、关键数字与证据边界。
- 采用绿色/琥珀/红色 + 图标 + 文本三重状态编码，不只依赖颜色。
- 详情仅保留一层 `<details>`，不嵌套；所有关键模块带 `data-updated` 时间戳，JavaScript 标记陈旧模块。
- 中文通俗词优先，专业词随后括注；保留可复制命令短语。

## 红线

- 不打开 Automation Builder，不运行 `tools/ab_scripting`，不做任何 git 操作。
- 不修改或重生 PPTX。
