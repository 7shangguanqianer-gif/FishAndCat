# 0716 · Claude 对 S3 三轮收敛与五方案的只读复核回执

复核人:Claude Code(主会话)
范围:只读复核,未实现任何 HTML/sim/PLC 改动,未做 Git 写
依据:`0716_ClaudeCode同步提示词_S3三轮收敛与五方案.md` 的复核要求

## 一、已读

1. `research/0716/S3重构决策记录_0716.md`(全文,含 DR-S3-10~15 与实现前置门;pre_round3 备份对照)
2. `research/0716/S3重构12问一页规格_0716.md`
3. `research/0716/S3五套差异化设计方案_0716.md`
4. `_通信/codex_out/0716_S3重构第3轮与12问收敛回执.md`
5. `.claude/handoffs/2026-07-16-014130-*.md` 之 Round 3 Addendum
6. `research/0716/run_manifest.json`(此前已读,agent 句柄重派记录知悉)

SHA256 基线核对:Round 3 Addendum 报告的正式版 `B59954…`、候选版 `849BEA…`、runtime `F26207…` 与本人 0716 早间独立复算值逐位一致——本阶段确未触碰 S3 文件,采信。

## 二、裁决:接受(无拒绝项)

- **13 条必须继承的新事实**:全部接受。其中"133 预占 + 267 管理 = 400/400 静态终点""满库后 mixed cycle 无空位承接""fill 必须新建独立数据链""AWRA-LS 仅离线批量参考""失败域吞吐虚高风险""LAP n>m 守门缺陷""Python/ST accel 评分未证同源"七条为硬边界,后续 Claude 产出(含 54 格证据页、导览、面板)一并遵守。
- **DR-S3-10~15 与实现前置门(8 条)**:接受。特别认可"先数据与验证门、后前端"的实施顺序——与本项目 F1 教训("数字能复现≠断言有据")同构。
- **数据合同与对比指标合同**:接受。`kg·m 不是 kWh`、吞吐 fail 域 N/A、"完美摆放比例"禁用词三条将同步进全仓口径审计基线。

## 三、五方案结构差异核验(提示词指定动作)

**结论:五方案是真实的结构性差异,非换皮。** 叙事单位五者互斥(A=单件货物/B=容量阶段/C=算法/D=空间货位/E=评委问题),左栏主角、3D 构图(斜上近景/建筑立面/中性固定/近正交/展陈总览)、对比核心与失败风险各不相同;差异矩阵与正文逐节一致。

**口径遗漏检查:1 项换算约定建议,无实质遗漏。**
- 方案 A 与 DR-S3-12 的书签用百分比(`0/25/50/75/90/100%`),数据合同 checkpoints 用绝对件数(`0/67/134/201/241/267`)。四舍五入下一致,但实现时建议统一为绝对件数并写明"百分比按管理位向上取整"换算约定,避免 240 vs 241 类 off-by-one 争议,也呼应 B 方案自己强调的"管理位百分比≠总仓占用"。
- `warehouse_sim.py` CLI help"默认 and"与实际默认 `sum` 不一致——Codex 已列入后续小修复提案,确认知悉,建议随 fill-only runner 一并修。

## 四、Claude 独立第二意见(供用户选型参考,不替拍板)

1. **同意推荐组合 A 主壳 + B 容量书签 + D linked 2D/3D,C 独立对比模式,E 后置**——A 直接命中已拍的 DR-S3-01(算法决策为主),B 落实 DR-S3-09/10(0→满与双计数),D 落实 DR-S3-03(Top3 空间解释),C 落实 Q11 且把双 3D 性能风险隔离在按需页面。
2. **建议同时明确降级预案**:若数据门(fill-only runner+267 证书+候选审计导出+LAP 修复)耗时超预期挤压 8 月,最小闭环为 **A 主壳+B 书签**(已满足全部已拍硬要求),D/C 作为增强追加。选型时一并拍"降级线",避免 deadline 逼近时临时争论。
3. **数据层是关键路径,建议用户选定当天即授权开工**:前置门 6 项全部在 sim/tools 层,预估工作量明显大于前端;它同时也是 B12 录屏链的上游。另注意:官方三问(G1)中"交互界面输入货物属性"若被官方确认为决赛硬要求,单件沙盒入口的优先级需要上调——建议在 G1 回执到达后复核一次选型。

## 五、承诺

不改正式/候选 S3 HTML;不改 sim、PLC、canonical、报告/PPT;不启动 AB/FIO;不跑 ab_scripting;无 Git 写;不把 54 格、20×20、S1、S3、PLC/FIO 证据混层。到此停止,等待用户选型。
