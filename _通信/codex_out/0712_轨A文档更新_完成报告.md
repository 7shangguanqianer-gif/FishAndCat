# 0712 轨A文档更新完成报告

## 结论

批次已按顺序完成：基线门确认 → 三份 `1_给你看` 源文档与 DOCX → 报告生成器与报告 DOCX → 桌面作战面板 → 全量验收。PPT 未修改、未重生。

执行红线遵守：未打开 Automation Builder，未运行 `tools/ab_scripting`，未执行任何 git 命令。

## 1. 基线与备份

- `python -B sim/run_all.py core_smoke`：32/32 tests OK，`core_smoke PASS`。此前 `plc_evidence.csv` 逗号列宽阻塞已解除。
- 修改前备份：`_通信/codex_out/backup_0712_trackA_20260712_215921/`。
- 备份含三份 Markdown/DOCX、`sim/build_report.py`、旧报告 DOCX、旧桌面面板及 SHA256 manifest。

## 2. 三份用户文档

修改点均按 canonical C12 区分“5-seed 回归锁”与“30-seed U3 泛化主证据”：

- `1_给你看/周报_第1期_0710.md:23-24`：保留 8.40±0.48s 回归锁，新增 8.17±0.67s、61.6%、30/30、失败/违规 0。
- `1_给你看/项目通俗导览.md:78-91,274-278`：在成绩单后补 C12 解释，并把“三个数字并存”明确拆成证据角色与两把尺子。
- `1_给你看/实验发现.md:23,225-228`：F14 与历史节改为 5-seed 回归锁 / 30-seed 泛化主证分工。

仅重生指定三份 DOCX；未批量触碰 `1_给你看` 的其他文件。WPS 实际分页与全页视觉检查：周报 3 页、项目通俗导览 21 页、实验发现 19 页，未见截断、重叠、乱码或表格溢出。

## 3. 报告生成器与报告 DOCX

### 数据与口径

- `sim/build_report.py:90-92,118-151` 接入 `final_test_30seed.csv`、`final_test_30seed_per_seed.csv` 和 `oracle_gap.csv`。
- 重要复核发现：C12 的 61.6% 是“30 个 AWRA 均值 / 30 个 seq 均值”的比值降幅。直接用汇总 CSV 已舍入的 8.168/21.242 会显示 61.5%，逐 seed 降幅再平均则为 61.3%；生成器现从 per-seed 明细的两组均值之比复现 61.6%（`sim/build_report.py:138-145`），与 `final_test_30seed.py:88-96` 和 canonical C12 一致。
- 修正旧字段映射：`source_real_unverified_cases` → 当前 `source_real_cases`（`sim/build_report.py:316-319`），报告正确注入 `T01-T75`。

### 正文注入

- 摘要：30-seed 作主证，5-seed 作回归锁；75/0 与 sim KPI 分层（`sim/build_report.py:454-465`）。
- B1：Hausman、Schwarz 与 Graves（1976）方法谱系锚 + 内部 C12/夹逼/oracle 证据链（`sim/build_report.py:497-507`）。
- §5：75/0 全真实断言、AWRA/CB/TOB 黄金向量与 lex 执行链边界（`sim/build_report.py:691-706`）。
- §6：三页 HMI、三项可视化辅助、21 个交互件长跑、B10 oracle-gap 与 Python↔ST 有限一致性（`sim/build_report.py:730-765`），并嵌入 P1/P2/P3 三张实景截图。
- 结论和附录 A/C 同步到 C12、75/0、21 项交互及未验证边界。

生成链结果：145 个数字注入、来源清单待补 0、正文实扫待补 0。文本门确认含 8.17±0.67、61.6%、30/30、Hausman、§6、98.5%、75/0、21 个交互件，且不含旧“非U3”话术或模板占位。

视觉 QA 首轮抓到 §6.3 相邻字符串漏 `f` 导致页面显示 `(LEX_ATTAIN)%`，已修生成器并重生。最终报告经 WPS 实际导出为 23 页并逐页复核，无截断、重叠、缺图或占位。

## 4. 桌面作战面板

文件：`C:\Users\86177\Desktop\智储优控_作战面板.html`。

六条设计约束均已落地：

1. 首屏五秒内给出总灯、唯一动作、C12、75/0、21/21；当前唯一动作是验收报告，PPT 另行确认。
2. 详情保持单层 `<details>`，共 4 个，最大嵌套深度 1。
3. RAG 状态均采用颜色 + 图标 + 文字三重编码。
4. 10 个关键模块带完整 ISO `data-updated` 时间戳；JS 同时兼容日期与带时区时间戳，超过 72 小时标陈旧。
5. 保留并更新指挥口令区：`验收报告`、`重生PPT`、`对齐面板`、`今天干了啥`、`给我讲讲`、`quiz我`。
6. 主文案先用中文人话，再括注 sim H、AB PC、oracle attainment 等技术词。

同时清除了面板中的旧状态：30-seed“暂不重跑”、长跑“待执行”、CB/TOB“正在补”、59/0、98.5%“理论最优/将变真机值”、canonical“正在修正”等。Chrome 本地首屏渲染检查通过，截图为 `_通信/codex_out/0712_作战面板_首屏QA.png`。

## 5. 最终验收

- `python -B tools/doc_check.py`：PASS。
- 三份用户 DOCX：ZIP/文本门 PASS；WPS 3+21+19 页全页视觉 PASS。
- 报告 DOCX：内容门 PASS；WPS 23 页全页视觉 PASS。
- 面板：HTML parser PASS；4 个 details、最大深度 1、10 个时间戳；Chrome 首屏渲染 PASS。
- PPT 保持未改：`sim/out/答辩PPT_draft.pptx` 修改时间 `2026-07-12 04:17:12`，SHA256 `633F264257A997998998188A3861B2B4E951BC9E2C5CE7AEB043ADFB97F482F6`。
- 最终进程检查无 AutomationBuilder、ScriptEngine、WinWord；仅见任务开始前已存在的 WPS 后台进程。

## 6. 最终制品 SHA256

- 周报 DOCX：`BBCFBAB03AF9D293DA71117C7EED49F85F7BB2071A25F6998531AC57326BD020`
- 项目通俗导览 DOCX：`390E1FEA6F7BCC95A55F5D4A6792466FFDBA270EB32B47A7EF7C8F54EE9EB825`
- 实验发现 DOCX：`1B17F25522E4C347E776FEC95967839B3CEAF6F903B3478FC2AE1AB64AA14F61`
- 验证报告 DOCX：`C2DAB181FDF21439AC588C67A29D118E2CEAE4F252262CC56BF08AACC71B7123`
- 桌面面板 HTML：`ED91E306011BA0FDB746CEE1BE487848441219C5E1A9091D5264F7CBAAD4364A`

## 7. 剩余边界

- 本批次按任务 1 只统一三份文档中的“非 U3 / 30-seed”过时表述；文档内明确标为历史记录的旧批次数字未做全篇事实前推。
- `98.5%` 仍是 sim oracle-gap attainment；H KPI、年化能耗、吞吐均非实体 AC500 实测。
- 400 件最坏时延/watchdog、实体 AC500、多客户端并发仍未验证。
- PPT 等用户验收报告后另行签发，不应由 Claude 在复核本回执时顺手重生。
