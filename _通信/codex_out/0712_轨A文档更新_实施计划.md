# 0712 轨A文档更新实施计划

1. 重跑 `python -B sim/run_all.py core_smoke`，以 `32/32`、`core_smoke PASS` 为继续门。
2. 备份三份 Markdown/DOCX、`sim/build_report.py`、现有报告 DOCX 与桌面面板。
3. 更新三份用户 Markdown：加入 C12 30-seed 主证据，同时保留 5-seed 回归锁。
4. 调用既有 `tools/make_user_docs.py` 仅重生三份对应 DOCX，并做 ZIP/文本检查。
5. 更新报告生成器的数据装载与正文：C12、B1、B10、§6、AB 75/0 与交互长跑边界；仅重生报告 DOCX。
6. 按六条约束优化桌面作战面板，清除过时任务和错误边界。
7. 执行 `tools/doc_check.py`、DOCX 结构/文本检查、面板静态检查和 PPTX 未变更核验。
8. 将完整发现、改动、验证与剩余边界写入 `_通信/codex_out/`，并在 `_通信/致Claude.md` 顶部追加回执。

> 本批次用户明确禁止 git 操作，因此计划不含提交步骤。
