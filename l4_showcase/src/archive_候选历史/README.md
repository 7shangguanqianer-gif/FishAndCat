# 候选历史存档(0717 目录清理)

本目录存放 S3 页面族的历史候选/评审版/风格样张,**内容原样未改**(SHA 与挪档前一致)。
决赛动线只用 src 根的四件:`00_导览.html` / `01_连续填仓.html` / `02_入出闭环.html` / `03_FIO物理证据.html`(另 `样张_S3定稿.html` 为 0706 风格文物,按拍板原地封存)。

⚠️ 本目录页面内部以 `./xxx.js` 引用 src 根的 runtime/store——**直接双击打开会缺资源**。
需要运行时:临时拷回 `src/` 根,或从 git 历史检出对应版本(改名/挪档均为 git mv,历史连续)。
QA 侧:v7 冻结基线 `tools/capture_s3_fill_candidate_qa.mjs` 通过 hash-snapshot 机制照常可跑(它把本目录源文件拷进快照的 src/ 下原名,相对引用自然成立)。

| 文件 | 身份 |
|---|---|
| 样张_S3_fill恢复候选.html | fill v7 布局(01_连续填仓 的前身;v7 QA 20 项基线对象) |
| 样张_S3_三方向候选.html | mixed 入出闭环 0714 版(02_入出闭环 的前身;task12 QA 对象) |
| 样张_S3_三方向候选_失败版备份_0714.html | 0714 失败版备份 |
| 样张_S3_版式候选_1/2/3 | 0714 三份版式候选(v7 家族) |
| 样张_S3_母版增量方案_1/2/3 | 0716 三份母版增量评审稿 |
| 样张钦点.html | 0706 S1-S5 风格切换器 |
