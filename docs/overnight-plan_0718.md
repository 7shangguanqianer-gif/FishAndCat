# Overnight 出发单 · 2026-07-17 深夜 → 07-18 清晨(8h 无人值守)

> 本文=出发前锁定的任务队列+边界+全部拍板记录+本日对话收获。执行断点看
> `docs/overnight-resume.md`(滚动更新);清晨交接看 `docs/overnight-report.md`。

## 一、四轮十四问拍板全录(2026-07-17 深夜,用户在场逐条拍板)

| # | 议题 | 拍板 |
|---|---|---|
| 1 | 左栏三联图下方大片空白 | **差异热力图常驻左栏**(放大层保留大图版) |
| 2 | 顶栏「镜头与演示倍率」风格不统一 | **就地风格化**(位置不动:去红粗滑块、按钮/字号/间距与轴行对齐、融入而非移植感) |
| 3 | 夜间主工程 | **#16 mixed + #17 T7 都做**,完成后继续往后推进 |
| 4 | #27 what-if 边界 | **数字支持则夜间改生产**;判据=五条全满足(四指标均值全不劣容差+0.5% / expected 或 hot20 配对≥20/30 改善 / 首件收敛 / 守恒+违规零 / uniform 同样不劣)→ **实测不满足,不改生产**(见 §三) |
| 5 | 后续推进 | **T6 录屏候选片**;规格=先录 01 页一段(90s)定调,过目后再批量 |
| 6 | 监督机制 | **双层**:每里程碑 fresh-context verifier(claude-opus-4-8 / effort high)+清晨前全盘终验 |
| 7 | git 隔离 | **主分支小步 commit**(QA 全绿+verifier 过才 commit);另授权 overnight 期间自行使用平时需手动触发的 skills |
| 8 | 额度自动续跑 | **CronCreate 心跳接力**(会话内每小时两次心跳读断点文件,turn 失败后自动重试到额度恢复;断点文件兜一切死法) |
| 9 | 额度用尽策略 | **主循环+后台 QA+verifier 并行,队列永远排满**;不为烧而烧 |
| 10 | 双命令调度设计稿 | **文档+离线原型 30 seed 数字**(不动 ST、不入 02 页,待用户过目) |
| 11 | 追加入队 | **报告周素材预备包 + #12 三课课件草稿 + 00_导览全页口径体检** 全入队;授权"技术类欠账仔细搜查后都可加入" |
| 12 | 观感仲裁 | **verifier 观感为准**(≥4/5 才 commit;3 次不达取最高分版本 commit 并记待拍板) |
| 13 | 清晨交接物 | **精简版**:overnight-report.md + 桌面作战面板同步(其余额度让给主工程) |
| 14 | T6 完成度 | 先 01 页定调段,不批量 |

## 二、夜间队列(优先级序;每步完成即 commit+更新 resume)

| 里程碑 | 内容 | 验收 |
|---|---|---|
| M0 | 出发单+断点文件+心跳 cron 落地 | 本文件+resume+CronList 可见 |
| M1 | 绿框①顶栏工具风格化+绿框②差异热力图常驻左栏 | QA 46 门全绿+新断言;verifier ≥4/5 |
| M2 | #27 结论文档(负结果+τ 失效机理实数+dynfmax 排序不变证明) | 文档落 docs/;引用证据文件可复现 |
| M3 | 技术欠账清账:sim 测试回归+未跟踪文件归档 commit | 测试全绿;git status 干净(除 1_/2_) |
| M4 | #16 mixed 升 A 壳:02_入出闭环.html+交互+新 QA+双命令调度设计稿(文档+离线原型数字) | 新 QA 全绿;verifier ≥4/5;调度稿"待过目" |
| M5 | #17 T7:00_导览 04 章挂决赛四件实链+全页口径体检(77/0/动线/文物排除/旧术语) | 体检清单+修正 commit;verifier |
| M6 | T6:01 页 90s 定调片(候选,待过目) | mp4 落 l4_showcase/media/ |
| M7 | 报告周素材预备包+三课课件草稿(docs/,不碰 1_/2_) | 文件落盘 |
| M8 | 03 页与 01 页风格一致性体检(时间余量项) | 体检记录,改动仅小修 |
| M9 | 全盘终验 verifier+overnight-report.md+桌面作战面板同步 | 报告含五要素 |

## 三、#27 What-if 已定结论(负结果,证据先行的兑现)

30 seed×2 情景(`l4_showcase/evidence/s3_score_whatif_{skew,uniform}_2026_2055.json`):
- skew:τ 饱和 hot20 **劣化 1.4%**(配对 4/30);expected +0.5% 无改善;**首件 30/30 仍 (19,00)**——0 层等时区 t_norm(19,0)≈0.25<τ=0.5,饱和没覆盖 0 层;dynfmax 为排序不变旋钮(全局缩放不改相对序)。
- 判据五条不满足 → **生产评分/数据门/ST/页面口径一切不动**;M2 落结论文档供清晨拍板。

## 四、红线(夜间不可越)

- 不碰 `1_给你看/`、`2_你要操作/`(M 不 commit);不改生产评分定义/数据门 release/ST(#27 负结果已定);不覆盖冻结物(样张_S3定稿 SHA b59954cb / skew d75a06b0 / uniform 683e25d1 / archive_候选历史)。
- 视频全部标「候选待过目」;"演示可在 PLC 复现"禁语;77/0 口径(历史锚带日期)。
- AB GUI 相关:夜间**不碰 AB 一切**(ST 不改故无需在线验证)。
- 新决策点选保守项记入待拍板清单,不停车;同一任务连续失败 3 次挂起转下一项。
- 扇出子代理 sonnet/medium 显式传参;verifier 用 opus-4-8/high。
- git add 只点名;commit 中文说明+Co-Authored-By。

## 五、本日对话收获总结(0717 全天,供长期沉淀)

1. **"完成≠解决,须用户验收"再次验证**:三反馈一轮修复技术上落盘但观感不解决,二轮按用户拍板(发光框+箭头/说明标签/dock 三格)才过;由此确立 verifier 观感仲裁机制。
2. **视觉实现的投影陷阱**:沿视线方向(y 轴)的 3D 几何投影成一个点——箭头必须沿层高(z)竖直布置。
3. **共享文件改动必跑全家桶**:mapKey 图例撑爆候选页 1.3px、挪档漏 task12 URL、旧文案断言被 404 掩盖、GPU 亚像素 flaky——四套 QA(46/8/20/task12)缺一不可。
4. **满仓守恒下的呈现学**:能区分算法的只有货-格配对与过程序——三联同帧+金顶热门+差异热力图+冷门解说卡四件套把"隐形的胜利"显形。
5. **证据先行避免了一次伤主指标的改动**:τ 饱和直觉合理,30 seed 实测伤 hot20 主叙事指标且首件不收敛(0 层等时区机理);dynfmax 是排序不变旋钮。
6. **额度自省**:花不完=队列不满+闲等;重启失败=断点未落盘+无自动拉起——本次以 resume 断点+cron 心跳接力对治。
7. 用户习惯性需求画像更新:落盘一切/验收权在用户/数字可复现/额度纪律/观感一等公民/完整路径汇报。
