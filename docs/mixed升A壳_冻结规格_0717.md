# mixed 入出页升 A 壳 · 冻结规格(0717,待用户二次确认后编码)

> 授权依据:0717 用户拍板"升 A 壳+治理叙事"。流程:本规格确认 → 编码 → QA → 你验收。

## 1. 产物与不动产

- **新建** `l4_showcase/src/样张_S3_入出闭环候选.html` + `s3_mixed_layout_a_interactions.js`(壳层交互,仿 fill 页的 interactions 模式)。
- **不动**:`样张_S3_三方向候选.html`(0714 冻结原样保留)、`s3_ac_runtime.js`/`s3_trace_store.js`(数据/渲染层零改动优先;若 AUTO 话术等文案写死在 runtime 内,允许最小文案补丁,逐处列账)、trace 数据链(out/s3_traces 五 lane,由已收编的 generate_s3_tier3_trace.py 可再生)。
- 冻结页家族(fill 恢复候选/三份 v7/三方向/母版增量/风格文物)一律不覆盖。

## 2. 信息架构映射(A 壳五要素 → mixed 语义)

| fill 页(方案 A) | mixed 新页 | 说明 |
|---|---|---|
| 顶栏:算法/拟真档/货物画像 | 算法(五 lane:SEQ/NEAR/SCORE/**默认策略 SCORE**(原 AUTO)/AWRA-LS 离线参照)+情景(t1_heavy 单情景,disabled 标注) | AWRA 保持「离线批量参照 · 非同信息集」分组,不冒充在线 |
| 进度轴·容量节点(0/67/134/201/241/267) | **入出 cycle 轴**:节点=cycle 里程碑(0/25/50/75/100 cycles),点击暂停到已验证快照 | 语义从"容量推进"换成"入出交替推进" |
| 左栏 决策区(Top3+tieNote) | 决策区沿用 runtime 现有证据结构;若 trace 无 Top3 逐件审计,如实显示现有决策依据,不造新数据 | 数据边界:本批不重跑 trace,不新增审计字段 |
| 左栏 六格统计(治理 A) | **取货实测块**:高频货取货时间实测(从 trace 事件提取)+入出闭环 KPI(cycle 数/取货均时/热门件取货);有守恒量语义的指标(如总 cycle 路程)标徽章 | 「高频近格兑现」的秒表位——本页核心卖点 |
| 2D 容量联动图 | 2D 占用图(入出动态:格位随取放变化) | 复用 runtime 现有 2D |

## 3. 治理叙事接入(与 fill 页同语言)

1. 「智能路由(AUTO)」→「默认策略 SCORE」(下拉+决策头,同 fill 页话术)。
2. 取货实测块 title 注:取货时间=本页实测事件,与 fill 页"期望取货(SIM 期望值)"口径不同,禁混引;高频近格红利在此页为实测兑现(C1 30 seed 方向背书)。
3. 演示循环重置遮罩(cycleReset)保留原语义"归位不计 KPI"。
4. 口径徽章:sim 标注、"演示补段不计 KPI"、77/0 不在本页宣称(本页是 Web sim,不引 PLC 数字)。

## 4. QA 与验收

- 新 QA `capture_s3_mixed_layout_a_qa.mjs`:仿 layout_a QA 骨架——视口三档无溢出、五 lane 绑定、AUTO 改名断言、AWRA 分组不冒充在线断言、cycle 轴节点快照语义、取货实测块存在与数值来自 trace(可复算)、禁词(容量书签/智能路由)零命中。
- 全绿后截图人工复核,交你验收;验收通过才进入 T6 录屏分镜。

## 5. 边界与不做

- 不重跑/不改 trace 数据与 schema;不给 AWRA 造在线叙事;不做多情景(t1_heavy 单情景如实标注);不引入 fill 页的 30 seed 数字到本页(两页口径隔离,对照关系由导览 04 章文字承担);导览/全景联动改动属 T7,本批不动。

**确认点(回复"规格确认"即开工;或指出修改)**:①新页命名 ②cycle 轴节点取 0/25/50/75/100 ③取货实测块为本页第一统计块。
