# Codex 任务 B · 01/02 顶栏·品牌·token 对齐全站规范

## 0. 项目底座(自包含,codex 必读)

- 仓库根 `F:\abb_wh_work`;展示站 `F:\abb_wh_work\l4_showcase`。页面 `src/01_连续填仓.html`、`src/02_入出闭环.html`(03 归任务 C,本任务不碰 03)。
- **视觉基准 = `docs/全站设计规范_0724.md`(必读全文,尤其 §1 配色 / §2 顶栏 / §3 字号间距 / §4 证据入口)。**
- QA/回归命令(cwd=`F:\abb_wh_work\l4_showcase`):契约 `node tools/test_s3_runtime_contract.mjs`(30/30 fail0)、01QA `node tools/capture_s3_layout_a_qa.mjs`(61/61)、02QA `node tools/capture_s3_mixed_layout_a_qa.mjs`(8/8)、uniform `node tools/capture_s3_fill_uniform_qa.mjs`(8/8)。
- Playwright 底座:Chromium `C:/Users/86177/AppData/Local/ms-playwright/chromium-1223/chrome-win64/chrome.exe`,args `['--use-angle=d3d11','--enable-unsafe-swiftshader']`,静态服务器 root=仓库根。
- 双分辨率自检:壳真实内容区 **1552×900**(窗口 1600−48 侧栏)+ 用户常用大窗 **1945×1094**,两档都要看(此前只看裸 1920 漏过问题)。
- 红线见规范 §6:命名总表词根、契约断言读 archive_ 勿误改、禁 nth-of-type、禁黄牌、白底 ABB 红、用户文档禁触、不动 sim/trace/manifest。

## 1. 背景

用户要 01/02/03 三页统一风格,以 02 现行白红工程风为基准。本任务负责 **01 与 02** 的顶栏/品牌区/设计 token 对齐到 `docs/全站设计规范_0724.md`;03 的对齐在任务 C(03 整页重设计)内按同一规范完成。三页一致由共同规范保证。

**现状差异(需 codex 先侦察落差,再对齐)**:
- 01 顶栏 `#topbarA`(`01_连续填仓.html:1533`)近期多次返工(0722 系列),含 railHead/runtimeControls/runtimeActions(playPauseBtn+证据中心钮)/topbarRaceSummary;02 顶栏结构不同(有 tierSelect 三档、profileSelect、cycleAxis 等)。
- 品牌区(ABB logo + 页名 h1 + 副标 eyebrow)两页字号/排布/letter-spacing 可能略有出入。
- 字号/间距节律(块头/正文/小字/gap/分隔线)两页各有历史地层,未必对齐规范。
- 证据中心:01 已于 commit 401b5d2 改成 02 同款模态(全屏遮罩+居中卡+×+点遮罩/Esc 关);确认两页证据入口按钮样式与模态形式一致。

## 2. 目标与改动

1. **顶栏**:01/02 顶栏都符合规范 §2——白底、底部 3px ABB 红线一线、左品牌区、中控件、右功能钮;高度节律一致(~48px)。**保留各页功能差异**(02 有三档/货物画像,01 有赛马总分条),只统一"外壳语言"(底色/红线/品牌区/控件样式/字号),不删各页功能控件。
2. **品牌区**:ABB logo + 页名 + 副标 eyebrow 的排布/字号/letter-spacing 两页逐字对齐规范 §2;页名格式「智储优控 · <页名> · S3」(01=连续入库,02=存取闭环)。
3. **设计 token**:配色变量收敛到规范 §1(消除硬编码色值,改用变量);字号/间距/分隔线按规范 §3 对齐(以 02 现行为准,把 01 的历史地层拉齐;01 若有 data-style A/B/C 死层,可顺手清理但**不得破坏 01QA 61 项断言**——被断言的元素/文案不动)。
4. **证据/口径入口**:确认 01/02 证据中心按钮+模态、口径带样式同款(规范 §4)。

## 3. 验收标准(须实测)

1. **回归全绿**:契约 30/30、01QA 61/61、02QA 8/8、uniform 8/8。四命令原样贴。
2. **双分辨率×两页顶栏截图**:1552×900 + 1945×1094,01 与 02 顶栏并排对比,证明白底红线一线、品牌区同构、无深色残留、无横向溢出。截图用 `{animations:'disabled'}` 稳定底座。
3. **token 落差表**:改动前后 01 顶栏关键样式(底色/红线/h1 字号/eyebrow/控件高)对齐 02 的对照。
4. 无新增 nth-of-type;命名总表词根未违反(页名/术语不乱改)。

## 4. 交付格式

`git diff --stat`;现状落差侦察结论;顶栏/品牌/token 对齐清单;双分辨率两页顶栏对比截图路径;四命令回归输出;清理的死层(如有)与"未动被断言元素"的证明;偏离规范之处与理由。**不 commit**,主会话验收。

## 5. 与任务 C 的边界(防冲突)

本任务**只改 01/02**,不碰 `03_FIO物理证据.html`。03 的顶栏白红化+品牌对齐由任务 C 在整页重设计时按同一规范 `docs/全站设计规范_0724.md` 完成。两任务共用规范→三页自然一致,文件不重叠→可独立并行与验收。
