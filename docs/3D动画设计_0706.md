# 3D 回放动画 · 正式版设计(0706,两轮用户反馈后定稿)

> 一轮反馈:①遮挡穿帮 ②2D 背景突兀 ③三档三动画 ④左图表右 3D。
> 二轮反馈:⑤左列信息量加码 ⑥**按档 3(最高拟真)锚点设计,防返工** ⑦左右 3:7,3D 更大。
> 定稿设计图=**sim/figs/design_3d_dashboard_tier3.png**(档 3 满配);tier2 旧版已被取代。

## 1. 构图(档 3 满配 superset;三档共用版面)

- **品牌顶栏**:黑底+ABB 红字标+白标题+红底线,右侧=档位徽章+时间戳+SIM(与 v3 画面页眉同构);
- **KPI 横带(顶栏下,6 卡)**:已入库/到达排队/累计行程/**平均响应**(不说"吞吐",防档 3 语义漂移)/累计能耗(L5 卖点)/活动报警(仅异常红框红字,ISA);
- **左列(28%)三图**:①速度曲线=梯形加减速+**装卸驻留 v=0 平台**+回程淡线(L1+G1 一图双讲)②入口到达队列阶梯曲线(泊松,档 3 专属)③单件响应时间**分解堆叠条形**(行程+装卸,真行程值,琥珀=在途);
- **右侧(72%)全 3D**:地面+货架双排立柱+地轨/立柱/在途箱+**入口到达队列实体箱**+报警格(深红+黑粗边,Q2 拍板)+▲故障浮标+当前路径实线+**历史路径残影**+目标格琥珀线框+图例行;
- **色板服从 v3**:红=报警专属;货物=蓝系三档;预占=半透明灰封格;琥珀=当前/目标;
- **降档规则(防返工核心)**:档 2=队列图置灰标"本档未建模:泊松到达"、驻留段消失、报警卡=0;档 1=再置灰驻留与分解图的装卸段,速度曲线变矩形。版面/渲染器零改动,置灰本身就是拟真阶梯的可视化。

## 2. 三档拟真 × 三段动画(录屏"拟真阶梯"章节,每段 20-30s)

| 档 | 运动/场景 | 左侧图表 | 3D 看点 | 一句话故事 |
|---|---|---|---|---|
| 1 数据模型 | 匀速+纯入库 | 矩形速度曲线+期望取货时间收敛 | 顺序入库回放 | 教科书基线 |
| 2 最大杠杆(主口径 H) | 梯形加减速+存取混合 | **梯形速度曲线(与档1矩形叠影对比)**+单件耗时 | 双命令周期:入库顺路出库 | 近位不"免费",混合作业更真 |
| 3 工业场景 | 泊松到达+装卸驻留+故障注入 | 到达队列长度+响应时间分布 | 入口货箱排队、放置驻留停顿、**报警格深红+堆垛机改道** | 真实工况下依然稳 |

数据源:档 1/2=assignment CSV+canonical A4/A4b;档 3=sim/realism.py tier3 输出(需补导出帧序列 CSV,小任务)。

## 3. 遮挡问题:根因与修复路线(0706 定向枪,全 URL 见枪报告)

- **根因(官方定性)**:mplot3d=画家算法,无 Z-buffer;多集合间只按整体均值深度排序,包围盒相交必错;官方 FAQ 原文称此为 "intractable problem...until matplotlib supports 3D graphics rendering at its core"(https://matplotlib.org/stable/api/toolkits/mplot3d/faq.html);
- 单一 Poly3DCollection(zsort='average')=最佳缓解(设计图已用),但相机旋转时相邻盒子仍可能穿帮;ax.voxels 更慢(400 体素约 1-3s/帧)不解决跨集合;
- **正式版路线=混合渲染**:右侧 3D 视口用 **PyVista**(VTK 硬件 Z-buffer,真深度测试根治遮挡;off_screen+open_movie/write_frame 原生 mp4;新依赖 pyvista+vtk 约 81MB wheel),左列图表+中文字幕+品牌栏留 matplotlib(中文零障碍),PIL 逐帧合成→ffmpeg;
- 后备:纯 matplotlib+锁定相机扫角(限制在不穿帮的角度窗),零新依赖但牺牲运镜自由度。

## 3a. 故障注入的动画叙事(0706 深夜定,回应用户"报警红格准备怎么处理")

设计依据:§2 档 3 行"报警格深红+**堆垛机改道**"+ v3 拍板(深红+黑粗边)+ ISA-18.2(活动-未确认保持显示)。
**五拍叙事**(60s v2 已实现):①第 6 件取货驻留时,原分配目标格 (17,2) 检测故障→深红+格口红框+红锥,KPI 报警卡红「1(未确认)」;②状态行红字「库位(17,2) 故障 → 在线重排至 (x,y)(次优解,作业不中断)」;③目标线框+轨迹曲线切换到重排格(=CSV 真实格,最终布局不变,诚实);④堆垛机照常执行——**演示的是在线算法故障鲁棒性,不是装饰**;⑤报警保持未确认红到片尾。闪烁不做(与 CODESYS 画面拍板一致,静态深红)。

## 3b. 60s v2 修复账(0706 深夜,用户三问+自查五项)

用户抓的:①水平/垂直 vmax 不一致=A4 物理事实(提升受功率限制),但演示未解释→图例/脚注补;②**路径直线≠真轨迹**(双轴完成时间不同,真轨迹=先斜后平的参数曲线)→路径线改双轴梯形积分采样;③队列箱统一色→绑定具体货物类色+队首前移。
自查的:④入口取货无驻留(与放货不对称)→加 T_LOAD=4s,速度曲线两端双平台;⑤KPI"平均响应"实为服务时长,不含排队→改真口径(入库完成-到达,本批);⑥队列拣走是队尾消失→随③修;⑦到达均值随服务时长变化重标(36s,ρ≈0.9;ρ>1 队列爆炸=0706 实测教训,留作超载彩蛋素材);⑧加全宽状态字幕条(当前作业/故障插播/收尾语)。

## 4. 施工清单(拍板后执行)

1. **帧事件流 schema v1 先锁**(防返工数据闸):CSV 列=时刻/堆垛机位(x,z)/载货态/事件(到达|入库|出库|故障|恢复)/KPI 快照(已入库/队列长/累计行程/响应/能耗/报警数);sim 侧补 export_anim_frames.py,tier1/2/3 同构输出;
2. ~~PyVista PoC~~ ✅0706 直接以 10s 小样完成(tools/anim3d_sample.py → sim/figs/anim3d_sample_10s.mp4):遮挡零穿帮/中文全在 matplotlib 层零障碍/150 帧渲染约 2 分钟/1280×720@15fps。**两个命门**:①off-screen 每帧必须显式 `pl.render()` 否则冻帧(症状:matplotlib 层在动 3D 层不动);②`camera.zoom()` 跨帧累积,要显式设 `camera.view_angle`;取景公式 半视场=R·tan(视角/2),R=42/视角32°/焦点z=8.3 时 0..20 全入框;
3. 渲染器模块 tools/anim3d/(读帧事件流→PyVista 3D 视口→PIL 合成 matplotlib 面板→ffmpeg);
4. 档 3 先做(满配,素材=realism tier3 帧导出);档 2/1=同管线换数据+置灰;
5. 产物:sim/figs/anim_tier{1,2,3}.mp4 + 三张单帧海报图(报告/PPT 用,Q5 全载体拍板)。

## 5. 决策记录

- 0706 二轮:①**遮挡路线拍板=PyVista 混合渲染**;②设计锚点=档 3 满配、档 2/1 置灰降档(用户指令"按最高拟真度设计,确定后依次定调另外两项");③左右 3:7、左列信息量加码(KPI 横带+队列图+分解堆叠)落地于 tier3 设计图;④三档内容分配待用户看 tier3 图后连同构图一并终拍;⑤动画字幕口径:档 3 用"响应时间"不用"吞吐"(防与报告 §3 口径冲突,深思清单 #2 的动画版)。
