# 画面输入"间歇性全哑"攻坚战况(0711 凌晨;路线A已执行,长跑通过,观察期)

> **0711 深夜定案(§7)**:路线A(GUI 洗 P1 全部动作)已由 Claude 通过 computer use 执行完毕,
> 洗后 10.7 分钟长跑 45+ 次交互零失效。含压力项(策略键 5 连击)。判定:**洗后未再复现**。
> 间歇缺陷的本性决定这不是数学证明——后续每次演示录屏都是延长观察窗口,若再发作按 §4 采证。

## 1. 症状与复现规律

- 失效态:点击任何按钮/输入框无反应;文本/颜色变量**正常刷新**;VISU_TASK Cycle count 增长。
- 健康→失效:冷启(关 AB→重开→Login with download)后健康;运行/操作一段时间后突然全哑。
- **今晚两轮发作**:轮1(P1 全哑时 **P2 仍活**)→冷启复活→轮2(P1 再哑,连全局 Reset 也不响,
  黄格残留为证;此轮 P2 未测——补测是关键证据!)。
- 干扰案已剥离(都不是本病):①"透明组框盖点击"(已删换边线条)②CmdLoadDemo 卡 TRUE 风暴
  (N=260;疫苗已写进 05)③输入积压 download 后重放 ④状态机语义(DONE/SUGGEST 态不收部分命令)。

## 2. 已排除项(证据在案,勿重查)

| 排除项 | 证据 |
|---|---|
| VISU_TASK 挂死 | Cycle count 增长+显示刷新活(0711 枪:显示与输入同任务,任务死则全死) |
| 动作序列化损坏 | P1/P2 输入框元素 XML 逐字段 diff 同构(仅参数差) |
| 画面对象骨架 | 唯一差异 UniqueIdGenerator 已证伪(回收复用型指针,非故障位) |
| 命令变量卡死 | 监视全 FALSE 时仍失效 |
| 工程编译态 | F11 0 errors;ab_sync 45/0 |
| 全网已知修复 | 0711 枪:症状同构帖(Forge《Visualization stops responding》等)全部无根因无解,官方让提付费工单 |

## 3. 最强线索(下会话首选路线)

**"GUI 洗过的动作疑似免疫"**:P2 的 7 个输入框+按钮经 GUI 打开 Input configuration→OK 重存后,
**再未死过**(轮1 P1 全哑时 P2 仍活)。P1 的 22 处动作全部是脚本(ScriptVisualization)建的,
从未 GUI 洗过。假设:**脚本建的动作存在 GUI 序列化补不全的运行时注册缺口,间歇性掉注册;
GUI 重存后进入稳定态**。

→ **路线 A(首选,约 20 分钟人工)**:P1 全部 22 处动作 GUI 洗一遍(13 按钮+5 输入框+4 权重框;
洗法=打开 Input configuration→直接 OK→下一个),Ctrl+S→F11→login download→长跑观察是否再死。
验证设计:洗完后连续操作 10 分钟+来回切 P1/P2,若不死=定案+落经验(以后脚本建画面必须 GUI 洗一遍收尾)。

## 4. 弹药清单(路线 A 失败后按序试)

1. **失效瞬间的分辨采证**(每次发作先做,别急着重启):①P2 还活吗(区分全局死/单画面死)
   ②Device Log(设备树 PLC_AC500_V3→Log 标签)有无 visu 错误行 ③编辑器切到另一画面再切回,复活吗
   ④Element List 面板看元素状态。
2. Build→**Clean all**→F11→login(全量 download)——0711 枪:同类 visu 异常社区验证率最高的通用手段。
3. VISU_TASK 参数:双击 VISU_TASK 看周期/优先级(与 10ms 主任务竞争?)试改 50ms/降优先级冲突。
4. 删除重建 VISU_TASK(查无案例,实验性)。
5. **釜底抽薪:CODESYS Control Win 软 PLC + WebVisu**(独立运行时,绕开 IDE 集成 visu 全部毛病;
   Banner/WebVisu 顺带解锁;成本=装软 PLC+平行工程/target 适配——最重但最治本)。
6. 兜底运维:冷启工作法(每轮演示前重启 AB+download,健康窗口足够 3-5 分钟录屏)——已证可用,
   用户不接受为终态,仅作录屏保险。

## 5. 环境版本(0711 实锤)

AB 2.9 Premium = CODESYS V3.5 **SP21 Patch 3**(枪证;3SLicense 3.5.20.0 是许可组件版本,勿混)。
SP21P3 已含 CDS-85095/CDS-90200 输入类修复(TargetVisu 场景,与本症状无关)。

## 6. 本轮已落盘待生效项(下会话开工前先 ab_sync)

- plc/05:①脉冲命令消费即复位疫苗(段尾)②Reset 清统计/路径显示残留;
- plc/02:GVL_Visu.dwHeaderBg(P2 版1 表头底纹用);
- tools/ab_scripting/archive/relayout_p2.py:P2 版 1 历史施工脚本(分组边线+表头底纹+提示语改字+红灯探针),
  **已备好未跑**——注意:跑完脚本后按路线 A 教训,P2 新增装饰无动作可不洗,但若日后脚本再碰动作,必 GUI 洗。
- ⚠️ ab_sync 后 iPassed 必须仍 45/0。

## 7. 0711 深夜战报:路线A 执行与定案(computer use 全程代打)

### 执行记录
- 开工前自动化四连全绿:ab_sync -SyncOnly(05疫苗+02常量,0错)→ relayout_p2.py(P2版1)
  → audit_pages.py 审计 642 元素(越界0/真重叠3处当场修:fix_p2_polish.py)→ 完整 ab_sync **45/0**。
- **洗动作实数 21 处**(攻坚文档原记 22 为笔误:按钮 12 不是 13):
  COMMANDS 8 + STRATEGY 4 + 输入框 5 + 权重框 4。
- 动作类型实测分布:6 按钮=OnMouseDown/Up 双动作(Execute ST-Code TRUE/FALSE);
  P2 Records=OnMouseClick 切画面;Live/Snapshot+4 策略键=OnMouseClick 单动作;
  9 输入/权重框=OnMouseClick Write Variable。
- 洗法(经官方文档核证):选中元素 → Element properties 面板 → Input configuration →
  事件行 **Configure...** → 对话框不改直接 OK。全程左键可达。
- 洗后 Ctrl+S → F11(0 errors, 1 历史 warning)→ Online Login(经许可提醒弹窗)→ F5 RUN。

### 长跑验证(仿真钟 01:59:57→01:49:13,10.7 分钟,零失效)
Load demo(一击 N=20,**疫苗生效:不再 N=260 风暴**)/ Run assign(DONE_OK+绿格+统计)/
策略 4 键含 **5 连击压力** / Save snapshot(灯亮)/ Live-Snapshot 切换 / P2 切页 /
P2 Query 901 未命中(-1)与 ID=5 全字段命中+Found 灯 / Back to P1 / Reset(**清统计修复实证:四项归零**)/
P1 五输入框+四权重框全部弹 Numpad-Keypad(IEC 引号 title 修复实证:标题正常)/
单件 Add goods 901→SUGGEST_X=0_Y=19 / **Animate 实跑**(crane I/O→(0,19))/ P2 翻页 First row:11。

### 新发现(未结,按优先级)
1. **P2 RECORDS 表头行整行不渲染**(Goods ID/Column X/Tier Y/Path/Travel 连框带字消失)——
   五表头绑 GVL_Visu.dwHeaderBg 后出现,疑似 VISU_ST_STYLE 元素绑 Color variables 的层3 变体。
   候选修:撤绑恢复(稳)/删重建 RELATIVE 模式表头(对齐 400 格病理修法)。**待拍板**。
2. 演示对齐清单新增:①Total goods=21(SUGGEST 未确认件也计入 aGoods 口径)
   ②Animate 在 DONE 态无 pending 目标时无动作(语义正确,演示 v3 须写前置)。
3. **AB 许可 22 天后到期**(licensing 弹窗实证,约 8/2)——比赛 8/26 前必须处理续期,记入待办。

### 举一反三(脚本建画面的终局纪律)
ScriptVisualization 建的**任何带输入动作的元素,交付定义=生成→GUI 洗→在线长跑**三段,
"生成完编译过"不算完。visu_gen/README 与经验文档 §四b 已同步此纪律。

## 8. 0711 04:00 终局:冷启免疫复验通过 + 12 步全线贯通 + FB 沿冻结案(块3 功能收口)

- **冷启免疫复验 ×2 通过**:两次全新 AB 进程(冷启→login→操作),合计 50+ 次交互零失灵——
  路线A 定案升级为**跨会话铁定案**。
- **12 步演示全验收**(v3 台本,每步截图核对):Load demo N=20 / 策略切换 / 批量 DONE_OK+统计 /
  快照灯 / **Reset 真重建**(READY_PRECOUNT 重算+格子回棋盘)/ **二次批量统计恢复非零**
  (186.0/9.3/0.4/370.0)/ 单件 SUGGEST 黄格+**路径蓝列**首次全程可见 / Animate crane 实跑到位 /
  超重 ALARM_NO_FEASIBLE_SLOT+灯翻转+**Banner 报警行带时间戳实弹**(批次 D 全链路首次实证)/
  P2 重建表头在线渲染+翻页+Query 命中 / 调参 T1=7→落位件 Handling=7.00 联动。
- **第六案:FB 沿检测冻结**(12 步实跑首踩,已修+45/0):FB_Stats/FB_InitWarehouse 属
  "单拍完成+完成拍输入 TRUE+完成后停调"模式,`xOld:=xExecute` 冻结→Reset 不真重建仓库、
  二次批量统计跳算(疫苗清零后暴露的老 bug;旧版被残值假象掩盖)。修=完成即撤沿
  `xOld := xExecute AND NOT xDone;`(04 两处)。全 8 处沿检测排查:其余 6 处健康
  (完成拍电平已回 FALSE)。判定口诀入经验文档 §五d.6。
- **AB 许可 22 天弹窗二次实证**(这次未被遮罩,直接截图留档)。
- 残留项处置:红灯换图像**关闭**(灯黑↔亮圈翻转已是有效报警反馈,YAGNI);
  Lamp 属性 GUI 路径不再追。
- 块3 剩余:录屏(用户解说)→ 收口复盘。
