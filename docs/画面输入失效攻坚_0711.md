# 画面输入"间歇性全哑"攻坚战况(0711 凌晨,compact 交接;未解,继续攻)

> 唯一未解问题:**IDE 在线画面的输入(按钮/键盘)间歇性全部失效,显示层正常**。
> 用户拍板:今晚必须解决。本文档=完整证据链+已排除项+按优先级排序的弹药清单。

## 1. 症状与复现规律

- 失效态:点击任何按钮/输入框无反应;文本/颜色变量**正常刷新**;VISU_TASK Cycle count 增长。
- 健康→失效:冷启(关 AB→重开→Login with download)后健康;运行/操作一段时间后突然全哑。
- **今晚两轮发作**:轮1(P1 全哑时 **P2 仍活**)→冷启复活→轮2(P1 再哑,连全局 Reset 也不响,
  黄格残留为证;此轮 P2 未测——补测是关键证据!)。
- 干扰案已剥离(都不是本病):①"透明组框盖点击"(已删换边线条)②CmdLoadDemo 卡 TRUE 风暴
  (N=260;疫苗已写进 05)③输入积压 download 后重放 ④状态机语义(DONE/SUGGEST 态不收部分命令)。

## 2. 已排除项(证据在案,勿重查)

| 排除项 | 证据 |
|---|---|
| VISU_TASK 挂死 | Cycle count 增长+显示刷新活(0711 枪:显示与输入同任务,任务死则全死) |
| 动作序列化损坏 | P1/P2 输入框元素 XML 逐字段 diff 同构(仅参数差) |
| 画面对象骨架 | 唯一差异 UniqueIdGenerator 已证伪(回收复用型指针,非故障位) |
| 命令变量卡死 | 监视全 FALSE 时仍失效 |
| 工程编译态 | F11 0 errors;ab_sync 45/0 |
| 全网已知修复 | 0711 枪:症状同构帖(Forge《Visualization stops responding》等)全部无根因无解,官方让提付费工单 |

## 3. 最强线索(下会话首选路线)

**"GUI 洗过的动作疑似免疫"**:P2 的 7 个输入框+按钮经 GUI 打开 Input configuration→OK 重存后,
**再未死过**(轮1 P1 全哑时 P2 仍活)。P1 的 22 处动作全部是脚本(ScriptVisualization)建的,
从未 GUI 洗过。假设:**脚本建的动作存在 GUI 序列化补不全的运行时注册缺口,间歇性掉注册;
GUI 重存后进入稳定态**。

→ **路线 A(首选,约 20 分钟人工)**:P1 全部 22 处动作 GUI 洗一遍(13 按钮+5 输入框+4 权重框;
洗法=打开 Input configuration→直接 OK→下一个),Ctrl+S→F11→login download→长跑观察是否再死。
验证设计:洗完后连续操作 10 分钟+来回切 P1/P2,若不死=定案+落经验(以后脚本建画面必须 GUI 洗一遍收尾)。

## 4. 弹药清单(路线 A 失败后按序试)

1. **失效瞬间的分辨采证**(每次发作先做,别急着重启):①P2 还活吗(区分全局死/单画面死)
   ②Device Log(设备树 PLC_AC500_V3→Log 标签)有无 visu 错误行 ③编辑器切到另一画面再切回,复活吗
   ④Element List 面板看元素状态。
2. Build→**Clean all**→F11→login(全量 download)——0711 枪:同类 visu 异常社区验证率最高的通用手段。
3. VISU_TASK 参数:双击 VISU_TASK 看周期/优先级(与 10ms 主任务竞争?)试改 50ms/降优先级冲突。
4. 删除重建 VISU_TASK(查无案例,实验性)。
5. **釜底抽薪:CODESYS Control Win 软 PLC + WebVisu**(独立运行时,绕开 IDE 集成 visu 全部毛病;
   Banner/WebVisu 顺带解锁;成本=装软 PLC+平行工程/target 适配——最重但最治本)。
6. 兜底运维:冷启工作法(每轮演示前重启 AB+download,健康窗口足够 3-5 分钟录屏)——已证可用,
   用户不接受为终态,仅作录屏保险。

## 5. 环境版本(0711 实锤)

AB 2.9 Premium = CODESYS V3.5 **SP21 Patch 3**(枪证;3SLicense 3.5.20.0 是许可组件版本,勿混)。
SP21P3 已含 CDS-85095/CDS-90200 输入类修复(TargetVisu 场景,与本症状无关)。

## 6. 本轮已落盘待生效项(下会话开工前先 ab_sync)

- plc/05:①脉冲命令消费即复位疫苗(段尾)②Reset 清统计/路径显示残留;
- plc/02:GVL_Visu.dwHeaderBg(P2 版1 表头底纹用);
- tools/ab_scripting/relayout_p2.py:P2 版 1 施工脚本(分组边线+表头底纹+提示语改字+红灯探针),
  **已备好未跑**——注意:跑完脚本后按路线 A 教训,P2 新增装饰无动作可不洗,但若日后脚本再碰动作,必 GUI 洗。
- ⚠️ ab_sync 后 iPassed 必须仍 45/0。
