# 真相源与文档层级声明（0712）

> 目的：本项目文档量大（docs/ 70+ 篇 + 根 + `_通信` + 各 README），存在"同一事实多处记载、谁为准不清"的问题（一致性审计_0711pm、N2-N5、真相源体检均反复指出）。本文一次性声明**谁管什么、冲突时谁赢、按什么顺序读**。**本文只声明层级、不含具体口径数字**（数字看 canonical / 代码锁）。

## 0. 铁律（高于一切文档）
1. **git 真实为准**：任何"当前状态/HEAD/是否已提交"的事实，以 `git -C F:\abb_wh_work rev-parse origin/master` + `git log` 为终审，**文档叙述次之**（历史有 court 伪造 push 教训）。
2. **代码/数据锁 > 文档叙述**：具体数字以 `sim/out/*.csv`、`sim/tests/test_sim.py` 的回归锁、`plc/*.st` 源码为终值；文档里的数字是**引用**，可能滞后。
3. **诚实口径红线**：任何 sim 值（含对 PLC 的代理）不得称"AC500 实测/真跑"。

## 1. 三个真相源：谁管什么
| 真相源 | 位置 | 管辖域（问这类问题看它） | 现状提示 |
|---|---|---|---|
| **口径/假设真值** | `docs/canonical_assumptions.md` | 所有数字口径、几何/运动学/KPI 定义、官方答复、实验发现锁、升级包参数（A/B/C/D/F/U 条款） | ✅ 0712轨A/N5已校准：H=8.40±0.48 sample SD，sum锁8.6232，legacy-and锁7.6202；以该文件+当前sim输出/测试锁为准 |
| **技术方向全景** | `docs/技术方向总库_0711.md` | 技术路线、块1-4 进度、七策略/检测器方向、驻留点、**已完成归档(D区)**、**明确不做(E区)** | 方向类"做不做/怎么做"看这里；个别状态行（如 A5）可能滞后于下游 |
| **项目状态/决策/交接** | 根 `现状与任务.md` | 历史决策记录、块级状态、已定格局 | ⚠ 其 §5"resume 点"止于 0711、**不覆盖 0712 审查成果**；当前状态**改看 §2 接管入口** |

## 2. 接管入口（新会话/新接管按此顺序读）
> 根 `现状与任务.md §5` 已不是最新 resume 点。**当前唯一接管入口 = 下面第 1 项。**
1. **`docs/接管启动提示词_0712pm.md`** —— 唯一接管入口，指向一切当前优先级（含【最新增补】）。
2. `git rev-parse origin/master` —— 核实真实 HEAD。
3. 口径裁决终值：`docs/Claude_N5复核回执_0712.md`（N5-01~35）+ `Claude_N4复核回执_0712.md` + `Claude_N2N3复核回执_0712.md`。
4. 待办与分工：`docs/ASK决策_0712.md`（拍板）+ `docs/给Codex_口径修正+N5修复_合并提示词_0712.md`（轨A+N5 交办）。
5. 背景补充：canonical（口径）/ 技术方向总库（方向）/ 现状与任务（历史）。

## 3. 冲突裁决顺序（同一事实多处不一致时）
```
git 真实值  >  代码/CSV 锁  >  复核回执(N2-N5 裁决)  >  canonical(口径条款)
          >  接管启动提示词pm(当前状态)  >  技术方向总库(方向)  >  现状与任务(历史)
          >  其余派生文档(报告草稿/README/导览/面板)
```
- 派生文档（README、报告草稿、导览、面板、答辩材料）**永远是引用方**，与上游冲突时**改派生方**，不改上游去迁就。
- 口径数字的单一修正路径：**先改真相源(canonical)/代码锁 → 再改报告文本 → 最后重生二进制**（N4-27 图源铁律），绝不反向。

## 4. `_通信` 信箱（Claude↔Codex 协作，非真相源）
- `_通信/致Codex.md` = Claude 派给 Codex 的任务；`_通信/致Claude.md` = Codex 执行汇报。
- `_通信/codex_out/` = Codex 历次独立审查产物（N2-N5、V1-V4），属**历史证据**，须留档。

---
> 维护：本层级若变动（如轨A 改完 canonical 头部、或接管入口更替），同步更新本文与 `docs/README.md`。
