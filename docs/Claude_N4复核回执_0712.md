# Claude N4 增量裁决与处理回执 — 2026-07-12

> **历史快照说明**：本文记录修复前源码的亲验结论。0712随后已有guard前置、LoadDemo容量护栏、near统一与CB/TOB/lex等并发源码修改；正文“阻塞/未实现”状态保留作历史证据，不代表当前源码，也不代表这些修改已通过AB。最新证据边界见`docs/canonical_assumptions.md:C11`。

> 复核对象：Codex N4"审计的审计"（N4-01~41，8 阻塞[6 新+2 既有复核]/18 高/11 中/4 正面）。
> 方法：以**当前磁盘为起点**读全 N4 P0-P5 + 汇总 + 增量提示词；对 3 处载重新阻塞回原始 file:line 亲验。未改任何文件、未开 AB、未跑 ab_scripting/git。**不扩大上一轮授权，PLC ST 与 AB 项单列待确认，N4 后停手不扩 N5。**

---

## §0 当前态盘点（关键：Codex 未执行整改，我上一轮只新增 docs）

- git 亲查：Codex **无任何提交/文件改动**（工作区仅 `_通信/致Claude.md`、`致Codex.md` M + `codex_out/` 未跟踪）。故 **N4 发现的源问题基本全在磁盘上、状态=未修**。
- 我上一轮（N2/N3 复核后）**只新增 docs**（`D2量化`/`sim_plc第二层`/`报告草稿_B6B7B11`/`答辩QA卡_C7`/`接手任务表`/`Claude_N2N3复核回执` + 作战面板编辑），**未改任何现有错文件**。因此：
  - **我的这些新 docs 本身是 N4-01 的新增实例**——把 8.757/8.76 称"PLC 可实现代理/AC500 真跑"，按 N4-01 须再降级为"Python accel+static"（见 §1）。这算"上一轮改动引入的新冲突"，我认。
- 3 处载重亲验（回原始码）：N4-15 `05_PRG_Main.st:100-107` 无 MAX_GOODS 检查✓；N4-01/24 `03_Functions.st:174-177` FC_TMax 匀速式不看 xUseAccel、直接除速度✓；N4-04/26 `run_all.py:19-20` 直跑 test_sim.py 非 discover✓。**N4 全部载重结论成立，无 file:line 可反驳的实质不同意。**

---

## §1 8.757/8.794 口径再降级（N4-01，最重要增量；含更新我的口径字典）

**N4-01 亲验**：`03_Functions.st:174-177` FC_TMax 用匀速 `(COLS-1)*CELL_W/rVx` 与 `(TIERS-1)*CELL_H/rVy` 取 MAX（默认 =38s），**不随 xUseAccel 改**；Python `make_score_fn` 在 ACCEL=True 时分母 `t_max_now≈39.6667`。无测试锁加减速评分（T19 匀速评分、T22 加减速行程）。

**更新后的口径字典（本回执 §1 覆盖 N2/N3 回执 §4 中 8.757 的旧标签）**：
| 数字 | 是什么 | 不是什么 |
|---|---|---|
| **8.757** | **Python 引擎的加减速+静态权重 sim 结果**（评分分母 39.6667） | **不是"PLC 可实现代理"、不是与 PLC 数学式同源、不是 AC500 真跑** |
| **8.794** | 按当前 ST 固定 FC_TMax(38)+ST tie-break/epsilon 的**更近似 Python 只读代理**（8.794/降 60.24%/能耗 67.07%/heavy 0.5417） | **不是新头条、不是 PLC/AB 实测、不证二进制逐值复现**；用途仅=揭示 Python↔ST 评分分母差异 |
| 8.40/8.6232/6.443/7.6202/98.5/81.6/59/0 | 定义不变（见 N2/N3 回执 §4） | — |

**降级落点（我可代做的文档面）**：`D2量化_0712.md`（P 列改"Python accel+static，非 PLC 可实现"）、`sim_plc第二层_0712.md`、`报告草稿_B6B7B11_0712.md:58,127`、`答辩QA卡_C7_0712.md`、`接手任务表_0712.md`、作战面板 `:83,133`；**并纠正我 N2/N3 回执 §4 里 8.757="sim PLC-可实现代理"的措辞**。搜索全工作区仍把 8.757/8.76 绑 PLC 处一并改。**8.794 只在解释 Python↔ST 差异时出现，绝不进头条。**

---

## §2 N4-01~41 完整裁决矩阵

> 状态：❌未修（源在磁盘、需处理）｜🟡新增冲突（我上一轮引入）｜✅正面无需改。Codex 未整改→绝大多数 ❌。

| # | 严重 | 状态 | 裁决/动作（亲验或依据 file:line） |
|---|---|---|---|
| N4-01 | 阻塞 | ❌+🟡 | 8.757 再降级为 Python accel+static；8.794 不做头条。亲验 `03_Functions.st:174-177`。我的 docs 是新增实例，一并改 |
| N4-02 | 高 | ❌ | oracle golden test 读冻结 `instgen_detail.csv`、非真重跑性能；说明改准确（`test_oracle_gap.py:13-17` vs `oracle_gap.py:75-84`） |
| N4-03 | 高 | ❌ | oracle test 写正式产物、非只读；改为临时目录化（`oracle_gap.py:149-156`） |
| N4-04 | 高 | ❌ | run_all 只跑 21 测试(直跑 test_sim.py)、漏 2 oracle；"23/23"是另一次全量。亲验 `run_all.py:19-20` |
| N4-05 | 高 | ❌ | H锁/legacy锁/零违规无 `failed==0` 断言(当前 failed=0 不翻值)；补断言 |
| N4-06 | 中 | ❌ | δ=0 退化 test 只比集合、非严格等价；限定 |
| N4-07 | 中 | ❌ | T25/T58 单扫描 10万/100万 WHILE watchdog 风险；补 done/timeout 断言 |
| N4-08 | 中 | ❌ | N_CASES 非常量可在线改；改 CONSTANT |
| N4-09 | 高 | ❌ | T58 `aSlotOps>=1` 可被旧履历垫绿；改前后差值 |
| N4-10 | 中 | ❌ | T21/T55/T59 名字强于断言(烟雾测试)；改名/补断言 |
| N4-11 | 中 | ❌ | PLC 测试顺序依赖；59/0 限定为"干净初态+当前参数" |
| N4-12 | 中 | ❌ | 新增模块多数无自动覆盖；补测试 |
| N4-13 | 高 | ❌ | doc_check 不够宽+当前 exit1(11 断链+1 过时 DOCX)；扩规则+**"收尾全绿"表述须删**（我面板也误称已收口） |
| N4-14 | **阻塞** | ❌ | ParamGuard 晚于消费者→越权值改布局再回滚(值回滚布局不回滚)。`05_PRG_Main.st:57-187`消费/`:198-206`守卫。**A4"双闸"须再收窄；ST 修复待授权** |
| N4-15 | **阻塞** | ❌ | LoadDemo 无容量检查→第 21 次写 `aGoods[401]` 越界。亲验 `05_PRG_Main.st:100-107` 无 MAX 判断。**ST 修复待授权** |
| N4-16 | 高 | ❌ | Goods ID 可 0/负/重复破坏哨兵/查询；**ST 修复待授权** |
| N4-17 | 高 | ❌ | 策略中途切换竞态(启动锁/结束读实时)；**ST 修复待授权** |
| N4-18 | 高 | ❌ | nBatch/nPairs<=0 状态机静默挂起；**ST 修复待授权** |
| N4-19 | 高 | ❌ | 运行中参数无批次快照→混合口径布局；**ST 修复待授权** |
| N4-20 | 高 | ❌ | VisuViolationCount 只查重量/体积、非全约束；报告"违规0"须收窄为"承重/容积复查0"(`build_report.py:509-513`) |
| N4-21 | 中 | ❌ | SceneDetect 400 件最坏单周期只估算；标"待 AB/目标机压测"。**AB 实跑待授权** |
| N4-22 | 中 | ❌ | 快照捕获瞬态状态；改从 aSlots 重建或限 DONE 态 |
| N4-23 | 阻塞复核 | ❌ | 检修存量冻结仍未修(=我 B-2/A3)；`04_FB_Warehouse.st:286-326`。文档降级已在 N2/N3 面，**ST 修复待授权** |
| N4-24 | 阻塞复核 | ❌ | FC_TMax 除零仍无护栏(=我 B-1)+不随 xUseAccel 改分母。亲验 `03_Functions.st:174-177`。**ST 修复待授权** |
| N4-25 | 正面 | ✅ | 合法输入下核心数组/路径上界设计稳；保留 |
| N4-26 | **阻塞** | ❌ | run_all 非全量再生(漏专项/报告/PPT/oracle、打印旧图as产物)。亲验 `run_all.py:19-30`。拆 core/report/verify |
| N4-27 | **阻塞** | ❌ | H 主结果页嵌匀速+静态 fig1、图内写 and(code sum)、PPT 标 3 seeds(实 5)。**先改 plots/experiments 口径标签再重生** |
| N4-28 | 高 | ❌ | `项目全景说明书.docx` 孤儿(7.42/356/23-18 tests 绕 doc_check)；归档加水印或重建源 |
| N4-29 | 高 | ❌ | `验证报告_draft.docx` 写 45 项+H/自适应/AC500 全链；改生成器后重生 |
| N4-30 | 高 | ❌ | build_report 缺输入仍 save+exit0；加 body_miss==0 门 |
| N4-31 | 中 | ❌ | 用户 Word 只靠 mtime、1 份过时；加 source_sha256 |
| N4-32 | 中 | ❌ | 二进制元数据不证本轮生成(默认模板 2013 日期)；嵌 manifest |
| N4-33 | 正面 | ✅ | 内嵌图无旧残骸(16/16、8/8、10/10 SHA 一致)；改图源后重生即可 |
| N4-34 | 正面 | ✅ | 图片关系完整无孤儿；保留 |
| N4-35 | **阻塞** | ❌ | 无依赖声明(requirements/lock)，干净机跑不了；建 core/report/tools 三档锁 |
| N4-36 | 高 | ❌ | sim/out/figs 忽略但 run_all 不能全重建；建发布制品包+哈希 |
| N4-37 | 高 | ❌ | 无提交 manifest；143MB 含不应提交(59MB AB bootinfo/28.5MB 视频/PDF/_通信)；建 allowlist |
| N4-38 | 中 | ❌ | 工具绑 F:/用户目录/spike 路径；ROOT 参数化 |
| N4-39 | 中 | ❌ | 无 LICENSE/第三方再分发清单；建引用清单 |
| N4-40 | 高 | ❌ | doc_check 当前 exit1、非全绿；"收尾全绿"表述须删 |
| N4-41 | 正面 | ✅ | 相对路径+固定种子基础较好；保留 |

**汇总**：38 项 ❌未修（含 N4-01 兼 🟡我引入的新冲突）+ 4 项 ✅正面（25/33/34/41）。**实质不同意：无。**

---

## §3 联动检查结论（用户点名 5 组）

1. **图源污染（N4-27）**：成立且是根因——`plots.py:37-51` 默认 ACCEL=False+静态，fig1/5 图内写 and(code sum)，PPT/报告 H 段嵌这些静态图。**处理顺序铁律：数值/口径真相源 → 图表计算与图内标签(plots/experiments) → 报告/PPT 生成器 → 二进制重生 → 文本+图片哈希+视觉 QA。绝不先重生二进制再修图源。** 正面：内嵌图无旧残骸(N4-33/34)，问题在图源语义非藏旧图。
2. **测试假绿（N4-02~13）**：run_all 只 21 测试且末尾 doc_check exit1(N4-40)→**"一键复现全部/全量回归/收尾全绿"全部过强，须删/限定**（我的报告与面板也犯此错）。oracle test 非真重跑+写正式产物；H/legacy/零违规缺 failed==0。
3. **生成链（N4-26/29/30）**：run_all 非产物 DAG；build_report 缺输入 save+exit0；孤儿全景 DOCX 绕联动。**验收须 body_miss==0 + required source 齐 + manifest。**
4. **PLC 状态副作用（N4-14~24）**：ParamGuard 副作用不回滚(14)、LoadDemo 越界(15)、ID 契约(16)、策略竞态(17)、分片挂起(18)、无批次快照(19)、ViolCnt 非全约束(20) + 既有 A3 冻结(23)/FC_TMax 除零(24)。**均需改 ST，单列待授权（见 §4）。**
5. **提交包（N4-35~40）**：无依赖锁/manifest/allowlist/LICENSE；143MB 含缓存/视频/内部材料/PDF；干净机不可复现。**当前不宜整仓压缩提交。** 提交期未到，非违约，属交付准备缺口。

---

## §4 授权边界（不扩大上一轮授权）

- **我这轮可代做（沿用上一轮"整改交 Codex"授权，文档/生成器/文本/计划层）**：§1 的 8.757/8.794 口径降级；N4-13/20/26/27/40 的"全绿/一键复现/违规0/双闸"话术收窄；N4-27 图源标签修计划；N4-28/29/30 生成器与孤儿 DOCX 处理计划；N4-35~39 的 requirements/manifest/allowlist/LICENSE **计划**（起草，不代跑提交打包）。
- **单列待你确认（不在本轮动手）**：
  - **PLC ST 修复**：N4-14(guard 前置/快照)、N4-15(容量检查)、N4-16(ID 契约)、N4-17(策略锁存)、N4-18(分片钳位)、N4-19(批次快照)、N4-20(ViolCnt 全约束)、N4-23(检修源位冻结)、N4-24(除零护栏)——改 ST + 补测试 + ab_sync，属方案乙同类，需你拍板+在场。
  - **AB 实跑**：N4-21(400 件压测)、N4-07(watchdog 风险)、N4-P5 的 guard 副作用/重复 LoadDemo/策略中途切换/非法分片/sum+ACCEL 六组反例——需 AB，单列待验。
- 我可先给这些**逐文件补丁计划 + 测试反例 + 风险顺序**（不动手），你拍板后再执行。

---

## §5 敏感信息（N4-P4 预检，遵 CLAUDE.md 红线）

Codex 只做文件名级候选扫描（key/secret/token 命中 17 文件、邮箱/路径 7 文件），**未读取/输出任何值**。我同样：**不打印、不摘要、不写入任何密钥/凭据/PII**。提交前须跑专门 secret/PII scanner（只报文件+规则，不落值）。此项我不代做（涉密，待你亲自或授权工具跑）。

---

## §6 停手，不扩 N5

N4 复核完成，未改任何文件。待你授权后，我按"§1 口径降级 → 图源标签 → 生成器 → 二进制重生 → README/面板/审计"顺序**起草 Codex 修改提示词**（含把 N4 项并入上一份口径修正提示词），PLC ST 与 AB 项另出补丁计划待你拍板。**不自动扩 N5。**
