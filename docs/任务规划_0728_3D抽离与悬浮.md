# 任务规划 · 0728(3D 公共模块抽离 + 热力升级 + 双页悬浮全覆盖)

> 本文是 compact 后的恢复入口。上游:docs/会话断点_0722_一屏返工收口.md。

## 0. 0728 七问拍板(用户原话逐字)

1. 3D 架构 → **「抽公共 3D 模块,两页共用(推荐)」**
2. 热力形式 → **「整格色阶 + Golden Zone 热区(推荐)」**
3. 货物运输速度 → **「两者都要」**(01 运动表现力提升 + 口径差异披露更显眼)
4. 悬浮覆盖 → **「全覆盖 + 首次进页引导」**
5. 抽离策略 → **「分三步递进,每步全绿才下一步(推荐)」**
6. 悬浮文案 → **「1,但是注意用词要学术」**(= 三段式:是什么 / 为何这么做 / 优在哪;学术工程风用词)
7. 优先级 → **「先打地基:3D 抽离 → 热力+悬浮 → D/W4」**

## 1. 关键诊断(已核实,compact 后勿重查)

### 1.1 两页 3D 是「同源分叉」——代差根源
- 都从 `tools/anim3d_formal.py` 衍生,共享几何契约(`addBox`/`RACK`/`INFEED`/`TRANSFER_ANCHOR`;01 有 59 处同名标记、02 有 64 处)。
- **但各自内联在自己的 html 里独立演化,无共享文件**:01 的 3D 母版在 `src/01_连续填仓.html` 内联 script(约 1594-2088 行,含常量 1596、FRAME_BOUNDS/相机 ~1925-2010、targetFx ~1858-1890、堆垛机 ~1890+);02 的在 `src/02_入出闭环.html` 内联 + `src/s3_ac_runtime.js`。
- 谁改了特性另一页不跟进 → 热力/FX/运动细节全面代差。

### 1.2 热力图差距(用户点名)
| | 01 现状 | 02 现状(目标形态) |
|---|---|---|
| 载体 | 货箱**顶盖**薄片色阶 `heatLidMesh`(`s3_fill_candidate_runtime.js` ~319 行,0722c 我加的) | **整格**铺热度色阶 `cellHeatMesh`(`s3_ac_runtime.js:880`) |
| 热区包络 | 无 | **近 I/O 热区包络 Golden Zone**(`s3_ac_runtime.js:943`,热门货所在格包围盒,半透明琥珀+亮描边) |
| 可读性 | 单层、视觉弱 | 双层;总览尺度直读「热门贴底近 I/O」(`s3_ac_runtime.js:1570` 注释:heat 开=整格只铺热度色阶,一图一事) |
- 色阶口径两页一致:冷灰 `#aeb8c0` → 琥珀 `#e7b800` → 热红 `#c0392b`;01 用 `freqRankByGid` 秩归一,02 用 `freq_true` 秩归一。

### 1.3 悬浮解释现状
- 01:`metricHelp` + `.tipBubble` 共 39 处,但**主要挂在 KPI 数字上**(弹层内居多),主界面常驻区覆盖少。
- 02:`hasFixedTip` + `tipBubble` 共 11 处(`TIER_TOOLTIP`/`PROFILE_TOOLTIP` 等),机制不同。
- 目标:两页都做到「主界面整洁,悬浮出解释」,**三段式学术文案**。

### 1.4 速度(A 任务已定论,勿再当 bug 修)
- 两页 sim 模型本就不同:01 = `trapezoid_accel_plus_laden_vertical_factor`(满载升降折减 `laden_vy_factor=0.8`,`warehouse_sim.py:78`,行业依据 Mecalux 0.818/Muvro 0.667);02 = `trapezoid_accel` 无折减。
- 运动学常量位置:01 在 `01_连续填仓.html:1596`;02 在 `s3_ac_runtime.js:88`(另 `02_入出闭环.html:1216` 有同值重复声明)。载货降速改动点:01 `s3_fill_candidate_runtime.js:214`;02 `s3_ac_runtime.js:688-689`(无折减)。
- 曲线函数 `axisT/axisPos/axisV` ↔ `axisTime/axisPosition/axisVelocity` 默认分支**逐字数学等价**。
- 已做:01 口径卡 + 02 档位悬浮如实披露(commit `ddf27f4`)。本轮补:①01 运动表现力观感提升 ②披露更显眼。

## 2. 任务分解(按拍板优先级)

### 阶段一:3D 公共模块抽离(地基,三步递进,每步全绿才下一步)
- **步1 几何+材质**:抽 `src/s3_scene_core.js`(场景搭建/货架/堆垛机/传送带/材质表/几何契约),两页 html 改为 `<script src>` 引入 + 各自差异配置(01 双链传送带、02 单链等)。风险最低、同源度最高。
- **步2 热力+FX**:抽热力层(整格色阶 + Golden Zone)与目标 FX;**01 顺带升级到 02 形态**(本阶段解决用户点名的热力差距)。01 的 `heatLidMesh` 顶盖方案退役。
- **步3 运动学**:抽 `axisT/axisPos/axisV` 与 `motionPoint`;两页各自传入自己的 motion 配置(01 带 `laden_vy_factor`,02 不带 + 匀速档)。**严禁改数值口径**。
- 每步验收:契约 30/30 fail0 + 01QA 61/61 + 02QA 8/8 + uniform 8/8 + 03QA 19/19,截图双视口(1552×900 / 1945×1094)亲验,独立 commit。

### 阶段二:热力升级 + 双页悬浮全覆盖
- 01 热力升级(若步2 未完全覆盖则补齐):整格色阶 + Golden Zone,图例同步。
- **悬浮全覆盖(01+02)**:所有 KPI 数字、图例、控件、图表区块、状态徽章都有悬浮解释。
  - 文案三段式:①指标含义 ②设计/算法理由 ③优势数字或依据。**用词学术工程风**(参照 docs/升级设计依据_0721.md 的措辞纪律:硬标准/跨域类比/厂商实践/自主设计分级,不得把架构标准包装成规范)。
  - **首次进页引导**:一次性微提示「悬浮查看解释」,localStorage 记忆已看过(键名与既有 `s3_collapse_01_v1` 风格一致),不打扰二次访问。
- 01 运动表现力提升(观感)+ 速度口径披露更显眼。
- 验收:五套全绿 + 悬浮覆盖率清单(逐元素列出已加悬浮)+ 双视口截图。

### 阶段三:收尾
- **D 00 导览页对齐**:衬线品牌(Georgia/宋体)→ 白红工程风,与 01/02/03 顶栏一致。
- **W4 打包 portable exe**:electron-builder portable + extraResources 素材进包 + main.js 开发/打包态 `resourcesPath` 分支 + SmartScreen 说明 + 浏览器直开后备 + 打包后自检(四页/素材/控制台)。
- W5 评委机模拟(真机,用户在场)。

## 3. 红线(全程)
- 负结果七条(03 `#neg`)h3/p innerHTML 逐字节不动;54/66 口径句含「份」字;`scopeBand` 保留。
- 命名总表词根;契约断言读 `archive_` 归档勿误改;禁 nth-of-type;黄牌 `#targetBeacon` 永不复现;金顶盖已废止勿复活。
- 不动 sim/trace/manifest 与 IO/TRANSFER_ANCHOR/RACK 锚;`l2_factoryio/` 素材只读;根目录 `1_给你看/`、`2_你要操作/` 用户文档禁触。
- 白底 ABB 红(`#ff000f`)工程风;禁深色大底顶栏。
- **施工纪律**:主会话亲手做,不外包 codex(0724 codex 交付质量差且清空了 pnpm 缓存致 QA 全崩,教训在案)。

## 4. 环境与验收命令(cwd=`F:\abb_wh_work\l4_showcase`)
```
node tools/test_s3_runtime_contract.mjs      → tests 30 / pass 30 / fail 0
node tools/capture_s3_layout_a_qa.mjs         → PASS 61/61
node tools/capture_s3_mixed_layout_a_qa.mjs   → PASS 8/8
node tools/capture_s3_fill_uniform_qa.mjs     → PASS 8/8
node tools/capture_fio54_evidence_qa.mjs      → PASS 19/19
cd app_shell && node verify_material_route.mjs → ALL PASS 4/4
```
- Playwright **已装进项目本地**(`l4_showcase/node_modules`,依赖记入 `l4_showcase/package.json`);环境再坏执行 `npm i` 恢复。Chromium:`C:/Users/86177/AppData/Local/ms-playwright/chromium-1223/chrome-win64/chrome.exe`,args `['--use-angle=d3d11','--enable-unsafe-swiftshader']`。
- 自写脚本:静态服务器 root=仓库根;01 就绪判据 `window.__S3_FILL_QA && !snapshot().loading`;02 用 `window.__S3_QA`;03 用 `load` + `document.fonts.ready`。壳内容区 1552×900,用户大窗 1945×1094。

## 4.5 步1 完成记录(0728)

**产物**:`l4_showcase/src/s3_scene_core.js`(新建,约 320 行)。两页内联三维母版从 475 行(01)/436 行(02)压到 154 行 / 110 行,总行数 01 = 2089→1768、02 = 1660→1334。

**切分口径**:
- 进内核:常量(N/Z0/VX-AZ/IO/TRANSFER_ANCHOR/RACK/C)、渲染器-场景-相机-OrbitControls、材质表 M 全量、几何契约(addBox/addInstances/deriveInstanceBounds/addSegmentInstances/partBox/partCylinder/partFaceLabel/addFloorFlowLabel)、地面与地贴、货架全套 + `__S3_GEOMETRY_AUDIT`、巷道轨道、传送带构件函数、五座工位 + 传感器 + 防撞柱、addPath/PiecewiseLinearCurve、目标 FX 基体(glow/face/box/pad)、堆垛机全套、灯光、相机限位与 constrainCamera。
- 留页面(拓扑差异):`S3_SCENE_CONFIG`(infeed/outfeed/topology/station/path)、01 的 CROSSBAR 与 T 形交接台、02 的 I/O 交接台与队列延伸带、各自的 addTransferBridge 签名、传送带走线与地贴文字、`__S3_TARGET_FX` 组装、setCamera/resizeScene(01 自适应取景 vs 02 固定机位)。
- **未动**:`axisT/axisPos/axisV` 仍在各页(步3 范围,改动牵连速度口径)。

**参数化的四处两页差异**:`station.sensorYOffset`(01 = 0 / 02 = −.18)、`station.bollardYOffset`(−.24 / −.25)、`station.bollardStyle`(amber / hazardStripe 黄黑环纹)、`path.arrowCountMin`(1 / 0)。02 的 INFEED/OUTFEED 用 `IO_X/IO_Y` 保留原逐字算式(避免十进制字面量与浮点减法结果不等:`0.5-0.95 = -0.44999999999999996`,非 `-0.45`),内核载入后立刻断言 `IO_X===IO.x`。

**等价性硬核验**(scratchpad/parity.mjs):拦截 runtime/interactions/store 脚本使场景=纯母版产物,对 `git show HEAD:` 取出的改造前页与改造后页各算场景签名(对象类型/名称/几何类型与参数/材质色-透明度-贴图-emissive-粗糙度/位置-旋转-缩放/可见性-投影-接收阴影-renderOrder/InstancedMesh 全量实例矩阵哈希),排序后 sha256:
- 01:220 对象,新旧同为 `5ff425e1b9faef2a`;灯光 `e987caf72d891ae8`;`__S3_GEOMETRY_AUDIT` 逐字段一致
- 02:239 对象,新旧同为 `b0bc7f2cf22ee5ca`;灯光 `270111dcb066ac93`;同上
(唯一噪声是 three.js 每次加载给 Shape 新生成的随机 uuid,已剔除。)

**QA 脚本改动**:`tools/capture_s3_layout_a_qa.mjs` 的哈希快照清单补 `sceneCore`。**坑**:该脚本把页面依赖复制进 `_local_hash_snapshot_<id>/` 再服务,清单漏文件不会报错而是页面 3D 起不来 → 断言归零显示 `FAIL 0/0`(首跑就踩了)。新增页面级依赖时必须同步 `liveFiles` 与 `snapshotTargets` 两处。

**步2 待拍板**:02 页仍有大头针式竖直箭头(`targetArrow`,02html 原 1552-1560,现移至页面尾部),而 01 已于 0722 淘汰,当时的理由「02 早已无箭头实体」经本次实测**不成立**(02 截图可见琥珀箭头浮在目标格上方)。步2 统一 FX 时需拍板:两页都保留箭头,还是两页都只留发光壳 + 格口面框。

**回归**(cwd=`F:\abb_wh_work\l4_showcase`):契约 30/30 fail 0、01QA 61/61、02QA 8/8、uniform 8/8、03QA 19/19、壳素材路由 4/4;双视口(1552×900 / 1945×1094)截图亲验两页三维正常。

## 4.6 步2 完成记录(0728)

**热力**(commit `b3fef20`)——新建 `l4_showcase/src/s3_heat_layer.js`(96 行):色阶 + 前罩 InstancedMesh + 热区包络三件两页共用。
- 01 实质升级:载体从**箱顶薄片**(`stockLidGeometry` .86×.88×.08 叠在箱盖上方,正视角只露一条薄边)改为**整格前罩** .96×.96 铺满格口。前罩 y 由本页货箱尺寸留出的净空带定:货架前缘梁上界 .042、箱盖前脸 .06(比箱体 .07 更靠前,是真正下界)→ 中心 .051、厚 .012,两侧各 3 mm。02 侧只换实现,数值全不动。
- 01 新增热区包络(原本没有)。
- **修真 bug**:旧实现 `heat3DOn=false` 时不写实例矩阵,关掉热力再打开会一直空白到下次库存变化。改为矩阵/颜色恒写满、开关只切 `visible`。
- **图例文案自查纠错**:初版「热门货聚集区 · 近存取口(P&D)」在终态是**假陈述**(267 件全入库时 53 件热门必然摊满 20×20)。改为只写口径「热门货包络 · freq 前 20% 所在格」。
- 核验:02 前罩 120 实例矩阵+颜色哈希 `3a359138c519f2a7` 与 HEAD 逐字节一致;01 前罩 y 带 [.045,.057] 与实测的箱体 [.07,.93]、箱盖 [.06,.94]、前缘梁 [-.022,.042] 不互穿。
- 三算法 × 五节点热区实测(`activeLane` 回读自证):67 节点 SEQ=col0-4/tier4-19(竖长条)、NEAR=col0-14/tier0-5、SCORE=col0-15/tier0-4(贴底);267 全满三者都是全场(容量必然)。

**FX**(commit `de1f409`)——目标箭头 `targetArrow` + `__S3_TARGET_FX` 契约收进 `s3_scene_core.js`,01 按用户 0728 拍板恢复箭头。
- **前情更正**:0722c 我删 01 箭头的理由「02 早已无箭头实体」是错的,02 一直有(0717 #28 用户拍板原文即「发光壳 + 大头针式竖直箭头」)。
- **修真缺陷**:旧遮挡守卫 `Math.abs(frame.machine.x - col) < .60` 只看水平距离;箭头插进载货台的条件是**三维**的(水平接近目标列 **且** 升降高度扫过目标层)。改为包围盒真实相交判定 `targetArrowBlocked()`。同一检测器下 168 帧采样:修前箭头可见 46 帧 / 被吞没 **2** 帧,修后 44 帧 / **0** 帧;出画 0、越取景框顶 0、相位可复现。
- 首版扫描脚本用桅杆∪载货台并集 AABB 误报 2 帧,已改逐部件判定(上述数字出自修正后的检测器)。
- QA:作废 `arrowRetired` 恒真断言;新增 `fx28ArrowNeverSwallowed`;01QA 61 → 63 项。

## 4.7 步3 完成记录(0728,commit `1e5c5fc`)

新建 `l4_showcase/src/s3_motion.js`(82 行):`axisTime/axisPosition/axisVelocity/axisState/point` 两页共用。两页差异由调用方传参声明,**不在模块里统一**:01 传 `ladenVzFactor: loaded ? 0.8 : 1`,02 传 `motion: trace.meta.motion`(含 `constant_speed` 匀速档)。

顺带清死代码:`02_入出闭环.html` 页内的 `axisT/axisPos/axisV` **从未被任何脚本调用**(02 runtime 用自己那套),随抽离删除;01 页内那份改走 S3Motion 后同样删除。

**永久口径锁** `l4_showcase/tools/test_s3_motion_parity.mjs`(5/5 全过):抽离前的原始实现逐字钉为参照,逐点 `Object.is` 比对(不是约等于)——单轴梯形档 700+ 点、单轴全档位、01 双轴含折减 104,000 点、02 双轴含匀速档 208,000 点全部一位不差;外加**反向断言**「两页口径差异必须仍然存在」。以后谁再想顺手统一速度,这个测试立刻红。

接线核验(`scratchpad/motionparity.mjs`):两页各 420 帧密集 seek,逐帧比对桅杆/载货台/货物世界坐标与轴 HUD 文本 —— 01 哈希 `e8ca2c5c941006d6`、02 哈希 `6754cf4e8a33b1cc`,新旧相同。

契约测试修:它在 Node 里直接 `require` runtime 并调 `motionPoint()`,故须先 `require` `s3_motion.js` 把 S3Motion 挂上 globalThis,顺序不能反。

**三步累计**:01 页 2089 → 1746 行、02 页 1660 → 1302 行;内联 3D 段 01 = 179 行、02 = 129 行(抽离前 475 / 436);公共模块 `s3_scene_core.js` 423 + `s3_heat_layer.js` 96 + `s3_motion.js` 82 行。

**当前全绿基线**(cwd=`F:\abb_wh_work\l4_showcase`):运动学口径锁 5/5 fail0、契约 30/30 fail0、01QA **63/63**、02QA 8/8、uniform 8/8、03QA 19/19、壳素材路由 4/4。

## 4.8 悬浮全覆盖完成记录(0728,commit `ac148d0`)

新建 `l4_showcase/src/s3_tooltip.js`(悬浮层)与 `l4_showcase/src/s3_tooltip_copy.js`(文案表)。

**关键实现决策**:用**事件委托 + 选择器注册表**,不用逐元素 `addEventListener`——两页的指标区/图例/步骤条都是每帧 `innerHTML` 重渲染,逐元素绑定第一次重渲染就全失效(挂上去看着有,演示到第二帧就没了)。气泡 `position:fixed` 脱离 overflow 裁剪链,自动上下翻转并夹进视口;hover + focus 双通道;Esc 关闭;自动摘掉元素原生 `title`(否则系统气泡与自定义气泡叠在一起)。

**覆盖实测**:01 注册 28 项命中 **62** 个元素、02 注册 25 项命中 **37** 个元素;未命中 0、三段不全 0。02 的 7 个 KPI 芯片原有各自具体口径说明,**没有用泛化文案覆盖掉**——改为打 `data-metric` 标记后逐项写三段式;01 的 5 个指标芯片同法。

**首次引导**:localStorage 键 `s3_tip_hint_01_v1` / `s3_tip_hint_02_v1`,15 s 自动收起;语义定为「出现过一次就算数」而非「点过按钮才算数」;位置 `bottom:150px`(贴底 18px 会盖住两页 110–130 px 高的仪表 dock)。

**QA**:新增 `tooltipFullCoverage` 门(注册表逐条命中 + 三段齐全 + 实测悬停 8 项确认气泡弹出/三段渲染/不出画)。01QA 63 → **64** 项。

**自查纠错**:批量注入 `data-metric` 的脚本把 `conservedCell` 挤出了 class 属性,已修回。

## 5. 本会话已完成(commit 链)
| commit | 内容 |
|---|---|
| `5f4d374` | 修复并加固 QA 环境(codex 清空 pnpm 缓存致全崩 → playwright 本地化) |
| `ddf27f4` | 任务A 速度口径:查明非 bug,不改运动学,改如实披露 |
| `2b19067` | 任务B 01/02 顶栏对齐(实测本已一致,只修暂停钮为深蓝主按钮) |
| `2b2f883` | 任务C 03 页 4-Tab → 大图沉浸展陈风滚动式重设计 + QA 重写 |

codex 的 0724 交付完整存于 `stash@{0}`(未采用,可查证)。
