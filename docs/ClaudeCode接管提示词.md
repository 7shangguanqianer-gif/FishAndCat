# Claude Code 新账号接管提示词(整段复制发给新会话)

> 与 docs/Codex总对接提示词.md 的区别:这份面向有完整工具能力的 Claude Code,
> 授权其直接开发代码;Codex 版只授权执行。两份都自包含,可独立使用。

```
你接管「2026 ABB杯赛道一·智储优控·立体仓储仓位优化」项目的全部工作(算法开发/PLC/文档/战略),
前任是另一个 Claude Code 会话,你没有它的记忆——一切以仓库文件为准,不要凭空假设任何进度。

项目根:F:\abb_wh_work(git 管理,远程 GitHub 仓库 FishAndCat,做完活必须 commit+push)。

【接手三步,做完才许动手】
1. 读 现状与任务.md:§1 目标与拍板项、§3 排期、§5 当前状态与待办任务清单、§6 已定决策(不许翻案);
2. 读 docs\canonical_assumptions.md(一切建模假设/参数/指标口径的唯一真相源)
   和 docs\实验发现.md(F1-F11,已有结论不要重复发现);
3. 跑 python sim\run_all.py(1-2分钟)——18 用例全绿+违规0 = 底盘完好;红了先报告,不许自修。

【铁律(前任用血泪换的,违反=返工)】
- 数字口径只认 canonical:引用任何数字必带口径标签(匀速/加减速、单程/双程、seed、规则、分布);
  文档禁止硬编码可再生数字。改评分函数/参数=口径变更:①用户批准 ②同步 canonical
  ③重跑 python sim\export_st_vectors.py 重生成 plc\07(生成文件,禁手改)④全量回归。
- 一切随机固定 seed;策略对比必须同一批货物同一负载。
- plc\*.st 与 sim\warehouse_sim.py 是同一算法两个实现,改一边必须同步另一边并过一致性向量。
- 回归测试红了=有人动了口径,定位报告,不许改测试或放宽容差。
- 结果如实写:负结果/平结果也是工程叙事(见 F11 的朴素重排反例),绝不编数字。
- 每完成一个任务:更新 现状与任务.md §5(整体重写非打补丁)+ git commit + push。
- 遇题面歧义或需用户拍板的事:列清选项停下来等,不许猜一个当已定。
- pip 安装统一加 -i https://mirrors.aliyun.com/pypi/simple/

【用户画像与协作方式】
- 用户零基础但要学习全部技术,喜欢边实操边学:交付时附"怎么跑、看什么、学到什么";
- 用户目标是赢不是交差:进度超前时主动提差异化,拒绝"继续打磨"式空答(读 docs\差异化战略 §1/§7);
- 每周五出周报+验收清单(每条=用户可自查的硬标准);
- 有 3 个待拍板项挂着(现状与任务 §5 末尾),用户拍板前不要擅自变更相关默认值。

【当前待办(以 现状与任务.md §5 为准)】
T11 场景自适应权重策略表(adaptive_weights.py)→ T12 鲁棒性压力矩阵(stress_matrix.py)
→ T13 DRL 小型对照(可选)→ W4(7/27起)决赛可视化+录屏 → W5 报告生成器(T9)→ W6 红队+提交。
设计意图与验收标准写在 docs\差异化战略_如何超过别人.md §7 和 现状与任务.md §3。

先完成接手三步,汇报你看到的状态,然后从待办第一项开工。
```
