# Claude Code 新账号接管提示词(整段复制发给新会话)

> 与 docs/Codex总对接提示词.md 的区别:这份面向有完整工具能力的 Claude Code,
> 授权其直接开发代码;Codex 版只授权执行。两份都自包含,可独立使用。
> 维护规则:本文件不写任何易变状态(用例数/任务进度/头条数字)——那些以命令输出
> 和 现状与任务.md §5 为准,所以本文件不需要随进度更新。

```
你接管「2026 ABB杯赛道一·智储优控·立体仓储仓位优化」项目的全部工作(算法开发/PLC/文档/战略),
前任是另一个 Claude Code 会话,你没有它的记忆——一切以仓库文件为准,不要凭空假设任何进度。

项目根:F:\abb_wh_work(git 管理,远程 GitHub 仓库 FishAndCat,做完活必须 commit+push)。
目录角色:1_给你看\=用户阅读层;2_你要操作\=用户动手层;sim\ plc\ docs\ _ref\=机器工作区;
现状与任务.md 在根目录(单一真相源)。

【接手三步,做完才许动手】
1. 读 现状与任务.md:§1 目标与拍板、§3 排期、§5 当前状态与待办(唯一进度真相)、§6 已定决策(不许翻案);
2. 读 docs\canonical_assumptions.md(一切假设/口径的唯一真相源)
   和 1_给你看\实验发现.md(F 系列已有结论,不要重复发现);
3. 跑 python sim\run_all.py(1-2分钟)——全绿+违规0=底盘完好;红了先报告,不许自修。

【铁律(前任用血泪换的,违反=返工)】
- 数字口径只认 canonical:引用任何数字必带口径标签;头条数字只从 sim\headline.py 的输出取,
  文档禁止硬编码可再生数字,更禁止在静态文档里写"当前N个用例/N个任务"这类易变状态。
- 改评分函数/参数/场景=口径变更:①用户批准 ②同步 canonical ③重跑 python sim\export_st_vectors.py
  重生成 plc\07(权重策略表另涉 warehouse_sim.WEIGHT_POLICY 与 plc\08,三处同源)④全量回归。
- 一切随机固定 seed;策略对比必须同一批货物同一负载。
- plc\*.st 与 sim\warehouse_sim.py 是同一算法两个实现,改一边必须同步另一边并过一致性向量;
  **ST 注释密度是评委点名的得分项(2025评语原文),任何改动不许删注释**。
- 回归红了=有人动了口径:定位报告,不许改测试或放宽容差。
- 结果如实写:负/平结果也是工程叙事(看 实验发现 的 F11 反例),绝不编数字。
- 遇题面歧义或需用户拍板的事:列清选项停下来等,不许猜一个当已定。
- 用户层文档(1_给你看\ 与 2_你要操作\)一律配同名 Word 版:md 是唯一的源,
  改完 md 跑 python tools\make_user_docs.py 再生 docx(doc_check 会核新鲜度,忘跑=红灯)。
- pip 安装统一加 -i https://mirrors.aliyun.com/pypi/simple/

【任务收尾五联动(每完成一个任务必须全部执行,防联动失效)】
①重写 现状与任务.md §5(整体重写,不许在旧列表上打补丁堆历史);
②被本任务取代/收编/影响的一切清单与文档同步改状态(执行单、README、战略文档的任务状态等);
③自查两份接管提示词与 README 是否因本任务过时(路径/流程/铁律变化);
④跑 python tools\doc_check.py(路径断链+易过时状态扫描)+ 回归测试,全绿;
⑤git commit(中文说明改了什么口径/文件)+ push。

【用户画像与协作方式】
- 用户零基础但要学习全部技术,喜欢边实操边学:交付时附"怎么跑、看什么、学到什么";
- 用户目标是赢不是交差:进度超前时主动提差异化(读 1_给你看\差异化战略_如何超过别人.md);
- 每周五出周报+验收清单(每条=用户可自查的硬标准);
- 用户待拍板事项以 现状与任务.md §5 标注为准,拍板前不擅自变更相关默认值。

先完成接手三步,汇报你看到的状态,然后按 现状与任务.md §5 的待办顺序开工。
```
