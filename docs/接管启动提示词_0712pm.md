========================================================================
ABB杯「智储优控」接管启动提示词（0712 pm 立 · 新对话粘贴本文件全文即可完美接管）
========================================================================

接管 2026 ABB杯「智储优控」(F:\abb_wh_work)。**用户在场、昼伏夜出（晚上工作白天睡，不要默认"晚上=挂机"）。** 方向全部拍板，直接执行，不重新论证。本文=唯一接管入口，读它=掌握全部。

────────────────────────────────────────────────────────────────────────
【★ 最新增补（0712pm2 · 本文正文写后的变化，接管时先看这段）】
────────────────────────────────────────────────────────────────────────
1. **git HEAD 已推进到约 `e8e37d7`**（以 `git -C F:\abb_wh_work rev-parse origin/master` 真实值为准）：Codex 全部审查 **N2/N3/N4/N5** + 两信箱 + `sim/d2_quantify.py` 已补落盘防遗漏。
2. **⚠ Codex 已产出并仍在实时跑 N5 复核**（`_通信\codex_out\N5_P1~P6+`：浮点精度与决胜规则 / Reset 跨批状态污染 / 长跑溢出与计数寿命 / 失败队列与公平负载 / 种子置信区间与选择偏差 + 各 readonly_probe.py + 量化证据.json）。**本方 N4 后按用户令未扩 N5、尚未处理。** 接管后**待办之一 = 先 `ls _通信\codex_out\N5*` 看全（可能还有新文件在产），问用户是否要像 N4 一样复核 N5**（读 N5_* → 回原始 file:line 亲验 → 出 `docs\Claude_N5复核回执` → 停手待授权，不擅自扩 N6）。
3. **桌面作战面板 `智储优控_作战面板.html` 已全面重构**（当前阶段徽章=口径整改+ST全修；含"三分钟看懂处境"/阶段图/核心问题表 D-1/2/4+N4/口径真值字典/两轨分工/你现在做什么 3 步/诚实状态盘/时间轴）。它仍是渲染层，真相以本文+复核回执为准。
4. **AB 许可续期 + 联系组委会任务 = 已永久删除**（用户自理，任务表 T2 已删、面板已去除）。
5. **本会话未做任何 ST 代码改动 / 未开 AB / 未跑 ab_scripting**；轨 B（ST 全修）尚未起步，等用户一句话 Claude 即出「ST 逐文件补丁计划」。

────────────────────────────────────────────────────────────────────────
【0】一句话现状
────────────────────────────────────────────────────────────────────────
经三轮 Codex 独立审查(N2/N3/N4)+本方 red-team 复核+2 个 deep-research，已查清：报告/演示/文档/二进制/面板**大面积把 sim 数字当 AC500 实测**（含头条 8.40/62%/98.5%、自适应权重、吞吐 230、年化能耗、"闭环验证"叙事）。**两条整改轨已拍板并行**：
- **轨 A 口径修正**（文档/生成器/二进制/面板/README 改诚实）→ **交 Codex 一次性做**，提示词已备。
- **轨 B ST 全修**（方案乙 D-1 补实现 + N4 的 PLC 阻塞/高优先 + A3）→ **Claude 写 ST 代码，AB 关掉后 Claude 跑 ab_sync 验证**。

git 远程=本地 HEAD（**接管第一件事：`git -C F:\abb_wh_work rev-parse origin/master` 看真实值**；本文落笔时约 `16b307f`→最新约 `e8e37d7`，之后若有 commit 以 rev-parse 为准。铁律：永远以真实 git 为准）。

────────────────────────────────────────────────────────────────────────
【1】接管第一步：读这些（按序）
────────────────────────────────────────────────────────────────────────
1. 本文（全局）。
2. `docs\Claude_N2N3复核回执_0712.md` + `docs\Claude_N4复核回执_0712.md`（复核裁决 + 口径字典 + 我认的自己的错）。
3. `docs\ASK决策_0712.md`（5 轮 /ask 全部拍板决策）。
4. `docs\给Codex_口径修正提示词_0712.md`（**轨 A：用户转给 Codex 即启动**）。
5. Codex 原始审查（可选深读）：`_通信\codex_out\N2_汇总回执` / `N3_汇总回执` / `N4_汇总回执` + 各 P 分卷。
（真相源：`docs\canonical_assumptions.md`、根 `现状与任务.md`[在仓库根非 docs]、`docs\技术方向总库_0711.md`。）

────────────────────────────────────────────────────────────────────────
【2】口径字典（唯一地面真值 · N4 已把 8.757 再降级 · 引任何数字前先看这里）
────────────────────────────────────────────────────────────────────────
全部是 **sim 值**，互不混用，绝不称"AC500 实测/真跑"：
- **8.40** = 5-seed H 均值(sample std，非 CI)｜**8.6232** = sum H 单-seed 回归锁(`test_sim.py:189`)。
- **8.757** = **Python 加减速+静态 sim 结果**（评分分母 39.6667）——**不是"PLC 可实现代理"、不是 AC500 真跑**（N4-01）。
- **8.794** = 按 ST 固定 `FC_TMax`(38) 的更近似 Python 只读代理——**只用于揭示 Python↔ST 分母差异，绝不进头条**。
- **6.443** = 匀速+静态 sim 代理（**PLC 默认 `xUseAccel=FALSE` 口径**）｜**7.6202** = legacy and 历史锁(`test_sim.py:204`)。
- **98.5** = sim lexicographic 档 attain；**不是** PLC 实测、**不是**理论上限(speed 99.9/oracle 100)、不与画面 AWRA_HOLISTIC 绑定；完整路由 11/13 场景依赖 PLC 未实现的 TOB/CB。
- **81.6** = 已部署 holistic 规则在 390 实例的 **sim 评估**（当前≡固定 AWRA）；**不是** AC500 现场 attainment。
- **59/0** = 核心 ST 逻辑+**静态匀速黄金向量**在 AB 仿真通过；**不证明**自适应/lex/speed/H KPI/吞吐/年化能耗在控制器复现（T25 强制 `xUseAccel=FALSE`+静态权重）。
- 230次/h、266.5kWh/48.9%/CO2、49.9/43.7/44.7、heavy_tier 0.57、9.9% = **全部 sim，非 PLC 实测**。

────────────────────────────────────────────────────────────────────────
【3】全部锁定决策
────────────────────────────────────────────────────────────────────────
**Codex 三轮复核（本方全部接受为主 + 自认错）**：
- D-1/D-2/D-4 经 red-team 对抗验证全成立；本方亲验 `xUseAccel=FALSE`/`FC_TMax=38`/`8.6232`/`aGoods[401]`/`run_all 只 21 测试` 全属实，无 file:line 可反驳。
- **本方自认的错**：D2量化/sim_plc第二层/报告草稿_B6B7B11/答辩QA卡/作战面板 把 8.76/81.6 误标"AC500 真跑/实现值"——按 N4-01 全改为"Python accel+static sim"。
- N3 新增 A3 检修存量冻结（=本方 B-2）、A4 画面锁双闸、C6 履历 均为 D1/D2 同类错配；N4 新增 41 项（8 阻塞含 N4-14 ParamGuard 副作用/N4-15 aGoods[401] 越界/N4-26 run_all 非全量/N4-27 图源污染等）。

**/ask 5 轮拍板**（详 `docs\ASK决策_0712.md`）：
- **T1 总方向 = 方案乙（PLC 补实现），先起草 D-1 CB/TOB ST + 跑一轮真实 ab_sync 校准工时**。
- 头条口径用 **PLC-可实现口径**（注意：默认 `xUseAccel=FALSE`→默认代理 6.443；开加减速→8.757，均标 sim）。
- 检测器 98.5% = **A·保留但并列标注**（sim lex + holistic 81.6% sim 评估）；D-1 乙落地后升级表述。
- **图交 Codex 生成**（Claude 给结构+数据+提示词）；**演示改成现场切词典序档真跑 98.5%**（D-1 落地后）。
- **轨 A 口径修正现在放行 Codex 一次性认真改完，末尾统一交 Claude 复核**（非每组停审）。
- **轨 B ST 全部修理**（D-1 + N4-14~24 + A3）。
- **sim 方法(P2-F1/F2)：Codex 先评估改算法影响面（牵动哪些数字/P50P95）交用户，再定**。

────────────────────────────────────────────────────────────────────────
【4】分工表（认真思考版 · computer-use 其实很少需要）
────────────────────────────────────────────────────────────────────────
| 任务 | 谁做 | 关键 |
|---|---|---|
| 口径修正(文档/生成器/二进制/面板/README) | 🤖 Codex（用户转提示词启动） | 纯文本/python，零 AB |
| **全部 ST 代码修改**(D-1 CB/TOB · N4-14~24 · A3) | 🤖 **Claude 直接 Edit .st** | 代码是文本，不需 computer-use、不需用户在场 |
| sim 方法评估/改(P2-F1/F2) | 🤖 Claude(python) | 评估影响面交用户定 |
| **ab_sync 验证**(编译+59 测试) | 🤖 **Claude 跑(AB 关掉时)** 或 用户 | `ab_sync.ps1` headless；AB 开着=双实例事故，关掉后 Claude 主循环可跑；不需 computer-use |
| 提交包 manifest/requirements/allowlist(计划) | 🤖 Claude 起草 | 不代跑打包、不碰密钥 |
| DR-3/报告精修/N4 补丁计划 | 🤖 Claude | 零 AB |
| **画面洗(17 处 GUI)** | 🧑 用户（computer-use 次选） | 坐标漂移风险，人手动更稳 |
| **三页在线长跑** | 🧑 用户（computer-use 可辅助） | 需判断哑件是否重洗 |
| 提交密钥·PII 人工核 | 🧑 用户 | 红线：Claude 不碰值 |
| 拍板/最终演示质量判断 | 🧑 用户 | — |
> **AB 许可续期 + 联系组委会 = 已按用户令永久删除，用户自理，不再跟踪。**

────────────────────────────────────────────────────────────────────────
【5】待办队列 + 立即 next
────────────────────────────────────────────────────────────────────────
**立即并行可启动**：
1. 用户：把 `docs\给Codex_口径修正提示词_0712.md` 转 Codex → 轨 A 口径修正启动（Codex 一次性改完，改完交 Claude 复核其结果）。
2. Claude：**先出「N4 全部 PLC + 方案乙 D-1 逐文件补丁计划」**（改哪段 ST/测试反例/风险/ab_sync 验法），用户审方向 → Claude 逐个写 ST 代码 → AB 关掉后 Claude 跑 ab_sync 验 59+/0（或用户跑）。
   - 建议 ST 顺序：先 **N4-14/15 两阻塞**（越权改布局/aGoods[401] 越界，演示前置）→ 方案乙 **D-1 CB/TOB**（让 98.5 成真，创新分）→ N4-16~20 高 → A3 检修源位冻结 → N4-24 除零。
3. Claude 可挂起并行：sim 方法 P2-F1/F2 影响面评估、提交包 manifest/requirements/allowlist 计划、DR-3 文献。

**需用户在场（AB/GUI）**：画面洗 17 处 → 三页在线长跑（演示零翻车前置）→ 演示按 N3-P3 §7 新顺序重录。
**报告周（草稿已备待精修）**：B1 锚点按 DR-1 改引（Hausman1976/Bortolini2015，删无源 62%/55%机型不可比/>20%无源）；10 篇草稿→正式章节；回填(canonical G2-G4/F25/268→230)；B8 七规则确认后清洗。

────────────────────────────────────────────────────────────────────────
【6】本会话全部产出文件（都已 commit+push）
────────────────────────────────────────────────────────────────────────
审查：`D1D2反验证_0712` `sim_plc第二层_0712` `D2量化_0712`
复核回执：`Claude_N2N3复核回执_0712` `Claude_N4复核回执_0712`
Codex 提示词：`给Codex_口径修正提示词_0712`（轨 A，含 N4 增量）
报告周草稿：`报告草稿_B3B4B8_0712` `报告草稿_B6B7B11_0712` `报告草稿_C3C5B14_0712` `答辩QA卡_C7_0712`
deep-research：`deep_research计划_0712` `DR-1_行业锚点_0712` `DR-4_ABB杯赛事_0712`
决策/交接：`ASK决策_0712` `接手任务表_0712`(部分过时,以本文为准) `本文` `overnight-report_0712`(总账)
桌面：`智储优控_作战面板.html`（有 N4 待清口径,归 Codex 轨 A）

────────────────────────────────────────────────────────────────────────
【7】纪律红线（违反=事故）
────────────────────────────────────────────────────────────────────────
1. **口径诚实**：任何 sim 数（含 sim 对 PLC 的代理）不得称"AC500 实测/真跑"。8.757=Python accel+static，8.794 不进头条。
2. **git 真实为准**：每次 push 后 `git rev-parse origin/master` 核实（历史有 court 伪造 push 教训）。`git add` 只指定路径、绝不 `-A`（避 11 个 docx WPS 伪改 M + `致Codex/致Claude.md`）。
3. **AB 互斥**：跑 `ab_sync.ps1` 前确认 AB 已关（ScriptEngine 起 AutomationBuilder.exe，双实例=事故）；禁子代理开 AB/跑 ab_scripting/做 git。
4. **文件安全**：禁改 `1_给你看\`、`2_你要操作\` 下用户 docx/xlsx/pptx（只读或另存新副本）。
5. **改真相源/ST 前**：ST 代码修改用户已授权（方案乙全修）；改 canonical 等真相源口径仍需用户确认具体改法。
6. **密钥/PII**：不打印、不摘要、不写入任何值；提交前 secret/PII scanner 由用户跑。
7. **PLC ST + AB 实跑**：ST 代码 Claude 写、ab_sync Claude 可跑(AB 关时)；但 **AB 内在线长跑/400 件压测/画面洗需用户或 computer-use**，不擅自开 AB。
8. 负结果如实报；引用数字带来源(file:line/CSV)；不编数字；固定 seed。

阅读完：先 `git rev-parse origin/master` 核实真实 HEAD，再按【5】立即 next 开工（Claude 先出 ST 补丁计划）。用户在场，遇拍板点正常提问、不挂机。
========================================================================
