# 01/03 页 3 版设计规格(0721,fable 主会话设计,sonnet 施工)

> 上游:docs/01_03升级头脑风暴_0721.md(方向)、docs/升级设计依据_0721.md(权威锚与措辞纪律)。
> 目的:两页各 3 版真实数据渲染供用户拍板(0719 三版纪律)。拍板前**不改 src/ 任何原件**。

## 0. 施工总纪律(6 版共同)

- 变体目录:`F:\abb_wh_work\l4_showcase\design_mock_0721\`(新建);截图输出:`F:\abb_wh_work\l4_showcase\out\redesign3_0721\`。
- 变体=从 src 复制的独立副本,改版面结构+新增数据区块;**严禁改动 src/ 下任何文件**。
- 真实数据硬令:01 变体必须真跑 runtime(payload 来自 s3_fill_store.js);03 变体必须引用真实素材文件(l2_factoryio/media 与 img);数字一律来自数据文件,禁止编造示例数。
- 路径:变体引用 src 资源用 `../src/…`(01 页 document.write 注入的 `./s3_fill_store.js` 须改 `../src/s3_fill_store.js`);素材 `../../l2_factoryio/…` 保持。截图静态服务器 **root=F:\abb_wh_work(仓库根)**,MIME 表含 .html/.js/.css/.json/.png/.jpg/.mp4,URL 走 `/l4_showcase/design_mock_0721/….html`。
- Playwright 底座:Chromium `C:/Users/86177/AppData/Local/ms-playwright/chromium-1223/chrome-win64/chrome.exe`,args `['--use-angle=d3d11','--enable-unsafe-swiftshader']`,viewport 1600×900;01 就绪判据 `window.__S3_QA && !__S3_QA.snapshot().loading`(变体保留 QA 探针),03 就绪判据 networkidle+500ms。
- 视觉基因:白底、ABB 红线(--abb #e2001a 或全站既有变量)、命名总表词根、学术工程风;禁黄牌 #targetBeacon、禁 nth-of-type 选择器、禁深色大底(顶栏可保持现深色惯例)。
- 每版截图 ≥2 张:默认态全页 + 一张关键交互态(弹层/拖动/放大)。
- 3D 容器尺寸变化后调用页内既有 resize/相机适配;若 3D 在新布局初始化异常,记录问题并用 CSS transform 缩放兜底(截图说明里注明,不隐瞒)。

## 1. 01 页三版(基于 src/01_连续填仓.html + s3_fill_candidate_runtime.js + s3_layout_a_interactions.js 副本)

三版共同底座(先做再分叉):删「继续/从头回放/导出3D帧」按钮群(F1①);保留 W6 顶栏一线/折叠/芯片行。

### 版A「算法赛马场」(文件 01_变体A_赛马场.html)
- 主版面易位:三算法三联网格(现右栏 slotMap)+ 终态差异热力图 diffMap(现藏于放大视图)升为**主区横排四图**(SEQ|NEAR|SCORE|SCORE−SEQ差异),3D 缩为右侧辅视图(约 40% 宽)。
- 主区下方「赛段比分条」:6 个容量节点(0/67/134/201/241/267)× 三算法,每节点显示 lane.metrics 关键两项(expected_retrieval_s、violations)的三算法横向对比微条。数据源:payload.online_lanes[].checkpoints 与 metrics。
- 底部「终态总分牌」:现 renderStats 六格改三算法并排矩阵(行=指标,列=SEQ/NEAR/SCORE,SEQ 列标注「基线」),涨跌徽章沿用。指标相邻展示(jbb2015 惯例,设计依据 §1)。
- 常驻「同源口径声明」条:「三算法同一到达队列 · 同一数据门 · 单 seed(30-seed 佐证见证据)· SIM」——SPEC 跨域类比措辞。

### 版B「评分解剖室」(文件 01_变体B_解剖室.html)
- 3D 主区保持;右栏重构为解剖塔(自上而下):
  1. 当前件评分瀑布:score_terms 四项 raw→归一化(f/t/w_norm)→weighted→score_total 的横条分解(四项用全站四色),数据源 decision_evidence…top3[selected].score_terms。
  2. Top-3 候选对比小表:rank/slot/objective_value/travel_time_s + tie_size 披露(现 tieNote 语义并入)。
  3. 30-seed 分布带:fetch `../evidence/s3_fill_seed_sweep_skew_2026_2055.json` 真实加载,渲染三算法关键指标分布条(min-P50-max),不再只是 title 文字引用。
  4. 固定权重口径卡:score_policy.density_contract 原文 + 「固定权重口径,非 swing-weighting 论证产物;敏感性佐证见 whatif」(设计依据 §2 硬约束措辞)。
- 七步条/进度轴保持现位。

### 版C「终态审计报告」(文件 01_变体C_审计.html)
- 左 3D 缩至约 45%,右侧大审计流(自上而下):
  1. validator.checks 具名清单卡:12+ 项逐项名称+通过态(绿勾),数据源 payload.validator.checks 逐 key 真读。
  2. checkpoints 哈希链时间线:6 书签节点纵向时间线,每节点 event_count + audit_prefix_sha256 前 12 位 + grid 校验态。
  3. 终态账本:managed_fill_ratio、total_occupancy_ratio、placed、violations、makespan_s(metrics 真读;fixed_score_diagnostic_total 语义未明**不上屏**)。
  4. 口径与边界卡:registry.active_entry.evidence_boundary 原文 + binding_approval 时间戳 + arrival_model 口径声明(F8:「即到即处理 · 单机 · 无队列,与 02 排队场景形成口径对照」)。
- 审计风:多留白、细分隔线、等宽字体哈希。

## 2. 03 页三版(基于 src/03_FIO物理证据.html 副本;体量小,三版均先做白底化+治理统一底座)

三版共同底座:删「Claude 候选 · 独立证据页」游离标签,改副标「工作站 · 物理实证」;色板并入白底 ABB 红线基因(顶栏可留深色);口径带保留;负结果七条一字不改(C8 红线:只改导航呈现,不改措辞)。

### 版D「证据链卡片流」(文件 03_变体D_证据链.html)
- 主轴:矩阵表 9 行重构为证据链竖卡流,每卡:主张→缩略图(有图证据)→原始文件完整路径(等宽字体)→采集时间→限制条件徽章(ok/warn/stop 三色沿用)。
- mp4 真嵌入:`<video controls preload="metadata">` 引 F3_cell30_全链素材.mp4。
- 底部 40 帧缩略条:f_001-f_040 横向缩略带(懒加载 loading="lazy"),点击开大图弹层。
- 负结果七条升格右栏专区,每条尝试关联已有截图(G4_lower_NOSTART 等),无对应图的不硬配。

### 版E「物理-数字对照剧场」(文件 03_变体E_对照剧场.html)
- 主轴:上下分屏对照——上=实拍帧带(40 帧 scrubber,拖动滑条切帧,img 真序列);下=数字侧(用 `../out/s3_layout_a_qa_0716/` 或 `../out/w1_0720/` 现成 3D 截图 2-4 张作数字侧真素材,标注来源文件路径)。
- 对照免责卡(常驻):「形态对照 · 非逐帧等价 · 适当保真度」+ ISO 23247「物理-数字同步」定义引用(设计依据 §3 措辞,标【跨域引用:架构标准非呈现规范】)。
- 六站点表与证据矩阵收入下方折叠区;三张既有站点图并入帧带右侧固定位。

### 版F「I/O 矩阵仪表」(文件 03_变体F_矩阵仪表.html)
- 主轴:66 份 F3_diagnose log 结构化为时间组×6相位矩阵热格图(先写只读脚本解析 `../../l2_factoryio/logs/` 文件名按时间戳分组,构建期生成 json 内嵌;实测 11 组×6)。**高亮规则:如能依据 F3_H5证据保全_0716.md 确证哪 54 份属 H5 验收链则高亮之、余灰显「调试组」;若无法确证映射,按 11 组如实渲染并标注「54/66 精确清单待拍板」——禁止虚构链号归属。**
- 六站点点表 Tab 保留并强化(每站配已有站点图,无图站位不硬配)。
- 右栏:三张关键证据图 + 负结果七条压缩卡。工程仪表风(密集网格+等宽数字)。

## 3. 验收口径(主会话 fable 执行)

- 6 版截图齐全(≥12 张)、控制台零 error(warning 记录)、真实数据点抽查(每版至少 1 处数字回溯到数据文件)、src/ 零改动(git status 验证)。
- 不跑 47 项 QA(变体非 src);拍板后施工阶段才回归全量 QA。
