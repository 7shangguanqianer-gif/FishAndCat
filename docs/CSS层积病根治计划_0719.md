# CSS 层积病根治计划(02/01 页)· 2026-07-19

> 起因:用户批评「几轮重构隔靴搔痒,格式互相顶撞的问题没有一丁点解决」。**判定:成立。**
> 此前所有"重构"(A 壳、V2 映射、b 变体)都是**加层**——在旧代之上叠新代、靠特异性和 !important 压制;
> 层积结构本身没动过。三天内 3 次 CSS 静默失效(!important 压制/注释吞规则/aspect-ratio 顶撞)= 层积税的直接证据。

## 0. 病情实测(2026-07-19,02 页)

| 指标 | 值 |
|---|---|
| inline CSS 行数 | **848** |
| `!important` | **31 个** |
| `@media` 块 | **24 个** |
| `#decisionWrap` 被规则触及 | **107 处** |
| `#sceneTools` / `#workHud` / `#phaseRail` | 28 / 25 / 26 处 |
| 布局代数 | **5 代**:基础网格 → data-style A/B/C → data-s3-layout="A" 壳 → V2 rail 映射 → data-s3-b 变体 |

**结构性死代码已核实(不是猜测)**:
- `dataset.style` 唯一赋值点 = 硬编码 `'A'`(02:910)→ `[data-style="B"]`(4 行)与 `[data-style="C"]`(4 行)永不可达;
  **且 171/174 行藏着废止黄牌 #targetBeacon 的装饰样式**。
- `dataset.s3Layout` 唯一赋值点 = interactions 无条件 `"A"`(interactions:24)→ **非 A 兜底布局全部不可达**。
- immersive 化石(75-77 行):引用的 `#brand/#kpis/#modebar/#bar/#foot/#immersiveExit` 在 02 里 **0 个存在**,
  抄自祖先页面,无任何 JS 触发。

## 1. 目标形态(单代制)

```
<style> 分五节,单一顺序,禁止跨节回写:
  ① tokens        设计令牌(:root;原 data-style="A" 的变量并入)
  ② components    组件样式(卡/chip/metricHelp/oe*/热度图例…)只管长相,不管位置
  ③ layout        唯一布局层(现 A 壳规则去前缀转正;所有 position/grid/order/inset 只在此节)
  ④ variants      页面级映射(rail-right 在共享 s3_shell_v2.css;s3b 变体规则)
  ⑤ media         断点统一(24 块归并成按断点分组的少数块)
!important 目标:31 → 0(层积对手删净后逐个试摘)
```

## 2. 分阶段执行(每刀带验证,每阶段一 commit)

## 进度看板(0719 更新)

| Phase | 状态 | commit |
|---|---|---|
| P0 基线 | ✅ `geo_baseline.json`(2 视口×20 元素) | — |
| P1 结构死码(B/C+immersive+data-mode 12 行) | ✅ 几何零差+QA 8/8 | `38955f8` |
| P2a-gen1(化石强制/taskline/targetBeacon 装饰/#slotMap 弹性) | ✅ 同判据 | `437cef5` |
| P2a-gen2(指挥台 grid/railHead/runtimeControls/taskline/min-height:72) | ✅ 同判据 | `333bf73` |
| **P2a-gen3(0715 Task12/3/7/16/17 五小段)** | ⏳ **下一步**,同刀法:布局绑定删、密度垫底留 | — |
| P2b A 壳转正+中和器删除 | 排队 | — |
| P3 !important 摘除 | 排队(对手已删两个:gen1 min-height:120 / gen2 min-height:72) | — |
| P4 五节重排+media 归并 | 排队 | — |
| P5 01 同方治疗 | 排队 | — |

**工装纪律**:geosnap.mjs 已修 close 泄漏(此前进程不退=僵尸后台任务,diff 因等 node 退出而从未执行——
教训:`A && B` 链里 A 不退出则 B 永不跑,后台任务"Running 27min"≠在干活,先查产物文件再查进程)。
**计数陷阱**:!important 统计含墓碑注释里的字样,以实际规则数为准。

| Phase | 动作 | 风险 | 验证判据 |
|---|---|---|---|
| **P0** | 几何基线快照(`scratchpad/geosnap.mjs`:2 视口 × 19 元素 rect+display) | — | 基线落盘 `geo_baseline.json` |
| **P1** | 删结构性死代码:B/C 块 8 行 + immersive 3 行(黄牌 `display:none!important` 保护令**保留**,#targetBeacon DOM 保留——runtime setTarget 仍写它,删 DOM 会 null crash) | 零(不可达代码) | 几何 diff ≤1px + QA 8/8 + 无新页错 |
| **P2a** | 删「被 A 壳覆盖的前代布局属性」:非 A 兜底的 position/grid/order/inset/width(保组件样式) | 低 | 同上 + 截图抽查 |
| **P2b** | A 壳规则去 `[data-s3-layout="A"]` 前缀转正(§1 的 ③),删除 `grid-template:none` 类中和器 | 中 | 同上;interactions 的 dataset 开关保留作 QA 探针 |
| **P3** | `!important` 逐个试摘(31 个),几何零差才摘定;摘不掉的注明对手 | 低 | 每摘一批跑几何 diff |
| **P4** | @media 归并 + 五节重排 + 节头注释 | 中 | 全套:QA 8/8 + 几何 + 双视口截图 + smoke |
| **P5** | 01 页同方治疗(01 有自己的层积;QA 46 项保护) | 中 | 01 QA 46/46 + 01 几何基线 |

## 3. 已知依赖与坑(动刀前必读)

- `#targetBeacon`:**样式可删、DOM 与 display:none 令不可删**(s3_ac_runtime setTarget 每帧写 textContent)。
- 640 行 `flex:none!important` 旧规则:P2a 删除对象——它死后,`#slotMap` 的 A 壳反制 `!important` 群(726 区)即可在 P3 摘除。
- `s3_shell_v2.css` 在 inline 之后加载:P2b 转正时注意别让 inline ③ 节反超共享文件的映射层。
- b 变体(dock/rail/axis)规则属 ④ 节,清扫不碰;用户选定后删掉未选两版。
- 提交包副本是旧版,不受影响;R5 同步时一并换新。

## 4. 与任务链的关系

- 本根治 = 用户 0719 指令「更加彻底的执行重构并且重新规划版面」的执行体。
- **b 三版选择顺延到根治后**:在干净底座上重渲三版再交选(避免在将拆的地基上定型)。
  三版效果图已交付:`F:\abb_wh_work\l4_showcase\out\b_variants_0719\CMP_3b.png`(上=甲dock/中=乙rail/下=丙axis,
  红框=块位置;单版全图 B_dock/B_rail/B_axis_full.png;数字全真实:热门取货 31.0s vs 非热门 36.0s、
  交织腿 9.1s、省一趟 18.8min/100 周期)。
- 视觉方向不推翻:V2(01 左栏/02 右栏)与已批内容层不变——**本次重构改的是代码结构,不是视觉设计**;
  若用户对视觉另有指示,以用户为准。
