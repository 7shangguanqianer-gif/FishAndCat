# 报告周素材 · 0718 增补包(#11 清账 · 折入本轮 overnight 新证据)

> 承接 `docs/存取效率设计_素材包_0716.md`(取货叙事)。本增补把 2026-07-18 overnight 产出(可视化决赛件呈现升级 + 评分修订证据先行 + 双命令调度)整理为报告周可直接引用的素材。**口径:所有数字标 (sim);禁「孪生/完全闭环」;77/0 不在 Web/sim 语境宣称(见 §5 待核)。**

## 1. 可视化决赛件 · 呈现升级(#26 呈现四项 + M1 绿框修复)

决赛可视化「连续填仓」页(01_连续填仓.html)本轮完成两批呈现治理,回应「满仓终态三算法占同一 267 格集合、makespan/行程数学恒等」下「如何让隐形的胜利显形」:

| 呈现件 | 作用 | 报告用法 |
|---|---|---|
| 三算法同帧对比(SEQ\|NEAR\|SCORE 20×20) | 同批同进度并排,当前 lane 高亮 | 图:三算法货-格配对差异一眼可辨 |
| 金顶热门通道 | 在库热门货(频次降序前 1/5=53 件)金色顶盖 | 「高频近格」视觉锚 |
| 终态货-格配对差异热力图(SCORE−SEQ) | 每格显两算法所放货物的访问频次差(橙=SCORE更热/蓝=更冷) | 图:差异全在「哪件货配哪格」 |
| 冷门送远解说卡 | 冷门首件送最远角的正面转译(为高频腾近格) | 叙事:反直觉但正确的保留策略 |

M1 绿框修复(0718 verifier PASS 4/5):顶栏控件就地风格化(去红粗滑块、蓝系与进度轴统一)、差异热力图常驻左栏(消除大片空白)。**报告可视化章可直接引用整页截图,风格已统一、无空白。**

## 2. 评分修订 what-if · 证据先行(M2 负结果,方法论亮点)

报告「设计严谨性/方法论」章可用的**证据先行**案例(docs/S3评分修订what-if结论_0718.md):

- 质疑:冷门首件送最远角(观感过激)、猜测常数 f_max=100 在 skew 下压低 f_norm 致效率项失活 → 两个修订假设(τ 距离饱和 / f_max 动态化)。
- 30 seed×2 情景实测:决胜 skew 上 τ 使主叙事指标 **hot20 劣化 1.44%**、首件仍 30/30 送最远角(饱和点 19s 落高层,未覆盖首件 t_norm=0.25);dynfmax **近乎无效**——但非「排序不变」,而是 23/30 seed 批次 max freq 恰=100(=生产常数故逐项同值),f_norm≈塌缩的前提本就不成立。判据五条中三条于 skew 全败。
- **结论:生产评分零改动**。方法论价值:用 30 seed 实测拦住了一个「直觉合理但伤主指标」的改动——报告可写成「我们对每个改进假设先跑多 seed 证据再决定,避免观感驱动的评分退化」。

## 3. 存取效率 ·「取」半边 + 双命令调度(M4a,承 0716 素材包 §3)

补齐赛题「存取效率(单位时间存取量)」的取货侧量化(docs/双命令调度设计稿_0718.md,30 seed):

- **两个正交杠杆**(报告须讲清,防误读):
  1. **双命令调度**(存完顺路取,省一次空驶回程)≈ **28–31% 时间节省**,几乎与布局无关(几何红利)。节省% 由行程几何比值决定(**非**单由基线绝对量——near 单命令基线最便宜却节省%第二高,是「基线越便宜省%越低」的反例)。**节省% 是调度模式度量,非布局质量度量**,勿把 seq 的高节省%(31.5%)误读为 seq 更优。
  2. **布局质量**(高频近格)在出库侧兑现:出库均时 seq 18.70s vs near/score/awra ≈9s,**降约 52%**,awra vs seq **逐 seed 30/30** 全胜。
- **复合吞吐**:seq 142 → near 288 单操作/h(布局为主、双命令为辅)。fails/viol 全零。
- 报告用法:一张表 + 一句「单位时间存取量翻倍主要来自好布局,双命令联程再叠加几何红利」。

## 4. 报告周清账 · 遗漏扫描(#11)

已覆盖:存取双边(0716 素材包)、呈现升级(§1)、方法论证据先行(§2)、双命令(§3)。**报告周仍需补的小节**(建议清晨排期):

- [ ] 可视化章配图:用 01 页整页 + 三联/差异图截图(M1 已使页面达标,可直接截)。
- [ ] 方法论章:把 §2 证据先行写成 200 字案例。
- [ ] 存取效率章:把 §3 双命令表 + 出库均时降幅并入 0716 素材包 §3。
- [ ] 口径统一:75/77 AB 自检数(§5)清后再定稿引用。
- [ ] 02 入出闭环页:M4b 页体待用户在场窗口做完后,04 章「入出闭环版」实链才可挂(见 implementation-notes_02入出闭环页_0718.md)。

## 5. 已核口径:AB 自检 = 77/0(0718 用户授权后核实并统一,R1 完成)

- **真相源确认**:`plc/06_PRG_Test.st:12` `N_CASES := 77`(注释「0717 治理D 扩容 T77 FC_TMax 口径;常量化=N4-08,在线不可改」)、`aResult: ARRAY[1..77]`;0717 AB 在线 ab_sync `iPassed=77/iFailed=0/xAllPass=TRUE`(续窗交接_0717:23,日志 `tools/ab_scripting/logs/*20260717_010514*`)。→ **77/0 为当前口径,75 是 0712 批次旧值(N_CASES 演进 59→75→76→77)**。
- **已改**(0718):`00_导览.html` 4 处 75→77(:130/:131/:204/:207,保留证据级别免责话术);`03_FIO物理证据.html:186` 本就 77/0,现三处一致。
- **仍待处理(非本会话可动)**:① `docs/存取效率设计_素材包_0716.md` 的 75/0 属 0716 历史日期文档,若报告要引用请标日期或更新为 77;② **报告 WORD 若写 75/0,属用户文档红线——请用户自行改成 77/0**(勿动原件)。
- **注**:`implementation-notes.md:153`「75/0=0712 四轮日志历史铁证不改」与 `Claude_*回执_0712` 等**故意引用 0712 批次**的历史文档保持 75 不动,那是正确的历史锚,非 stale。
