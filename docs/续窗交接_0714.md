# 续窗交接 · 2026-07-14(上下文满,换窗继续)

> **这是换新会话继续时读的第一份文件。** 一句话现状 + 真相源清单 + 用户门 + 待办 + 红线 + 关键教训。
> 核心提醒:**Codex 一直在并发高频迭代,本文件也会滞后**——动手前先按 §1 重新对齐最新真相,别信任何"我以为已完成"的旧判断。

## 0. 一句话现状

2026 ABB 杯"智储优控"智能仓储赛题(F:\abb_wh_work,截止 8/26,自定目标 8/15,初赛+决赛全套合交冲第一)。
**验证线(算法 sim/PLC 75-0/物理 H5 9-9/孪生数据层)大体完成;交付线(决赛可视化 HTML 十件内容/演示录屏/报告终稿/PPT)在推进中。**
Codex 已清完复核+重做队列并在自主精修 S3 三维件;当前主瓶颈=一批**卡在用户身上的门**(§3)。

## 1. 换窗第一步:重新对齐最新真相(按序读,别跳)

1. `_通信/致Claude.md` **顶部**(Codex→Claude 最新回执,含"队列完成总回执"+0715 S3 各次终验)——**最新真相源**。
2. `_通信/codex_out/0715_*.md`(Codex 对 S3 的自主精修:立体货架/6h视觉终验/双工位/真实时序货位图/多算法AUTO/AC事件流/去视角配色)——Codex 已把 S3 做到什么程度看这些。
3. `docs/overnight2-report_0712night.md` **末尾若干段**(Claude 侧总账本,倒序最新)。
4. `docs/ASK决策_0714_HTML展示件与提交包.md`(D1-D17 战略拍板,HTML=决赛主交付等)。
5. `docs/canonical_assumptions.md`(口径真相源,**只读不改**)。
6. 现场文件时间戳核对:`ls -t l4_showcase/src/*.html`、`ls -t _通信/codex_out/`、桌面面板 mtime——**谁的 mtime 最新谁是真相**(本项目反复出现"文档滞后于并发执行")。

## 2. 本会话(0713夜→0714)已完成

- **F3 现场**:签绿→Codex 复核**打回降级**(不签"完全闭环",H5 9/9=运行成功,根因待补证);3 项修复建议(R1 移除 LOWER_STALLED 正常放行/R2 补 Target A-B 原始采样+场景哈希/R3 修 goto 静止即在位盲区)**未落地**。
- **F4 OPC UA**:仿真无组件,负结果结案(Codex 首审接受)。
- **F5 Modbus 主站**:重做→Codex 二审**结案=当前 VirtualAC500 环境主站直驱不可用**(保守口径:非全产品线定理);C4/C5/F6 转真机/软 PLC。`5_ac500_modbus_master_实验记录.md` 附录 C 有结案。
- **C6 数字孪生**:双栈 demo(PyVista mp4+Three.js)已交付桌面评审夹;数据层+相位契约锁 143/143。
- **S3 三维样张 + 00_导览三版**:Claude 首版被用户批砸(轨迹画成直线/背景装背板后/机构脱节)→出规格 `docs/Codex任务_S3与导览重做规格_0714.md`(黄金参照=`tools/anim3d_formal.py`,逐项移植)→**Codex 接手重做并自主精修**(见 §1.2 的 0715 回执)。
- **作战面板**:Codex 按 `docs/作战面板重构规格_0714.md` 重构为暗色 SCADA 版(桌面 `智储优控_作战面板.html`),含正确 F3 降级/F5 结案口径。
- **算法层 5 缺口全部补跑完善(commit 7495c64+51e2306)**:
  - F1 `sim/lifecycle_stats.py`:U5 配对 t 检验,主组三档 p<1e-5(***)/240 组 ns;build_report "p<0.001" 已 sourced。
  - F2 `instance_gen.py`/`oracle_gap.py` 加 `--accel`:H 口径 lex attainment=**99.19%** vs 匀速 98.5%(稳健);§6.4 加口径披露。
  - F3 `sim/cb_st_equiv.py`:CB Python↔ST 分支等价 **420/420 mismatch=0**。
  - F5:§1.1 头条加 skew 口径注。
  - 验证:build_report 147 数字待补 0 exit0;pytest 32 passed;legacy 零污染。
- **口径同步**:导览三版 F5"待二审"→结案;桌面面板已正确。

## 3. ⛔ 当前卡在用户身上的门(最高优先,清一个通一片)

| # | 门 | 触发/动作 | 解锁什么 |
|---|---|---|---|
| G1 | **发官方三问** | 复制 `docs/问官方工程师三问_0714.md` 发赛事工程师;回执贴回归档 | HTML=决赛必交件的前提证实(否则最大投入方向有作废风险);合交细则/体积/录屏规格 |
| G2 | **验收 WORD 报告** | 看 `sim/out/验证报告_draft.docx`(build_report.py 生成,已含 F1/F2/F5 完善) | **PPT 重生**(Codex 预研已就绪,卡在此门);报告定稿 |
| G3 | **钦点 S3 + 导览三版** | 看 `l4_showcase/src/` 的 S3(Codex 已精修)与 00_导览_A/B/C,回"选 X 版" | B12 录屏定档;3D 量产;导览定稿并入提交包 |
| G4 | **批 canonical U5 证据指针** | 同意在 U5 证据列加 `;lifecycle_stats.py(配对t:主组p<1e-5***/240组ns)`(Claude 不碰 canonical) | F1 的 p<0.001 在真相源里可溯源 |
| G5 | **定 F4 报告章节归属** | B6(scan_g2g4/correlated_storage 鲁棒性)放 §4.7 还是并入 §4.3 | 鲁棒性证据接入主报告 |

## 4. 待办(非用户门,Claude/Codex 可推进)

- **F3 三项修复**(R1/R2/R3,见 `codex_out/0714_F3独立复核.md`)——降级后补证,排报告周后。
- **F4 报告 §集成**——待 G5 定章节后,把 scan_g2g4/correlated_storage 接进 build_report。
- **报告周(7/20-8/1)**:10+ 篇草稿→正式章节;§6 可视化章;B1 按 DR-1 重做。
- **B12/B13 录屏**:需用户在场+操作方案未定;素材报告周同期录(AB 三页动线/Factory I/O F12/PyVista 段);**一切视频需用户过目**(历史欠账:F3_cell30 素材等从未过目)。
- **400 件满仓压测/watchdog(T7)**:赛题核心场景唯一空白证据,需用户在场 AB,排 8 月上旬。
- **提交包实体灌入**:`提交包/` 五目录现为占位,排 8 月;`00_导览.html`+README 双导览已就位。
- **quiz 补课 / L2 科普并入 HTML**:排报告验收后。

## 5. 🚫 红线(完整复述,无例外)

- **禁碰** `1_给你看/` `2_你要操作/` `canonical_assumptions.md` `账号/密码/密钥`(这些即使 git 显示 Modified 也不是你改的,**不提交、不动**)。
- **push 仅限** `l2_factoryio/` `docs/` `_通信/`(+ `sim/` `l4_showcase/` 本会话已在推)三/五路径;`git add` **只点名路径,绝不 -A/-.**。
- **口径**:sim 数据全标 (sim);红色仅报警;物理=Factory I/O 54 位技术预演,**禁称 400 格孪生/AC500 闭环**;**F3 不签"完全闭环"**(H5 9/9=运行成功);**F5=当前环境负结果**(非全产品线定理);关键数字带**口径标签**(匀速 vs H,禁混引)。
- **fault lock 语义不可绕**(--confirm-* 不能绕,F6 不算重启);**AB GUI 开着绝不跑 ab_scripting**;**AB 许可话题永久关闭**(赛事方管续费,禁提到期/8-2)。
- **视频**:不录入 computer-use 痕迹;**一切视频需用户过目**;过目入口=作战面板。
- **模型档**:多 agent 扇出枪 sonnet/medium 显式传 model+effort(禁 fable 扇出,两次烧额度事故);synthesize 单次可 fable/high。
- **Codex 协作**:每完成一个 F 级任务→签 Codex 独立复核(新常态#10);Codex 是复核门不是橡皮章。

## 6. 关键教训(本会话血泪,务必内化)

1. **"完成"不可信**:F3"签绿"、F5"待二审"、报告"已生成"、面板"已对齐"——四个都在我以为完成后被推翻或过时。每个结论要带"谁在什么时间用什么证据验的"时间戳;结论一变即全仓关键词扫描。
2. **"能一键复现"≠"统计断言有依据"**:F1 的 p<0.001 数字能复现,但断言从没算过,连"145 数字全溯源"自检都放过(只查数字有源,不查断言有据)。
3. **复用勿重写**:S3 做砸的根因=有已验证黄金参照 `tools/anim3d_formal.py` 却凭感觉重写。第一动作是读它、port 它。
4. **真相源滞后于并发**:多 agent/Codex 并发时,审计快照必然过时;结论要留"再核当前 mtime"的对抗环。
5. **口径标签强制**:多套物理口径(匀速 vs H)并存时,同名指标必须分别标注、禁混引(F2/F5 同病)。

---
**续窗提示词见本文件同目录用户消息,或直接把本文件路径给新会话。**
