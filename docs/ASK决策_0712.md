# /ask 收敛决策记录 — 2026-07-12（第 1-2 轮，接手前）

> 本文=本轮 /ask 已锁定的决策草稿，落盘备查。第 3+ 轮在 Codex N2+N3 复核回执之后继续。
> ⚠ 数字口径以 Codex N2 fact 表为准（见 `_通信\codex_out\N2_汇总回执_0712.md` 及用户接手提示词 section 二）；下列涉及具体 sim 数值处，**最终值待「Claude N2+N3 复核回执」确认**，本文先记决策方向。

## 已锁定决策

| # | 决策 | 结论 |
|---|---|---|
| **Q1 总方向** | D-1/D-2/D-4 走哪条路 | **走方案乙（PLC 补实现），但先起草 D-1 的 CB/TOB ST + 让用户 ab_sync 跑一轮真实迭代校准工时**；D-2 待 sim 验证分类器值不值再定；其余任务填空余时间选择性做。**重估工时（弃旧"8处/1.5-2h"）**：乙全做 ≈ 4-6 天 / 只 D-1 ≈ 2-3 天（我起草快能压，你 ab_sync 迭代轮数 + D-2 分类器从零设计风险压不动）。 |
| **Q2 头条口径** | 报告正文头条性能数字用哪个 | **用 PLC-可实现口径当头条、标注为"sim 对 PLC 可实现档的代理值（非 AC500 现场实测）"**。⚠ 待复核：该口径具体值取决于 PLC 默认 `xUseAccel`——若默认 FALSE（匀速）则代理=6.443、若加减速开则=8.757；**旧稿"8.76=AC500 真跑"措辞错误，须改为 sim 代理并注明开关态**（见 Codex fact 5/7）。 |
| **Q3 检测器 98.5%** | D-1 的 98.5% 怎么呈现 | **A·保留但并列标注**：98.5% = sim lexicographic 档结果（非 PLC 实测、非理论上限）；并列 holistic 规则的 **81.6%/closed 0.0%（= 该规则在 sim 13 场景×30seed 上的评估，非 AC500 现场测量）**。D-1 乙落地后 98.5% 档若真在 AC500 跑通，再升级表述。 |
| **Q4 图/排版分工** | 报告图谁产出 | **我给图结构+数据+生成提示词，用户交 Codex 生成图片**（问题域图/两口径对照表/决策依据图/孪生阶梯/稳健性双轴图）。 |
| **Q5 演示口径** | D-1 乙落地后演示改不改 | **演示改成现场切词典序档真跑 98.5%**（D-1 补实现后）；需重录 v4，风险自担。落地前演示仍显示 holistic。 |
| **Q6 空余时间填** | 我可代做的活优先级（多选） | 全选：**D-2 分类器 sim 验证 + 报告 10 篇草稿精修成正式章节 + B1 外部锚点按 DR-1 改引 + DR-3 文献综述**。 |

## 关键纪律（本轮确立）
- **口径诚实红线升级**：不得把任何 sim 数（含 sim 对 PLC 的代理值）表述为"AC500 现场实测/真跑"。8.40/8.6232/8.757/6.443/7.6202 各是什么、不是什么，以复核回执逐字确认表为准。
- 走乙不改"诚实"原则：乙落地前一切按诚实降级口径；乙落地后按真实实现值升级，仍标 sim 评估 vs AC500 实测的边界。
- 待办：Codex N2+N3 复核回执（正在做）→ 给 Codex 修改提示词 → /ask 续第 3 轮。
