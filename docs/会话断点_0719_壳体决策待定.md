# 会话断点 · 2026-07-19(a 完成 / C1 完成 / 壳体方案待定)

> 承接 `docs/会话断点_0718c_热力图与R2.md`。本文=0719 这一轮的落盘与恢复指引。

## 0. 一句话现状
`a`(01/02 几何穿模)与 `C1`(结构件配色统一)已完成并提交;**壳体方案(重构 vs 一致性补齐)待用户拍板**,
用户偏向**重构**并要求先做 deep-research 充分调研避免返工 —— 调研已派出(`wf_6cb80274-09f`),**结果未回**。

## 1. 本轮已提交
| commit | 内容 |
|---|---|
| `ea0eb3c` | R2 几何穿模修复(02)+ 两份 deep-research 落盘(含两处自我更正) |
| `27d6fd2` | R2步骤2 左栏信息架构重排(证据两块→顶栏 chip 叠层;2D 图 133→313px) |
| `068e6bc` | 任务a:01 页几何穿模修复 + 01/02 壳体一致性审计 |
| (待提交) | C1:02 补中性钢灰结构件配色 |

## 2. 关键实测数字(可复现)
| 项 | 前 | 后 | 工装 |
|---|---|---|---|
| 02 几何相交 | 141 对 / 81 类 | **123 / 67** | `scratchpad/geomaudit.mjs` |
| 01 几何相交 | 155 / 87 | **137 / 75** | 同上,`node geomaudit.mjs "01_连续填仓.html"` |
| 02 左栏溢出 | [914, 850, true] | **[850, 850, false]** | `scratchpad/probe.mjs` |
| 2D 图每格 | 6.6px | **13.8px**(1600×900) | 同上 |
| QA | — | **02 = 8/8、01 = 46/46 PASS** | `tools/capture_s3_mixed_layout_a_qa.mjs` / `capture_s3_layout_a_qa.mjs` |

## 3. 待用户拍板:壳体方案(**最高优先,阻塞 b/c/e**)
用户选了「先看三版壳体原型再定」,并注明**偏向重构**、要求 deep-research 充分调研、认真规划避免返工。

**恢复后要做**:
1. 读 deep-research `wf_6cb80274-09f` 结果(任务 id `wbcyi3hom`,输出在 `tasks/wbcyi3hom.output`)。
   ⚠ **必须先核返回值有效性** —— 上一轮同类工作流的 synthesize 步返回过字面占位符 `summary:'测试'`。
   若无效:读 `journal.jsonl` → 用加固 prompt(禁占位符)`resumeFromRunId` 只重跑综合步。
2. 据调研结论渲染**三版壳体原型对比图**(现状 / 一致性补齐后 / 重构版),交用户选。
   渲染工装:`scratchpad/shoot_both.mjs`(双页同相机截图 + 拼接)。
3. 用户选定后再动 b / c / e。

## 4. 一致性缺口清单(详见 `docs/01-02壳体一致性审计与重构决策_0719.md`)
| # | 项 | 状态 |
|---|---|---|
| C1 | 结构件配色(01 钢灰 / 02 蓝青) | ✅ **已补**:02 加 `M.infeedRail=M.outfeedRail=mat(0x6f7c87,…)` |
| C2 | 镜头倍率面板位置 | ⏸ **未做**。两页 `#sceneTools` 基础 CSS 完全相同(`right:18px;bottom:18px`),差异来自 **01 的 A 壳把 `#sceneDock` 重排成三列网格(01:817-820)而 02 无此覆盖**。比预想复杂,须与壳体方案一起定。 |
| C3 | 左栏信息架构(02 已按 ISA-101 分层,01 未动) | ⏸ 等壳体方案(**这条是我 0718 单方面造成的分叉,已认领**) |
| C4 | I/O 拓扑(01 Y向+CROSSBAR / 02 X向) | ✅ 判定**不强求统一**(01 只入库、02 双向,几何本就该不同) |
| C5 | 顶栏按钮排列 | ⏸ 等壳体方案 |

## 5. 用户已授权、可立即执行且不受壳体影响的
- **d stale 根治**(任务 #42):建口径卡单一真相源 + 谨慎更新 `canonical_assumptions.md` C11
  (仍写 75/0 且带 0712 历史细节 → 需改成 0717/77 为现值 + 保留历史),过 `/milestone-gate`。**未开始。**
- **R4 报告中不依赖版面的部分**(任务 #36):先码证据与正文,截图/版面描述等壳体定稿再补。**未开始。**

## 6. 未解决 / 如实记录
1. **「建模缺失」未定位** —— 用户点名的这一项,我排查后**没找到对应缺陷**;
   之前那个金色破面查实是**目标 FX 箭头被堆垛机框架遮挡**(已修),不是缺模型。**需用户指认位置。**
2. **1366×720 下 2D 图仅 7.8px/格** —— 该视口左栏 674px 已无余量,要再放大须压缩 `insightStack` 或
   `decisionWrap`。1920×1080(录屏目标)达标 14.4px。
3. **C1 的取舍**:02 原本用蓝/青编码入库/出库,改钢灰后方向语义改由地贴 + 动态路径承担。
   已截图确认仍可读,但**这是可回退的设计判断**,用户可否决。
4. 01/02 共有的机构内重叠(行走轮埋底座、顶部滑轮组三件叠、叉齿×锁扣)判定为非视觉缺陷,未动。

## 7. 可复用工装(本轮新建,别重造)
| 工装 | 用途 | 坑 |
|---|---|---|
| `scratchpad/geomaudit.mjs` | 场景 AABB 两两求交,按重叠占比排序;01/02 通吃 | 必须查**祖先可见性**与排除**半透明 FX**,否则大量假阳性;AABB 对**旋转体**会高估 |
| `scratchpad/fxsweep.mjs` | 扫 op×progress,测目标 FX 与堆垛机的距离/显隐 | — |
| `scratchpad/probe.mjs` | DOM/画布/左栏实测 + **计算样式**;支持 `node probe.mjs <w> <h>` | 布局 bug 必须读 `getComputedStyle` 才能定位 |
| `scratchpad/verify_rail.mjs` | 左栏截图 + 叠层开合 + 多视口 | — |
| `scratchpad/shoot_both.mjs` | 01/02 同相机双页截图 | 01 用 `__S3_FILL_QA`、02 用 `__S3_QA` |

## 8. 方法论(本轮新增教训)
- **CSS 静默失效有三种,只有读计算样式能发现**:①`!important` 旧规则压制;②注释写成两个 `/*` 块把规则吞掉;
  ③`aspect-ratio` 在列向 flex 里从宽度推高度。本轮这处改了**三次**才生效,前两次完全无效。
- **frac(重叠体积比)会骗人**:小物体全埋会让 frac 飙高,但绝对重叠可能反而减小 —— 必须配截图判定。
- **workflow「跑完了」≠「结果有效」**:synthesize 返回过 `summary:'测试'`,须核 `journal.jsonl`。
- **同一个 bug 在两页要分别设计修法**:01 照搬 02 的桥板修法会把支线截成断头。

## 9. 红线(不变)
不碰 sim/*.py · trace 数据 · manifest · invariant 链(`validateVisualTopology`)· 加载序;不碰 1_/2_ 用户文档;
不覆盖冻结物;**黄牌 #targetBeacon 永不再引入**;git add 只点名 + 中文 commit + Co-Authored-By: Claude Fable 5;
数字回真相源核;扇出 agent 一律 sonnet/medium。
