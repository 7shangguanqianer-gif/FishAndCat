# 会话断点 · 2026-07-20(九块整改 · T2 完成 T3 待启)

> compact 后唯一恢复入口。规划依据三件套:本文 + docs/命名总表_0720.md + docs/设计整改依据_0720.md。

## 0. 用户拍板逐字(0720,一个都不能丢)

- 3 问:F 轴「整行删除」;按钮「删从头回放+删导出3D帧+证据两键保留重构」;命名「学术工程风」。
- 5 问:命名范围「全站分批(02 先行)」;施工顺序「按推荐(命名表→3D→3版→实施→三档+弹层)」;三档视觉「四项全选(运动学差异可视化+建模精度LOD+分段按钮机理小字+切档差异条)」;货物信息「大字货物行」;证据「合并证据中心(单键,tab 分本页证据链/S1 受控对照)」。
- 带转向已实施完毕(用户原拍板「彻底重构整个位置的摆放设计…采用我给你的大体方案」)。
- 0719 沿承:版面重构必出 3 版真实数据渲染供选;蓝框列序「当前作业→存取对置→运动遥测→货物热度」;绿框顶栏与周期轴合并一体;紫框统计完全重构;黑框 AUTO 卡去冗余;整体构图密度统一。

## 1. 任务状态(TaskList #1-#6,0720 暂停点:用户令「暂停任务,等一下接着继续」)

- T1 命名总表:✅ docs/命名总表_0720.md(用户未提异议,已按表施工)。
- T2 3D 带转向:✅ commit `f29ea1b`(契约 30/30、QA 8/8)。
- T3 整页 3 版:✅ 用户拍板**版B(右栏KPI塔)+「字间距/可读性/稀疏程度进一步优化」**;三版图 out/rework3_0720/v_a|v_b|v_c.png 留档。
- T4 版B 转正+精修:✅ commit `98d0f10`(契约 30/30、QA 8/8、01 页 46/46;final_normal|final_fault.png)。
- T5 证据中心(B1-B7 全修)+三档四件套:✅ commit `4d9348a`(38 项专项验证;验收抓获并修复 nth-of-type 错隐 Y 行→语义类 .axisFork;五帧 out/t5_0720/)。
- T6 01/00 命名批次:✅ commit `6aecdbd`(QA46/46+契约30/30;拦截补丁仅删容量书签分支保留另两治理;B6 列宽回归自查自修)。**九块整改 T1-T6 全部完成收官。**
- 恢复后遗留队列:R3 录屏/R4 报告/R5 导览+提交包副本换新;#25/#38-41 待用户拍板勿动。
- **0720 完全停机记录(用户退出 Claude Code 前)**:所有后台 agent/workflow 已终止并经系统确认不存在;浏览器预览面板已关闭;无在飞任务、无未提交项目改动(工作区余留=更早会话历史遗留,与本轮无关)。代码 HEAD=`4d9348a`(T5),断点=`7e4279b`。**恢复动作(用户说「继续」)**:重派 T6(规格=命名总表 B1-B8+本文件 §1 T6 条目)→ 验收(QA46/契约30/两截图)→ commit → 九块整改总汇报 → 遗留队列。

## 2. T3 规格(3 版设计,fable 定稿后派 sonnet 渲染)

共同底座(三版都用):命名总表 A 表全部应用;F 轴删;顶栏按钮=播放(暂停/继续)+证据中心(单键)+已校验徽章(删从头回放/导出3D帧);底 dock 列序=当前作业(大字货物行:货物名 12px 加粗+重量 14px Consolas 大数字+等级/热冷带色徽章)→出入库耗时分解→轴状态(X 行走/Y 升降两行)→货位访问频率;批次 KPI=核心四大格(P50/P95/利用率/故障)+次级三小格(峰值等待/约束违规/周期均行程);AUTO 卡「启动时一次选定」只留一处;设计 tokens:12px 基线节律、块头 9px/700 深色、分隔线仅 1px #d6dde2 一种、每块 3-5 条信息、参照 ISA-101 分组一致性原则(不称标准条款)。

三版结构差异(不是密度微调):
- **版A「顶栏一体带」**:顶栏两行一体(上行=品牌+情景组+播放/证据;下行=作业周期进度轴全宽);右栏=AUTO 卡+批次KPI+趋势+2D 图;底 dock 四列。最接近现状,风险最小。
- **版B「右栏 KPI 塔」**:周期进度轴移入右栏顶部(竖排空间换横排),顶栏单行极简;批次 KPI 改 2×2 大格塔+次级横条;底 dock 四列不变。3D 视野最大化。
- **版C「左证据右画面」**:2D 货位图+趋势移到左侧新窄栏,右栏只留 AUTO+批次KPI,顶栏一体带同版A;底 dock 三列(货位访问频率图例并入左栏 2D 图头)。信息分区最学术(证据左/画面中/状态右)。

渲染:同一 html 用 `html[data-rework="a|b|c"]` 三套顶层 CSS(+必要 DOM 搬运 JS 小段)实现,起服务器各截 1600×900 常态帧,三张图纯文本路径交用户选。**不 commit 变体态**(选定后才实施为唯一版)。

## 3. T5 备料(已完成的侦察,勿重做)

- 证据弹层 BUG 清单(7 条,3 阻断):B1 对照证据挂 #workHud 下→窄条化(need position:fixed 挂 body);B2 表格窄条溢出;B3 打开不 inert 背景(顶栏可切算法→证据与状态脱节);B4 z-index 被祖先层叠上下文困死(workHud z=5<topbarA z=12)→左上死区;B5 本屏证据无焦点陷阱(comparisonLab 的实现可直接抄);B6 焦点逃逸后 Esc 关不掉;B7 judgePath 受旧 @media(min-width:1600px) 死约束→1366×768 整块消失。截图 out/evidence_bugs_0720/(看 3 张:13/12/19vs22)。
- 低置信线索:冷启动偶发「双工位拓扑坐标非法」断言抛出→按钮全失效(1/9,GPU 时序嫌疑,留观)。
- 三档事实:档1 匀速/档2 加减速/档3 +故障+潮汐(trace.meta.motion/tier3_params)。

## 4. 红线与工装(不变)

- 不碰 sim/*.py·trace·manifest·IO/TRANSFER_ANCHOR/RACK canonical 锚·加载序;不碰 1_/2_ 用户文档;#targetBeacon 黄牌与保护令不动;runtime 每帧写入元素只能隐藏不能删 DOM;git add 只点名+中文 commit+Co-Authored-By: Claude Fable 5;不编数字;扇出/执行 agent 一律 ≤sonnet;deep-research synthesize 占位符不重跑(journal 综合,本会话已第 4 次);QA=`node tools/capture_s3_mixed_layout_a_qa.mjs`(8/8)+契约=`node tools/test_s3_runtime_contract.mjs`(30/30);01 页 QA=capture_s3_layout_a_qa.mjs(46/46)。
- 契约测试的 html.includes 断言读的是 archive_候选历史/样张_S3_三方向候选.html(归档快照),不是 02 页——改 02 文案不会打红它们,别误改归档。
- Playwright 底座:Chromium ms-playwright/chromium-1223 + args d3d11/swiftshader;静态服务器需 MIME 表;就绪判据 __S3_QA && !loading。
