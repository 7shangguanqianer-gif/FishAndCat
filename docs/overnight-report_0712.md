# Overnight 续跑报告 — 2026-07-12（第二轮审查加深）

> 承接 `docs\overnight-report_0711.md`（上一会话完整交接，因 court 故障转此新会话续跑）。
> 本会话原则（用户令 0712）：**取消"每会话只跑一批"限制**，继续把 R1→R5 队列往后推；保留两条——调用间保留一句话叙述（降 court 触发）、扇出子代理一律 sonnet·medium。
> 起点真相（已 `git rev-parse` 亲核）：HEAD == origin/master == `33054d5`（已同步）。

---

## 状态总览

| 项 | 状态 |
|---|---|
| R1 D-1/D-2 反验证（红队自审） | ✅ 完成 → `docs\D1D2反验证_0712.md` |
| R2 sim↔PLC 一致性第二层（指标口径） | ✅ 完成 → `docs\sim_plc第二层_0712.md` |
| R3 D-2 量化（静态 vs 自适应权重） | ✅ 完成 → `docs\D2量化_0712.md` |
| R4 报告周安全手术包（B3/B4/B8） | ✅ 完成 → `docs\报告草稿_B3B4B8_0712.md` |
| R4b 报告周清扫（B6/B7/B11/B14/C3/C5/C7） | ✅ 完成 → 3 份草稿（见下） |
| R5 循环加深 + 收尾更新作战面板 html | ⏳ 待做（收尾） |

---

## R1 结果：D-1/D-2 反验证 ✅

**结论：D-1、D-2 均 UPHELD·置信 high，无一被推翻**（3 sonnet/medium 红队 + Claude 独立亲验交叉裁决）。反验证不仅未削弱，反补强证据并暴露新问题。全文 `docs\D1D2反验证_0712.md`，要点：

1. **D-1 成立**：0 反证。新增自认铁证源 `plc\README_PLC.md:41`（除 `04_FB_Warehouse.st:914-915` 外第 2 处代码自认"lex/speed 档 PLC 未含"）。标定表 13 场景 tob×9/cb×2/awra×2，PLC 只能跑 ≈15%。
2. **D-2 成立**：唯一"疑似反证"（`FB_SceneDetect` 每周期真跑）经查不构成反证——它写**策略选择（A2，活）**，非**权重系数（A1，死）**。
3. 🔴 **新雷（待拍板补充）**：`docs\报告草稿_B1B2B10_0711pm.md:349-350` 草稿自己写"98.5% 与 v4-1 演示台词口径一致，精修时两处应保持数字同步"——把错配数字**制度化锁死**，方案甲须删/改此句（Claude 逐字亲验属实）。
4. ⚠️ **措辞边界（方案甲精修点）**：D-2 降级口径必须精确指向"**A1 权重自适应表未接入运行时**"，**不可**泛写"PLC 无场景自适应"（会反向误报、埋没真跑的 A2 策略选择）。
5. ✏️ 纠错：子代理一处笔误"holistic 68.5%"已纠为真相源值 **81.6%/closed 0.0%**。
6. 措辞精修：D-2 原述"`08:9-19` 整段注释"不精确——实为"数据表是活常量（:8-14），只有赋值用法块被注释（:16-19）：表在、线没接"。

---

## R2 结果：sim↔PLC 第二层·指标口径 ✅

**结论**：头条 6 数字中 **5 个（8.40s/62%/67.3%/230/0.57）是自适应权重算的、PLC 静态权重复现不出**（D-2 在指标层的落点）；演示 16 镜头**仅 v4-1（=D-1）一处错配、其余 15 干净**；新挖 **D-4 叙事层错配**。全文 `docs\sim_plc第二层_0712.md`。要点：

1. **头条 5/6 = 自适应口径**（Claude 亲验 `headline.py:68` H=caliber(accel=True,adaptive=True)）。PLC 真实配置 accel=True+adaptive=False 这个组合 headline.py 根本没算 → 头条 KPI 本身口径存疑。gap 9.9% 是唯一静态权重项（低风险）。
2. 🔴 **D-4（新·高危·叙事层）**：`报告草稿:148-149` 建议正文写"仿真结论已在目标控制器上闭环验证"——59/0 回归只证 ST 逻辑正确（且核对向量是 `export_st_vectors.py:28` 静态权重导出），**不证** PLC 复现自适应头条 KPI。会误导评委。Claude 逐字亲验属实。
3. ✅ **正向确认**：§1.1 头条数字表标注干净（溯源 sim CSV，未称 AC500 实测）；演示脚本除 D-1 外无隐藏错配。错配集中在少数叙事句 + v4-1 台词，方案甲可覆盖。
4. ✏️ **纠子代理量化误差**：子代理"PLC 复现 = 6.443s/68.2%"用了匀速+静态 L 口径（同时动加减速+权重两变量），错误隔离 → R3 用 `caliber(accel=True,adaptive=False)` 只切权重干净重算。

## R3 结果：D-2 量化 ✅（关键·利好拍板）

**结论：D-2 对取货头条的真实影响很小，诚实降级几乎不损故事。** 主循环纯 python 跑（不碰 AB），只切权重一个变量（加减速固定 True）。自检：H 口径 8.402s/62.01%/67.31%/0.567 与 headline.py 逐位吻合。全文 `docs\D2量化_0712.md`。

| 头条指标 | 报告 H(自适应) | **PLC 实际 P(静态)** | 差 |
|---|---|---|---|
| awra 取货 | 8.402s | **8.757s** | +0.35s(+4.2%) |
| 较 seq 降幅 | 62.01% | **60.41%** | −1.60 pp |
| 能耗降幅 | 67.31% | **67.19%** | −0.12 pp(≈不变) |
| 重货层高 | 0.567 | **0.550** | −0.017(更安全) |
| 违规Σ | 0 | 0 | 0 |

- **利好拍板**：AC500 静态权重真跑仍 = 降 60.4%/省能耗 67.2%/违规 0，"降 60%+"核心卖点完全成立。方案甲可把 P 口径当"AC500 实现值"、H 口径降为"自适应权重设计余量(+1.6pp 展望)"——数字全诚实、PLC 名副其实。
- **纠 R2 子代理误差**：6.443s 是 L 口径（匀速+静态，动了两变量），真隔离值是 P=8.757s。
- **勿混**：本量化只覆盖取货时间/降幅头条（走 awra，不经检测器）；D-1 的 98.5% 检测器 attain 是另一条线，未触及、仍按 D-1 方案处理。
- 边界：仅 sum 口径单场景；多场景差幅可能不同（记为可选加深）。

## R4 + R4b 结果：报告周安全手术包/清扫 ✅（用户令：耗 token 任务一并做掉）

**10 件报告周写作任务落盘，全部独立于 D-1/D-2/D-4 争议口径（触及处占位，诚实数字用 R3 的 PLC 静态权重实现档）。**

| 产出文件 | 覆盖任务 |
|---|---|
| `docs\报告草稿_B3B4B8_0712.md` | B3 问题域标注图文字稿 · B4 黑盒功能测试表(11 行带真实 T 编号) · B8 de-AI 七规则草案(待确认) |
| `docs\报告草稿_B6B7B11_0712.md` | B6 G2-G4 稳健性小节+关联存储改进池 · B7 吞吐边界声明+L4 三层口径合并 · B11 孪生成熟度阶梯 L0/L1/L2 决赛展望 |
| `docs\报告草稿_C3C5B14_0712.md` | C3 能耗/worker-safety 叙事 · C5 δ 管理语义包装 · B14 有轨巷道可靠性引用(0711pm 已做,本次索引确认) |
| `docs\答辩QA卡_C7_0712.md` | C7 三大创新点答辩 QA 卡(争议点双版本 a/b + 陷阱题诚实答案) |

**要点/副产物**：
- 子代理如实标注多处"待回填/待编号"待拍板项：G2-G4 canonical 回填(`canonical:99-101`)、关联存储 F25 编号、`现状与任务:77/281` 旧值 268→230 次/h。
- 📌 情报：真相源 `现状与任务.md` 在**仓库根**（非 `docs\`）——B3/B4 曾按错路径取材、已更正。
- B7 独立给出"三层时间口径框架"消解深思#2/#3；B11 完整三段式(可行性/经济性/技术先进性)。
- C7 答辩卡把 D-1/D-2/D-4 陷阱题全部备好诚实答案，答辩风险大幅降低。

## 待拍板清单（本会话新增，接 _0711 报告 §3）

- **N-1【补入方案甲】** 删/改 `报告草稿_B1B2B10_0711pm.md:349-350` 的"保持数字同步"句。需核该处是否已在方案甲"报告草稿4处"计数内，否则方案甲改动数应 8 → 9。
- **N-2【方案甲措辞精度】** D-2 降级文案加 A1/A2 区分（见 `D1D2反验证_0712.md` §2.2）。
- **N-3【方案甲措辞精度】** D-2 措辞由"整段注释"改为"表在、赋值线注释掉"准确版。
- **N-4【补入方案甲·高危】** `报告草稿:148-149` "仿真结论已在目标控制器上闭环验证"裸句须精确限定为"ST 逻辑正确性已在 AB 仿真闭环验证（编译 0 错+59/0 单元回归+静态权重向量逐位一致），不等于头条性能 KPI 已在 AC500 复现"。**建议报告周执行方案甲前先做一次"sim 数字 vs AC500 成果"叙事句全扫**（D-1/D-2/D-4/新雷 349-350 同类）。

> 以上 4 条（N-1~N-4）均为**方案甲内部精修/扩容**，不改推荐方向（诚实降级仍是首选）；反验证未发现任何"PLC 其实已实现、无需降级"证据，方案乙必要性未上升。

---

## 逐项 Checkpoint

### R1（本会话起）✅
- Claude 主循环独立亲验 `08_GVL_WeightPolicy_generated.st` 全文 + `oracle_gap_summary.txt` + `04_FB_Warehouse.st:900-940`，提前锁定 D-2 措辞精度问题。
- 派 3 红队 critic（sonnet/medium）并行推翻 D-1/D-2，全 UPHELD·high，合计 118.7k tokens。
- 亲验新雷 `报告草稿:349-350` 逐字属实。
- 产出 `docs\D1D2反验证_0712.md` + 本报告；commit 核实。

### R2（进行中）
- 目标：报告每个头条数字（awra 8.40s / 降 62% / 能耗 67.3% / 吞吐 230 / 夹逼 9.9%）的 sim/PLC 来源是否一致；演示 v4 之外全量镜头对照有无类似 D-1 的画面台词错配。
- 产出：`docs\sim_plc第二层_0712.md`。

---

## 收尾交付物（用户令 0712）

overnight 结束时把整轮成果汇总进 **`C:\Users\86177\Desktop\智储优控_作战面板.html`**（现存 217 行/26KB，07-11 11:43 版），做有整体感的全面进度更新。

**执行纪律（关键）**：面板里的 **98.5% / 降62% / 场景自适应权重** 正是 D-1/D-2 待拍板争议口径（散布在 intro/七步流水线第2步/A1 行/周日程多处）。收尾**不擅改这些数字**（改=替用户执行方案甲，违铁律 3），而是：
- 新增「overnight 第二轮审查发现」区（R1-R4 结果 + D-1/D-2 + 待拍板 N-1~N-3）；
- 加醒目「⚠ 待拍板」警示条，标注上述头条数字与 PLC 实现档错配、精修前勿对外用；
- 保留原结构/样式，只**增/标**不**改**争议口径；刷新日期与「我们走过的每一步」进度叙事。

## 纪律与安全（本会话遵守）
- 扇出全 sonnet·medium，无一升 fable（额度事故防线）。
- 只新增产出文件（`docs\*_0712.md`），未改任何现有代码/真相源文档。
- git 只显式 `git add` 指定新文件，绝不 `-A`（避 11 个 docx WPS 伪改 M）。
- 未开 AB、未跑 ab_scripting、子代理禁 spawn 子代理/禁 git/禁 AB。
- 每 push 后 `git rev-parse origin/master` 核实真实远程 HEAD（court 防线）。
