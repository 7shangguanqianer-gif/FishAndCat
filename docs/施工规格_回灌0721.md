# 01/03 拍板方案回灌 src 施工规格(0721,拍板依据 docs/3版拍板包_0721.md 拍板结果节)

## §0 总纪律(两页共同)

- **本次动 src 正式页**。每改/删一处文案,先 grep 它是否被 tools/test_s3_runtime_contract.mjs 或对应 QA 脚本逐字断言;被断言的文案与断言**同 commit 同步改**。契约测试部分断言读 archive_候选历史/样张_S3_三方向候选.html(归档)——勿误改归档、勿误判断言目标。
- 变体实现最大化复用:design_mock_0721\ 内已验证代码搬回 src,资源路径 ../src/ 改回 ./、../evidence/ 改回 ../evidence/(src 出发同层级,自行核对)。
- IO/TRANSFER_ANCHOR/RACK canonical 锚与 sim/trace/manifest 红线不动;命名总表词根;禁 nth-of-type;禁黄牌 #targetBeacon;1_给你看/2_你要操作 用户文档禁触。
- Playwright 底座同规格 0721(chromium-1223、d3d11 args、MIME 静态服务器、就绪判据 __S3_FILL_QA——01 页实名,03 页 networkidle)。
- agent 施工完成后**不 commit**,报告后由主会话 fable 验收再分批 commit。

## §1 01 页(三层结构:赛马场为台、解剖为深、审计为底)

基:src/01_连续填仓.html + s3_fill_candidate_runtime.js + s3_layout_a_interactions.js(W6 底座,commit 4be3e9b)。参照变体:design_mock_0721\01_变体A/B/C 三文件。

1. **底座清理(F1)**:删「继续/从头回放/导出3D帧」按钮群(先 grep 断言);rewriteObserver 文案拦截内化——把改写规则的目标文案直接写进 runtime 输出,删拦截补丁(syncAxis 是功能,保留);清 queueCard/cycleReset 死 CSS+DOM(grep 断言先行)。
2. **主版面=版A 方案**:三算法同帧三联+SCORE−SEQ 差异热力图升主区(变体A 实现);赛段比分条(6 容量节点×三算法,expected_retrieval_s+round_trip_path_m,数据 checkpoints[].metrics_to_date;violations 无节点级数据的替代披露照搬变体屏上注明);终态总分牌(指标行×SEQ/NEAR/SCORE 列,SEQ 标基线,涨跌徽章沿用);同源口径声明条常驻(变体A 文案);3D 缩右侧(变体A 的 --rail-w 60% + resize 方案);30-seed 分布带置总分牌旁(真 fetch ../evidence/s3_fill_seed_sweep_skew_2026_2055.json,min-均值-max 带+非百分位披露,变体B 实现)。
3. **放大层=版B 方案**:「决策解剖」并入放大视图/弹层——评分瀑布(score_terms 四项 raw→f/t/w_norm→weighted→score_total,变体B 实现)+Top-3 候选对比表(含 tie_size 并列披露)。
4. **证据抽屉=版C 压缩**:现 toggleEvidence 浮层升级为抽屉:validator.checks 18 项具名清单+checkpoints 哈希链 6 节点(sha256 前 12 位)+终态账本(managed_fill_ratio/total_occupancy_ratio/placed/violations/makespan_s;fixed_score_diagnostic_total 不上屏)+口径卡(evidence_boundary 原文/binding_approval 时间戳/arrival_model 口径:「即到即处理 · 单机 · 无队列,与 02 排队场景形成口径对照」)。
5. **两情景兼容**:?profile=uniform 数据门二选一机制不破坏,两情景都跑通(uniform 的 30-seed 文件是 s3_fill_seed_sweep_uniform_2026_2055.json,fetch 路径按 profile 切换)。
6. **QA 适配**:tools/capture_s3_layout_a_qa.mjs 在 47 项上增删:被删按钮相关断言移除;新增 ≥6 项(口径声明条存在且含「同一到达队列」;比分条节点数=6;总分牌三列齐;瀑布 score_total 与 payload 一致;validator 清单=18;30-seed fetch 成功且三算法均值上屏)。**完成判据:新版 QA 全绿 + 契约 30 全绿 + 02QA 8 不回归。**

## §2 03 页(D 主骨架 + F 矩阵模块)

基:src/03_FIO物理证据.html。参照变体:design_mock_0721\03_变体D_证据链.html(主骨架)+ 03_变体F_矩阵仪表.html(矩阵与站点表模块)。

1. **治理统一(变体D 底座)**:删「Claude 候选 · 独立证据页」→「工作站 · 物理实证」;白底 ABB 红线基因;负结果七条**措辞一字不改**(变体施工同款红线,逐字符 diff 自验)。
2. **D 主骨架**:证据矩阵 9 行→证据链卡片流(主张→缩略图→原始文件路径→采集时间→限制徽章三态);mp4 `<video controls preload="metadata">` 嵌入;40 帧缩略条+大图弹层(loading="lazy");负结果七条附图升格。
3. **F 矩阵模块并入**:「I/O 与动作链」区升级=11 组×6 相位矩阵热格。gen_f3_log_matrix_0721.mjs 移入 l4_showcase/tools/(命名 gen_f3_log_matrix.mjs),产物 json 内嵌页面;**54/66 口径按已批准落地**:「66 份含验收前预演与验收后复核,其中 54 份=H5 验收链(9链×6相位)」——页面口径句、矩阵高亮、判定方法折叠区三处一致;六站点表强化(每站配已有图,无图不硬配)。
4. **口径联动**:77/0 引用(约 03:186)与 00 导览一致性不破坏(升级中该数字保留原语义位);scopeBand 口径带保留。
5. **QA**:先找既有 03 QA 脚本(l4_showcase/tools 下 grep「03\|FIO\|物理证据」;0717 记录 03 QA 8/8)。找到则适配升级,找不到则新写 tools/capture_s3_fio_qa.mjs(≥10 项):负结果七条逐字符与基线一致、素材引用文件存在性抽查(≥5 个真实路径 200)、矩阵数字 66/54/11 与 json 一致、video 元素存在、40 帧缩略数=40、口径句含「54 份=H5 验收链」、游离标签已删。**完成判据:03 QA 全绿。**

## §3 壳白名单路由(已批准,主会话施工)

app_shell/main.js 静态服务:pathname 以 `l2_factoryio/` 开头 → 解析根改 PROJECT_ROOT(仍做前缀闸:target 必须在 PROJECT_ROOT\l2_factoryio 下);MIME 表核对补 .mp4/.jpg;verify_w3b.js 重跑不回归 + 新增素材可达检查(壳内 fetch 03 引用的一张 png 与 mp4 各返回 200)。

## §4 验收与 commit(主会话 fable)

分批:①01 页+QA 同 commit;②03 页+QA+工具同 commit;③壳路由+verify 同 commit。每批验收=对应 QA 全绿+截图亲验+数字抽查;全量回归清单:契约 30、02QA 8、01QA(新数)、03QA(新数)、壳 verify allOk。
