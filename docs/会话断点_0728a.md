# 会话断点 · 2026-07-28a(codex 交付推翻 → 主会话全面接手;3D 抽离待开工)

> 压缩/新窗唯一恢复入口。配套:docs/任务规划_0728_3D抽离与悬浮.md(七问拍板+诊断行号+验收命令)。

## 1. 一句话战况

用户判定 codex 0724 交付质量差,令「全部接手并且全部重新开始」;已回滚 codex 全部改动(未采用,存 stash@{0})、修复它造成的环境破坏,并由主会话亲手重做完 A(速度口径)/B(顶栏对齐)/C(03 滚动重设计)三份;0728 七问拍板了「3D 公共模块抽离」等新方向,**下一步开工 3D 抽离步1(几何+材质),尚未动手**。

## 2. 已完成账(证据=commit,均已 push)

| commit | 内容 | 验证 |
|---|---|---|
| `5f4d374` | 修复并加固 QA 环境 | 基线五套复绿 30/61/8/8/19 |
| `ddf27f4` | 任务A 速度口径:查明非 bug,不改运动学,改如实披露 | 契约30/01QA61/02QA8/uniform8 |
| `2b19067` | 任务B 01/02 顶栏对齐(实测本已一致,只修暂停钮→深蓝主按钮) | 四套全绿 |
| `2b2f883` | 任务C 03 页 4-Tab → 大图沉浸展陈风滚动式重设计 + QA 重写 | 03QA 19/19、壳路由 4/4 |
| `51393ff` | 落盘 0728 任务规划 | — |

**环境破坏与修复(重要)**:codex 装 electron-builder 时清空了共享运行时 `.pnpm` 缓存(只剩 1 个条目,playwright 消失),导致四个 Playwright QA 脚本全崩(`packageDir undefined`)。修复=playwright 装进项目本地(`l4_showcase/node_modules`,脚本 `localRequire` 第一顺位命中)、依赖记入 `l4_showcase/package.json`、`node_modules` 入 `l4_showcase/.gitignore`。**环境再坏执行 `npm i` 即恢复**。

## 3. 拍板与否决记录(用户中文原话逐字)

- **接手令**:「codex完成的很差,你全部接手并且全部重新开始」
- **0728 七问**:
  1. 3D 架构 →「抽公共 3D 模块,两页共用(推荐)」
  2. 热力形式 →「整格色阶 + Golden Zone 热区(推荐)」
  3. 货物运输速度 →「两者都要」(观感提升 + 口径披露)
  4. 悬浮覆盖 →「全覆盖 + 首次进页引导」
  5. 抽离策略 →「分三步递进,每步全绿才下一步(推荐)」
  6. 悬浮文案 →「1,但是注意用词要学术」(=三段式:是什么/为何这么做/优在哪,学术工程风)
  7. 优先级 →「先打地基:3D 抽离 → 热力+悬浮 → D/W4」
- **0724 早前**:「整个03重新设计,之前的上下滚动依次介绍的方式就可以,但是需要重新设计,要求更加美观好看」;「01-02-03还有哪些可以统一风格的?比如上栏位」;00 导览是否纳入统一风格 →「要」
- **原始疑问(已解答)**:「01和02的3D演示,货物的速度显示不一致,都是1倍速时速度不一样.这是为什么,是bug吗?」→ 结论:**不是 bug**(详见 §4)
- **沿承否决**:金顶盖 gold-cap 已废止勿复活;黄牌 `#targetBeacon` 永不复现;「从头回放/导出3D帧/总览」按钮已删。

## 4. 数字口径(全部可复现)

验收命令(cwd=`F:\abb_wh_work\l4_showcase`),当前基线:
- `node tools/test_s3_runtime_contract.mjs` → tests 30 / pass 30 / **fail 0**
- `node tools/capture_s3_layout_a_qa.mjs` → **PASS 61/61**
- `node tools/capture_s3_mixed_layout_a_qa.mjs` → **PASS 8/8**
- `node tools/capture_s3_fill_uniform_qa.mjs` → **PASS 8/8**
- `node tools/capture_fio54_evidence_qa.mjs` → **PASS 19/19**
- `cd app_shell && node verify_material_route.mjs` → **ALL PASS 4/4**

速度口径定论(任务A,铁证):
- 01 数据门 `payload.shared_input_provenance.motion.model` = `trapezoid_accel_plus_laden_vertical_factor`,含 `laden_vy_factor=0.8`(来源 `sim/warehouse_sim.py:78`,注明行业依据 Mecalux 0.818 / Muvro 0.667 取 0.80)
- 02 trace meta `motion` = `trapezoid_accel`(来源 `l4_showcase/out/s3_traces/t3_heavy_auto.js`),**不含**折减
- 运动学常量两页同值 VX=2.0/VZ=0.5/AX=0.5/AZ=0.3:01 在 `src/01_连续填仓.html:1596`,02 在 `src/s3_ac_runtime.js:88`(另 `02_入出闭环.html:1216` 有同值重复声明);FORK_MAX 两页均 `.80`
- 载货降速改动点:01 `src/s3_fill_candidate_runtime.js:214`(`vz = loaded ? VZ*0.8 : VZ`);02 `src/s3_ac_runtime.js:688-689`(无折减)
- 曲线函数 `axisT/axisPos/axisV`(01html:1619-1640) ↔ `axisTime/axisPosition/axisVelocity`(ac:648-677)默认分支**逐字数学等价**

03 重设计度量:总高 3309 → **4367px**(≈4.9 屏,1552×900 实测);证据链节 522 → **1398px**;证据画面图高 230 → 507px。

## 5. 待办优先序(第一动作在最前)

1. **【第一动作】3D 抽离步1:几何+材质** —— 打开 `src/01_连续填仓.html`(内联 3D 母版约 1594-2088 行)与 `src/02_入出闭环.html` 对应段,抽出公共场景搭建(货架/堆垛机/传送带/材质表/几何契约 `addBox`/`RACK`/`INFEED`/`TRANSFER_ANCHOR`)为新文件 `src/s3_scene_core.js`,两页改 `<script src>` 引入 + 各自只留差异配置(01 双链传送带、02 单链)。**验收五套全绿 + 双视口(1552×900 / 1945×1094)截图亲验,独立 commit,全绿才做步2。**
2. 3D 抽离步2:热力+FX —— 抽热力层,**01 从 `heatLidMesh` 顶盖薄片升级为 02 的 `cellHeatMesh` 整格色阶 + 近 I/O 热区包络 Golden Zone**(02 实现在 `s3_ac_runtime.js:880` 与 `:943`)。
3. 3D 抽离步3:运动学 —— 抽曲线函数,两页传各自 motion 配置(01 带 laden_vy_factor、02 带匀速档),**严禁改数值口径**。
4. 双页悬浮全覆盖 —— 01+02 所有 KPI/图例/控件/图表/徽章;三段式学术文案;首次进页一次性引导(localStorage 记忆,键名风格同 `s3_collapse_01_v1`)。
5. 01 运动表现力提升 + 速度口径更显眼披露。
6. D:00 导览页(`src/00_导览.html`)衬线品牌(Georgia/宋体)→ 白红工程风,与 01/02/03 顶栏一致。
7. W4 打包 portable exe:electron-builder portable + extraResources 素材进包 + `main.js` 开发/打包态 `resourcesPath` 分支 + SmartScreen 说明 + 浏览器直开后备 + 打包后自检。
8. W5 评委机模拟(需用户在场);03 补拍素材(computer-use,方案见 docs/补拍作战方案_0722.md)。

## 6. 待拍板清单(勿混进待办)

- 03 补拍素材清单与排期(需用户在场,方案已备)
- W5 评委机模拟排期
- 遗留旧队列:#25(1_/2_ 收编)、#38-41(NN贪心/MLTRL/答辩题库/西门子杯研究)、R3 录屏/R4 报告/R5 导览

## 7. UNVERIFIED / 勿重做

**UNVERIFIED**:03 新版滚动式在 Electron 壳 iframe 内的完整交互(素材路由已验 4/4、页面本身滚动已验,但壳内滚动/控制台未全套跑);打包体积仍为预估(codex 打的 173MB exe 未采用)。

**勿重做**:
- 本会话四批已收口(§2 commit 链),勿回退;
- 任务A 结论已定——**速度不是 bug,不许再"统一渲染层"**(改任何一边都会让显示与该页 trace 时刻脱节);
- 任务B 已实测确认 01/02 顶栏外壳语言本已一致,勿重复对齐;
- 03 负结果七条 innerHTML 逐字节保真已由 `negResultsVerbatim` 断言守住,版式已改完;
- 环境修复已加固,勿再依赖 codex 共享 pnpm 缓存;
- codex 0724 交付(03 重设计 + W4 打包)**未采用**,完整存于 `stash@{0}`,勿误 pop。

**机制坑**:①壳内容区=窗口−48px 侧栏(默认 1552×900),只看裸 1920 会漏问题;②CSS 地层——改样式若不生效先实测 computed style,优先用「多 id 提升 specificity」(照 02:713 范式)而非再加 `!important`;③滚动式页面粘性头部会让 Playwright 原生 `click` 判元素被遮挡而超时,改用 `page.evaluate(() => el.click())`;④中文路径在 git bash 下 `git add <中文文件>` 可能失败,改用 `git add docs/` 目录级添加。

## 8. 文件索引(本会话新建/大改)

- 新建:`l4_showcase/.gitignore`、`l4_showcase/package.json`、`l4_showcase/package-lock.json`(playwright 本地化)
- 大改:`l4_showcase/src/03_FIO物理证据.html`(滚动式重设计)、`l4_showcase/tools/capture_fio54_evidence_qa.mjs`(QA 重写)
- 小改:`l4_showcase/src/s3_fill_candidate_runtime.js`(口径披露)、`l4_showcase/src/s3_ac_runtime.js`(档位悬浮口径)、`l4_showcase/src/01_连续填仓.html`(暂停钮深蓝)
- 文档:`docs/任务规划_0728_3D抽离与悬浮.md`(七问拍板+诊断)、`docs/codex任务A|B|C_0724.md`(已作废,仅存档)、本文
- 待建(步1):`l4_showcase/src/s3_scene_core.js`
