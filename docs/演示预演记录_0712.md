# 演示预演记录（0712 · Claude computer-use 亲跑）

> 环境：Automation Builder 2.9，`ABB_WH.project`，仿真在线（SIMULATION+RUN）。用户已打开 AB，Claude 用 computer-use 驱动。
> **安全边界（全程守住）**：只做在线监视 + 在线预备值写入（Ctrl+F7）+ HMI 按钮点击；**未改源码、未保存工程、未开第二实例**。所有写入=运行时态，Logout 即清，源码磁盘=已验证 75/0 代码，未动。

## 一、预演流程与实测（截图为证）

| 步 | 动作 | 观测结果 | 判定 |
|---|---|---|---|
| 1 | Online → Login → F5(Start) | 底部 **RUN**；HMI 实时渲染；Status=**READY_PRECOUNT=133**（133 预占=官方 sum 口径） | ✅ 在线运行时活着 |
| 2 | 点 HMI **Load demo**（20 件） | Status=**DEMO_LOADED_N=20**；检测器实时算出画像 **N=20 / DENS=0.075 / SKEW=0.53 / W=46.2 / HEAVY=0.20**；**REC=AWRA_HOLISTIC** | ✅ holistic 默认档路由正确（skew≥0.35→AWRA） |
| 3 | 写 `GVL_Visu.SelObjective=1`（GVL_Visu 在线表预备值+Ctrl+F7） | 值 0→1 写入成功；切回 VisuMain：**REC 当场从 AWRA_HOLISTIC 变为 TOB_LEX** | ✅✅ **词典序档检测路由在线端到端成立**（N=20/密度0.075→lex→TOB(5)，与 select_strategy 一致） |
| 4 | 写 `GVL_Visu.SelStrategy=5`（TOB）+ 点 HMI **Run assign** | Status=**DONE_OK**；20 件全放（左下角 20 绿格聚 I/O 附近=TOB 高频靠近特征）；**Violations=0**；Total inbound 122.0 / Avg 6.1 / Util 0.4 / Path 222.0；Current strategy=5 | ✅✅ **TOB 分配器在线实跑成功**（FB_AssignClassTurnover iMode=5） |

**结论**：D-1 的两块新拼图（词典序档检测路由 + TOB 分配器）在**真实在线运行时**跑通，非仅测试台。加上 ab_sync 三轮 75/0（含 T68/T69 CB/TOB 黄金向量逐位锁），CB/TOB/lex 的运行时正确性已双重坐实。

## 二、预演暴露的待办（画面洗，演示前必做）

1. **HMI 缺 SelObjective 开关**：本次靠在线写变量切档；正式演示要在 **P3 管理页**加一个"目标档：Holistic / Lexicographic"开关（绑 `GVL_Visu.SelObjective`），否则"现场切词典序档"没入口。
2. **HMI 缺 CB/TOB 策略按钮**：策略区只有 FIFO/Nearest/Score/AWRA-LS（0-3），无 CB(4)/TOB(5)；要补 2 个按钮（或下拉扩项），否则不能手选新策略演示。
3. **HMI 标签"Tests 59/59"过时**：可视化底部 caption 仍写 59/59，应改 **75/75**（在 VisuMain 可视化文本元件里）。
4. **CB 在线演示需密集批**：CB 路由要密度≥0.70（≈200 件），当前 Load demo 只 20 件（→lex→TOB）。要么加"密集载入"按钮、要么演示时只演 TOB（CB 已由 ab_sync T63/T64/T68 黄金向量证）。

## 三、收尾状态
- AB 留在 DONE_OK + TOB 布局（可供查看）；`SelObjective=1`/`SelStrategy=5` 是**在线预备写**，Logout 即回源码默认。
- **未保存工程**；源码磁盘 = 75/0 已验证代码，零改动。
- ⚠ 提醒：本 AB GUI 仍开着+在线。**再跑 ab_sync 前须先关掉本 GUI**（ab_sync.ps1 已加进程守门，检测到 AB 运行会 exit 3 拒绝，防双实例事故）。

## 四、画面洗收口（0712 下午 · §二清单核销）
**§二的 4 处待办全部落地并在线验证**（用户拍板"克制版"：P1 冻结只改字，新控件进 P3；密集载入不加——CB 由 ab_sync 黄金向量背书，现场只演 TOB）：
1. ✅ SelObjective 开关 → P3 新组 STRATEGY + OBJECTIVE：Holistic/Lexicographic 两钮，**在线实测 Obj 0↔1 切换生效**。
2. ✅ CB/TOB 按钮 → 同组 CB/TOB 两钮，**在线实测 Strat 4/5 生效**（附 "(0-3 on P1)" 指引）。
3. ✅ "Tests 59/59"→**75/75**（脚本 by-text 改字，在线可见）。
4. ✅（衍生）Detector 推荐镜像行进 P3——切档时 REC=AWRA_HOLISTIC→TOB_LEX **当场跳变**，成为演示高光。
5. 工程已保存（12:36），末次核对无星号；三页截图=`docs/img/P{1,2,3}_*_0712.png`；科普文档=`docs/画面科普_三页界面指南_0712.docx`。

**施工方式与关键弯路（教训固化）**：
- 元件/文本/布局=脚本管线（`tools/ab_scripting/wash_visu_0712.py`，ABORT 护栏+0 errors 才保存，一次过）。
- **雷：脚本 `add_input_action` 写入的 execute-ST 动作"配置在、GUI 里肉眼与活按钮零差异，但在线一律不执行"**；换会话结构重建（`fix_p3_buttons_0712.py`）同死。同页 build 轮（`create_visualobject` 新建页首会话）绑的动作全活——差异仅"页是否新建后首会话"。**修法=GUI 手工把 4 个动作删了重配（内容一字不差），全部复活。** 结论：对已存在 visu 页补"交互动作"走 GUI，元件本体仍可脚本。
- 次雷：type via clipboard 被第三方进程（疑 QQ/输入法剪贴板同步）反复覆写成"开工"→ 用 write_clipboard 显式写+立即粘贴+行首 Home/Delete 手术修复。

**旧 17 处 input-action 洗单核销状态**（build_three_pages_result.txt 尾部清单）：
- ✅ 在线验证活：P1 nav P3(312,8)、P2 nav P3、P3 nav P1/P2、P3 Login tap、P1 Auto strategy toggle（0712 下午实际点击均生效）。
- ⬜ 未逐一显式验证（同管线同轮建，推定活但留三页长跑时顺手点验）：P1 View cycle、P1 Tier input、P3 Logout tap、P3 PIN input、P3 Maint X/Y input、P3 Set/Clear tap、P3 weights×4 input。
- 长跑时如遇死件，按本节"GUI 手工重配"修法处理即可。
