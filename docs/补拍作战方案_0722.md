# 03 补拍素材 · 作战方案(0722,等用户确认新版后执行)

> 拍板背景:0721 用户原指令「补拍需用户在场」;0722 用户更新:「等我觉得没问题了,你使用computer use去自己补拍素材和继续下一步」——授权 Claude 自主补拍(以用户对新版说「没问题」为发令枪)。
> 红线:补拍必须真实运行不摆拍(C6);不改 sim 代码;不动 0713 原素材与 fault_state.json 等证据文件;一切新素材进新目录。

## 1. 目标素材清单(C6 四项细化)

| # | 素材 | 内容 | 产物 | 用途 |
|---|---|---|---|---|
| P1 | 全景运镜 | 54 格全链作业全景:Entry→Load→叉臂→Rack 跟随一次真实取放(Python 直控驱动),相机 2-3 个机位各一段 | 帧序列 png ≥20 张(或 Factory I/O 内置录制如可用) | 03 页对照剧场/证据画面升级、答辩开场 |
| P2 | 站位高清特写 | G2 取箱带载、H5 格位、P3 放箱、Entry 入场、Exit 出场,各 1-2 张(1920×1080+,运行中抓拍) | 8-10 张 png | 03 页六站点表配图补齐(现状无图站位不硬配→补齐后全配) |
| P3 | 负结果过程重现(谨慎项) | G4 NOSTART 类过程记录——仅重现**已知已归档**的负结果现象,不制造新故障;若现场行为与 0713 记录不符,立即停手记录差异,不硬拍 | 3-5 张 png+过程记录 md | 负结果七条的过程性配图(现仅有结果图) |
| P4 | .factoryio 场景工程文件 | 定位当前场景文件本体(File→Save As 或安装目录/文档目录搜 *.factoryio),复制入项目并记录 sha256 | 场景文件+登记 | 补 materials.config.json 已标注的缺口 |

## 2. 执行方式

- **动作真实性**:Python 直控脚本驱动(l2_factoryio 现有 F3 系列,只跑不改);computer-use 仅负责 Factory I/O 应用侧:启动、加载场景、相机调度、抓拍。
- **相机操控**:Unity 系右键拖=mouse_event 相对位移(SetCursorPos 无效),复用既往 rdrag.ps1 经验;每机位先 zoom 核对取景再拍(坐标漂移教训)。
- **抓拍**:优先 Factory I/O 内置截图(如有);否则系统级截屏落盘。
- **素材落盘**:`F:\abb_wh_work\l2_factoryio\media\补拍_0722\`(新建,与 0713 素材隔离);文件名沿用「站位_动作_内容_时间戳」惯例。
- **证据保全**:拍摄批次结束即写 `l2_factoryio/补拍登记_0722.md`(文件清单+mtime+对应动作链时段+Python 脚本日志指针),沿 F3_H5证据保全_0716.md 模式。

## 3. 开拍前检查单(computer-use 首动作)

1. Factory I/O 安装与版本(须 2.5.10 Ultimate,与 03 页口径一致;screenshot 确认标题栏)。
2. 场景加载:Automated Warehouse 场景(0713 同款);Modbus TCP Server 127.0.0.1:502 就绪(F3 脚本探测)。
3. Python 环境:l2_factoryio 脚本可跑(先 dry 探测:只读 tags 不驱动)。
4. 磁盘与分辨率:目标屏 ≥1920×1080;素材目录已建。
5. 任何一步不符 → 停,报告差异,不带病开拍。

## 4. 拍后流程(「继续下一步」)

素材验收(文件数/分辨率/内容抽查)→ 补拍登记落盘 → 03 页按需吸收新素材(站位配图补齐等,QA 15 项跑绿)→ commit → **W4 打包**(electron-builder portable+extraResources 素材进包+SmartScreen 说明)→ W5 评委机模拟。
