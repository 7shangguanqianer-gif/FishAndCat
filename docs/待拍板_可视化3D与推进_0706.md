# 待拍板三大问题:材料与 9 问计划(0706 compact 交接专用)

> 用途:用户 compact 后,新上下文按本文件执行「3 轮 × 3 问 = 9 问」问询(AskUserQuestion,
> 每轮一发 3 问,带推荐;用户答完一轮再发下一轮)。拍板结果回填本文件+§5,然后按结果开工。

## 三大问题界定

1. **可视化画面风格与细节**(设计稿 v2 三风格已出:2_你要操作/可视化屏幕设计稿.html,9d02b47)
2. **3D 仿真方案**(六方向清单见下;独立决策,不阻塞批次 C)
3. **批次 C 签发与近期推进优先级**(录屏规格/下一任务排序)

## 支撑材料 A:三个事实答复(已同步进设计稿 v2 顶部说明)

- HTML 设计稿=施工蓝图非交付物;真画面=CODESYS Visualization 在 AB 工程内搭(批次 C),仿真登录实时运行;
- 评委看到的=录屏里它跑起来的样子(0705 拍板:录屏+PPT 是初赛唯一演示载体);决赛可现场跑+改权重;
- 风格切换施工成本≈0(六色集中在 FC_StateToColor,画面底色一个属性)。

## 支撑材料 B:盲区扫描(用户不熟领域的关键事实)

1. CODESYS 画面表现力天花板低:纯色块/文本/基础按钮,无渐变阴影圆角动效——工业 HMI 不是网页;
2. "可视化仿真 20%"评的是 AB 内可视化(题面交付物明写),外部 3D 只能加分不能替代;
3. **ABB 红主题与报警红冲突**:风格 A 已做防混淆处理(报警=深红 #B71C1C+黑粗边);风格 B 深色底反而报警最醒目;
4. 3D 的"仿真"与"渲染"要分开:仿真数据已完整,3D 只是呈现层——是"给现有数据做回放器"不是"再做一个仿真";
5. WebVisu(浏览器跑 CODESYS 画面)存在但初赛不需要(录屏载体已定)。

## 支撑材料 C:3D 六方案(便宜→激进)

| # | 方案 | 成本 | 判定要点 |
|---|---|---|---|
| 1 | 等轴测静态 3D 图(matplotlib 3D 单帧,进报告封面/PPT) | 半天 | 3D 观感的 1/10 成本版 |
| 2 | **Python 3D 回放动画**(assignment CSV+路径数据→mp4,剪进录屏) | 1-2 天 | **性价比之王**:零新依赖、数据与 AB 画面同源,"同一数据三个视图"加固数字孪生叙事 |
| 3 | three.js 交互 3D | 2-3 天 | 炫但录屏载体下交互性落不到评委手里 |
| 4 | Factory I/O 级仿真软件(现成 AS/RS 场景,OPC UA 连 AC500) | 3-5 天+商业授权 | "真数字孪生"(类比汽车 HIL),采纳前须核授权价 |
| 5 | 不做 3D | 0 | 20% 评的是 AB 可视化;时间投转化 |
| 6 | 决赛展望条目(只写不做+方案1一张图) | 半天 | 买"想到了且有规划"的印象分 |

Claude 倾向(非拍板):风格看用户口味(A 最有比赛记忆点);3D 走 **1+2 组合**(约 2 天全复用现有数据),4 不碰。

## 9 问计划(compact 后照此执行)

**轮 1 · 画面风格**
- Q1 整体风格:A·ABB红白瑞士(推荐,评委品牌记忆点)/ B·深色工业 HMI / C·蓝灰工程(与报告 PPT 同族)/ 混搭(说法);
- Q2 报警格呈现:深红+黑粗边(A 配套,推荐若选 A)/ 标准红常亮(B/C 配套)/ 闪烁(定时器切色,施工+半天,录屏里更抓眼);
- Q3 右面板:权重区保留(R6"评委现场改权重"杀手锏,推荐)/ 去掉;缺什么元素(开放:ABB 字标、时间戳、口径角标…)。

**轮 2 · 3D 方案**
- Q4 做哪档:1+2 组合(静态图+回放动画,推荐)/ 只做 1 / 不做(5)+展望(6)/ 更激进(3 或 4);
- Q5 产物进哪些载体:录屏+报告+PPT 全进(推荐)/ 只报告封面 / 只录屏;
- Q6 排期:S2 录屏周同步做(推荐,动画正好剪进录屏 v1)/ 批次 C 后立即 / 8/26 后决赛阶段。

**轮 3 · 批次 C 与推进**
- Q7 批次 C 签发:风格拍板后立即签(推荐;执行=Codex 指导+用户操作,同批次 B 模式)/ 等课 0 小测交卷后一起 / 用户本人照操作卡搭(边学,时间×2);
- Q8 录屏规格:3-5 分钟解说+字幕(推荐)/ 1-2 分钟精华版 / 8-10 分钟全面版;
- Q9 下一优先:R1 报告 §2 样章化(推荐,S1 收尾)/ 录屏筹备前置 / 首期周报提前出 / 课 0 小测批改优先。

## compact 后 resume 指令(给新上下文)

读 §5 → 见「compact 交接」节 → 打开本文件 → 用 AskUserQuestion 发轮 1(Q1-Q3)→ 收答案回填本文件 →
轮 2 → 轮 3 → 全部拍板后:批次 C 签发(信箱)+3D 开工(若选)+按 Q9 排下一任务;全程五联动收尾。
