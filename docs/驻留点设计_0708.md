# 堆垛机驻留点(dwell-point)设计与定论(0708 实装完毕)

> 触发:对标 deep-research F2(docs/调研档案_0706/对标升级_deep研_0707.md)——dwell-point 选择是
> AS/RS 公认的独立优化维度(Egbelu 1991 / Hwang & Lim 1993 起数十年子文献),我方基线未显式建模。
> 状态:**sim 实装+30-seed 全量完成(0708),定论见 §0;ST 侧判 YAGNI 零改动(官方口径已最优)**。

## 0. 实验定论(sim/dwell_point.py,30 seeds×200 周期×2 填充档×2 先验,CSV=out/dwell_point.csv)

| 结论 | 证据 | 含义 |
|---|---|---|
| **官方口径=精确最优** | p_store=0.5 主口径下,400 格点全枚举的期望最优驻留点 **=(0,0),30/30 seeds、44%/89% 两填充档恒成立** | AWRA-LS 把高频货聚在 I/O 邻域(y 轴时间贵),布局优化吃掉了驻留点优化的全部增量空间;官方 B2("每个获取的初始入口位置都是(0,0)")钦定的 io 口径**恰是可证明的最优**——现状不是"没建模",是"枚举证明的最优" |
| **last 就地停靠免费更优(展望级)** | last vs io:总响应 **-0.83s\*\*\*(44%填充)/-1.91s\*\*\*(89%填充,-4.4%)**,且均次空驶 4.11/7.28s→**0** | 机理=事件流"回库→取货"交替下,回库毕停货区、下次取货第一段天然短——**双命令周期(dual-command,Bozer-White/C10)效应的退化形式**;不在固定点枚举空间内,与上行不矛盾(固定点里 io 最优,动态策略 last 免费更优)。**但违官方 B2 口径,只作报告"工程优化空间"展望,不进主口径** |
| **纯出库场景才有驻留点的肉** | p_store=0 下 demand 移到 (9,3)~(10,7) 带,出库响应 **-12%**(51.89→45.45s@89%填充),但回库变差更多、总响应反升;cog(O(n) 重心)与 demand gap 仅 0.4-0.7s=良好近似 | 给出收益边界:"出库远多于入库"的场景(如发货高峰时段)驻留点优化才值得;届时 ST 用 O(n) 重心即可(gap 已量化)。主口径下**不实装** |
| **ST 侧零改动(YAGNI)** | 官方口径(io)已是现状;last 的 ST 语义="作业毕就地停"=什么都不做,比 io 还简单 | 不给 PLC 加行为不变的代码;iPassed=41 不动。块3 画面可加"驻留点=I/O(枚举证明最优)"叙事标注 |

**报告叙事(评分细则"算法创新"素材)**:我们显式建模了业界公认的 dwell-point 决策维度,用
400 格点精确枚举证明官方口径的最优性(而非默认接受),并给出超出题面的工程展望(last 免费
-4.4%+零空驶/纯出库场景 -12%)——"每个假设都被系统性攻击过"的又一例(与 F23 装卸探针同门)。

## 1. 概念

驻留点 = 堆垛机完成一次作业、下一请求到来前的**空闲停靠位置**。选得好 → 下一次作业期望行程更短。
我方当前隐含假设:每次作业后回 I/O 点 (0,0)(canonical A3「作业起点/终点」+ C1「双程 I/O→仓位→返回」)——
即默认驻留点固定在 (0,0)。这留下一个低成本、高辨识度的优化空间。

## 2. 候选策略(文献经典 4 类)

| 策略 | 规则 | 目标 | 备注 |
|---|---|---|---|
| `io` | 停 I/O 点 (0,0) | 基线 | **我方当前隐含**,作对照锚 |
| `last` | 停最后作业位置(空闲不动) | min 重定位移动 | 单命令场景常用 |
| `center` | 停仓库几何中点 | min 最大行程(minimax) | 无需求信息时的稳健选择 |
| `demand` | 停需求加权期望位置(高频货区重心) | min **期望**行程 | 收益最大,需在库频次信息 |

## 3. 我方落地方案

### 3.1 sim 侧
- 加参数 `dwell_policy ∈ {io, last, center, demand}`;
- cycle time 计算从固定 (0,0) 起算改为**从 dwell_point 起算**,作业毕按策略停靠;
- **收益主场景=出库(取货)**:取货请求 ∝ 频次,驻留点在高频区附近 → 期望取货时间 C3 ↓;
- 对照实验:复用 **30-seed 统计框架**比较 4 策略的 C3(期望取货时间)/C1(双程时间),给收益量化+95%CI;
- 默认 `io` 保回归,升级项开 `demand` 验证。

### 3.2 ST 侧
- PRG_Main 状态机加 **DWELL 态**:作业完成后堆垛机移动到 dwell_point;
- `demand` 策略下 dwell_point = 扫描在库高频货重心(O(n) 轻量,不触看门狗);
- 可视化:堆垛机停驻留点(VisuCraneXY)+ 画面加「驻留点」标记(块3 画面亮点)。

### 3.3 验证
- sim 对照:`demand` vs `io` 的期望取货时间差(量化收益);
- ST:AB 实跑堆垛机空闲移到驻留点 + 可视化正确;
- 一致性:sim↔ST 驻留点位置一致(复用 consistency_probe 思路)。

## 4. 待定项(实装时 / 用户拍板)

1. **默认策略**:`io` 保守 vs `demand` 主推——建议 `demand` 主推 + `io` 对照(呼应我方"主口径+对照"传统);
2. `demand` 期望位置口径:在库高频货重心 vs 历史请求热力重心;
3. **是否进决赛演示**(画面亮点)vs 仅报告分析项;
4. 实时性:`demand` 每次空闲扫在库货算重心,400 库位 O(n) 可行(已初判无压力,实装时实测)。

## 5. 任务定位与工作量

- **归属**:算法增强(块1 延伸),画面部分并入块3(驻留点可视化);
- **依赖**:无——可**独立于块3先做 sim 对照验证**(拿收益数字),ST/画面随块3;
- **工作量**:sim ≈ 半天(参数+cycle time 改造+对照实验),ST ≈ 半天(DWELL 态+可视化),验证 ≈ 半天;
- **性价比**:对标评「值得抄·高性价比」(低成本、AS/RS 公认维度、报告可写、画面加分)。

## 6. 来源
对标 deep-research F2;Egbelu(1991)、Hwang & Lim(1993)驻留点选择规则(经 canonical A10 登记)。
