# 01 页过时设计审计(0722,用户三次打回后全面清查;真相源=0718-0720 断点否决史+02 现行源码)

> 用户令:「蓝框里面的货物黄色横杠是过时的设计(同时01里面还有很多02里面早就过时的设计,一并找出并且从0开始彻底重构)」「绿色方框设计也很有问题」。
> 反思记录:0718 已有「穿模+金顶+黄牌」三连批评与「改前端 UI 前先查该元素是否已废止」纪律(memory 在案),Claude 仍在侦察/brainstorm 中把金顶当「01 独有资产」——本清单即该纪律的补课与制度化执行。

## 一、确认过时(重构中清除,附废止依据)

| # | 01 现状 | 02 废止/现行依据 | 重构对齐 |
|---|---|---|---|
| P1 | **热门货金顶盖** hotLidMesh(s3_fill_candidate_runtime.js:319-344,0717 #26-2)+ 2D 金色角标 + 图例「金顶 = 热门前20%」 | 0718b 断点:「gold-cap 被用户否」;展示研究结论「金顶失败=逐箱装饰把聚集拆成孤立点;行业标配=货位色阶热力图(instanceColor 按 freq)+近 I/O 热区包络(Golden Zone)」;02 现行=s3_ac_runtime.js:879-1026 instanceColor 平涂热度色+heat3d/heat2d 双开关 | 01 按 02 模式重做:货位色阶热力(freq 连续分)+3D/2D 开关+近存取口热区;图例改「访问频率:低→高」(A7 词根) |
| P2 | **目标格大头针箭头** targetFx.arrow(0717 #28 老版:发光壳+箭头) | 0718b:「目标 FX:入库=金色收敛…只在行进段显示、handle 段收起(修穿模)」——02 已重构,箭头淘汰 | 01 对齐 02 现行 FX 语言:目标格收敛高亮、行进段显示、就位收起,无箭头实体 |
| P3 | **绿底「数据链已验证」状态签当按钮**(顶栏右上) | 命名总表 A14:0720 版B 拍板「证据两键→单键『证据中心』」;02 现行=单键学术工程风(s3_ac_runtime.js:2133) | 01 顶栏改「证据中心」单键(同 02 样式),点开=弹层 Tab(本页证据链:validator 18/哈希链/终态账本/口径卡);「已验证」是内容里的状态行,不是按钮皮肤 |
| P4 | **固定悬浮提示机制缺失**(title= 与 hasFixedTip 全 0 命中,自制 pop 群风格自立) | 02 现行 .hasFixedTip+tipBubble 机制(0720 W1-6 悬浮解释) | 平移 02 机制;01 自制 pop 仅保留进度轴节点跳转类交互 |
| P5 | **2D 图无热力层**(三联仅等级色) | 02 heat2dToggle+2D 热力层(0718c) | 三联/放大层增加「访问频率」着色模式,与 P1 同一数据口径 |
| P6 | **CSS 历史地层**:data-style A/B/C 三版候选层、0716/0717 断点层、被 !important 互搏的多代规则(1713 行中约半数为死层;本轮 604 行裸 #slotMap !important 互搏即其恶果) | 02 的 CSS 为 0719-0720 整改后现行单代 | **从 0 重写**:新 html 以 02 现行层为母版,只含现行代;历史层整体不带入 |

## 二、02 专属能力,01 不搬(数据边界正当,已有拍板)

故障链路(01 payload 无故障字段,硬加=虚构)、三档拟真(F9:导流去 02)、入场排队带(F8:arrival_model=no_queue 口径转正)、AUTO 路由决策卡(B5:01 无路由,词=默认策略)。

## 三、保留待用户裁决(不在本轮清除)

- **F 轴行(dock 轴状态第三行)**:0720 A9 拍板「F 轴行删」范围标注为 **(02)**;01 有伸叉/回叉两步,F 轴(货叉行程)有真实数值与语义。本轮保留;若用户认为同样过时,一并删(改动极小)。

## 四、重构蓝图(从 0)

1. **新 01 html**:以 02_入出闭环.html 现行结构为语言母版(顶栏一线/口径带/dock 四列/热力开关/证据中心单键/tipBubble/现行 CSS 单代),内容区替换为 01 的赛马叙事(0722b 已达标的一屏骨架沿用:3D 主角+精华条竖排三联+双指标微条+弹大层九件+容量节点进度轴+自动播放)。
2. **runtime 改造**(数据逻辑零动):金顶→instanceColor 色阶热力+开关(借 02 实现模式);targetFx→02 现行 FX 语言;新增 heat 数据通道(freq 连续分,数据已在 cargo_catalog)。
3. **interactions**:清理对死层的适配代码;tooltip 换 hasFixedTip。
4. **QA**:61 项逐条适配(金顶断言→热力断言;绿签断言→证据中心断言;新增 no-hotLid/no-arrow/heatToggle 断言);零滚动/双分辨率/自动播放断言沿用。
5. 完成判据:五套全绿+diag 双视口(1552×900/1945×1094)亲验+过时清单逐项「已清除」核销。
