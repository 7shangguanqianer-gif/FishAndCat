# 会话断点 · 2026-07-28b(3D 抽离三步 + 悬浮全覆盖已收口;剩 01 运动表现力 / D 导览页 / W4 打包)

> 压缩/新窗唯一恢复入口。配套:`F:\abb_wh_work\docs\任务规划_0728_3D抽离与悬浮.md`(含步1/步2/步3/悬浮四份完成记录与全部实测数字)。上一份断点:`F:\abb_wh_work\docs\会话断点_0728a.md`(已过时,只作历史)。

## 1. 一句话战况

0728 七问拍板的「先打地基」全部做完:3D 公共模块三步抽离(几何+材质 / 热力+FX / 运动学)与双页悬浮全覆盖均已完成、全绿、push;**下一步是 #23「01 运动表现力提升 + 速度口径显眼披露」(0728 拍板③「两者都要」里尚未动的那一半),尚未开工**。

## 2. 已完成账(证据=commit,全部已 push 到 origin/master)

| commit | 内容 |
|---|---|
| `440cd67` | 3D 抽离步1(几何+材质):两页内联三维母版 → 共用 `l4_showcase/src/s3_scene_core.js` |
| `b3fef20` | 3D 抽离步2(热力):新建 `l4_showcase/src/s3_heat_layer.js`;01 从箱顶薄片升级为整格前罩 + 近 I/O 热区包络 |
| `de1f409` | 3D 抽离步2(FX):目标箭头收进公共内核、01 恢复箭头、修好遮挡守卫的三维漏判 |
| `1e5c5fc` | 3D 抽离步3(运动学):新建 `l4_showcase/src/s3_motion.js` + 永久口径锁 `l4_showcase/tools/test_s3_motion_parity.mjs` |
| `2cc1acc` | 落盘步2/步3 完成记录到 `docs/任务规划_0728_3D抽离与悬浮.md` |
| `ac148d0` | 双页悬浮全覆盖:新建 `l4_showcase/src/s3_tooltip.js` + `l4_showcase/src/s3_tooltip_copy.js` |
| `00f5f45` | 落盘悬浮完成记录 |

**重要更正**:上一份断点 `会话断点_0728a.md` 称前 5 个 commit(`5f4d374`/`ddf27f4`/`2b19067`/`2b2f883`/`51393ff`)「全部已 push」是**错的**。本会话首次 push 时区间为 `c9a3363..440cd67`,说明它们此前只在本地。现已全部在远端。

## 3. 拍板与否决记录(用户中文原话逐字)

- **0728 七问**(沿承自 `会话断点_0728a.md`,未变):
  1. 3D 架构 →「抽公共 3D 模块,两页共用(推荐)」
  2. 热力形式 →「整格色阶 + Golden Zone 热区(推荐)」
  3. 货物运输速度 →「两者都要」(观感提升 + 口径披露)
  4. 悬浮覆盖 →「全覆盖 + 首次进页引导」
  5. 抽离策略 →「分三步递进,每步全绿才下一步(推荐)」
  6. 悬浮文案 →「1,但是注意用词要学术」(=三段式:是什么/为何这样/优在哪)
  7. 优先级 →「先打地基:3D 抽离 → 热力+悬浮 → D/W4」
- **本会话新增拍板(目标 FX 箭头)**:选项①「两页都保留箭头(恢复 01)」+ 原话追加 **「1,但是注意排查箭头相关的bug」**。已按此恢复 01 箭头并完成缺陷排查(见 §4)。
- **沿承否决(仍有效)**:金顶盖 gold-cap 已废止勿复活;黄牌 `#targetBeacon` 永不复现;「从头回放/导出3D帧/总览」按钮已删;速度**不是 bug**,不许再「统一渲染层」。

## 4. 数字口径(全部可复现,cwd=`F:\abb_wh_work\l4_showcase`)

### 4.1 当前全绿基线(七套)
```
node tools/test_s3_motion_parity.mjs          → tests 5  / pass 5  / fail 0
node tools/test_s3_runtime_contract.mjs       → tests 30 / pass 30 / fail 0
node tools/capture_s3_layout_a_qa.mjs         → PASS 64/64
node tools/capture_s3_mixed_layout_a_qa.mjs   → PASS 8/8
node tools/capture_s3_fill_uniform_qa.mjs     → PASS 8/8
node tools/capture_fio54_evidence_qa.mjs      → PASS 19/19
cd app_shell && node verify_material_route.mjs → ALL PASS 4/4
```
01QA 项数演进:61(会话初) → 62(加热区包络门 `fx28HotZoneEnvelope`) → 63(加箭头不被吞没门 `fx28ArrowNeverSwallowed`) → **64**(加悬浮覆盖门 `tooltipFullCoverage`)。

### 4.2 步1 等价性(工装 `scratchpad/parity.mjs`,拦截 runtime 后比对纯母版产物)
- 01:**220** 对象,改造前后场景签名均为 `5ff425e1b9faef2a`;灯光 `e987caf72d891ae8`
- 02:**239** 对象,改造前后均为 `b0bc7f2cf22ee5ca`;灯光 `270111dcb066ac93`
- `__S3_GEOMETRY_AUDIT` 逐字段一致。唯一噪声=three.js 每次给 Shape 新生成的随机 uuid,已剔除。

### 4.3 步2 热力(工装 `scratchpad/heatcheck.mjs`)
- 02 等价:前罩 **120** 实例矩阵+颜色哈希 `3a359138c519f2a7` 与 HEAD 逐字节一致;前罩参数 `{count:120, opacity:.78, size:[.96,.02,.96], centerY:.10}`;热区 `{pos:[5.5,.5,3], scale:[11.14,1.07,6.14]}`
- 01 新前罩:`size:[.96,.012,.96]`、`centerY:.051` → y 带 **[.045, .057]**。同场景实测其他实例 y 包络:货架前缘梁 **[-.022, .042]**、箱盖(.86×.88×.08)**[.06, .94]**、箱体(.84×.86×.72)**[.07, .93]** → 净空带成立、不互穿
- 01 前罩实例数 = 在库件数(201/267 节点实测 201);热区 `bounds.cells` = 该帧在库热门件数(实测 40)

### 4.4 步2 热区形状实测(工装 `scratchpad/zonewalk.mjs`,每步回读 `mapAudit.activeLane` 自证 lane 切换生效)
option value 是**小写** `seq/near/score`(首次用大写导致三算法结果全同,是我的脚本错,已更正):

| 节点 | SEQ | NEAR | SCORE |
|---|---|---|---|
| 67 | col 0-4 / tier 4-19 | col 0-14 / tier 0-5 | col 0-15 / tier 0-4 |
| 134 | col 0-10 / tier 0-19 | col 0-19 / tier 0-10 | col 0-18 / tier 0-9 |
| 201 | col 0-14 / tier 0-19 | col 0-19 / tier 0-14 | col 0-18 / tier 0-13 |
| 241 | col 0-17 / tier 0-19 | col 0-19 / tier 0-17 | col 0-19 / tier 0-16 |
| 267 | col 0-19 / tier 0-19 | 同 | 同 |

结论:中途节点形状差异有判别力(SEQ 竖长条 vs NEAR/SCORE 贴底扁条);267 全满三者都是全场,是**容量必然**(400 格 − 133 预占 = 267,53 件热门必然摊满),非实现缺陷。

### 4.5 步2 FX 箭头缺陷(工装 `scratchpad/arrowsweep.mjs`,6 周期 × 7 步 × 4 进度 = 168 帧)
同一(修正后)检测器下:
- 修前(HEAD 版 02,旧守卫 `Math.abs(frame.machine.x - col) < .60`):箭头可见 **46** 帧,被载货台吞没 **2** 帧(`cyc2 LINK_TRAVEL p=.6`、`cyc5 LINK_TRAVEL p=.3`)
- 修后:箭头可见 **44** 帧,被吞没 **0** 帧
- 其余四项均 0:出画 0、越过取景包围盒顶(z>20.9)0、箭头可见而发光壳不可见 0;浮动相位可复现(同一 seek 两次均 `3.143901737227545`)
- **典型漏例**:`cyc2 LINK_TRAVEL p=.6` —— 箭头世界 x=1.5、桅杆 x=2.14,水平距离 0.64 > 阈值故守卫放行,但载货台包络 x[1.72,2.56] z[1.36,3.04] 与箭头包络 x[1.01,1.99] z[2.31,4.07] 三轴全重叠
- **我自己的返工**:首版扫描脚本用「桅杆∪载货台」并集 AABB,把载货台 x 跨度与桅杆 z 跨度拼成虚构大盒子,误报 2 帧;已改逐部件判定,上面数字均出自修正后检测器

### 4.6 步3 运动学
- 口径锁 `tools/test_s3_motion_parity.mjs` 逐点 `Object.is`:单轴梯形档 700+ 点、单轴全档位、01 双轴含折减 **104,000** 点、02 双轴含匀速档 **208,000** 点,全部一位不差;外加反向断言「两页口径差异必须仍然存在」
- 接线核验(`scratchpad/motionparity.mjs`):两页各 **420** 帧密集 seek(01 = 3 算法 × 7 事件 × 4 阶段 × 5 进度;02 = 12 周期 × 7 步 × 5 进度),逐帧比对桅杆/载货台/货物世界坐标 + 轴 HUD 文本 → 01 哈希 `e8ca2c5c941006d6`、02 哈希 `6754cf4e8a33b1cc`,新旧相同
- 口径常量:VX=2.0 / VZ=0.5 m/s、AX=0.5 / AZ=0.3 m/s²;01 `LADEN_VY_FACTOR = 0.8`(来源 `F:\abb_wh_work\sim\warehouse_sim.py:78`,行业依据 Mecalux 0.818 / Muvro 0.667);02 `trace.meta.motion` = `trapezoid_accel`,另有 `constant_speed` 匀速档

### 4.7 悬浮覆盖(工装 `scratchpad/tipcheck.mjs`)
- 01:注册 **28** 项,命中 **62** 个元素;02:注册 **25** 项,命中 **37** 个元素;未命中 0、三段不全 0
- 悬停实测:气泡弹出 ✓、三段(是什么/为何这样/优 · 边界)渲染 ✓、完整落在视口内 ✓
- 首次引导:首访出现 ✓,二次访问不再出现 ✓(localStorage 键 `s3_tip_hint_01_v1` / `s3_tip_hint_02_v1`)

### 4.8 瘦身账
- `01_连续填仓.html` 2089 → **1748** 行;`02_入出闭环.html` 1660 → **1302** 行
- 两页内联 3D 段 475 / 436 → **179 / 129** 行
- 公共模块:`s3_scene_core.js` 423 + `s3_heat_layer.js` 96 + `s3_motion.js` 82 + `s3_tooltip.js` + `s3_tooltip_copy.js`

### 4.9 磁盘治理(未处理,已开任务卡 `task_7f2d1b66`)
`F:\abb_wh_work\l4_showcase\out\s3_layout_a_qa_0716\` 累积 **44** 个 `_local_hash_snapshot_*` 目录、合计 **428 MB**(`du -sh` 实测)。`l4_showcase/out/` 在 `.gitignore` 内,纯本地磁盘问题。

## 5. 待办优先序(第一动作在最前)

1. **【第一动作】#23 01 运动表现力提升 + 速度口径显眼披露** —— 0728 拍板③「两者都要」尚未动的那一半。
   - 口径披露侧:现状是 `F:\abb_wh_work\l4_showcase\src\s3_fill_candidate_runtime.js` 内 `raceBannerMotionText` 真读 `payload.shared_input_provenance.motion` 输出模型与折减系数(commit `ddf27f4` 所做),用户要求**更显眼**。可考虑放进新建的悬浮层(`s3_tooltip_copy.js` 的 `#axisHud` 项已写了两页差异,可提升为常驻可见的口径条)。
   - 观感侧:01 的运动表现力(尚无具体方案,需先看当前播放效果再定;**严禁改运动学数值**——`tools/test_s3_motion_parity.mjs` 会红)。
2. **#17 D:00 导览页风格对齐** —— `F:\abb_wh_work\l4_showcase\src\00_导览.html`(295 行),衬线品牌(Georgia/宋体)→ 白红工程风,与 01/02/03 顶栏一致。用户 0724 原话「要」。
3. **#18 W4 打包 portable exe** —— electron-builder portable + extraResources 素材进包 + `app_shell/main.js` 开发/打包态 `process.resourcesPath` 分支 + SmartScreen 说明 + 浏览器直开后备 + 打包后自检。**注意新增了 5 个 src 下的 js 模块,打包清单必须包含**。
4. W5 评委机模拟(需用户在场);03 补拍素材(computer-use,方案见 `F:\abb_wh_work\docs\补拍作战方案_0722.md`)。

## 6. 待拍板清单(勿混进待办)

- **02 页热区图例文案**:01 已改为只写口径「热门货包络 · freq 前 20% 所在格」(因 01 终态必然全场,写「近存取口」会成假陈述);02 当前框确在近 I/O 一角故其「热门货聚集区 · 近存取口(P&D)」暂时为真,是否也统一改成只写口径以防将来数据变化 → 待拍板。
- 03 补拍素材清单与排期(需用户在场,方案已备)。
- W5 评委机模拟排期。
- 遗留旧队列:#25(`1_给你看`/`2_你要操作` 收编)、#38-41(NN贪心/MLTRL/答辩题库/西门子杯研究)、R3 录屏/R4 报告/R5 导览。

## 7. UNVERIFIED / 勿重做

**UNVERIFIED**:
- 新增的 5 个 src 模块在 **Electron 壳 iframe 内**的完整表现未逐项跑(壳素材路由 4/4 已过、页面本身七套全绿,但壳内悬浮气泡的 fixed 定位、首次引导位置未在壳内亲验)。
- 打包体积仍为预估(codex 打的 173MB exe 未采用)。
- 03 新版滚动式在壳内的完整交互(沿承自 0728a,未变)。

**勿重做**:
- 本会话 7 个 commit 已收口,勿回退。
- **速度不是 bug**,不许再「统一渲染层」——现在有 `tools/test_s3_motion_parity.mjs` 的反向断言把关,改了会立刻红。
- 01/02 顶栏外壳语言本已一致(commit `2b19067` 实测确认),勿重复对齐。
- 03 负结果七条 innerHTML 逐字节保真已由 `negResultsVerbatim` 断言守住。
- 环境修复已加固(playwright 本地化,`l4_showcase/package.json`),坏了跑 `npm i` 即恢复,勿再依赖 codex 共享 pnpm 缓存。
- codex 0724 交付**未采用**,完整存于 `stash@{0}`,勿误 pop。
- 工作区里 `l4_showcase/app_shell/dist/`、`verify_portable.js`、`评委机运行须知.txt`、`design_mock/assets/` 是 codex 0724 遗留的**未跟踪文件**,本会话未提交;W4 打包时先判断是否复用还是重做。

**机制坑(本会话新增/复现)**:
1. `tools/capture_s3_layout_a_qa.mjs` 把页面依赖复制进 `_local_hash_snapshot_<id>/` 再服务。**新增页面级依赖必须同步改 `liveFiles` 与 `snapshotTargets` 两处**,漏了不会报错而是页面 3D 起不来、断言归零显示 `FAIL 0/0`(本会话踩过一次)。目前清单含:source / runtime / sceneCore / heatLayer / motion / tooltip / tooltipCopy / store / interactions / shellCss / three / orbit / pointer / registry / manifest / audit×3 / evidenceSeedSkew。
2. `tools/test_s3_runtime_contract.mjs` 在 Node 里直接 `require` runtime 并调 `runtime.motionPoint()`,故必须**先 `require` `src/s3_motion.js`** 把 `S3Motion` 挂上 globalThis,顺序不能反。
3. 两页的指标区/图例/步骤条每帧 `innerHTML` 重渲染 → 任何逐元素 `addEventListener` 都会在第一次重渲染时失效;交互必须走事件委托。
4. 01 页 `#strategySelect` 的 option value 是**小写** `seq/near/score`(`AUTO` 才是大写);用大写 setValue 会静默变成空串,三算法结果看起来全同。
5. 01 页 EOL=CRLF、02 页 EOL=LF,批量脚本改文件时要保留各自 EOL。
6. Windows 终端 `print` 中文文件名会撞 cp1252 编码报错,但文件**可能已经写成功**——报错后先 `wc -l` 核对,别急着重跑(`io.open(f,'w')` 会先截断)。
7. 沿承:壳内容区=窗口 −48px 侧栏(默认 1552×900);改样式不生效先实测 computed style,优先多 id 提升 specificity;粘性头部会让 Playwright 原生 `click` 判遮挡超时,改 `page.evaluate(() => el.click())`;中文路径 `git add <中文文件>` 可能失败,改用目录级添加。

## 8. 文件索引(本会话新建/大改)

**新建(全部在 `F:\abb_wh_work\l4_showcase\`)**
- `src/s3_scene_core.js` —— 三维公共内核(几何 + 材质 + 货架 + 堆垛机 + 工位 + 目标 FX 实体 + 箭头遮挡守卫 `targetArrowBlocked()`)
- `src/s3_heat_layer.js` —— 货位热度层(色阶 + 整格前罩 + 热区包络)
- `src/s3_motion.js` —— 双轴运动学
- `src/s3_tooltip.js` —— 悬浮解释层(事件委托 + 选择器注册表)
- `src/s3_tooltip_copy.js` —— 三段式文案表(**这是给评委看的内容,措辞需审阅**)
- `tools/test_s3_motion_parity.mjs` —— 运动学永久口径锁

**大改**
- `src/01_连续填仓.html`(2089 → 1748 行)
- `src/02_入出闭环.html`(1660 → 1302 行)
- `src/s3_fill_candidate_runtime.js`(热力升级 + 箭头恢复 + 运动学接线 + 悬浮注册 + data-metric)
- `src/s3_ac_runtime.js`(热力/运动学改走公共模块 + 箭头守卫 + 悬浮注册 + data-metric)
- `tools/capture_s3_layout_a_qa.mjs`(哈希快照清单 +5 项;新增 3 道门,61 → 64 项)
- `tools/test_s3_runtime_contract.mjs`(先 require s3_motion.js)

**文档**
- `F:\abb_wh_work\docs\任务规划_0728_3D抽离与悬浮.md`(新增 §4.5/4.6/4.7/4.8 四份完成记录)
- `F:\abb_wh_work\docs\会话断点_0728b.md`(本文)

**临时工装(在 scratchpad,不入库,但可复用)**
`C:\Users\86177\AppData\Local\Temp\claude\C--Users-86177\c0960d42-d184-418d-ab8a-4f674d927071\scratchpad\` 下:`parity.mjs`(场景签名等价)、`heatcheck.mjs`(热力等价+几何自证)、`arrowsweep.mjs`(箭头缺陷扫描)、`motionparity.mjs`(运动学接线逐帧)、`tipcheck.mjs`(悬浮覆盖)、`zonewalk.mjs`(热区演化)、`smoke3d.mjs`、`splice.mjs`、`inventory.mjs`
