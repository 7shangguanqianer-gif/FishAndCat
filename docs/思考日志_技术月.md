# 思考日志 · 技术月(冻结期报告素材缓存;8/01 后写作时检索)

## 0706
- 评分细则九条 vs 现状差距表(见当日会话/总纲):核心缺口全是"显式化"——FC_PathPlan/统计页/变量字典/边界矩阵/FB接口表。
- 报告伏笔:①"合理性/高效性/准确性"三词组织论证章;②L型路径(画面)与双轴同动真曲线(学术)双口径并存,标注等效性;③NFL 定理框定"就近贪心在受限场景合法占优"→策略库动机;④多工况分治血统(ABB杯2023最高奖)/虚拟调试/BEAR错误驱动语录/ACS880挂钩/分层模式语言五术语;⑤ρ=1.7队列爆炸实测=排队论教学素材+超载彩蛋段候选;⑥平均响应含排队(89.3s)vs服务时长(32s)的口径区分=档3语义关键。
- 参数账:垂直laden×0.80(Mecalux0.818/Muvro0.667);水平不折减(行业惯例);装卸三档=情景假设需标注;FEM9.851把pick/deposit当常数=我们可变装卸是超出标准的细化,报告可点。

## 0706 块1施工枪(D统计/E公式/F求解器)落地裁决

- **枪F 裁决(T1.3 方案修正)**:我们的目标函数四项(效率/稳定/能耗/δ预留)全是 (货,位) 独立成本、无货物间交互项 → 纯线性分配问题 LAP,**scipy.optimize.linear_sum_assignment(JV 算法)直接给精确最优,400×400 约 10ms**——比 CP-SAT 更快且给的是精确最优而非下界,弃 OR-Tools(决策梯:已装依赖 scipy 能干)。承重/容积硬约束用禁止边 big-M 编码(正规名 "forbidden assignment via prohibitive cost",Valmadre 笔记)。复杂度背书:线性 SLAP=LAP 多项式可解,含货物亲和项才变 QAP/NP-hard(Reyes 2019 综述/Burkard QAP)——报告可写"证明线性口径精确最优+指出亲和扩展的 NP-hard 边界"。gap 报告惯例:列名 Gap(%),口径 (Heur−Opt)/Opt×100(SLAPRP BCP 2024);文献量级参照 ALNS vs Gurobi 0.32%。
- **决策梯命中**:analytic_bounds.py 已有 BW 公式(E[SC]=T(½+b²/6) 单程口径)+重排不等式松弛下界 → T1.3 改造为**三级证据链**:松弛下界 ≤ LAP 精确最优(新增,含约束) ≤ AWRA-LS 实测;T1.4 的 BW 锚底子已有,增量=枪E 公式核对+30seed 对照+加减速口径差异说明。
- 30-seed 框架冒烟即出苗头:tiny_light(极小批全轻货)awra 领先 near 从主口径 -34.8% 缩到 -9.7%——用户"轻货小批贪心追近"直觉方向初步成立,等全量定论(将成 U1 检测器的动机数据)。

## 0706 T1.1 全量 30-seed 定论(12场景×30seed×5策略=1800 runs,违规0)

- **表层负结果**:12 场景 awra 全部最优、全部 p<0.001,"贪心反超"未发生——但这是 awra(批量上帝视角+局部搜索)vs near(在线逐件)的**跨信息量对比**,赢在批量已知,不全是算法本身;报告对比必须分层(在线组内比/批量跨级比,canonical 在线/批量二分沿用)。
- **深层支持用户直觉(在线组内公平对比 score vs near)**:tiny_light 差 -2.9% **ns(统计无差异)**;flat_info 差 -0.12%(实务为零)——**极小批或频次无画像信息时,多目标评分退化≈贪心**,这就是 U1 检测器的第一批规则:①频次偏斜低→near 即可 ②n≤20→near(省 PLC 算力)③高密度>75%→必须 awra ④其余→score/awra。
- **fail 维度的硬差异(F19 最强素材)**:dense_240(90%填充)near 平均失败 8.8 件、score 7.63、**awra 0**——贪心高压下的真实代价不是时间是"放不进去"(可行位被先到者占死),awra 的 Rank 让难放者先挑。dense_200 同方向(3.03/2.43/0)。
- CI 侧写:uniform/flat 场景 CI 半宽极小(±0.01~0.04),skew 系 ±0.2~0.5(pareto 长尾致 seed 间方差),自洽;30-seed 半宽/均值≈0.5%~4%,优于文献判停惯例(arXiv 2407.06394 半宽≤1%)大部分场景。
- score-near 差距最大处在低密度(light_sparse -18.2%/heavy_sparse -17%)=δ 预留项发挥空间大;高密度反而缩到 -3.3%(都被迫捡剩位)。

## 0706 枪E(Bozer-White)落地裁决(T1.4 施工依据)

- **公式核对通过**:analytic_bounds.py 现有实现与文献一致——单程 E=T(b²/6+1/2)、E(TB)=T(1/3+b²/6−b³/30),b=min(th,tv)/T,T=max(th,tv)(Le-Duc 2005 ERIM 博士论文 p.96-109 教材级核对,标注"As proven by Bozer and White 1984";Roodbergen & Vis 2009 EJOR p.355 交叉)。E(SC)=2×单程是对称性推导(枪如实标注非逐字引用),报告引用时写单程口径最稳。
- **原文出处定版**:Bozer & White 1984, IIE Transactions 16(4):329-338, DOI 10.1080/07408178408975252(付费墙;引用链=原文出处+Le-Duc 2005 公式转述+R&V 2009 综述)。
- **离散 vs 连续误差:文献无现成数字**(Lee et al. 1999 只有定性"small";搜索摘要曾冒出"0.25%"被枪鉴定为 AI 幻觉不采信)→ **T1.4 自己跑 20×20 离散 vs 连续对照,产出可复现数字,本身成报告贡献点**。
- **加减速口径锚**:恒速 vs 梯形速度剖面偏差 E(SC) 6-12%/E(DC) 10-16%(Oser & Drobir 2012 CICMHE 公开论文已核对;对象是弯道式 AS/RS,量级参考)。我们 ACCEL=False/True 双口径的实测差可与此对照;精细解析线=Hwang & Lee 1990(IJPR 28(10):1779-1789)/Chang et al. 1995(IIE Trans 27(1):108-111,页码 2:1 取此)。
- 模型假设六条逐核对齐(随机存储/连续矩形/Chebyshev/恒速/I-O 角点+四变体/单巷道 FCFS)——与我们仿真假设差异只有"恒速vs梯形"和"连续vs离散"两处,正好是 T1.4 两个对照实验。

## 0706 深夜 块1 挂机施工账(T1.2/T1.3/T1.4 主体)

- **T1.2 七策略全景(2520 runs)**:无全能冠军=检测器实证地基。TOB(全周转)exp_t 单项 12 连胜(4.56,对 AWRA +38.8%)但 heavy_tier 3.99/energy 20715/dense_240 fail 9.7 件全线崩;AWRA 均衡冠军(heavy_tier 0.52 近全底层/energy 14698 最低/高压唯一 fail=0);CB 中庸(exp_t 6.20 但 heavy_tier 5.61 最差);四个 n=120 场景 TOB exp_t 位级相同=频次序列相同+承重不 binding 的必然(非 bug,反证 TOB 与重量解耦)。术语正名表落 strategy_lib.TERMS(RB/COL/CB/TOB/COI 等,HSG 1976+Reyes 2019);seq 正名待拍板(暂标"工程基线/SAP Next Empty Bin 对应")。
- **T1.3 LAP 精确最优锚(冒烟)**:score 口径 gap 0.00-0.20%(AWRA 距精确最优;文献参照 ALNS vs Gurobi 0.32%)!exp_t 口径 gap 27-48%=多目标为安全/能耗付的效率价(与 TOB 现象同源,报告合并叙述);30 seeds 全量待跑(排队,lifecycle 先)。
- **T1.4 BW 锚(bw_anchor.py 已出数)**:纯离散化误差 20×20 单程 **0.60%**/E(TB) 5.39%,规格扫描 10→80 clean O(1/n) 收敛(1.28→0.14%)=文献定性"差异小"的量化+收敛率,自产贡献点;sum 预占几何效应仅 +0.16pp;**加减速 +10.5%(单程)/+15.3%(TB),正落 Oser-Drobir 6-16% 带内**=独立建模互证。
- **T1.4 δ 生命周期(冒烟负苗头)**:10% 汰换率下 δ=0.15 vs 0 全指标 ns 且方向偏负——机制:循环访问口径"未来新热货"流入太少,δ 预留没有对象。已参数化汰换率轴(10%/33%/50%)30 seeds 跑全量:若高汰换率下 δ 转正→"δ 收益=新货流入率的函数"成为检测器又一规则;若仍负→如实报负结果+δ 定位改为"高流入场景选项"(canonical v3.1 记)。负结果也是结果。
- **δ 全量定论(30 seeds,三档汰换×两档填充)**:45% 填充下 δ=0.15 **显著净亏**(响应 +0.93~1.92s***、总时 +376~650s***,汰换率越高越亏——近位不紧缺,预留无对象,只剩推远冷货的日常代价);90% 填充下响应 ns 持平但**回库失败 -26%(128 vs 172)**——δ 兑现在"放得进"维度。**重定位:δ=高压可行性保险,非效率工具;检测器规则=density 高才开 δ**。主动划定自家创新点适用边界=审稿人思维,报告专节。
- **词典序标定表 v1**(fail=0→exp_t+CI 并列→heavy_tier 低):dense→cb(tob/near/score 被 fail 否决;cb 比 awra 快且 fail=0,但 heavy_tier 6.2 vs awra 1.5);低密度→awra(与 tob 并列时安全键胜出);常规→tob(效率主导,heavy_tier ~4 为代价)。**检测器的目标口径本身是输入**:纯效率目标→TOB 多数;题面全要素综合→AWRA 多数——select_strategy 需 objective 双模式(收口实现)。
- 待拍板(挂机保守项):①seq 的学术正名(现标工程基线)②默认口径 δ=0.15 是否条件化(倾向保留+报告披露适用条件)③CB 分界 70/90 未调参④**dense 场景检测器推荐 cb(快)还是 awra(重货低 4 倍+能耗低)——涉及主打算法叙事,需用户拍板**⑤检测器 objective 双模式的默认档。
  (0707 更新:待拍板①-⑤已由用户拍板并落地,commit 288de79。)

## 0707 块2 T2.1 开工 brainstorm(用户令:每 task 先风暴质量保证手段)

主题:AWRA-LS"全移植"的全面性/工程质量/统一标准怎么保证。侦察结论先行:plc 现状远好于
总纲预期——分片状态机/重定位+交换双邻域/23 用例/T19-T20 一致性向量护栏都已在,T2.1 实际
=**一致性审计与补差**,不是新写。十方向(便宜→激进)与裁决:

1. 【删问题】"全移植"重定义为"全流程一致性证明":diff-driven 逐段对照 sim run_awra_ls
   vs FB_*,产出 gap 表只补 gap 不重写。→ 采纳,施工步 a
2. 【复用】export_st_vectors.py 从函数级向量(T19/T20)升级**全流程终局向量**:sim 跑
   AWRA 全程导出终局布局+swaps/relocs/轮数,ST 侧新增 T25 逐位比对=端到端一票否决护栏。
   → 采纳,施工步 c(T2.1 核心交付)
3. 【统一标准】算法常量单源化:lam 权重/max_iter/改进阈值由 export 脚本生成进 07,
   消灭两边手改漂移。→ 采纳,施工步 d
4. 【数值】REAL 单精度平局翻转:构造性平局探针专测 tie-break;有问题再升 LREAL(README
   预留)。→ 采纳,并入 T25 容差设计
5. 【跨领域】DO-178C 式三向追溯表(sim 行号↔ST 段落↔用例编号)。→ 采纳,并入 T2.2
6. 【性能实测】AB 仿真 SysTime 打点测分片参数×周期耗时(枪G 空白自补)。→ 采纳,
   出操作卡给用户,数据回填报告
7. 【流程】ab_sync 检查清单固化进《ST编写标准》。→ 采纳,并入 T2.2
8. 【激进】Python 写 ST 语义解释器全自动比对。→ 否决(YAGNI:向量法已覆盖,解析成本
   远超收益)
9. 【全面性】移植完备性清单机械生成(run_awra_ls 调用链全函数/常量/分支逐项对勾 ST)。
   → 采纳,施工步 a 的工具
10.【报告增值】nBatchPerCycle 画面现场可调,评委亲见算力-时延权衡。→ 转块3 T3.2 待办

## 0707 块2 T2.2 开工 brainstorm(功能块重组+ST编写标准)

侦察:plc 组织已专业——PRG_Main 七态状态机(INIT→WAIT_INPUT[交互添加/载入/启动]→
ASSIGN→IMPROVE→STATS→DONE)、五字段输入已在(GVL_Visu.InGoodId/Name/Weight/Freq/
Volume)、FB_Stats(C1-C8齐)、DUT完整。重组≠重写,=评分细则显式化+统一标准+护栏不破。

十方向裁决(便宜→激进):
1. 【删问题】只加"评分细则→POU映射表"文档不动代码。→ 部分采纳(追溯矩阵),缺口仍要补
2. 【复用】现有块对应多数细则,补缺口:货物输入未封装FB、路径规划无门面。→ 采纳
3. 【标准驱动】先写《ST编写标准》,再按标准补头部注释块。→ 采纳,施工步1
4. 【追溯】评分细则九条×POU×测试 三向矩阵。→ 采纳,施工步2(评委导航+自查)
5. 【门面模式】FC_PathReport/FB_GoodsInput 作细则门面,内部调现有FC,不动底层护栏。→ 采纳
6. 【分层施工】纯文档/注释(零风险先做)vs 结构改动(抽FB需回归)。→ 采纳,风险分层
7. 【全面性】命名前缀一致性机械审计(b/i/r/s/a/st,枪H:沿用Beckhoff系)。→ 采纳,施工步3
8. 【激进】按细则重排文件结构(独立.st)。→ 否决(破坏README导入流程,YAGNI)
9. 【护栏】重组后 T19/T20/T25 全绿+consistency_probe 复跑。→ 采纳,收口硬门
10.【报告】《ST编写标准》本身=交付物(评分"变量定义完整/结构化设计")。→ 采纳

施工:①《ST编写标准》②评分细则追溯矩阵 ③头部注释块规范化(注释级低风险)④命名前缀审计
⑤门面块。收口 T19/T20/T25 复验。
待拍板:FB_GoodsInput 抽取是否动 PRG_Main 主状态机(现 work、抽取纯为评分可见性、无自动
回归→YAGNI张力);FC_PathReport 聚合低风险可先做。挂机保守=①②③④先做,⑤记待拍板。

## 0707 块2 AB 实跑验证大轮(ScriptEngine 管线 + FB_GoodsInput + 自查)——compact 落盘

### 重大纠错(教训)
我一开始错判"AB .project 二进制改不了、必须用户手动粘贴 06/07"。**错**——用户点醒:项目 0706 就建了 `tools/ab_scripting/` ScriptEngine 无头同步管线(我没侦察到 ab_scripting/ 就下结论)。手改 .project 二进制确实不行(这点对,魔数 2389ed33/熵8.0),但 ScriptEngine 让 AB 自己导入 .st 是正道,早建好了。举一反三:下结论前先 grep/侦察工具目录,别凭第一印象。

### 管线用法(接管必读)
- 命令:`AutomationBuilder.exe --profile="Automation Builder 2.9" --noUI --runscript="x.py"`(--profile 必填,缺了静默退出)。
- ab_sync.ps1 封装 sync_st.py(同步36对象+编译+save)+run_test.py(在线跑PRG_Test读iPassed)。**判绿硬编码 iPassed=INT#41**(改用例数要同步改ps1)。
- 我实操:直接 `Start-Process -FilePath $exe -ArgumentList @('--profile="..."','--noUI','--runscript="...sync_st.py"') -Wait`(绕过ps1),读结果文件 sync_result.txt / runtest_result.txt 自己判断(编译看"Compile complete -- 0 errors"、测试看"iPassed = INT#41")。**注意**:PS5.1 sandbox 拦 Remove-Item(误判$exe的"C:\Program"),别在命令里预删结果文件(脚本 open"w" 会覆盖)。

### 块2 AB 实跑结果:iPassed=41 / iFailed=0 全绿,编译 0 errors(1 既有 warning)
T19/T20/T22 一致性向量 + T23 双命令 + T24 预占133 + T25 AWRA端到端(iAwraPosDiff=0) + T26-T40 边界 + T41 FB_GoodsInput 全部通过。

### 过程抓到并修的问题(AB 验证的价值,都已 commit)
1. **T33 断言 bug(我的离线错误)**:测除零保护,我断言 score>=0——但保护后 rFMax→1、F=5>1 使 fNorm=5、resv=(1-5)负、score≈-0.53<0。保护正确(没崩),是我错误假设 score 非负。改为验有限值 (rV>-1E6 AND rV<1E6)。AB 第一次跑 iPassed=39/T33红 当场暴露。→ **这是"离线自审可靠度有限,ST 必须 AB 实跑"的铁证**。
2. **ST_AwraCell 误建为 FB**:sync_st.py create_missing 缺 dut 分支(首建时5个DUT都已在工程没触发过创建),块2新增的 ST_AwraCell DUT 落到 pou 分支默认建成 FUNCTION_BLOCK。功能 work(编译0错/T25绿,CODESYS 宽容按TYPE解析)但类型不纯。fix_awracell.py 删FB重建真DUT(create_dut+remove 动态API:dir()不列但 hasattr 可调,CODESYS ScriptEngine 动态接口特性)。已补 create_missing dut 分支(未来正确)。
3. **管线3缺陷全修**:①sync_st.py MAP 硬编码漏块2新增对象(补 ST_AwraCell + FB_GoodsInput,现36对象)②create_missing 缺 dut 分支③ab_sync.ps1 UTF-8无BOM被PS5.1当ANSI读→中文乱码解析失败(全文 ASCII 化,并注释警示"keep ASCII-only")。

### 1 warning(既有,未查明)
编译总结"0 errors, 1 warnings"。ST_AwraCell 纯化前后都在 → **非块2引入(既有)**。ScriptEngine `system.get_messages` 只返回总结行,查不到具体 warning 内容(probe_warn.py 实测)。需 **AB GUI 消息窗口** 看。0 影响功能(41/41绿)。**待用户 AB GUI 查明**。

### FB_GoodsInput 抽取(用户拍板"抽取,现在管线能验证了")
货物输入从 PRG_Main CmdAddGood 分支封装成独立 FB(输入 xAdd上升沿/iId/sName/rWeight/rFreq/rVolume → 输出 xOk/xFound/iSugX/iSugY/iErrId[0ok/1满仓/2负输入/3无可行位])。边界校验+写表+内部 fbSel 即时推荐;可视化(推荐高亮/路径/状态文本/报警)留 PRG_Main 编排=分层(FB算,PRG画)。PRG_Main 删 fbSelOne/rWMaxTmp/rFMaxTmp/xAddOld/i(移入FB或不再需要)。T41 测三分支(正常xOk&xFound&count=1 / 负输入iErrId=2不入表 / 超重150kg入表但iErrId=3无位)。iPassed 40→41。**改了演示核心 PRG_Main 但管线验证零破坏**。

### 自查块1-2
sim 回归 test_sim 21绿 + consistency_probe 演示批 diff=0;修5处文档滞后(FB_GoodsInput已抽取:ST标准追溯矩阵细则7/缺口段/修改记录 + 总纲T2.2/收口 iPassed=41)。

### ⭐ 待办 / 独立审查结果回来后的预案(接管核心)
- **独立 claude 审查中**:docs/块2独立验证任务书_0707.md(6维度批判性复核:①sim↔ST一致性论证 ②边界T26-T41假绿 ③FB_GoodsInput行为等价 ④管线iPassed=41复现 ⑤追溯矩阵挂靠 ⑥溯源引用真实性)已交用户,用户让本地另一 claude 审查,**结果待回**。用户明确:等审查完再继续对话。
- **审查结果回来后怎么接**:①若报真问题(边界假绿/FB不等价/追溯挂靠/一致性论证漏洞)→ 逐条核实(读代码/跑脚本/手算,别盲信也别盲改)→ 确认属实才改 → **改后必 ab_sync 重跑验 iPassed 不掉**(改 ST 必 AB 实跑,T33 教训)。②若审查提到 1 warning 查明 → 据内容定修不修。③无实质问题 → 开块3。
- **块3 待开工**:画面与联动,含 T3.3 批次C=用户亲手搭;开工前按常设规则 deep-research≥6点(候选:CODESYS Visualization 控件/WebVisu 发布/VisuDialogs.Numpad/Alarm Banner-Table/ScriptEngine 读PLC变量驱动3D实时联动/OPC UA;全 sonnet+medium,禁 fable)+brainstorm。
- **1 warning**:待用户 AB GUI 查明具体内容后告诉我。

## 0707 块2 独立审查处置(另一 claude 复核报告回收)

输入=docs/块2独立验证报告_0707.md(本地另一 claude 独立复现,区分【实测】/【推测】)。核心结论:
块2 主体扎实,iPassed=41/0 **独立复现为真**,**无算法缺陷、无假绿测试**;提 6 条发现,全部
文档/措辞级。我方逐条核实(读码+跑脚本+手算,不盲信不盲改)后全部属实,处置如下:

- F1【假绿·中·必改】gap 表"聚合 exp_t/energy 差异=0.00 完全相等"被证伪。我自跑
  consistency_probe.py 复现:exp_t 上界 1.09e-2 s、energy 相对 7.18e-4(非零),脚本第 11 行
  亲口打印此值——是人写总结抄成 0.00,与依据的脚本输出直接打架。→ 改 gap 表两处为实测值+
  "聚合近似等价(≤1.1e-2 s 可忽略)+演示批/T25 逐位零差异",诚实注明探针只测 exp_t/energy
  两量、其余外推。大结论"实质等价"不倒(差在 dense_240 90%填充极限,非报告头条 L120_skew)。
- F2【隐患·中低】gap 表"reloc tie-break 已一致"不准:ST reloc(04_FB:298)`<rBest-1e-6`
  逐步阈值 vs Python(warehouse_sim:355)全局精确 min,多空位 score 差<1e-6 时选位可能不同;
  且探针 tie_break 参数只切 assign(assign_key),不模拟 reloc 阈值选位=盲区。→ 改 gap 表 :15
  措辞为"机制略异+单精度噪声+探针不覆盖此通道,T25 兜底";探针头注释加盲区记账。不扩探针
  (实害<1e-6,T25 AB 实跑是真端到端兜底已绿,YAGNI)。
- F3【文档·低】"12 场景"实为 13(SCENARIOS=3legacy+4质疑+3压力+2漂移+1illegal_mix)。
  → 探针 :14/:94 改 len(SCENARIOS) 动态;gap 表 :27 改 13。重跑验证新版打印"13 场景"生效、
  数字不变(1.09e-2)。
- F4【夸大·低】追溯矩阵"最优路径规划"名不副实(FB_BuildVisuPath 先水平后垂直平铺无搜索;
  单巷道切比雪夫走线唯一)。→ 保留官方细则原文,ST标准追溯矩阵加"路径规划口径诚实说明":
  本题路径规划=路径生成+行程计算,真正被优化的是库位分配层(AWRA-LS)。
- F5【残留·低】ST标准 §5 "iPassed=25" 过时;连带自查发现同段 step3"AB 里替换粘贴 07"也
  过时(现 ab_sync 管线自动同步)。→ 两处一并改为管线口径+iPassed=41。
- F6【语义·低·不改码】T34 除零保护后 priority=6.075 溢出[0,1]归一语义。仅退化输入
  (FMax=WMax=VMax=0)触发,实际算法分母来自 max(属性)>0 不可达,无实害。审查明说"可选/留注"。
  → 决定不碰 .st(避免为无害注释触发 ab_sync 重验),仅此处记录处置结论。

关键判断:6 条修正**零 .st 逻辑改动**(全落文档 md + sim 探针展示/计数层),iPassed 由 .st
决定、.st 未动 → iPassed=41 不受影响。按预案"改 ST 才需 ab_sync",本轮无 ST 改动故不跑管线
(非偷懒,是改动物理上不进 AB)。已重跑 consistency_probe.py 两次确认 sim 侧数字复现+F3 生效,
py_compile 验语法通过。

安全事项:git status 见 9 个用户 .docx(1_给你看/、2_你要操作/)显示 M——非本轮所致,来源
不明(用户自编辑?进程 touch?)。已隔离:commit 只精确 add 我改的 3 文件+审查报告+本日志,
绝不 git add -A、绝不 git checkout 还原 docx(可能抹用户未提交编辑)。plc/AB_Project/
ABB_WH.project(二进制 M)是审查跑 ab_sync 的副作用(反映当前 .st 同步态),不纳入本轮 commit,
保留/还原由用户定。均待用户定夺。

1 warning:审查 §5-1 查明 msg_result.txt 里 'TO' expected instead of 'r' 是历史已修 error
(现 rw,非当前 warning);当前 1 warning ScriptEngine API 只报数量,须 AB GUI 消息窗口看,
【推测】大概率无害(未用变量/隐式转换类)。仍待用户在 GUI 确认。

## 0707 对标升级 deep-research(块3前战略扫描)

用户 /ask 收敛 Q1 选"先独立对标扫描"→启动 deep-research(全sonnet合规,95agent/547万token/23min,
48抽取→25验证→20confirmed/5refuted)。综合落 docs/调研档案_0706/对标升级_deep研_0707.md。

三主线:①算法版图我方双腿覆盖精确(LAP)+启发式(AWRA-LS),缺元启发式(GA/SA/TS/ACO/PSO)与ML/RL
②精确法400库位实时性铁证(MINLP 181s@30库位/BCP≤50库位2小时不闭合)证明我方LAP锚取舍正确
③"没有普适最优算法"(SL-LDA/slot-selection一致)=场景检测器三档的学术背书。

Top升级点(性价比序):①驻留点dwell-point(低成本值得抄)②关联存储correlated storage(中成本,
实时性需自验,赛题无订单序列则不适用)③能耗维度/worker safety叙事(加码)④RL/DRL(仅展望不实装,
400库位PLC实时性存疑+SOTA单案例无方差)。护城河:场景检测器三档/LAP锚/30-seed统计严谨(SOTA普遍
单案例无CI)/双实现+载重拟真。

重大盲区:竞赛获奖方案(西门子杯/挑战杯/ABB杯)+工业可视化(AutoStore/OptiSlot数字孪生)两类靶向
落空(中文竞赛一手资料稀缺在答辩视频/商业机密)。PLC实时性工程高招留块3开工时单独查(CODESYS/AC500社区)。

配置遗留:改deep-research.js让Synthesize也锁sonnet(死命令"含总结"),但meta.description旧文字
"only Synthesize may inherit main-loop"未同步+桌面面板未同步(待批处理)。

下一步待用户拍:补盲区检索 vs 直接开块3(我荐后者:学术侧已给足护城河+可落地升级点,竞赛/工业一手
资料稀缺再搜性价比低,PLC实时性留块3聚焦)。

## 0708 驻留点实装 + 盲区补检(两线并行)

**驻留点(A10)**:设计→实装→定论一天闭环。实装三个关键决策:①驻留点只改时间记账不改
物流事件→一遍事件流五策略(io/last/center/demand/cog)并行记账,CRN 完美配对省 5 倍计算
②记账拆两本:响应(作业段)vs deadhead(空闲空驶),io 的"回库空返"=其 deadhead,与旧
_t_store 口径对上 ③demand 不用重心近似,400 格点全枚举精确解(延续 LAP 锚哲学),cog(O(n)
重心)只作 ST 蒸馏候选由数据决定。冒烟 3 seeds 抓到 2 bug(ci95 解包/填充率口径)+1 信号
(demand 退化 (0,0)),修后全量 30 seeds 定论:
- **官方 B2 口径=可证明的最优**(枚举 30/30 seeds 恒 (0,0),两填充档)——"现状即最优"的
  精确证明,比"改进 X%"更硬的报告素材;
- **last 意外发现**:就地停靠免费 -0.83/-1.91s***+零空驶,机理=回库→取货交替下的双命令
  (dual-command)退化效应(挂 C10 Bozer-White 概念)——不在固定点枚举空间,与枚举结论不
  矛盾;违 B2 只作展望;
- 纯出库(p_store=0)驻留点才有肉(出库 -12%),cog≈demand(gap 0.4-0.7s);
- **ST 判 YAGNI 零改动**(官方口径已实现且最优;last 语义=什么都不做),iPassed=41 不动,
  省半天 ST 工+ab_sync 重验——有据的省,非偷懒。
- 回归 test_sim 21/21 绿;CSV 600 行落 out/dwell_point.csv。
- 模式复盘:又一例"质疑→精确验证→证明现状自洽"(与 F23 装卸探针同门)。

**盲区补检(两支 sonnet 定向枪,各 15 调用)**:综合=docs/调研档案_0706/盲区补检_竞赛与工业
可视化_0708.md。竞赛枪:一手获奖全文=零(CSDN/知乎/期刊反爬五发五拦,负结果如实),3 个
学术近亲(自适应 GA/多种群 GA/单堆垛机混合调度)不动摇我方取舍;**2 个人工待办链接留用户**
(西门子杯官网赛项资料+知乎往届案例汇总);"ABB杯智储优控"公开无痕迹→往届资料直接问官方
工程师。工业枪丰收:**Swisslog SynQ 左3D右详情分屏(一手界面描述)**+Mecalux 组件清单(含
treemap)+热力图"健康=热点向巷道口收拢"判读话术(D 档对比的天然论点)+**CODESYS WebVisu
能力对照表**(Trend 一手核实/热力着色参数化子可视化/Frame 分屏/3D=Path3D 线索但 Depictor
WebVisu 兼容未验证→保底 2.5D 伪 3D,开工半天最小验证定真伪)。判定:竞赛检索到顶,工业
可视化够用,块3 外部输入已齐。

## 0708 块3 开工:前置三件套 + ST 侧一把全绿

前置合规:侦察(GVL_Visu 已有 30+ 变量=增补不重建;发现 ST 侧缺 U2 载重运动学=拟真调参区
的真工作量;六步演示脚本已存在于存量操作卡=demo-driven 的锚)→ deep-research 8 点(0708
工业枪 4 点+本轮两支定向枪 4 点,块3施工依据_0708.md;关键澄清"integrated visualization=
IDE 预览≠WebVisu"警示解除;确认 AC500 V3=原生 CODESYS 3.5;T3.4 通道推荐 Modbus TCP,
ScriptEngine read_values 仅过渡)→ brainstorm 10 方向,用户拍板:1(删3D)不采纳,2-10 全
采用,基调"不缺时间,尽全力工程质量"。

设计双件套:画面编写标准_0708.md(命名前缀族/颜色单源+状态6查询青/Swisslog 分屏布局/
HUD 反馈原则/WebVisu 约束/徽章措辞)+块3设计_0708.md(画面追溯矩阵 细则→元素→变量→验收/
演示脚本 v2/ST 增补清单/风险预案)。

ST 侧施工(T3.1/T3.2,零回归设计=新增不改现有签名):02_GVL(U2 调参 6 参+查询/记录页/
快照/名称下拉变量组)、03(StateToColor+6;FC_TravelLaden 垂直×0.80 accel 同构;FC_HandleTime
三档全参数化)、04(FB_VisuRefresh 快照对比分支+跳过路径叠加;FB_QueryGood 查询+青高亮+
拟真对照;FB_RecordPage 10 行窗口+翻页钳位=拒 400 行整表)、05(常驻接线:查询/翻页/快照
整赋/名称消费即复位)、06(N_CASES 45:T42 查询四分支 laden(0,0→2,3)=7.5s 手算锚/T43 钳位链
1→11→16→6/T44 三档边界/T45 折减四断言;测试口径自给显式置 xUseAccel/xSimXY)。
管线:sync MAP+4 对象(40 objects)、ab_sync 判绿 45。

**AB 实跑一把全绿:synced=40 fail=0、编译 0 错(1 既有 warning)、iPassed=45/iFailed=0**
——对比块2 T33 首跑即红,手算锚+口径自给习惯生效。附带:管线输出确认 online API 有
read_values(批量读)=T3.4 过渡通道本地证据。

交付:2_你要操作/AB可视化搭建操作卡_块3增补.md(不动存量卡=新副本;纠正旧卡 OnMouseDown
→OnMouseClick;第0步改 ab_sync 一条命令;P1 升级/P2 新页/Banner 配置链/WebVisu/V1-V5/
演示脚本 v2/验收清单/坑表)。

球在用户:照增补卡施工画面(2-3h)+V1-V5 验证(半天)→反馈 V4(WebVisu 许可)/V5(内核版本)
/V2(Path3D 真伪)。我侧下一活:T3.4 visu_poll.py(ScriptEngine read_values 轮询→3D 实时
查看器)可与用户施工并行;Modbus TCP 长期方案等 V4 结果定。
