# 路线A:P1 全部 21 处动作 GUI 洗单(0711)

> 目的:验证攻坚假说「脚本建的动作存在运行时注册缺口,GUI 重存后免疫」(P2 七框洗后再未死过)。
> 实数勘误:audit_pages 实测 P1 动作元素 **21 处**(攻坚文档笔误 22:按钮实为 12 不是 13)。
> 洗法与你洗 P2 七框完全相同:**单击选中 → Properties 面板 Input configuration → 打开配置对话框 → 什么都不改 → OK**。
> ⚠️ 单击选中,**不要双击**(双击进文字编辑态;误入按 Esc 退出)。

## 洗前状态(已就绪,无需你操作)
- 05 疫苗 + 02 常量已 sync,编译 0 错;P2 版1 施工+审计三小修已完成;完整 ab_sync 45/0 收口(等我回报)。

## 清单(建议按组顺序,洗一个勾一个)

### 组1 COMMANDS(右上 8 按钮,2列×4行)——对话框里各有 2 行事件(P2 Records 除外)
- [ ] Add goods(左列行1)——2 行:OnMouseDown / OnMouseUp(Execute ST-Code)
- [ ] Load demo(右列行1)——2 行
- [ ] Reset(左列行2)——2 行
- [ ] P2 Records(右列行2)——**1 行:OnMouseClick(切换画面)**
- [ ] Run assign(左列行3)——2 行
- [ ] Animate(右列行3)——2 行
- [ ] Save snapshot(左列行4)——2 行
- [ ] Live/Snapshot(右列行4)——2 行

### 组2 GOODS INPUT(左上 5 个值框)——点标签**右侧的值框**,不是标签文字
- [ ] ID 值框(显示 %d)——1 行:OnMouseClick(Write variable)
- [ ] Name 值框(显示 %s)——1 行
- [ ] Weight 值框(%.1f)——1 行
- [ ] Freq 值框(%.1f)——1 行
- [ ] Volume 值框(%.2f)——1 行

### 组3 STRATEGY(中带 4 按钮)——各 2 行事件
- [ ] FIFO
- [ ] Nearest
- [ ] Score
- [ ] AWRA-LS

### 组4 SCORE WEIGHTS(底带 4 个小值框)——点标签右侧 %.2f 小框,各 1 行
- [ ] Alpha eff. 值框
- [ ] Beta stab. 值框
- [ ] Gamma en. 值框
- [ ] Delta res. 值框

**注意**:若洗的过程中发现清单外还有 Input configuration 非空的元素(比如某个灯/图形被误配过动作),同法洗掉并回报——清单以实物为准。

## 洗完之后(顺序执行)
1. Ctrl+S 保存工程;
2. F11 编译(应 0 errors)→ Online → Login with download;
3. **长跑验证 10 分钟**(节奏见下),验证目标 = 全程无一次输入失灵:
   - 第 0 分钟:Load demo(格子亮=健康基线);
   - 每 1-2 分钟一轮:填 ID→Add goods → Run assign → Animate → 换一个策略按钮 → Save snapshot → Live/Snapshot 来回 → P2 Records 切页 → P2 上 Query 一次 → Back to P1;
   - 中途穿插**快速连点**(报发作那两轮前你都在密集操作,这是压力项);
4. **若再次发作,别重启!先采证四步**(攻坚文档 §4.1):
   ① P2 还能点吗(切过去试 Query)——区分全局死/单画面死;
   ② 设备树 PLC_AC500_V3 → Log 标签,截图最近的错误行;
   ③ 编辑器切到另一画面再切回,复活吗;
   ④ 记下失效前最后一次成功操作是什么。
5. 10 分钟不死 → **定案**:脚本建动作必须 GUI 洗收尾(写入经验文档+visu_gen README),转演示脚本 v3。
