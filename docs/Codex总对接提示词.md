# Codex 总对接提示词(冷启动接手用)

> 用法:下面代码块整段复制给 Codex,作为它接手本项目任何任务的**开场白**;
> 后面再跟上你要它干的具体任务(从 docs/Codex执行单_搭建期.md 复制,或口头描述)。
> 若 Claude 不可用,Codex 只靠这段提示词+仓库内文档即可完整接管。

```
你现在接手「2026 ABB杯赛道一·智储优控·立体仓储仓位优化」项目,角色是执行工程师。
项目根:F:\abb_wh_work(已 git 管理,做完活要 commit,提交信息用中文写清楚改了什么口径/文件)。

【接手三步,不做完不许动代码】
1. 读 现状与任务.md —— §5 是当前状态与下一步,§3 是排期,§6 是已定决策(不许翻案);
2. 读 docs\canonical_assumptions.md —— 一切建模假设/参数/指标口径的唯一真相源;
3. 跑 python sim\run_all.py —— 全绿(15用例OK+违规0)说明底盘完好,红了先报告不许自修。

【铁律】
- 数字口径只认 canonical:引用/产出任何对比数字必须带口径标签(切比雪夫/曼哈顿、单程/双程、
  seed、规则 and/or/linear、货物分布 case);禁止在文档里硬编码可再生的数字。
- 一切随机必须固定 seed;策略间对比必须同一批货物。
- 改评分函数/场景参数/权重 = 口径变更:必须①用户批准 ②同步 canonical §E ③重跑
  python sim\export_st_vectors.py 重新生成 plc\07(生成文件,永远不许手改)④全量回归。
- 回归测试红了 = 有人动了口径,定位差异报告用户,不许改测试、不许放宽容差。
- plc\*.st 与 sim\warehouse_sim.py 是同一算法的两个实现,改一边必须同步另一边并跑一致性向量。
- 网络安装统一用阿里云源:pip install X -i https://mirrors.aliyun.com/pypi/simple/
- 每完成一个任务:更新 现状与任务.md §5(整体重写不打补丁)+ git commit。
- 遇题面歧义/需要用户拍板的事:停下来列清楚选项等用户,不许猜一个当已定。

【目录地图】
现状与任务.md(单一真相源/resume点) docs\(口径/发现/大纲/执行单/差异化战略/资料包指引)
sim\(仿真+测试+实验+图表,run_all.py 一键) plc\(ST 8文件,README_PLC.md 有导入步骤)
_ref\(Codex 研究资料包:文献/ST骨架/可视化方案,写报告和可视化前先查它)

【当前阶段目标】截止 8/26,团队目标 8/15 初赛+决赛全套提交;
当前在差异化提升期,六杠杆 L1-L6 见 docs\差异化战略_如何超过别人.md §3,
本周该干什么看 现状与任务.md §3 排期表和 §5 下一步清单。
```

## 附:用户(不懂技术)自己也能做的兜底操作

- 每周五对照周报里的「验收清单」逐条核对(每条都是肉眼可查的硬标准);
- 手动备份:复制整个 F:\abb_wh_work 文件夹到 C 盘/U盘(git 仓库含全部历史);
- 恢复:任何机器装 git + Python3.8+,克隆/拷贝仓库后跑 `python sim\run_all.py`,
  全绿即环境重建成功——报告数字、图表、ST 数据文件全部可从零再生。
