# S3 fill 三算法深审清单与治理方案(0716 深夜,用户全面批准)

- 触发:用户 T1 验收时质疑"算法像按顺序从左往右从下到上放货"。
- 审查:Claude 自查(3 项)+ opus-high 独立红队 agent 全量审查(9 项清单,23 次工具调用)。
- 复现脚本:`l4_showcase/tools/fill_audit_0716/audit.py|audit2.py|audit3.py`(运行:项目根下 `PYTHONIOENCODING=utf-8 python <脚本>`;audit=平局/四项/KPI/目录,audit2=等时区/落位/哈希/边界,audit3=指标重算/makespan根因/运动学分叉)。
- **用户拍板(0716 深夜):A/B/C1/C2/D 全面批准、彻底改动,以"评委 30 秒肉眼看出算法分化、零失误"为验收纲。**

## 〇、底线(负结果,先立信)

数据**干净无造假**:bundle/event 哈希链逐位复算吻合;expected_retrieval 三 lane 从落位独立重算逐位相等;容量契约 133/267 准确;Python↔ST 评分公式结构与权重(1.0/0.6/0.4/0.15)逐行一致;共线项三处诚实披露;AWRA-LS 未冒充在线;HTML 无虚标 headline。

## 一、P0(答辩现场可击穿)

### P0-1 旗舰 SCORE 在头号指标上输给 SEQ 基线
| 指标(越低越好) | SEQ | NEAR | SCORE | SCORE vs SEQ |
|---|---|---|---|---|
| **expected_retrieval_s(页面第一块)** | **20.039** | 22.046 | 21.478 | **+7.18% 更差** |
| hot20_retrieval_s(热门取货) | 21.418 | 20.899 | **20.119** | -6.07% 更好 |
| heavy20_mean_tier(重货均层) | 9.717 | 9.774 | **9.321** | -4.08% 更好 |
| lift_work_proxy_kgm(能耗代理) | 80747 | 79961 | **77112** | -4.50% 更好 |

页面默认 AUTO→SCORE 且期望取货排第一块——评委切下拉即见"智能被顺序击败"。SCORE 真赢的三项全排后面。

> **✅ 0717 C1 跑批翻案**:30 seed(2026–2055,case=skew)稳健性跑批显示上表是 seed=2026 的**单样本抽签噪声,不是结构性劣势**——期望取货均值 SCORE 20.565 < NEAR 21.264 < SEQ 21.703(SCORE 最优,配对 score<seq 22/30、score<near 30/30);热门取货/重货均层/能耗代理三项 SCORE 同样均值最优(配对胜 23–28/30)。**SCORE 四项全部均值领先**;跨 seed 标准差 ±1.5 s 远大于三算法差距,单 seed 横比本就在噪声内。守恒量(makespan/路程逐位相等)与零违规在 30/30 seed 全部复现。证据:`l4_showcase/evidence/s3_fill_seed_sweep_skew_2026_2055.json`(生成:`python sim/fill_seed_sweep.py --seeds 30`,评分定义零改动)。页面期望取货 title 已改双口径如实表述;本文件 A 包中"满仓先到先得,均值 SEQ 占优"的机理句**在均值意义上不成立,以本节为准**(单 seed=2026 展示样本中 SEQ 恰占优的事实不变,角标 ▲7.2% 如实保留)。

### P0-2 makespan 与总路程三 lane 逐字节相同
makespan=16626.201968194s、round_trip_path=10160.0m 完全一致。根因:填满口径三算法终态占**同一 267 格集合**,总量指标只依赖格集合+同批货,数学守恒。不预讲会被质疑"只跑了一个算法";预讲则是"我们懂守恒"的加分点。

## 二、P1(显著减分)

1. **平局兜底主导决策**:选中格与他格目标值相等的事件 NEAR 85.8%/SCORE 73.8%(平局组均 5.7-5.9 格,最大 14);Top3 行程完全相同 SEQ 6%/NEAR 72.7%/SCORE 66.7%。decisionWrap 把相同数值多行亮给评委,屏上无兜底规则(实际=tier,col 字典序)说明。
2. **四项分化塌缩**:加权占比=稳定 44.5%+能耗 29.7%(共线"载重×层高"合计 74.2%)+保留 19.2%+**效率仅 6.6%**;f_norm 均值 0.0416(freq 中位 1.87 vs f_max=100)。SCORE 在自己的目标函数上仅比 SEQ 好 2.9%。有效自由度≈"压层高+微弱列偏好"。〔注:此前"reservation 主导"的说法量级不准,已修正——它只主导列方向。〕
3. **PLC↔演示运动学口径分叉**:演示 accel=True(t_max 39.667),PLC 默认 xUseAccel=FALSE(FC_TMax 恒匀速 38.0),角格 (19,0) 行程差 42.1%;ST 若开 accel,分子变分母不变 → tNorm(19,19)=1.044>1,保留项可为负。**"演示可在 PLC 复现"现在不能说**。T08 测试向量只盖匀速。
4. **等时区结构性根因**:垂直轴慢 4 倍 → 400 格仅 39 个不同行程值,tier6-19 整层 20 列同行程(70% 格子行程与列无关);SCORE 的"远端列回填"只出现在 tier0-5,tier≥6 转为近端升序。
5. **单 seed=2026**:期望取货排序(seq<score<near)未证明稳健;δ=0.15 代码自认"单 seed 甜点";前端仅小字"单种子"。
6. **"智能路由 AUTO→SCORE"是硬编码常量**(laneForRequest 恒 score),无路由逻辑,名不副实。

## 三、P2(瑕疵)

1. 重量被压 5-60kg,层限重 62kg 永不触发——物理约束架空(已披露但安全叙事无牙齿)。
2. 入库顺序=gid 生成序(seed2026 固定序列);gid 与属性相关性 <0.1 无泄漏,但无"来料随机性"证据。
3. 平局兜底口径三处表述不统一(payload/前端/fill_only 两套键)。

## 四、治理包(已全面批准)

| 组 | 内容 | 改动面 | 状态 |
|---|---|---|---|
| **A 叙事重排** | 指标块重排:hot20/heavy20/能耗提前(SCORE 真赢项),期望取货标机理("满仓先到先得,均值 SEQ 占优");makespan/路程加"守恒量"徽章+机理句 | s3_fill_candidate_runtime.js(renderStats 区) | **✅ 0717 完工**(见下"A+B 完工记录") |
| **B 呈现治理** | Top3 增列平局组人数+兜底键说明("N 格并列,按更低层更小列");等时区一句解说;"智能路由"改"默认策略 SCORE" | 同上(decisionWrap 区)+文案 | **✅ 0717 完工**(同上) |
| **C1 多 seed** | ≥30 seed 期望取货均值±方差,巩固或如实修正排序叙事 | 独立新脚本 `sim/fill_seed_sweep.py`(import 复用 fill_only,原件零改动)+证据文件 | **✅ 0717 完工,P0-1 翻案**(SCORE 四项均值全优,见 P0-1 节勘误) |
| **C2 uniform 情景** | 补 freq 均匀(1~100)演示 lane:效率/保留项真正做功,"30 秒肉眼分化"的正解;与 skew 情景并列供切换 | fill_only.py(case=uniform 已有)重跑数据门+store 重生成+QA 全重验 | **✅ 0717 完工,预期半反转**(评分内效率项做功恢复✓,指标端分化反而小于 skew;uniform 定位改为适用条件对照组——见"C2 完工记录") |
| **D 口径修复** | ①立即:文档披露"演示=加减速 SIM、PLC 默认=匀速"②ST 批次:FC_TMax 随 xUseAccel+加减速测试向量 | ①docs ②plc ST(与 AB 瑕疵 4 条合并一个批次) | ①立即 ②排期 |

配套:mixed 入出页=「高频近格」真正兑现的主秀场(近格周转空出),纳入 T5/mixed 升级线。

### A+B 完工记录(0717,Claude)

改动面=仅 `l4_showcase/src/s3_fill_candidate_runtime.js` + `l4_showcase/tools/capture_s3_layout_a_qa.mjs`(补断言);五个引用页(fill恢复候选/三份版式候选/进度轴候选)HTML 零改动,行为随 runtime 同步生效。落地细节:

1. **renderStats 六格重排**:热门取货→重货均层→能耗代理(SCORE 真赢三项前置)→期望取货(title 注机理"填满口径下先到者占近格…该均值天然偏向顺序放置")→约束违规→**守恒量格**(`16626 s · 10160 m` 淡黄虚框,title 讲三 lane 同格集机理)。非 SEQ lane 每格右上角**相对 SEQ 角标**(▼绿=更优/▲橙=更差,SCORE=▼6.1/▼4.1/▼4.5/▲7.2,与本文件 P0-1 表逐值吻合;▲7.2 如实显示不粉饰)。
2. **decisionWrap 平局披露**:rank1 `tie_size>1` 时插 tieNote——NEAR 型"行程 X s 由 N 个货位并列,按更低层、更小列落位"、SCORE 型"总分 X 由 N 个货位并列,按更短行程、更低层、更小列落位"+等时区一句(垂直 0.5 m/s = 水平 2.0 m/s 的 1/4,行程由层高主导)。数据源=payload top3 自带 tie_size/tie_group(0716 数据门已埋,无需重跑数据)。
3. **假路由改名**:下拉/决策头/选择依据三处"智能路由 AUTO → SCORE"→"默认策略 SCORE"(option value=AUTO 保留,QA/书签兼容)。
4. **顺手修**:setAlgorithm 回写下拉 value(QA 钩子/书签切 lane 后下拉不再错显)。
5. **QA**:layout_a 新增 6 门(govStatsReordered/govConservedBadge/govNoSmartRoutingTerm/govExpectedRetrievalMechanism/govDeltaBadges/govTieDisclosure)→ **36/36 PASS**(30 项基线未跌);fill 恢复候选页 v7 QA 冻结脚本重跑 **20/20 PASS**。人工复核截图:`l4_showcase/out/s3_layout_a_qa_0716/gov_visual_*.png`(NEAR 13 格并列/SEQ 基线无角标/fill 页 3 列布局各一张)。
6. **QA 契约新增**:snapshot().narrativeAudit(statsSmallOrder/conservedCell/smartRoutingTermHits/tieNote 等),供后续 C2 情景切换复用。

### C2 完工记录(0717,Claude)

**管线**(skew 生产链零接触):uniform 数据门=`python l4_showcase/tools/generate_s3_fill_trace.py --outdir l4_showcase/out/s3_fill_data_gate_uniform --seed 2026 --case uniform`(独立目录,release `683e25d125a7057df472`,PASS)→ 独立 registry 登记=`tools/register_s3_fill_scenario_release.py`(新脚本,entry 全字段实算,loader 测试 10/10 凭据,**生产 skew registry 拒改硬闸**)→ 独立重放 validator PASS → store 打包=`tools/build_s3_fill_scenario_store.py`(新脚本,零改动复用 build_payload/render_bundle 及其 validator 闸门)→ `src/s3_fill_store_uniform.js`。out/ 目录沿用 gitignore 策略,registry 哈希+生成命令为复现锚。

**前端**:进度轴候选页尾部 store 选择器(URL `?profile=uniform`,并打 `data-s3-scenario-switch` 标记);runtime 以 payload 自证字段 `shared_input_provenance.profile.case` 判情景(不信 URL),带标记页启用 skew/uniform 双下拉(切换=整页重载,单 payload 队列契约不变),**冻结页(fill恢复候选/三份版式候选)无标记→行为零变化**(QA 有守恒断言)。期望取货 title 按情景分口径。

**QA**:新专项 `tools/capture_s3_fill_uniform_qa.mjs` **9/9 PASS**(uniform release 绑定/自证一致/治理断言组/lane 绑定/**skew 默认不受扰**/**冻结页零变化**);layout_a 全套 **36/36**、v7 冻结脚本 **20/20** 重跑保持。人工截图:`out/s3_fill_uniform_qa_0717/uniform_layoutA_*.png`。

**数据结论(负结果如实)**:深审预期"uniform=30 秒肉眼分化的正解"**被数据推翻一半**——
- ✓ 评分内部:效率项做功恢复(选中格效率项 0.28 级,skew 页仅 0.009 级;P1-2 塌缩机理在 uniform 解除);
- ✗ 指标端:分化反而更小(30 seed 均值 SCORE vs SEQ:期望取货 -1.6% vs skew 的 -5.2%;四项 pairwise 19-23/30 vs skew 的 22-28/30)。机理:freq 趋平后期望取货≈同格集均匀平均(准守恒化),"热门"不突出→近格红利天然小。证据:`l4_showcase/evidence/s3_fill_seed_sweep_uniform_2026_2055.json`。
- **定位改判**:主秀场=skew(C1 已证 SCORE 四项均值全优);uniform=**适用条件对照组**——"分层策略的收益随货流偏斜度增长,两种货流下 SCORE 方向均不输"是比"处处大胜"更可信的答辩姿态。uniform 单 seed=2026 页面恰好四项全绿(期望取货 ▼1.2%),两页对照无任何一页出现"旗舰被基线击穿"画面。

## 五、举一反三(opus 结语)

凡"填满/清空至完工"型对比,总量指标(makespan/总路程/总能耗)天然守恒,能区分策略的只有**取货侧/分布侧**指标——设计对比实验先判定哪些指标对落位顺序敏感,再决定拿哪个当头条。
