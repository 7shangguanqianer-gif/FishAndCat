# 会话断点 · 2026-07-28c(#23 已收口并 push;D 00 导览页三版候选待选;plc 77/0 口径已校正)

> 压缩/新窗唯一恢复入口。上一份:`F:\abb_wh_work\docs\会话断点_0728b.md`(已过时,只作历史)。
> 配套:`F:\abb_wh_work\docs\任务规划_0728_3D抽离与悬浮.md`(含 §4.5–§4.9 五份完成记录与全部实测数字)。

## 1. 一句话战况

#23(01 运动表现力 + 速度口径显眼披露)已完成、七套全绿、已 push;两个积压待拍板已清并 push;
`plc_evidence` 的 75/0 → 77/0 口径校正已完成(**未 commit**);
**当前卡点:D(00 导览页)已出甲/乙/丙三版候选并渲染完成,等用户选版才能施工**。

## 2. 已完成账(本会话新增,证据=commit)

| commit | 内容 | 状态 |
|---|---|---|
| `0370156` | #23 运动表现力 + 速度口径显眼披露:`src/s3_speed_profile.js`(两页共用双泳道速度剖面)+ 场景内加减速三段染色路径 + `#motionModelBadge` 口径标签 | 已 push |
| `00d9e35` | 清两个待拍板:02 热区图例统一改口径 + 两页剖面图加对页速度差异一句 | 已 push |

**未 commit 的改动(工作区内)**:
- `sim/out/plc_evidence.csv` —— 75/0 → 77/0 校正(该文件**未被 git 跟踪**,`sim/out` 在 .gitignore 内;原件已备份到 scratchpad `plc_evidence.csv.bak_0728`)
- `docs/canonical_assumptions.md` —— C11 条目同步校正(**已跟踪,待 commit**)
- `l4_showcase/src/assets/p01.png|p02.png|p03.png` —— 本轮新拍的三页缩略图(新增)
- `l4_showcase/src/_d00_variant_A.html|_B|_C` —— 三版候选(**选定后删除未选中的两版**)

## 3. 拍板与否决记录(用户中文原话/选项逐字)

### 本会话新增
1. **#23 观感方案** →「剖面图 + 场景分段路径(推荐)」
2. **#23 是否同步 02** →「同步,抽进公共模块(推荐)」
3. **下一主线** →「#17 D:00 导览页风格对齐(推荐)」
4. **导览页定位** →「对齐风格 + 重做成作品门面」
5. **两个待拍板** →「02 热区图例文案统一改口径」+「两页剖面图加"对页速度差异"一句」(两项都清)
6. **门面架构** →「合并为一,00 页当壳首页(推荐)」
7. **材料路径** →「先改指向仓库真实路径(推荐)」
8. **过时产物 plc_evidence** →「查清生成脚本并重跑」

### 沿承(仍有效)
- 0728 七问:①抽公共 3D 模块两页共用 ②整格色阶 + Golden Zone 热区 ③速度「两者都要」④悬浮全覆盖 + 首次进页引导 ⑤分三步递进每步全绿 ⑥「1,但是注意用词要学术」⑦先打地基
- 箭头:「1,但是注意排查箭头相关的bug」(两页都保留箭头)
- **否决**:金顶盖 gold-cap 勿复活;黄牌 `#targetBeacon` 永不复现;「从头回放/导出3D帧/总览」按钮已删;**速度不是 bug,不许再「统一渲染层」**

## 4. 数字口径(全部可复现)

### 4.1 当前全绿基线(cwd=`F:\abb_wh_work\l4_showcase`)
```
node tools/test_s3_motion_parity.mjs          → 5/5
node tools/test_s3_runtime_contract.mjs       → 30/30
node tools/capture_s3_layout_a_qa.mjs         → PASS 68/68
node tools/capture_s3_mixed_layout_a_qa.mjs   → PASS 10/10
node tools/capture_s3_fill_uniform_qa.mjs     → PASS 8/8
node tools/capture_fio54_evidence_qa.mjs      → PASS 19/19
cd app_shell && node verify_material_route.mjs → ALL PASS 4/4
```
01QA 演进:64 →(+`motionProfileOnTravel`/`motionProfileHiddenOffTravel`/`kinematicBandsConsistent`/`motionModelBadgeVisible`)→ **68**;
02QA 演进:8 →(+`speedProfilePerLeg`/`speedProfileAxisCalibre`)→ **10**。
既有 `speedCanvasPresent` 随 `#speed` 一并作废,改为 `speedProfileCardPresent`。

### 4.2 #23 运动学实测(工装 `scratchpad/bandsweep.mjs` / `p02bands.mjs`)
- 01 跨 8 事件(ev 4/18/42/89/141/191/234/261)全部 `bands=["accel","cruise","decel"]` + `rings=["cruiseStart","cruiseEnd"]`
- 主导轴切换与「等时区现象」吻合:ev4/18(tier 0/1)X 主导;ev42 起(tier≥2)**全部 Z 主导**
- 验算:X d=19 → `ramp=2/0.5=4 s`、`total=19/2+4=13.5 s`、`cruiseEnd=13.5−4=9.5 s`;三角形判定 d=2 → `2√(2/0.5)=4 s`、d=4 → `2√(4/0.5)=5.657 s`
- 02 逐腿:INBOUND_TRAVEL / LINK_TRAVEL / OUTBOUND_TRAVEL 显示剖面;LOAD_IN / STORE_HANDLE / RETRIEVE_HANDLE / SCAN_EXIT 收起
- 02 轴口径实测 `[["x",2,0.5],["z",0.5,0.3]]` —— **Z=0.5 不含 01 的满载折减**(01 载货腿为 0.4)
- 02 退化保护:cycle 5/9 主导轴三角形 → 如实 `single`;cycle 2/14 梯形 → 三段。Z d=1 → `1/0.5+0.5/0.3=3.667 s`
- 双视口(1552×900 / 1945×1094)× 双页 4 次加载 `pageerror` **0**

### 4.3 plc 在线自测口径校正(本会话新查证)
**演进链(全部有存档日志)**:
| 轮次 | 结果 | 日志 |
|---|---|---|
| 0712 第四轮 | 75 / 0 | `tools/ab_scripting/logs/{sync,runtest}_result_20260712_111501.txt` |
| 0716 | **76** / 0 | `runtest_result_20260716_135644.txt`(`iPassed = INT#76`) |
| 0717 | **77** / 0 | `runtest_result_20260717_010514.txt`(`iPassed=INT#77 / iFailed=INT#0 / xAllPass=TRUE`)+ `sync_result_20260717_010514.txt`(`Compile complete -- 0 errors, 1 warnings` / `Additional code checks complete -- 0 errors`) |

- 源码真值:`plc/06_PRG_Test.st:12` → `N_CASES : INT := 77;`(注:0717 治理D 扩容 T77)
- T76 = `FB_RetrieveGood` 取出演示四分支(不存在REJ/未分配REJ/正常取出落账/取后查询口径),0716 加入,`plc/06_PRG_Test.st:912`
- T77 = `FC_TMax` 跟随 `xUseAccel` 双口径(匀速 38.0 / 加减速 39.6667,最远格 tNorm 恒 1.0),0717 加入,`plc/06_PRG_Test.st:950`
- 汇总循环 `FOR i := 1 TO N_CASES` 覆盖全部 77 项,非恒 TRUE 预留
- **重要机制**:`sim/out/plc_evidence.csv` **不是脚本生成物**,而是 `sim/build_report.py:332` 用 `load_rows()` 读取的**手工登记源**。无"重跑"路径,只能更新登记。
- 校正后验证:`python -B sim/build_report.py` → `plc_evidence:verified_passed 77`、`source_real_cases T01-T77`,共注入 147 个数字、待补 0;`python -B -m pytest sim/tests/test_method_integrity.py -q` → **9 passed**

### 4.4 00 导览页 5 个成果数字溯源(全部亲验)
| 数字 | 真实来源 | 结论 |
|---|---|---|
| 8.17 ± 0.67 s | `sim/out/final_test_30seed.csv` 第 6 行 awra:`H_exp_mean=8.168`、`H_exp_std=0.666`、`fail_sum=0`、`viol_sum=0`、30 seeds(10001–10030) | 成立 |
| −61.6% | 由 seq `21.242` / awra `8.168` 复算 **61.5%**;官方口径 `docs/canonical_assumptions.md:61`(C12)记 61.6% | 近似成立,差 0.1pp 为四舍五入,**未 100% 数字级复现** |
| 77 / 0 | `plc/06_PRG_Test.st:12` + 0717 AB 在线日志(见 §4.3) | 成立(本会话刚校正登记源) |
| 98.5% / 99.19% | `sim/out/oracle_gap.csv` ALL 行 `attain_mean=98.55`;`sim/out/oracle_gap_accel.csv` ALL 行 `99.19` | **精确成立** |
| 9 / 9 | `l2_factoryio/F3_H5证据保全_0716.md:16-19`(revision 292)+ `media/g4_evidence_0713/H5_9of9_终验_三格齐放第三轮.png` | 成立;但「54 份诊断日志」细节与磁盘实测 66 份对不上,**引用时只用 9/9,不复述 54 份** |

### 4.5 门面层现状(本会话新发现)
| | `app_shell/launch.html`(壳首页) | `src/00_导览.html`(浏览器直开) |
|---|---|---|
| 风格 | 已对齐工程风 | 4 处衬线(`.brand`/`.rail h1`/`.chapter h2`/`.lede`) |
| 三页入口 | 有(缩略图为 **0720** 版,已过时) | 无 |
| 5 个成果数字 | **无** | 有(埋在顶部细条) |
| 材料区 | 4 项,**缺 04 决赛件** | 6 章详解 |
| 在壳导航 | 是首页 | **不在** `KEY_TO_SRC` |

- 壳导航表 `app_shell/shell.html:112-118` 的 `KEY_TO_SRC` 只有 `launch/materials/01/02/03`,**无 `00` key**
- 壳窗口:`app_shell/main.js:112-126` `useContentSize:true`,默认内容区 **1600×900**,最小 **1280×800**;左侧栏固定 48px → iframe 可用 **1552×900**(最小 1232×800)
- 壳静态服务白名单:路径须落在 `SHOWCASE_ROOT`(l4_showcase)内,例外仅 `l2_factoryio/`;MIME 白名单 html/js/json/css/png/jpg/jpeg/mp4
- `launch.html` 已有壳内/直开双模降级机制:`inShellFrame = window.parent && window.parent !== window`,顶层直开时 postMessage 静默降级

### 4.6 三版候选(已渲染,1552×900,`errors 0`、`imgOK true`、`__D00_READY__ true`)
| 版 | 结构 | 优点 | 弱点 |
|---|---|---|---|
| 甲 `_d00_variant_A.html` | 双 Tab(演示/材料),一屏不滚 | 最干净 | 演示态下方约 200px 留白 |
| 乙 `_d00_variant_B.html` | 整页滚动(门面在上、六章在下) | 第一屏信息量最大、场景卡最大、与 03 页滚动展陈风同源 | **滚动时无章节导航** |
| 丙 `_d00_variant_C.html` | 左栏 7 项 + 右内容区 | 导航最直观 | 左栏吃 216px,场景卡被压窄、缩略图变小,右下留白最多 |

三版共同做到:清除全部衬线;顶栏 48px + `border-bottom:3px solid #ff000f`;`.abbWord` 复制 01/02 的 `font:900 24px/1 Arial;letter-spacing:-1.2px`;`.eyebrow` 8px/1.45px/#5d6871;h1 18px/#16191d;删除 00 独有的 `.topline` 双色渐变条;5 个数字升级为主张条(hover 出三段口径);三页入口卡用**本轮新拍**缩略图;材料路径改指仓库真实位置。

## 5. 待办优先序(第一动作在最前)

1. **【第一动作 · 卡在等拍板】D 选版并施工** —— 用户选定甲/乙/丙之一后:
   - 用选定版覆盖 `F:\abb_wh_work\l4_showcase\src\00_导览.html`,删除另两版 `_d00_variant_*.html`
   - 若选乙:补一条随滚动高亮的章节浮标(补其唯一弱点)
   - 接进壳导航:改 `app_shell/shell.html` 的 `KEY_TO_SRC` 加 `'00': '../src/00_导览.html'`(**用户已拍板「00 页当壳首页」**),并处置 `launch.html`(见待拍板)
   - 补 00 页 QA(当前**零覆盖**,见 §7 机制坑 3)
   - commit + push
2. **#18 W4 打包 portable exe** —— electron-builder portable + extraResources + `main.js` 的 `process.resourcesPath` 分支 + SmartScreen 说明 + 浏览器直开后备 + 打包后自检。**打包清单现在需含 6 个 src 模块**(本轮新增 `s3_speed_profile.js`)+ `src/assets/` 三张缩略图
3. 两页失效 CSS 清理(`#liveChartWrap`/`#speed` 共 35 处,含组合选择器)—— 已定在 W4 打包前统一做 + 全量回归
4. W5 评委机模拟(需用户在场);03 补拍素材(computer-use,方案见 `docs/补拍作战方案_0722.md`)

## 6. 拍板与待拍板

### 6.1 已拍板(0728c 两轮六问,用户选项逐字)
1. **D 选版** →「乙 · 滚动式(推荐)」→ 施工时**补一条随滚动高亮的章节浮标**补其唯一弱点
2. **`launch.html` 处置** →「删掉,壳首页改指 00(推荐)」→ 改 `shell.html` 的 `KEY_TO_SRC`,`launch` 键改指 `../src/00_导览.html`;删 `launch.html` 与其 0720 旧缩略图
3. **W4 打包时机** →「D 做完就打(推荐)」
4. **材料打开能力** →「搬过来 + 双模降级(推荐)」→ 壳内 `postMessage` 交 Electron 用系统程序打开;浏览器直开降级为**点击复制路径到剪贴板 + 提示**
5. **00 页 QA 深度** →「加『文件存在』硬门(推荐)」→ 页面点名的每条路径去磁盘真实校验,外加零运行时错误 / 三页入口可点 / 五个数字与来源文件一致 / 六章可达
6. **`build_submission` 脚本** →「W4 打包时一并做(推荐)」→ 届时把 `sim/out`、`plc`、`l2_factoryio` 等真实产物按五目录拷进提交包;**那时 00 页里的路径可再切回提交包相对路径**

### 6.2 仍待拍板
- 剩余旧队列排序:03 补拍、W5 评委机、R3 录屏 / R4 报告 / R5 导览、#25(`1_给你看`/`2_你要操作` 收编)、#38-41(NN贪心/MLTRL/答辩题库/西门子杯研究)
- 「−61.6% vs 复算 61.5%」的 0.1pp 差异是否需要精确复现(当前按 canonical 官方口径写 61.6%)
- 两页失效 CSS(35 处)清理的具体时机:已定「W4 打包前」,但与「D 做完就打」的先后未细定

### 6.3 D 施工顺序(按上述拍板展开)
1. 乙版覆盖 `l4_showcase/src/00_导览.html`,删 `_d00_variant_A/B/C.html`
2. 补章节浮标(随滚动高亮当前章)
3. 材料行双模可点:壳内 postMessage → Electron 系统打开;直开 → 复制路径
4. 改 `app_shell/shell.html` 的 `KEY_TO_SRC`(`launch` → `../src/00_导览.html`);删 `launch.html` 与 `app_shell/assets/p0*_thumb.png`
5. 改 `app_shell/main.js` 的 `openMaterial` 处理与 `materials.config.json`(补 04 决赛件)
6. 新建 `tools/capture_d00_qa.mjs`:文件存在硬门 + 零错误 + 入口可点 + 数字一致 + 六章可达
7. 壳内亲验(Electron 实跑)+ 全套回归 + commit/push
8. 接 W4

## 7. UNVERIFIED / 勿重做 / 机制坑

### UNVERIFIED
- 三版候选**未在 Electron 壳内亲验**(只在浏览器 1552×900 下渲染过)
- 00 页接壳后 `postMessage` 导航链路未实测(代码已按 launch.html 同款写好,但没在壳里跑过)
- 打包体积仍为预估(codex 打的 173MB exe 未采用)
- 「−61.6%」未做 100% 数字级复现(见 §4.4)
- 「9/9」对应的「54 份诊断日志」与磁盘 66 份的差异原因未查实
- 提交包五目录是否会在提交前被真实填充,**无任何脚本可循**

### 勿重做
- 本会话 2 个 commit 已收口,勿回退
- **速度不是 bug,不许再「统一渲染层」**——`tools/test_s3_motion_parity.mjs` 反向断言 + 01QA `motionProfileOnTravel` + 02QA `speedProfileAxisCalibre` 三重把关
- 01/02 顶栏外壳语言本已一致(commit `2b19067` 实测确认),勿重复对齐
- codex 0724 交付**未采用**,完整存于 `stash@{0}`,勿误 pop
- 工作区里 `l4_showcase/app_shell/dist/`、`verify_portable.js`、`评委机运行须知.txt`、`design_mock/assets/` 是 codex 0724 遗留的**未跟踪文件**,W4 打包时先判断复用还是重做

### 机制坑
1. `tools/capture_s3_layout_a_qa.mjs` 的哈希快照清单**必须同步改 `liveFiles` 与 `snapshotTargets` 两处**,漏了不报错而是 `FAIL 0/0`。当前清单含:source / runtime / sceneCore / heatLayer / motion / **speedProfile** / tooltip / tooltipCopy / store / interactions / shellCss / three / orbit / pointer / registry / manifest / audit×3 / evidenceSeedSkew
2. `tools/test_s3_runtime_contract.mjs` 必须**先 `require` `src/s3_motion.js`** 再 require runtime,顺序不能反
3. **00 导览页目前零 QA 覆盖**(`tools/` 下 16 个脚本无一涉及)——这正是 04 章文件名过时至今无人发现的原因
4. 两页指标区/图例/步骤条每帧 `innerHTML` 重渲染 → 交互必须走事件委托
5. 01 页 `#strategySelect` 的 option value 是**小写** `seq/near/score`(`AUTO` 才是大写)
6. **【0728c 更正】两页 EOL 现均为纯 LF**(`CRLF=0` 实测)——`会话断点_0728b.md` §7 机制坑 5 称「01 页 EOL=CRLF」已不成立
7. Windows 终端 `print` 中文文件名会撞 cp1252 报错,但文件**可能已写成功**——报错后先核对再决定重跑
8. `#viewMat{display:grid}` 这类 **ID 选择器优先级高于 `.view[hidden]`**,tab 显隐必须同级或更高优先级才压得住(三版生成时踩过)
9. HTML 里写 `**文字**` 不会渲染成粗体(不解析 markdown),要用 `<b>`(三版生成时踩过)
10. 沿承:壳内容区 1552×900;改样式不生效先测 computed style;粘性头部让 Playwright 原生 `click` 超时,改 `page.evaluate(() => el.click())`;中文路径 `git add` 改用目录级添加

### 侦察结论中**不予采信**的一条
并行侦察 agent 报称「`.cgChip`/`#tierBadge`/`.metricChip` 在 01/02/03/00 四页中均未找到」——**错误**。`#tierBadge` 就在 `l4_showcase/src/02_入出闭环.html:1138`;`.cgChip` 是本会话在 `s3_fill_candidate_runtime.js` 的 `sceneCargoMeta` 里写的。该 agent 只搜了部分文件即下"不存在"结论。其余三路结论均已逐条复核来源。
(另:四路侦察中「提交包现状」那一路因 StructuredOutput 重试超限失败,其信息由「数字溯源」路覆盖。)

## 8. 文件索引(本会话新建/大改)

### 新建
- `F:\abb_wh_work\l4_showcase\src\s3_speed_profile.js` —— 双泳道行程速度剖面(两页共用)
- `F:\abb_wh_work\l4_showcase\src\assets\p01.png` / `p02.png` / `p03.png` —— 本轮新拍三页缩略图(640×400)
- `F:\abb_wh_work\l4_showcase\src\_d00_variant_A.html` / `_B.html` / `_C.html` —— 三版候选(**选定后删未选中的**)
- `F:\abb_wh_work\docs\会话断点_0728c.md` —— 本文

### 大改
- `F:\abb_wh_work\l4_showcase\src\s3_scene_core.js` —— `addPath` 加 `kinematicBreaks` 三段染色 + `sliceByProgress` + `setAppearance` 统一外观入口 + `dispose`
- `F:\abb_wh_work\l4_showcase\src\s3_fill_candidate_runtime.js` —— 剖面图接线 + `kinematicBreaksFor` + `#motionModelBadge` + QA 审计字段
- `F:\abb_wh_work\l4_showcase\src\s3_ac_runtime.js` —— 同上(02 侧)+ `MOTION_LEGS` + `legFrom/legTo/motion` 帧字段
- `F:\abb_wh_work\l4_showcase\src\01_连续填仓.html` / `02_入出闭环.html` —— 引入 `s3_speed_profile.js`;删除 `liveChartWrap`/`speedCanvas` 死代码;02 热区图例改口径
- `F:\abb_wh_work\l4_showcase\src\s3_tooltip_copy.js` —— 新增 4 条三段式文案(剖面图 / 本段行程 / `#motionModelBadge` / `#tierBadge`);热区条 title 改「热门货包络」
- `F:\abb_wh_work\l4_showcase\tools\capture_s3_layout_a_qa.mjs` —— +4 门,64 → 68
- `F:\abb_wh_work\l4_showcase\tools\capture_s3_mixed_layout_a_qa.mjs` —— +2 门,8 → 10
- `F:\abb_wh_work\sim\out\plc_evidence.csv` —— 75/0 → 77/0(未跟踪文件,已备份)
- `F:\abb_wh_work\docs\canonical_assumptions.md` —— C11 校正(**待 commit**)
- `F:\abb_wh_work\docs\任务规划_0728_3D抽离与悬浮.md` —— 新增 §4.9 #23 完成记录

### 临时工装(scratchpad,不入库但可复用)
`C:\Users\86177\AppData\Local\Temp\claude\C--Users-86177\c0960d42-d184-418d-ab8a-4f674d927071\scratchpad\` 下:
`d00_base.js`(三版共用真实数据 + 来源)、`d00_gen.js`(三版生成器)、`shot3v.mjs`(三版渲染自检)、
`bandsweep.mjs`(01 分段扫描)、`p02bands.mjs`(02 逐腿分段)、`profcheck.mjs`、`motionlook.mjs`、`pathzoom.mjs`、
`shot23.mjs`(双视口定妆)、`shot00.mjs`、`shotlaunch.mjs`、`thumbs.mjs`、`mkcards.py`(缩略图裁切)、
`fix_plc_evidence.py`、`plc_evidence.csv.bak_0728`(备份),以及沿承的 `parity.mjs`/`heatcheck.mjs`/`arrowsweep.mjs`/`motionparity.mjs`/`tipcheck.mjs`/`zonewalk.mjs`
