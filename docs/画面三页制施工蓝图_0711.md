# 画面三页制施工蓝图(0711 拍板;施工=画面终局一次性,等本蓝图全量一次做完)

> 拍板记录:0711 用户三问(P1/P2 分布合理吗/其他表现形式/以后还加按键吗)→
> 定案 **三页制 ISA-101 层级导航**(ANSI/ISA-101.01-2015,报告 B5 可引用)+
> **批次前置令**(先做完全部会上画面的批次 ST,画面只施工/洗/长跑一次)。
> 前置已清:A2-A4(56/0)+ C1/C2/C6(T57-59,59/0)ST 全部落地。

## 架构与扩展协议

| 页 | 角色(ISA-101 映射) | 内容 | 施工策略 |
|---|---|---|---|
| P1 操作台 | L2 单元操作 | 地图+图例8态+状态报警+输入+操作按钮+**智能中枢**+当前件 | 已洗 21 处动作:**17 处原位冻结**,删权重 4 输入迁 P3 |
| P2 记录分析 | L3 细节 | 记录表+查询+模型参数+汇总(现状) | **零接触**(仅加 1 个 P3 导航钮) |
| P3 管理配置(新) | L4 配置诊断 | 权限+检修+评分权重(迁入)+A5 实测表+RESERVED 留白 | 全新建 |

**扩展协议(对"以后还加按键吗"的制度性回答)**:今后新功能一律进 P3 或新开 P4;
P1 动作元素从此冻结——重洗成本被结构性锁死,决赛加需求不返工。

## P1 变更单(在当前工程 540 元素基础上)

- **删**(脚本 remove_id,几何谓词锁定):
  ①权重调参区 y626-672(Alpha/Beta/Gamma/Delta 4 输入+4 标签+2 说明行——4 输入是已洗件,损失已拍板);
  ②0711 临时左下三组全部 29 元素(y594-676 x40-600 的新增件,**未洗零损失**;图例第 8 格 2 件保留)。
- **加·智能中枢**(原调参区位置 x620-1260 y618-676,替换式利用空间):
  - 分组框(透明)+标题 `SCENE DETECTOR / VIEW`
  - 画像 6 值文本:N / DENS / SKEW / W / HEAVY / REC(绑 VisuProf* + VisuRecText)
  - `Auto strategy` 钮(execute:xAutoStrategy 翻转)+ 状态灯(绑 xAutoStrategy)【洗】
  - `View` 钮(tap:CmdViewCycle;状态/频次热力/损耗热力三态循环)【洗】
  - `Tier` 输入(Numpad→InTierSel,-1..19,层高亮)+ `Tier occ: %d` 文本(VisuTierOcc)【洗】
- **加·导航**:`P3 Admin` 钮(x1140 y320 与 P2 Records 竖排对齐)【洗】
- 净洗账 P1:**4 处**。

## P3 新建页(1280×720,风格延续 P1 分组框)

| 区 | 元素 | 动作 |
|---|---|---|
| 标题行 | `P3 - Admin & Configuration` + Status 文本镜像 | - |
| 导航 | `Back to P1` / `P2 Records`(change_shown_visualization) | 【洗×2】 |
| ACCESS 组 | USER 文本(VisuUserText)/ PIN 输入(Numpad→InPinCode 0-9999)/ Login / Logout(tap)/ `Params guarded` 提示(Invisible 绑 iUserLevel>=1)/ 失败计数(iFailCnt) | 【洗×3:PIN/Login/Logout】 |
| MAINTENANCE 组 | X/Y 输入(Numpad 0-19)/ Set-Clear(tap)/ 计数 / `purple=blocked` 说明 | 【洗×3】 |
| WEIGHTS 组(迁自 P1) | Alpha/Beta/Gamma/Delta 4 输入(Numpad)+ 默认值说明行 | 【洗×4】 |
| A5 实测表 | 静态表格文本:S20/S120/S240/S240b 四行(周期数/标称 s/ms 每件)+ `single suggest <= 1 cycle (10ms)` | - |
| RESERVED | 空分组框 `RESERVED (finals expansion)`(决赛留白) | - |

净洗账 P3:**12 处**。

## P2 变更单

- 仅加 `P3 Admin` 导航钮(410 行按钮排旁)【洗×1】。其余零接触。

## 总账与流程

- **洗单终版 17 处** = P1×4 + P3×12 + P2×1(一次洗完,长跑一次)。
- 流程:改 `add_cockpit_0711.py` → 第二版 `build_three_pages_0713.py`(删+建+P3 create_visualobject)
  → ab_sync 编译+59/0 → computer use 洗 17 处(先清场:关闭抢前台应用)→ 在线长跑
  (v4 四镜头 + 热力双模式 + 层高亮 + 12 步回归)→ commit。
- 回滚点:施工前 git commit 的工程文件即回滚基准(fix 先例)。
- 长跑追加镜头(v4 之外):`View` 循环两拍看频次热力(高频货应集中低层近端=AWRA 语义可视化!)
  → 再一拍损耗热力(跑过几批后有梯度);`Tier` 输 0 看底层高亮+计数。热力图即 C2 录屏开场素材。
