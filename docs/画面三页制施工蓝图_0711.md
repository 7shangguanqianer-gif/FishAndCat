# 画面三页制施工蓝图(0711 拍板;施工=画面终局一次性,等本蓝图全量一次做完)

> 拍板记录:0711 用户三问(P1/P2 分布合理吗/其他表现形式/以后还加按键吗)→
> 定案 **三页制 ISA-101 层级导航**(ANSI/ISA-101.01-2015,报告 B5 可引用)+
> **批次前置令**(先做完全部会上画面的批次 ST,画面只施工/洗/长跑一次)。
> 前置已清:A2-A4(56/0)+ C1/C2/C6(T57-59,59/0)ST 全部落地。
> **视觉方案定稿=丙(顶导航+流程栏,0711 用户选定)**,详见文末「## 方案丙定稿」;
> 上文架构/变更单为通用骨架,坐标以方案丙节为准。选型 mockup 留痕=桌面 P1-P3三页制_3方案选择_0711.html。

## 架构与扩展协议

| 页 | 角色(ISA-101 映射) | 内容 | 施工策略 |
|---|---|---|---|
| P1 操作台 | L2 单元操作 | 地图+图例8态+状态报警+输入+操作按钮+**智能中枢**+当前件 | 已洗 21 处动作:**17 处原位冻结**,删权重 4 输入迁 P3 |
| P2 记录分析 | L3 细节 | 记录表+查询+模型参数+汇总(现状) | **零接触**(仅加 1 个 P3 导航钮) |
| P3 管理配置(新) | L4 配置诊断 | 权限+检修+评分权重(迁入)+A5 实测表+RESERVED 留白 | 全新建 |

**扩展协议(对"以后还加按键吗"的制度性回答)**:今后新功能一律进 P3 或新开 P4;
P1 动作元素从此冻结——重洗成本被结构性锁死,决赛加需求不返工。

## P1 变更单(在当前工程 540 元素基础上)

- **删**(脚本 remove_id,几何谓词锁定):
  ①权重调参区 y626-672(Alpha/Beta/Gamma/Delta 4 输入+4 标签+2 说明行——4 输入是已洗件,损失已拍板);
  ②0711 临时左下三组全部 29 元素(y594-676 x40-600 的新增件,**未洗零损失**;图例第 8 格 2 件保留)。
- **加·智能中枢**(原调参区位置 x620-1260 y618-676,替换式利用空间):
  - 分组框(透明)+标题 `SCENE DETECTOR / VIEW`
  - 画像 6 值文本:N / DENS / SKEW / W / HEAVY / REC(绑 VisuProf* + VisuRecText)
  - `Auto strategy` 钮(execute:xAutoStrategy 翻转)+ 状态灯(绑 xAutoStrategy)【洗】
  - `View` 钮(tap:CmdViewCycle;状态/频次热力/损耗热力三态循环)【洗】
  - `Tier` 输入(Numpad→InTierSel,-1..19,层高亮)+ `Tier occ: %d` 文本(VisuTierOcc)【洗】
- **加·导航**:`P3 Admin` 钮(x1140 y320 与 P2 Records 竖排对齐)【洗】
- 净洗账 P1:**4 处**。

## P3 新建页(1280×720,风格延续 P1 分组框)

| 区 | 元素 | 动作 |
|---|---|---|
| 标题行 | `P3 - Admin & Configuration` + Status 文本镜像 | - |
| 导航 | `Back to P1` / `P2 Records`(change_shown_visualization) | 【洗×2】 |
| ACCESS 组 | USER 文本(VisuUserText)/ PIN 输入(Numpad→InPinCode 0-9999)/ Login / Logout(tap)/ `Params guarded` 提示(Invisible 绑 iUserLevel>=1)/ 失败计数(iFailCnt) | 【洗×3:PIN/Login/Logout】 |
| MAINTENANCE 组 | X/Y 输入(Numpad 0-19)/ Set-Clear(tap)/ 计数 / `purple=blocked` 说明 | 【洗×3】 |
| WEIGHTS 组(迁自 P1) | Alpha/Beta/Gamma/Delta 4 输入(Numpad)+ 默认值说明行 | 【洗×4】 |
| A5 实测表 | 静态表格文本:S20/S120/S240/S240b 四行(周期数/标称 s/ms 每件)+ `single suggest <= 1 cycle (10ms)` | - |
| RESERVED | 空分组框 `RESERVED (finals expansion)`(决赛留白) | - |

净洗账 P3:**12 处**。

## P2 变更单

- 仅加 `P3 Admin` 导航钮(410 行按钮排旁)【洗×1】。其余零接触。

## 总账与流程

- **洗单终版 17 处** = P1×4 + P3×12 + P2×1(一次洗完,长跑一次)。
- 流程:改 `add_cockpit_0711.py` → 第二版 `build_three_pages_0713.py`(删+建+P3 create_visualobject)
  → ab_sync 编译+59/0 → computer use 洗 17 处(先清场:关闭抢前台应用)→ 在线长跑
  (v4 四镜头 + 热力双模式 + 层高亮 + 12 步回归)→ commit。
- 回滚点:施工前 git commit 的工程文件即回滚基准(fix 先例)。
- 长跑追加镜头(v4 之外):`View` 循环两拍看频次热力(高频货应集中低层近端=AWRA 语义可视化!)
  → 再一拍损耗热力(跑过几批后有梯度);`Tier` 输 0 看底层高亮+计数。热力图即 C2 录屏开场素材。

---

## 方案丙定稿(0711 用户选定:顶导航 + 流程栏)

**总原则**:三页统一顶部导航条(ISA-101 层级显式化,报告 B5 可引用);P1 右栏按操作
流程①②③④从上到下编排(输入→选打法→运行结果→当前件),答辩讲解顺序即画面顺序。

### 顶部导航条(三页各建一条,y8-40 通栏)
- 三个 tab:`P1 操作台` / `P2 记录` / `P3 管理`;当前页 tab 高亮(dwHeaderBg 底纹)且不绑动作,
  另两页绑 create_change_shown_visualization(Assignment→VisuMain/VisuStats/VisuAdmin)。
- 洗账:每页 2 个可跳转 tab × 3 页 = 6 处导航动作要洗。

### P1 VisuMain(在现有 540 元素基础上)
- **删**:0711 凌晨临时左下三组(检测器/权限/检修共 29 元素,未洗零损失;图例第 8 格 2 件保留);
  评分权重区 4 输入+标签(迁 P3)。
- **移**(set Position,不 remove→input action 保留→**已洗元素不重洗**,长跑回归抽查即可):
  原输入 5 项、Add/Load/Reset/Run/Animate/快照、策略 4 键、当前件 4 值——按流程栏①②③④归位:
  - 顶导航 y8-40;Status+报警灯 y50-84
  - 地图 x40-600 y90-560 + 图例横排 y566-590(第 8 格检修紫已在)
  - 右栏 x620-1260:①输入区 y90-250 ②策略+检测器 y260-410(策略 4 键 + 画像 6 值文本 +
    Auto 灯钮 + View 循环钮 + Tier 输入+层计数)③运行+统计 y420-560(Run/Animate/快照 +
    5 统计)④当前件 Target/Path/Travel y566-600
- **新增要洗**:Auto 钮 / View 钮 / Tier Numpad / 顶导航×2 = **5 处**(画像 6 文本无动作不洗)。

### P2 VisuStats(现状零接触 + 顶导航)
- 仅在顶部加导航条(替换原 P2 Records 按钮的跳转职能);记录表/查询/模型参数/汇总全保留。
- **要洗**:顶导航×2 = 2 处。

### P3 VisuAdmin(新建)
- 顶导航条 + 标题;三组横排 + A5 表 + 留白:
  - 权限组:USER 文本 / PIN Numpad / Login / Logout / `Params guarded`(Invisible 绑 iUserLevel>=1)/ 失败计数
  - 检修组:X/Y Numpad / Set-Clear / 计数 / 说明
  - 权重组(迁自 P1):α/β/γ/δ 4 Numpad + 默认值行
  - A5 实测表:静态四行(S20/S120/S240/S240b 周期数·标称 s·ms 每件)+ `single suggest <= 1 cycle`
  - RESERVED 空框(决赛留白)
- **要洗**:PIN/Login/Logout(3)+ 检修 X/Y/Set-Clear(3)+ 权重×4(4)+ 顶导航×2(2)= **12 处**。

### 洗单终版(方案丙):5 + 2 + 12 = **19 处**(一次洗完,长跑一次)
### 流程
改施工脚本 `build_three_pages_0713.py`(P1 删+移+增 / P2 加导航 / P3 create_visualobject 全建)
→ ab_sync 编译 + 59/0 → git commit 工程(回滚点)→ computer use 清场后一次洗 19 处
→ 长跑(v4 四镜头 + 热力双模式 + 层高亮 + 12 步回归 + **已移动元素响应回归抽查**)→ commit。
### 风险点
"set Position 移动已洗元素不失效"是**待施工验证的假设**——长跑必须抽查移动过的原 17 元素
仍响应;若失效则退化为"移动的也重洗"(洗账升到 ~30)。

---

## 施工执行记录(0711 深夜,提前于 7/13 排期;脚本名保留 0713 编号)

**管线**:`tools/ab_scripting/build_three_pages_0713.py`(删64/移32/增29 + P2 导航 + P3 全建,
一次过)→ `fix_layout_0713.py`(audit 三查出的 4 处 P1 真碰撞微调)→ ab_sync **59/0 全绿**
→ audit 三页 706 元素:越界 0 / 真重叠 0(余 8 处=图例色样内嵌设计如此,P2 四处 1px=0711
既有基线,P3 一处=审计 TSV 拍平换行的假阳性)。P1=505 / P2=134 / P3=67 元素。

**对蓝图的三处主动偏离(工程判断,洗前可翻案)**:
- **D1 地图不整体下移**(蓝图字面"地图 y90-560+图例横排"不执行):避免 400 格+轴标 ~420 件
  搬移与 crane 像素常数的 ST 联动改;顶导航条 y8-36,格栅 y40 起零重叠;图例维持 2 列
  (第 8 格检修紫在位)。丙案精髓(顶导航+流程栏①②③④)完整保留。
- **D2 回收两个已洗跳转钮做导航 tab**:P1 的 `P2 Records` 钮移入导航条 x176 位;P2 的
  `Back to P1` 钮移入 x40 位改字 `P1 Console`——**洗单 19→17**,且两钮天然成为
  "移动已洗件"假设的回归样本。
- **D3 杂项**:P1 `State codes` 说明箱迁 P3 诊断区(③区让位按钮);P1 徽章过期数字
  45/45 改 **59/59**;P2 查询区大标题删除、页标题移导航条右区并入 demo ID 提示。

**洗单终版 17 处**(build_three_pages_result.txt 末尾同款,GUI 洗=Element properties→
Input configuration→事件行 Configure...→OK):
- P1×4:P3 Admin tab(312,8)/ Auto strategy(628,374)/ View(920,374)/ Tier 输入(1144,374)
- P2×1:P3 Admin tab(312,8)
- P3×12:P1 tab(40,8)/ P2 tab(176,8)/ PIN(116,148)/ Login(52,184)/ Logout(160,184)/
  MaintX(476,116)/ MaintY(576,116)/ Set-Clear(452,156)/ 权重α(946,116)β(1126,116)
  γ(946,156)δ(1126,156)

**长跑回归重点——被移动的已洗动作件 10 处**(假设验证样本;哑一处即触发退化预案):
策略 FIFO/Nearest/Score/AWRA-LS(y308→278)/ Run assign/Animate/Save snapshot/
Live-Snapshot(→③区)/ P1 导航 tab `P2 Records`(→176,8)/ P2 导航 tab `P1 Console`
(原 Back to P1,→40,8)。未移动已洗件(5 输入+Add/Load/Reset+P2 七输入等)同样过一遍
12 步回归但预期无恙。
