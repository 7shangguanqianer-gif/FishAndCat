# 会话断点 · 2026-07-22(一屏返工收口,待用户终审)

> 恢复入口。前情:docs/会话断点_0721b_回灌收口.md;拍板:docs/3版拍板包_0721.md(0722 九问节);规格:docs/施工规格_一屏返工0722.md。

## 0c. 0722e 证据中心模态化 + 弹层彻底排查(用户打回「打开无法关闭+与02形式不一」,commit `401b5d2`)

根因:0722a 给 01 证据中心做的是右侧抽屉(`.evidenceDrawer`,append viewport),无 × 钮、无遮罩点击关闭,唯一关闭入口(顶栏钮)被抽屉盖住 → 无法关闭;且形式与 02 模态完全不同。修复:整体改 02 同款模态——`#evidencePop.evidenceModal` 全屏遮罩(`position:fixed;inset:0;z-index:200`)+ 居中卡 `.evPanel`(min760/92vw,max86vh 滚动)+ `.evHead` 含 × 钮(`.evClose`)+ `.evBody`;evidencePop 改挂 `doc.body`(避 workHud 层叠上下文困顿,同 02:2202 教训);toggleEvidence innerHTML 包 evPanel/evHead/evBody;新增关闭委托(点遮罩 `target===evidencePop` 或 `.evClose`);showEvidence 返回 evPanel rect、新增 closeEvidence。**弹层开关对称性彻底排查(scratchpad/probe_overlays.mjs,17 路径全 PASS)**:01 证据中心(×/遮罩/Esc/再点钮 4 路径)+01 放大层(×/遮罩/Esc)+03 lightbox(×/遮罩/Esc)+03 bigModal(×/遮罩/Esc);03 两弹层关闭本就完备(html:1425-1431 三路径齐)。QA 白名单 evidenceDrawer→evPanel。五套全绿 61/30/8/8/19。

## 0a. 0722c 过时设计清剿追记(用户三次打回「金顶横杠过时+绿框有问题+从0重构」,commit `02106eb`)

审计文档 docs/01过时设计审计_0722.md(P1-P6 清单+依据)。已清:P1 金顶盖→02 式货位色阶热力(instanceColor 白罩 op.78、冷灰#aeb8c0→琥珀#e7b800→热红#c0392b、freqRankByGid 秩归一、heat3d/heat2d 双开关默认开、切换即重渲);P2 目标大头针箭头淘汰(html 实体+runtime 全引用+QA 探针 arrowRetired);P3 绿签→「证据中心」单键(A14,验证态改抽屉内徽章);P5 2D 金角标→频率色阶格色(heat2DOn 切换)。QA 断言 fx26HotVisual/fx28TargetGlowArrow 改新语义,61/61 全绿,契约30/02QA8/uniform8 不回归。**未完(下一窗):P4 tooltip 覆盖面扩大+P6 CSS 历史地层从0置换(data-style A/B/C 层等,1713 行中约半数死层)——「从0彻底重构」的 CSS 部分,方案=以 02 现行层为母版重写 style 块,DOM/锚点不动。反思已录:0718 金顶否决在案却被当资产,违「改前端先查废止」纪律。**

## 0. 0722b 排版返修追记(用户二次打回后,主会话亲手,commit `10822de`)

用户终审打回:01 大段空白、03 排版混乱、要求不派 agent 主会话亲改。根因与修复:①01 rail 底空白 526px——三联缩略图改竖排大图(drawSlotMap 竖排分支,容器高>宽×1.15 触发)+微条恢复双指标(期望取货+累计行程)+rail 加宽 clamp(300,19vw,400),bottomGap→9px;②3D 挤右显小——取景中心 FOCAL(偏心)→FRAME_CENTER(包围盒几何中心),球面半径→逐角投影紧拟合(fitDistanceForAspect 重写);③03 证据画面图 230px 扁盒 contain 大空白——viewStage 左右分栏(左大图 507px 高右四卡均布)。CSS 坑:裸 #slotMap 604 行带 !important 须同挂 !important 覆盖。五套全绿:61/19/30/8/8;1280×800 views Tab 曾溢出 23px,图高降档修复。**验收环境教训:壳内容区=窗口−48px 侧栏(默认 1552×900),QA 双分辨率已覆盖但此前亲验只看 1920 裸页——本轮诊断脚本(scratchpad diag_layout.mjs)按壳视口+用户实际窗口(1945×1094)双档实测。**

## 1. 一句话战况

用户亲验打回三问题(滚动/无开始钮/3D截顶)→ 九问拍板 → 一屏返工三批全部收口并推送;**等用户终审,通过前不谈补拍/W4**(补拍方案已备:docs/补拍作战方案_0722.md,发令枪=用户终审通过)。

## 2. 本轮 commit(全部已 push)

| commit | 内容 |
|---|---|
| `8851b40` | 03 页 4 Tab 一屏返工(QA 15→19)+ 桌面启动器图标 app_icon.ico |
| `28ca589` | 01 页一屏驾驶舱返工(QA 53→61):3D 主角 83%+精华条,九件深内容收弹大层,自动播放+暂停双态钮,双 FOV 镜头自适应 |

桌面已有「智储优控工作站.lnk」(直指 electron.exe 零黑窗,图标 ABB 红「智储」,已实测双击可起;不进 git)。

## 3. 数字口径(0722 全部独立复跑)

- 01QA **61/61**;契约 30/30 fail0;02QA 8/8;uniform QA 8/8(正式脚本 tools/capture_s3_fill_uniform_qa.mjs,此前未记录在案);03QA **19/19**;壳路由 4/4。
- 零滚动:01 workHud 由 scrollH2124/clientH680 → 两分辨率精确贴合;03 四 Tab×两分辨率 8/8 贴合。
- 3D:占视口 79-83%,canvas 拟合 100%(1280×720/1280×800/1600×900/1920×1080 四档)。

## 4. 关键机制(新会话必知)

- 一屏纪律=document+全部 overflow-y 容器零溢出,弹层(90vw/90vh)与横向带白名单;双分辨率 1920×1080+1280×800 是 QA 标配。
- 截图底座已换成熟规范:animations disabled+caret hide+document.fonts.ready+语义就绪断言+交互序列;禁 networkidle/裸 sleep。
- 01 弹大层九件定序(三联+差异图/比分条/总分牌/30-seed/评分解剖/决策卡/KPI/七步条),入口=精华条点击;审计抽屉独立右上。
- 01 逐块折叠已退役(只留栏级+dock 级);「继续」按钮由自动播放+暂停双态钮取代(0720 否决单从未含「继续」,上轮删除是 Claude 失误,已纠)。
- 3D 镜头:FRAME_BOUNDS 包围盒+垂直/水平双 FOV 约束求最小拉距×1.06,resize 重算;旧布局的「截断」实为 aspect≤0.85 时横向越界(投影扫描实证)。
- 03 的 54/66 口径实为 5 处(0721b 记 4 处,以 5 为准);「本页回答四个问题」收进证据画面 Tab 的 details 默认折叠。

## 5. 待办(严格顺序)

1. 【等用户】终审新版(壳双击即看)。通过 → 2;打回 → 按意见修。
2. computer-use 自主补拍(方案 docs/补拍作战方案_0722.md:P1 全景运镜/P2 站位特写/P3 负结果过程-谨慎项/P4 .factoryio 场景文件;素材进 media\补拍_0722\;先跑开拍前检查单)。
3. 03 页吸收新素材(站位配图补齐,QA 19 回归)→ W4 打包(extraResources+白名单路由同构)→ W5 评委机模拟。
4. 遗留旧队列沿承 0721b §5.4。

## 6. UNVERIFIED / 勿重做

**UNVERIFIED**:壳内(iframe)两页新版完整交互未全套跑(素材可达已验;W5 时全验);1280×800 的壳窗口实际观感(QA 断言过,人眼未看)。
**勿重做**:一屏返工已收口(§2);0721b 的勿重做全部沿承;uniform QA 有正式脚本勿再手写验证。
