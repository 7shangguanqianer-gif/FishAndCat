# 会话断点 · 2026-07-19c(功能双重构 · compact 前落盘)

> 本文=compact 后的**唯一恢复入口**。读完本文第 0/1/2 节即可开工,其余按需查。

## 0. 用户原话逐字引用(防转述偏移——本次 compact 的第一防线)

> 「我的重构指的是**功能与画面的双重构**,应该**先确立好功能再规划画面**」
> 「我希望你 compact 后**启动 deep-research 调研功能**,和我**商讨**启动之后,我们**再接着讨论画面的实现**」
> 「之前的 compact 造成了多次误读,这次你要好好反思做好措施」

解读(唯一合法读法):**顺序 = ①deep-research 调研"功能层" → ②拿结果与用户商讨、确立功能清单 → ③才讨论画面实现**。
**禁止**:跳过商讨直接改页面;把"功能调研"又做成布局/CSS 工作;在功能确立前碰画面。

## 1. compact 误读反思(为什么之前错了 + 本次措施)

| 误读事件 | 根因 | 本次措施 |
|---|---|---|
| 把「重构版面出3版」做成一个小块的三选一(CMP_3b) | 断点文档存的是**我的转述**("b 三版"),转述已带偏 | §0 用户原话逐字引用,不转述 |
| 把「重新规划版面」做成 CSS 代码根治 | 同上 + 我拿工程问题偷换设计问题 | §0 + §2 唯一下一步单入口 |
| 多任务并列让新会话自选,选错优先级 | 断点列了一堆"可做项" | §2 只写**一个**下一步;其余进"冻结区" |

## 2. 唯一下一步(compact 后第一动作)

启动 deep-research(扇出全 sonnet/medium,红线),问题草案(可直接用,允许微调措辞不改范围):

> 面向**竞赛评审/答辩演示**的工业仓储(AS/RS)可视化监控与演示系统,**功能配置**(不是布局)的成熟惯例与评审期待:
> ①工业 HMI/SCADA 标准(ISA-101 等)对**功能能力**的要求清单(告警管理/趋势/回放/下钻/报表…),区别于布局条款;
> ②数字孪生/仓储仿真演示系统通常具备的功能模块(时间轴回放、场景对比、KPI 趋势、异常注入与恢复演示、报告导出);
> ③学生竞赛/工业软件评奖场景中,评委实际考察的功能能力(复现性入口、基线对比、消融、统计置信、现场可操作性);
> ④「算法效果证明」类界面的功能惯例(A/B 同帧对比、what-if、置信区间、口径披露);
> ⑤故障/异常处理的展示功能惯例(事件标记、根因链、恢复过程可视化)。
> 每条区分一手依据与类比迁移;给出「常见功能清单 × 我们已有/缺失」可对照的输出。

结果回来后:**核对返回值有效性**(同类工作流曾返回占位符 `summary:'测试'`,须查 `journal.jsonl`)→ 与用户**商讨**功能清单(对照 §3 审计)→ 确立功能 → 才进画面实现。

## 3. 已拍板(全部有效,不重议)

| 决策 | 内容 |
|---|---|
| 版面方向 | **方向一 · 监控作业台**(V2 格局精修);三方向稿在 `l4_showcase/out/shell_directions_0719/CMP_三方向.png` |
| 删减组(0719 拍板「全部执行」) | ①删 railFacts 三格(全部有重复源)②队列块降级为事件浮现(等待>0 才出现)③mod3 说明进图例 tooltip ④AUTO 参数展开器移入证据叠层;腾出空间全给 2D 热力图 |
| 补缺组(0719 拍板「全部执行」) | M4 P95 口径小字 / M1 故障周期标记(轴红点+点击跳回放)/ M2 响应趋势 sparkline / M3 PLC 占位徽章(不虚构结论) |
| 方法论 | **先功能后画面**;deep-research 结果可能增删上述清单——商讨时以用户为准 |

依据文档:`docs/02功能零基审计_0719.md`(每块功能以「向评委证明什么」判生死)。
**注意:删减/补缺虽已拍板,但执行排在「deep-research 商讨确立功能」之后**——调研可能补充新条目,一次实施。

## 4. 冻结区(有状态但本阶段禁止碰)

| 项 | 状态 | 解冻条件 |
|---|---|---|
| CSS 层积根治 | 停在 P2a-gen2(五 commit,`c433d13`→`333bf73`,每刀几何零差+QA 8/8);P2a-gen3 起未做 | 功能确立后,与画面实现一起(目标形态=方向一) |
| b 三版(dock/rail/axis) | 已作废为选择题;`?s3b=` 机制与「存取对置·取货实测」数据块保留(dock 版在用) | 功能商讨里决定该块最终形态 |
| 任务 c/d/e(对比总览/stale 根治/R3R4R5) | 未动 | 功能+画面定稿后按 spine 恢复 |
| 01 页 | QA 46/46 稳定 | 02 功能定稿后做对照快审 |

## 5. 关键资产索引(compact 后免重找)

| 资产 | 路径 |
|---|---|
| 零基审计(功能生死簿) | `docs/02功能零基审计_0719.md` |
| 三方向稿 | `l4_showcase/out/shell_directions_0719/`(CMP_三方向.png + 3 单图) |
| 根治计划+进度看板 | `docs/CSS层积病根治计划_0719.md` |
| V2 实施记录 | `docs/V2区域重映射实施_0719.md` |
| 取货实测数字(真值,可引用) | 热门取货 31.0s(n=35) vs 非热门 36.0s(n=65);交织腿 9.1s;省一趟 18.8min/100 周期;入库腿 31.3s vs 取货腿 34.3s。算法:`s3_ac_runtime.js` buildOpsEvidence(七步 SIM 时刻差 + 官方 axisTime) |
| 工装(scratchpad/) | geosnap.mjs(几何基线,已修 close 泄漏)/ geomaudit.mjs / probe.mjs / shoot_both.mjs / shootdirs.mjs / smoke02.mjs;坑:01 用 `__S3_FILL_QA`、02 用 `__S3_QA`;就绪判据 `!snapshot().loading` |
| QA | 02:`node tools/capture_s3_mixed_layout_a_qa.mjs`(8/8);01:`capture_s3_layout_a_qa.mjs`(46/46,快照制,新静态依赖须登记清单+MIME) |

## 6. 红线(不变)

不碰 sim/*.py · trace 数据 · manifest · invariant 链 · 加载序;不碰 1_/2_ 用户文档;不覆盖冻结物;
黄牌 #targetBeacon 永不再引入(其 DOM 与 display:none 保护令也不可删——runtime 每帧写它);
git add 只点名 + 中文 commit + Co-Authored-By: Claude Fable 5;数字回真相源核、不编数字;
扇出 agent 一律 sonnet/medium,严禁 fable;workflow 返回值必核(占位符事故)。
