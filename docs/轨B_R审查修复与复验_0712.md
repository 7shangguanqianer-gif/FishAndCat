# 轨B · Codex 对抗审查(R1-R8) 修复记录与复验状态（0712）

> 上游：`_通信/codex_out/轨B_ST对抗审查_0712.md`（Codex 裁决"部分接受"，3 高+6 中低缺口）。
> **Claude 终裁：R1-R8 全部接受**（含两处抓我现行：R3"计划写快照、执行降级未改计划文本"、R4"注释称同款保护实际回落值不同"）；§4.2 REAL32 舍入反例与 CB 类界量化=接受为**安全域声明**不改代码（Codex 自己不建议加 epsilon；运行期锁参后批内参数不可变，任意速度属演示自伤域）；§5 三轮日志唯一副本=接受并**机制化**（ab_sync 自动存档）。
> **当前状态：修复代码全部落盘 ✓ / 07 已重生零漂移 ✓ / AB 复验已过 ✓（第四轮 ab_sync：0 errors + 75/0 + xAllPass=TRUE，日志存档 `tools/ab_scripting/logs/{sync,runtest}_result_20260712_111501.txt`）。**
> 复验过程记录：初版编译 8 errors——根因=R7 的 `iModeRun` 漏 VAR 声明（赋值/读取都在，声明忘加），补一行后 0 errors；顺手把 `sync_st.py` 消息扫描修为 severity+C0xxx 双路兜底（旧过滤只抓含"error"字样的行，具体错误行全漏、只剩汇总计数）。证据三处（plc_evidence/canonical C11/README:49）+ 致Codex 回执均已刷新。

## 一、R1-R8 修复对照表（全部已落码）

| # | 缺口 | 修复 | 落点 | 测试锁 |
|---|---|---|---|---|
| R1 高 | LoadDemo 绕过 ID 契约（二次载入=两套 Id 1..20） | 模板加 **ID 冲突拒载**：表内已有任一 Id∈[1..N_DEMO] 即整批拒（比"仅空仓"宽容且正确：允许先 Add Id=901 再载批） | `sim/export_st_vectors.py`(模板)→`plc/07_GVL_Data_generated.st`(FC_LoadDemoGoods 头部) | T61 增强：空仓载入成功后**二次载入必须 FALSE** 且件数不变 |
| R2 高 | Stats 缺双向/一货一位（幽灵格等 4 反例 ViolCnt=0） | **计数平衡扫描**：非预占占用格数(格侧) ≠ PlacedCnt(货侧) → ViolCnt+1；配合 R1+N4-16 入口唯一 ID 覆盖反例1/3/4；反例2(空格残留 GoodsId)无活性消费者=fbInit/Reset 清理，注释declared | `plc/04_FB_Warehouse.st` FB_Stats（nUsed 循环内+`nOccGoods` 声明） | T72 增强：幽灵格(17,18)造出→ViolCnt≥2→清理 |
| R3 高 | 参数快照未闭环（AWRA/TOB/统计读实时参） | 选 Codex 方案 B：**运行期锁参**——fbGuard 加 `xFreeze` 输入；ASSIGN/IMPROVE/STATS 期间**连管理员改参也回滚**到批开工快照 | `plc/04_FB_Warehouse.st` FB_ParamGuard(放行条件 `xInit OR (iLevel>=1 AND NOT xFreeze)`)+`plc/05_PRG_Main.st`(调用传三态 OR) | 演示预演人工验证项（PRG_Test 测不到主状态机——已在 T75 注释声明） |
| R4 中 | FC_TMax 回落 0.001≠行程函数的 1.0（tNorm 失真 T74 仍绿） | 回落值统一 **1.0**（与 `FC_CalcTravelTime:129-130` 同源） | `plc/03_Functions.st` FC_TMax | T74 增强：rVx=0 时**断言=38.0**（非仅>0） |
| R5 中 | aSlotOps 五写点无饱和 | 五处 `IF < UDINT#4294967295 THEN +1` | `plc/04_FB_Warehouse.st`（放置/CT放置/reloc/swap两格） | 类型级：编译+T58 回归 |
| R6 中 | 测试矩阵缺口 | ①初始化 `FOR 1 TO 59`→**N_CASES**；②T71 扩 nPairsPerCycle/nMaxLsRounds=0 钳位；③IMPROVE 状态文本补 `_RELOC=` 分列 | `plc/06_PRG_Test.st:46`、T71 段、`plc/05_PRG_Main.st` IMPROVE 文本 | T71 扩段 |
| R7 低中 | CT 忙时可重启/iMode 未锁存 | 启动门加 `AND (iPhase=0)`；`iModeRun` 启动锁存，phase2 尾/phase6 读锁存 | `plc/04_FB_Warehouse.st` FB_AssignClassTurnover 三处 | **T75 从预留升级为真断言**：中途二次沿+改档 4→5 → iAssigned=4(重启会=8)+第3件在 CB 位(1,1)(切档会走 TOB 落(4,0)) |
| R8 低 | T58/T59 非短路失败路径越界 | T58 嵌套 IF 先判两件 Assigned；T59 加 `IF NOT Assigned THEN FALSE ELSE …END_IF` 包裹 | `plc/06_PRG_Test.st` | 防御性（失败路径判红而非越界） |
| §5 | 三轮日志只剩最后一轮可复核 | ab_sync 每轮自动存档 `tools/ab_scripting/logs/{sync,runtest}_result_<时间戳>.txt`（红绿都存） | `tools/ab_scripting/ab_sync.ps1` | 机制化 |

**连锁自查（全部虚惊，已验证）**：T21/T25/T68/T69 调 FC_LoadDemoGoods 前均先 `iGoodsCount:=0` → R1 扫描域为空不误拒；T61 满仓段(395)由容量护栏先拒（顺序：容量→ID 冲突）。07 重生后 CB 2.495106/20/0、TOB 2.143856/20/0 与上轮**逐位一致零漂移**。

## 二、悬挂步与复验后动作链（compact 后按此续跑）
1. **【悬挂】AB 复验**：等用户关 AB → `powershell -File tools\ab_scripting\ab_sync.ps1`（守门:AB 开着会 exit 3）。预期 **75/0**（T61/T71/T72/T74 断言变强+T75 新真断言）。红则修（不带病推进）。
2. 绿后（日志已自动存档到 `tools/ab_scripting/logs/`）：
   a. 更新 `sim/out/plc_evidence.csv` + `canonical C11`：证据=最新一轮 75/0（**T60-T75 全部真实断言，预留=0**）+存档路径；历史"三轮"改注"登记性，独立可复核见 logs/ 起始于本轮"。
   b. 根 `README.md:49` 三态句同步（75 项全真实）。
   c. `_通信/致Codex.md` 顶部追加：R1-R8 修复+复验回执（引用本文档+logs 存档）。
   d. 面板刷新（NOW→画面洗）。
   e. git commit+push（指定路径，绝不 -A）+`rev-parse origin/master` 核实。
3. 后续队列（复验绿后的下一主线，用户拍板顺序）：
   - **画面洗 4+17 项**（需用户/computer-use）：SelObjective 开关(P3)、CB/TOB 按钮、"Tests 59/59"→75/75 标签、（可选）密集载入按钮——见 `docs/演示预演记录_0712.md §2`；再叠旧 17 处清单。
   - 三页在线长跑（演示零翻车前置，需用户在场）。
   - 报告周：B1 按 DR-1 重做 / B10 升级 98.5 话术（现在可写"CB/TOB 已入 PLC 并经 AB 黄金向量+在线预演验证"）/ §6 空当填充——见 `docs/报告大纲与草稿映射_0712.md §四`。
   - 提交包 manifest/requirements/allowlist 计划（Claude 起草，backlog）。
   - 补课+quiz（约定=ST 收口后）。

## 三、本会话全部成果索引（防遗漏总账）
**代码（未 commit→本文档落盘时一并 commit）**：`plc/01/02/03/04/05/06/07`（轨B 四阶段+R 修复）、`sim/export_st_vectors.py`（CB/TOB 向量+护栏+R1）、`sim/strategy_lib.py:202`（st_ifelse 补 n>20）、`tools/ab_scripting/{ab_sync.ps1(守门+判定75+存档), sync_st.py(MAP 登记新FB)}`、`sim/out/plc_evidence.csv`。
**文档**：`Claude_N5复核回执` `Claude_0712合并修复终审回执` `演示预演记录` `本文` `ST补丁计划_轨B` `DR-5_AI协作方法论` `docs/README(导览)` `真相源与文档层级` `报告大纲与草稿映射` `给Codex_合并提示词` `续跑提示词_0712R`；归档标注×6；`README.md`/`canonical C11` 证据刷新；`_通信/致Codex.md` 两节回执。
**桌面**：`智储优控_作战面板.html`（完全重构版+两轮刷新）。
**AB 实测证据**：三轮 ab_sync 75/0（阶段0+1/2/3；仅最后一轮日志在 `runtest_result.txt`，此后每轮自动存档）+ 演示预演在线跑通（TOB 实跑 DONE_OK/REC=TOB_LEX 切档，截图+`演示预演记录_0712.md`）。
**Memory（全局）**：fable 禁令(0712 令+named-workflow 缓存陷阱)、pacing-and-reporting(三层汇报/一次一主线)。
