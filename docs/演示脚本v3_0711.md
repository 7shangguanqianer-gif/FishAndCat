# 演示脚本 v3(0711 深夜定稿;12 步,每步带前置状态+预期+核对点)

> **✅0711 04:00 全 12 步已由 Claude 冷启实跑验收通过**(computer use,两次冷启跨会话,
> 50+ 交互零失灵;每步截图核对)。实跑修订三条:
> ①步 6 的布局跳变:同批 20 件下 Score/AWRA 布局相同(统计同值 186.0/370.0),快照对比要出
> 视觉差异应**先跑 FIFO 存快照,再 AWRA 重跑对比**(异质布局);
> ②步 10 实跑用 Weight=180(点键误差),与 150 等效(>100 即触发)——**Banner 报警行实弹**:
> "11.07.2026 03:58:19 Warehouse alarm: no feasible slot / invalid input",带时间戳,右上灯黑→亮圈翻转;
> ③步 12 调参对比必须在**落位态**做(未落位件 Handling=0):落位后 Query ID=1 实测
> Handling 7.00=改后 T1 ✓。红灯图像项关闭:灯用黑/亮圈翻转表达报警,功能完备,不再换红图(YAGNI)。

> v3 修正 v2 的全部步序坑(0711 实测定案):①单件 Add=登记+推荐**预览**,批量 Run assign 才真落位
> ②Animate 只在有 pending 推荐目标时有动作 ③DONE 态不收 CmdAddGood,重来先 Reset
> ④超重报警走 `ALARM_NO_FEASIBLE_SLOT` 通道(**演示值 150kg**,>BASE_CAP=100 全仓无可行位;
> Numpad Max=200 放行)⑤Total goods 含 SUGGEST 登记件(口径注记)⑥Simulation 时钟是倒计时,
> 解说勿当运行时长 ⑦每次 Login 先点掉许可提醒弹窗(22 天期,8/2 前须续)。
> 核对三类断言:**Status 文本 / 数值字段 / 颜色状态**。录屏时本表即台本。

## 前置(不算步):冷启上线
关 AB→重开→打开工程→F11(0 errors)→Online Login→(点掉许可弹窗)→F5 RUN。
**核对**:状态栏 RUN 绿;画面 Status: `READY_PRECOUNT=133`;400 格呈灰棋盘(133 预置深灰);
crane 橙椭圆停 I/O 点(左下)。

| # | 操作 | 前置状态 | 预期(Status/数值/颜色) | 解说要点 |
|---|------|---------|------------------------|---------|
| 1 | 点 **Load demo** | READY(冷启后) | `DEMO_LOADED_N=20`;格子不变色(货在队列未落位) | 演示批 20 件与 Python 仿真同源(seed=2026) |
| 2 | 点 **FIFO/顺序基线**(策略键) | 任意 | Current strategy: 0 | 先保存异质基线，保证快照对比肉眼可见 |
| 3 | 点 **Run assign** | 有货(N=20) | `DONE_OK`;四项核心布局统计非零（SwapCnt可为0，不要求全字段非零） | 顺序基线落位;绿=占用 |
| 4 | 点 **Save snapshot** | DONE_OK | Snapshot valid 灯**点亮** | 保存当前布局作对比基线 |
| 5 | 点 **Live**→**Reset**→Load demo→换 **AWRA-LS**→Run assign | DONE态 | Reset清当前批次统计；再跑`DONE_OK`,Current strategy:3 | 同批货对比FIFO与AWRA；Reset不等于会话/快照硬复位 |
| 6 | 点 **Live/Snapshot** 来回 | FIFO快照有效+当前AWRA布局 | 画面在FIFO快照与AWRA实时布局间跳变 | 只讲布局差异，不把快照切换当算法KPI验证 |
| 7 | 单件流程:点 **ID 值框**→Numpad 输 `901`→OK;同法 Weight=`61.5` Freq=`2.5` Volume=`0.3`(Name 可跳过) | WAIT_INPUT(DONE 态先 Reset!v3 步5 已 Reset 过→此处在 Score 批量后,**先点 Reset+Load demo 重建底盘**再单件,或接受在 DONE 态不响→标准流程=**先 Reset** | 输入框各弹 Numpad(标题 Goods ID/Weight kg/…);框内值更新 | 五属性录入,业务 ID 与数组下标解耦 |
| 8 | 点 **Add goods** | WAIT_INPUT+五属性已填 | `SUGGEST_X=?_Y=?`;推荐格**黄**;Target X/Y、Path m、Travel s 四框刷新 | 在线推荐:评分最优空位预览(此时未落位,已登记) |
| 9 | 点 **Animate** | SUGGEST 刚给出(有 pending 目标) | crane 沿路径走到黄格(几秒);Path 蓝格短暂叠加 | 堆垛机动画=路径可视化 |
| 10 | 超重边界:ID `902`、**Weight `150`**→Add goods | WAIT_INPUT | **`ALARM_NO_FEASIBLE_SLOT`**;右上红灯应亮(VisuAlarm=TRUE;若灯不变色=Lamp 图像项,解说口径:状态文本即报警) | 边界#9:150kg>底层限重 100kg,全仓无可行位 |
| 11 | 点 **P2 Records**→Next/Previous→Query ID=5→Query | P2 | 表格10行/页；完成901推荐与902失败登记后Total goods预期22；ID=5命中，999未命中 | 记录分页+业务ID查询；Total含已登记未落位件 |
| 12 | 若v4已Logout则先在P3重新Login；改Light handling T1→再Query→切**Live**→Logout→Back P1→Reset | 管理员态 | 派生值刷新；Reset清当前批次，但快照/检修等跨批状态按设计保留 | 收场显式切Live+Logout+Reset；不称“画面/会话全部回初始” |

## 已知口径与坑(解说员须知)
- **Space utilization 显示口径**:UtilPct=占用格/400 的**比例值**(含 133 预置),%.1f 显示——
  20 件+预置=153/400=0.38→显示 **0.4**(不是 0.4%,是 38%)。解说时说"约四成"。
- **同批 20 件下 Score 与 AWRA-LS 统计可能同值**(186.0/370.0,局部搜索零交换)——演示"策略
  差异"靠**布局分布对比**(步 6 快照跳变),不靠统计数字;要数字差异需更大批量。
- **0711 已修**:Reset 后二次批量统计三项显示 0 = FB 沿冻结 bug(FB_Stats/FB_InitWarehouse,
  完成即撤沿修法);若再见此症状=修复回退,查 04 文件两处 `xOld := xExecute AND NOT xDone`。
- **Total goods 含 SUGGEST 登记件**:单件 Add 后未 Run assign,P2 计数已+1——话术:"已登记 21 件,20 件已落位"。
- **DONE 态不收 Add/Load**:重来永远先 Reset(状态机驻留设计)。
- **SNAP_SAVED 文本被 10ms 覆写**:Save snapshot 的确认信号看 **Snapshot valid 灯**,不看 Status 文本。
- **901/902/903**:仅为单件演示的示例业务 ID(demo 批是 1..20),Query 它们未命中是正常语义(先 Add 过才命中)。
- **红灯(Lamp)**:VisuAlarm 变量通道已实证正确;灯体不变红属 Lamp 图像配置项(0711 GUI 待配/放弃项)。
- **快照不跨 download**(非 RETAIN):每次重新下载后快照灯灭,属正常。
- **录屏保险**:间歇输入失效若复发(0711 洗后未再现),按攻坚文档 §4 采证;录屏前冷启一次=最大健康窗口。

## v4 增补:驾驶舱四镜头(0711 画面一波流后生效;插入位=步 6 与步 7 之间,或独立加演)

> 前置:三页元素已生成；GUI洗/在线长跑状态须现场确认。**当前长跑待验证，不得预写通过。**
> 新动作元素 7 处:Auto strategy 钮 / PIN 输入 / Login / Logout / X / Y / Set-Clear 钮。

| # | 操作 | 前置状态 | 预期(Status/数值/颜色) | 解说要点 |
|---|------|---------|------------------------|---------|
| v4-1 | **检测器镜头**:Load demo后看SCENE DETECTOR | N=20 | REC=`AWRA_HOLISTIC` | 默认画面只讲当前holistic推荐；81.6%是其sim评估。当前源码虽有lex/CB/TOB切换但尚未AB复测；lex 98.5%仅在`excess_fail=0`可比域，不能绑在本画面推荐或源码存在上 |
| v4-2 | 点 **Auto strategy**(灯亮)→观察 STRAT | REC 已给出 | 灯亮;STRAT=3(推荐自动生效);再点一次灯灭=人工模式(覆盖开关,同类赛原文要求项) | 自动/人工双模:信任机器可全自动,专家可随时接管 |
| v4-3 | **ST守卫镜头**:未登录改参数→同一扫描前置守卫回滚；Login后再改→保持；Logout | 单客户端演示态 | 当前源码已前置ParamGuard并加T62值级断言，但尚未AB复测 | 不称画面锁/双闸/多客户端隔离；现场只在AB复测通过后演示 |
| v4-4 | **检修镜头**:管理员态首选X=`1` Y=`0`（确认空闲后）Set/Clear→紫；Add轻货应绕开；恢复；未登录应拒绝 | 管理员态 | 检修位禁新入库、存量可查询；Reset不清 | **不称存量原地冻结**，局部重定位仍可能搬动既有货 |

**待执行的长跑验收清单(GUI洗后、用户在场)**:①冷启RUN→READY_PRECOUNT=133 ②v4-1画像
③v4-2 开关双向 ④v4-3 双闸(跳回+保持+清 PIN)⑤v4-4 挂/恢+权限门+紫格 ⑥12 步回归抽查
(Load→AWRA→Run→DONE_OK→统计非零→Query 命中)⑦Reset 后检修紫格保持(设计验证)。
