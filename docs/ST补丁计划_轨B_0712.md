# ST 补丁计划（轨B · 0712）— D-1 CB/TOB + N4 全修 + N5-ST 子集

> **执行状态更新（0712）**：当前源码已落入near平局统一、CB/TOB/lex路由及T60-T69真实断言；T70-T75仍为恒TRUE预留，且没有新一轮AB读回。本文其余内容作为实施计划/历史依据保留，不得把“源码已写”当“AB已通过”。

> 执行人：Claude（全部 .st 由 Claude 写，Codex 禁入——分轨防冲突已双向确认：Codex 0712 实施计划明言"无 PLC 可执行 ST 变化"，其 04 注释已留话"统一代码另由 ST 批次处理"）。
> 验法：AB 关闭状态下 `powershell -File tools\ab_scripting\ab_sync.ps1`（编译 0 errors + `iPassed=INT#<新总数>` + `iFailed=INT#0`）。
> 规格来源：`Claude_N4复核回执_0712.md` / `Claude_N5复核回执_0712.md` / 只读采集（CB/TOB 算法、挂载点、测试地图）。数字口径以 N5 口径字典为准。

---

## 〇、人话摘要（你审方向用，5 分钟）

**要干什么**：给 PLC 补两个缺失策略（CB 分级存储 / TOB 全周转），让"检测器达最优 98.5%"从仿真值变成控制器上可真跑；同时修掉审计挖出的全部代码隐患（越界、越权竞态、溢出、跨批污染等 15 项）。

**顺序**（先保演示安全，再上创新，后清隐患）：
1. **地基**（半天）：测试数组扩容 + near 平局规则统一
2. **两个演示炸弹**（半天）：连点 21 次载入会数组越界；越权改参数有一拍生效窗口
3. **CB/TOB 补实现**（2-3 天，大头）：算法移植 + 检测器路由 + 黄金向量锁
4. **隐患清扫**（1-1.5 天）：ID 校验/策略锁存/参数钳位/批次快照/计数器加宽/检修冻结/除零
5. **验收**：每阶段 ab_sync 全绿；最后请 Codex 对新代码做一轮对抗审查

**需要你拍的 4 个设计决策（都带推荐，答"全按推荐"即可开工）**：
| # | 问题 | 推荐 | 理由 |
|---|---|---|---|
| D1 | near 平局两端相反，统一到哪边？ | **改 ST 跟 Python**（时间→层→列） | sim 是真相源且全部数字刚由 Codex 重生锁定；改 Python 会让 390 组布局/能耗再变一遍 |
| D2 | CB 的"类起点偏移"用哪种语义？ | **忠实 Python 动态版**（每件货对当前剩余空位重算序，分片进状态机） | 静态表近似会偏离 Python，98.5% 对不齐等于白补 |
| D3 | 检测器"词典序档"要不要上 HMI？ | **要**（P3 管理页加档位开关） | 已拍板的演示方案="现场切词典序档真跑 98.5%"，没有开关就演不了 |
| D4 | 检修位"存量冻结"语义？ | **真冻结**（重定位跳过检修源位） | 文档一直这么声称；代码只加一行判断，是最小修 |
另 2 个授权确认：N4-20 违规扫描扩展、N4-22 快照重建（N4 回执的 9 项授权清单没含它们）→ **推荐都做最小版**，一并授权即可。

**工时**：全做 ≈ 4-5 天（与 ASK 决策"乙全做 4-6 天"吻合）。你只需：拍上面 4+2 → 等我每阶段交 ab_sync 结果 → 阶段 3 后到 AB 里看一次演示预演。

---

## 一、与 Codex 轨的边界（防撞车，已核对其 0712 实施计划）
- Codex：sim/*.py、文档、生成器、二进制、CSV 拆分、图源——**不碰任何 .st 可执行代码**、不重生 `plc/08_*`、`--emit-st` 不传。
- Claude（本计划）：**只碰 `plc/*.st` + `sim/export_st_vectors.py`（扩黄金向量）+ 顺手修 `sim/strategy_lib.py:202` 的 st_ifelse 生成器 bug（漏 `n>20`，一行）**。
- 冲突面=零。唯一时序约束：阶段 2 的向量导出在 Codex Task 12 重生数据**之后**跑（用同一基线），避免向量与新 CSV 不同源。

## 二、逐文件补丁

### 阶段 0 · 地基
**P0.1 测试基础设施扩容**〔`plc/06_PRG_Test.st`〕
- `N_CASES := 59` → 常量化（呼应 N4-08）并升至 **75**；`aResult: ARRAY[1..59]` → `[1..75]`。
- 新测试统一从 T60 起编（T60-T74 规划见各补丁）；未用位先置 TRUE+注释"预留"。
- 风险：`ab_sync` 判定字符串是精确匹配 `iPassed = INT#59` → **同步改 `tools/ab_scripting/run_test.py`/判定处为新总数**（Grep 确认所有 59 判定点）。

**P0.2 near 平局统一（N5-01，按 D1 推荐）**〔`plc/04_FB_Warehouse.st:100-102`〕
- near 分支 `xBetter := rT < rBestTime;` → 等时显式次级比较：
  `xBetter := (rT < rBestTime) OR ((rT = rBestTime) AND ((y < iBestY) OR ((y = iBestY) AND (x < iBestX))));`
  即 (时间, 层tier, 列col) 与 Python `warehouse_sim.py:283` 全序一致。
- 同步把 Codex 刚改的注释再更新为"已统一 (时间,层,列)=Python 语义（0712 轨B）"。
- **T60**：构造只剩 (0,1) 与 (4,0) 两个等时可行位（预占其余），断言选 (4,0)（Python 语义）。
- 风险：T37/T47 用的是唯一最短位，不受影响（已核）；演示 20 件 near 布局可能变 10 件位置（探针已量化，exp_t 不变）——演示脚本以 AWRA 为主线，near 仅对比用，可接受；若演示卡口径，回退开关=保留旧行为常量。

### 阶段 1 · 两个演示阻塞
**P1.1 LoadDemo 容量护栏（N4-15）**〔`plc/05_PRG_Main.st:100-107` + `sim/export_st_vectors.py:116-128` 生成的 `FC_LoadDemoGoods`〕
- 调用前判 `GVL_WH.iGoodsCount + GVL_Data.N_DEMO <= 400`，不满足 → `VisuStatusText:='ERR_DEMO_FULL'`+红灯，**不做部分写入**。
- 生成器侧同步在函数体内加同判断返回（双保险；改的是 export 脚本模板→重生 `07_GVL_Data_generated.st`）。
- **T61**：填至 396 件后 LoadDemo，断言 iGoodsCount 不变+错误文本。

**P1.2 ParamGuard 前置（N4-14）**〔`plc/05_PRG_Main.st:198-206` → 移至 `:56` 状态机 CASE 之前〕
- `fbAuth`/`fbGuard` 调用块整体移到 CASE 之前：守卫先于一切消费者执行，越权写在**同一扫描周期内**先被回滚再被消费。
- 保序细节：fbGuard 依赖 fbAuth.iLevel，两者一起前移；段尾脉冲复位 `:248-264` 位置不动。
- **T62**：模拟越权（iLevel=0 时写 rDelta）→ 断言同拍 FC_CalcScore 用的是回滚后值（通过一次 fbSel 选位结果验证）。
- 风险：移动后 guard 每拍都跑（原本也是）；确认无对 CASE 内变量的前向依赖（已读原块：无）。

### 阶段 2 · D-1 CB/TOB（创新核心）
**P2.1 枚举与数据结构**〔`plc/02_GVL.st`〕
- `SelStrategy` 语义扩：`4=CB 5=TOB`（注释+HMI 下拉数据源同步）。
- `GVL_WH` 新增：`aSlotOrder: ARRAY[1..400] OF INT`（时间序表，存 x*20+y 编码）、`iCbClassOf: ARRAY[1..400] OF INT`（0/1/2）、`aCbStarts: ARRAY[0..2] OF INT`、参数快照结构 `stRunParam`（呼应 N4-19/P3.4，rVx/rVy/权重/开关一次拍齐）。

**P2.2 新 FB：FB_AssignClassTurnover（CB+TOB 共用，分片状态机）**〔`plc/04_FB_Warehouse.st` 新增〕
- 入参 `iMode(4=CB/5=TOB)`；批开工：拍 `stRunParam` 快照 → 按快照速度构建全库时间序 `aSlotOrder`（插入排序分片，≤50 格/周期，键=(travel,tier,col) 与 Python `_slots_by_time` 一致）。
- **TOB**：遍历序=按 `(-Freq, Id)` 选择排序（复用 FB_AssignAllGoods phase2 框架，换键）；每件在 `aSlotOrder` 从 0 起找首个 `FC_CheckFeasible` 位（=Python `run_full_turnover` `strategy_lib.py:76-92`）。
- **CB**：先一遍累计频次定类界（70%/90%，`strategy_lib.py:95-121`）；每件 `start = aCbStarts[class]`，**动态语义（D2 推荐）**：start 相对"当前剩余可行序列"——实现=游标跳过已占位计数，而非静态下标；找不到回退 start=0；仍无 → failed。
- 主程序 `05_PRG_Main.st:127-131`：SelStrategy 4/5 → 走新 FB；完成后直入 STATS（无 LS 阶段）。
- **T63** CB 类界单测（20 件已知频次→断言 A/B/C 划分）；**T64** CB 回退（填满近区→断言 C 类回退成功）；**T65** TOB 遍历序（断言首件=最高频）。

**P2.3 检测器词典序档（+D3 HMI 开关）**〔`plc/04_FB_Warehouse.st:998-1011` FB_SceneDetect + P3 管理页〕
- 加入参 `iObjective(0=holistic 现状/1=lexicographic)`；lex 分支=移植 `strategy_lib.py:162-167` **权威版**：`density>=0.70→CB(4)`；`density<=0.25 AND n>20→AWRA(3)`；`ELSE→TOB(5)`。
- ⚠ 权威=`select_strategy()`；`st_ifelse()` 生成文本漏 `n>20`（`strategy_lib.py:202`）——**顺手修该行**并注释"0712 修：与 select_strategy 对齐"。
- HMI：P3 页加 `SelObjective` 开关（演示"现场切档"入口）；GVL 加对应变量。
- **T66** lex 路由三分支（density 0.75/0.20+n=60/0.45 → 4/3/5）；**T67** tiny_light(n=20,density 0.075) → TOB（防 st_ifelse 旧 bug 复发的回归锚）。

**P2.4 黄金向量管线扩展**〔`sim/export_st_vectors.py` + `plc/07_GVL_Data_generated.st`〕
- 新增导出：同 demo 批（seed=2026/20 件/匀速/sum）跑 `run_class_based`/`run_full_turnover` → 终局逐位 `aCbExpect/aTobExpect` + 聚合（ExpT/Energy/Placed/Failed）。
- **T68** CB 端到端锁（逐位=0 差 + 聚合容差同 T25）；**T69** TOB 端到端锁。
- 时序：在 Codex Task 12 重生数据后执行（同基线）。

### 阶段 3 · 隐患清扫（N4 高优先 + N5-ST）
| 补丁 | 规格 | 改点 | 测试 |
|---|---|---|---|
| P3.1 ID 契约（N4-16） | 拒 `iId<=0` 与重复 ID → `iErrId=3`/`ERR_BAD_ID` | `04:743-756` FB_GoodsInput | T70 |
| P3.2 策略锁存（N4-17） | ASSIGN 完成分支用**锁存** `xUseRank`，不重读 SelStrategy | `05:141-147` | T71（中途切换不改路径） |
| P3.3 分片钳位（N4-18） | `nBatchPerCycle/nPairsPerCycle/nMaxLsRounds` 消费前 `MAX(1,…)` 本地钳位 | `04:198,288,337,384` 四处循环头 | T72（置 0 仍收敛） |
| P3.4 批次参数快照（N4-19） | `stRunParam` 批开工拍齐；Assign/Improve/新 FB 全程只读快照（P2.2 已建结构，此处接线 AWRA 路径） | `04` FB_AssignAllGoods/LocalSwapImprove 头部 | 并入 T62/T71 断言 |
| P3.5 违规扫描最小版（N4-20·待授权） | FB_Stats 加全仓一致性：坐标界+Occupied↔GoodsId 双向+一货一位 | `04:411-459` | T73 |
| P3.6 跨批统计清零（N5-05） | Reset 与批开工双清 `SwapCnt/LsRounds` | `05:35-54` Reset 块 + Assign 启动 | T74（AWRA→Reset→score 后=0） |
| P3.7 RelocCnt（N5-06） | `ST_Stats` 加 `RelocCnt`；主程序抄 `iRelocs`；状态文本 `SWAPS+RELOC` | `01_DUTs.st:55` + `05:159` | 并入 T74 断言 |
| P3.8 计数器加宽（N5-09/10/11） | `aSlotOps`→UDINT+饱和；`iSwaps/iRelocs/SwapCnt/RelocCnt`→DINT；`iFailCnt` 加 `<32767` 饱和 | `02:82`/`04:251-252`/`04:1036,1047`/`01_DUTs` | 现有 T58 回归+编译期 |
| P3.9 检修真冻结（N4-23/A3·D4 推荐） | 重定位 phase1 源位判 `aMaintBlock[X,Y]` 跳过 | `04:290` 加一条件 | T75（冻结位存量不被搬走） |
| P3.10 FC_TMax 除零钳位（N4-24） | 与 FC_CalcTravelTime 同款 `<=0` 钳位；**不改公式本体**（动公式=动 T19/T25 锁，仅注释说明 accel 不参与分母=已知口径） | `03_Functions.st:174-178` | T26 组已有分母保护测试扩一断言 |
| P3.11 快照重建（N4-22·待授权） | CmdSaveSnap 从 `aSlots.Occupied/PresetOcc` 重建纯态，不抄 VisuSlotState | `05:233-239` | 目测+演示预演 |

> 测试编号 T60-T75 共 16 个；若 P3.5/P3.11 未获授权则相应编号回收为预留。

### 阶段 4 · 验收与质量门
1. 每阶段末：AB 关闭 → `ab_sync.ps1` 全流程（0 errors + iPassed=75/iFailed=0——判定字符串已随 P0.1 同步）。
2. 阶段 2 后**演示预演**（需你在场一次）：AB 在线 → SelStrategy=4/5 手选各跑一批 → 切 SelObjective=lex → 检测器推荐 CB/TOB → 台词"现场切词典序档"成立。
3. 复杂度自查（DR-5 #7 技术债门）：新 FB 单文件 ≤300 行、嵌套 ≤4 层、每周期分片上限有常量。
4. 全绿后：**请 Codex 对新增/改动 ST 做一轮对抗审查**（N2-N5 同款，规格=本计划），审查通过才并入演示脚本重录。

## 三、风险与回退
- 最大风险=CB 动态 starts 的分片实现复杂度（O(n·400) 扫描×分片状态机）——回退方案：先交静态近似版（标注偏差），动态版随后；98.5 演示话术在两版间的差异由向量锁 T68 量化。
- near 统一后 demo near 布局变化（≤10 件、exp_t 不变）——演示以 AWRA 为主线，风险低；必要时演示脚本注一句。
- ab_sync 判定字符串多处硬编码 59——P0.1 先 Grep 全仓 `INT#59` 逐一同步，漏一处=假红。
- 一切失败如实报：任何 T 变红先修再进下一阶段，不带病推进。
