# L7 拟真度阶梯(三档展示)设计稿

> 状态:方向已由用户拍板(2026-07-05:"分3档展示""充分采纳""时间充足");
> 细则参数待 deep-research 应用场景研究回填(在途)。
> 本文件=机器工作区设计稿,定稿要点将进 canonical(参数)与差异化战略 §8(排期)。

## 0. 用户拍板原文与解读

- "把拟真度分为 3 档——尽可能贴切实际 / 最大杠杆 2 项 / 数据模型,我们现在处于哪一档?可以分 3 档展示。"
- "新加入的构想很好,充分采纳,时间充足。"(= 放宽"转化优先、新建模从紧"的预算假设)
- "用 brainstorm + deep-research 思考项目实际应用在哪、场景什么样、对机器什么需求,举一反三自己处理。"(= 应用场景研究授权自主,已启动)

## 1. 三档定义与现状映射

| 档 | 用户语汇 | 项目对应 | 状态 |
|---|---|---|---|
| 档1 | 数据模型 | **对照口径**:匀速+固定权重+纯入库流+装卸不计+频次全知 | ✅已有(文献可比层,F4) |
| 档2 | 最大杠杆项 | **主口径 H**:梯形加减速(A4b)+场景自适应权重(E v2);外加点状拟真实验:混合流/双命令(F8)、循环访问+夜排(F11)、漂移/退化压力(F12) | ✅已有(头条来源+独立实验) |
| 档3 | 尽可能贴切实际 | **综合工业场景**(新建):在 H 物理口径上**叠加**装卸时间+泊松到达/峰谷+频次估计噪声+故障注入,一个场景同时开全部拟真开关 | ❌不存在,L7 新建 |

**现状档位判断:处于档 2。** 档 3 的零件已有一半(负载层实验各自独立存在),缺的是"叠加在同一场景"与"三档并排呈现"。

## 2. 关键设计决策(实施前提)

1. **档 3 不动头条**:H 锁与回归锁全部不变。档 3 定位=「全拟真验证章」——证明主口径的结论(策略排序、降幅方向、违规 0)在最真实场景下依然成立。这样避免口径变更全流程(拍板→canonical→07/08→全量回归)对头条的冲击,实施成本降一个量级。
2. **分层成本纪律**:档 3 第一版只叠加**负载/运维层**四项(装卸常数、泊松到达、频次噪声、故障事件)= 纯仿真侧,PLC 零改动,不触碰双实现同步铁律。空间结构类(I/O 双口分离、异构仓位)= 档 3.5 扩展,做不做等档 3 结果再议;S 曲线 = 仅敏感性附注(F15 已证物理缩放不变性,"知道下一层在哪且验证过没必要"本身是叙事)。
3. **参数纪律**:档 3 全部新参数(T_load/T_unload、泊松强度 λ、噪声水平、MTBF/MTTR)进 canonical 新增 G 节,初值标「默认待确认」,deep-research 回填后升级为「行业典型值(来源:研究报告)」;一切固定 seed。
4. **展示形态**:同一组核心指标(C3 期望取货 / C8 违规 / C9 吞吐)× 三档阶梯表 + 阶梯图(fig16),报告新章"拟真度阶梯:从数学模型到工业场景"。叙事主张 = **R7 口径不变性的形状化升级**:"结论方向三档不变,绝对数逐档更真——每加一层现实,优势变小但不消失,这正是可信的形状"(预期:档 3 相对降幅小于档 2,提前声明,防评委反问)。
5. **与已定决策的关系**:评分函数四项结构不动(§6 不翻案)——brainstorm 方向 9(订单波次聚簇)与之冲突,**不实施**,进决赛展望一句话;方向 10(不加)被用户"充分采纳"实质否决,但其精神保留于"档 3 不动头条"决策中。

## 3. 实施拆解(L7.x,编入 S1-S2 空档;T 号让给 PLC 测试用例命名空间)

| # | 内容 | 产物 | 前置 |
|---|---|---|---|
| L7.1 | 档 3 场景引擎:`sim/realism.py`(装卸常数+泊松到达+频次估计噪声+故障注入,各自独立开关+组合模式,seed 纪律) | realism.py + out/realism_matrix.csv | 参数值等研究回填(先用占位默认跑通) |
| L7.2 | 三档对比实验:档1/档2/档3 同批货物同 seed 跑核心指标 | out/realism_matrix.csv + figs/fig16_拟真度阶梯.png | L7.1 |
| L7.3 | 报告新章+口径矩阵联动:build_report 加"拟真度阶梯"章(数字全 CSV 注入),附录 C 口径矩阵加"拟真档"列,PPT 加阶梯页 | 两个 build_*.py 更新 | L7.2 |
| L7.4 | canonical G 节登记+实验发现 F17 | canonical v2.1 + F17 | L7.1-2 |
| L7.5(可选) | ST 侧激活 T_load/T_unload 输入参数(A8 骨架已留,默认 0 保回归) | plc/03 小改+07 重生成 | 用户拍板是否要 PLC 侧也带装卸 |
| 档3.5(暂缓) | I/O 双口分离、异构仓位几何 | — | 档 3 结果+用户再拍板 |

## 4. deep-research 回填槽位(**首轮研究 0705 深夜撞 API 会话限额中断,限额北京 20:00 重置后重跑**)

> 首轮残骸处置:workflow 报 18 claims "refuted" 但全部 0-0 票 = 验证代理没跑起来的
> 假阴性,不是真证伪,一律不采信;仅存 1 条 primary 级线索(国标局官网):
> **中国现行堆垛机行业标准 = JB/T 7016-2017《巷道堆垛起重机》**(1993 版已废止)——
> 重跑时优先拿它的速度/加速度/试验条款,比 FEM/VDI 更贴中国比赛语境。
> Mecalux 博客"再生制动省 15-20%"等碎片未经验证,只作重跑线索不作引用。

- 〔G1 装卸时间〕货叉存取循环典型值(s):______(来源:____)
- 〔G2 到达过程〕工业库入出库节拍/峰谷模式:______
- 〔G3 故障画像〕堆垛机 MTBF/MTTR 数量级:______
- 〔G4 参数对标〕vx/vy/ax/ay 现值 2.0/0.5/0.5/0.3 vs 行业范围:______(A4/A4b 背书或修订建议;线索:JB/T 7016-2017)
- 〔G5 标准对标〕FEM 9.851/VDI 3561/JB-T 7016-2017 周期时间定义 vs 切比雪夫/C1/C3/C10:______(若一致=模型合规性背书,进报告 §1)
- 〔G6 场景锚定〕400 仓位单巷道对应的真实行业/货物单元/重量范围:______(进报告引言;B3 重量分布 U[5,80]kg 校准)
- 〔G7 能耗实践〕再生制动实际回收率 vs η_regen=0.40 假设(C4b 展望口径校准;线索:厂商宣称 15-20% 待核);AS/RS 客户选型指标(报告"工程价值"话术)

## 4b. L7.1/L7.2 首跑形状(0705 深夜,占位参数,数字不进报告)

- 引擎跑通:100 周期三档,**档1/档2 与 F8 逐位同源**(seq 4750/awra 2022/2689s,复用 run_mixed 兑现);
- 阶梯形状:awra vs seq 出库均时降幅 **60% → 53% → 53%**——匀速口径高估优势约 7 个百分点,工业场景下优势稳定(≈53%),方向叙事成立;策略排序三档保持,违规三档全 0;
- 两个候选新发现(参数定稿后重跑再定稿成 F17):①AWRA 布局质量对频次估计噪声 σ=0.3 稳健(Rank 多因子+LS 兜底);②档3 响应时间视角优势被排队放大(awra P95 689s vs seq 971s)——"可用性"语言,工业客户视角。

## 5. 验收标准(L7 完成的定义)

1. `python sim/realism.py` 一键跑三档,CSV/图确定性再生;
2. 三档下策略排序不变、awra 违规 0(若变,如实报告并定位机理——负结果也是结果);
3. 报告阶梯章数字零硬编码,doc_check+19 用例+H 锁全绿;
4. canonical G 节每个参数有来源标注;
5. 用户能用一句话向别人解释三档("数学上界—工程主口径—工业场景验证")。
