# 续跑提示词（0713 夜版 · compact 后接力用）

> 用法：compact 后新上下文读本文即恢复全部状态。本文=唯一接力入口；深挖按文内指针。
> 历史版本（0712R）已被本版取代——F3 已经历 Codex 现场终止与 Claude 0713 夜接管，旧版描述的"画面洗/主线队列"全部完成或改拍。
> 沟通惯例不变：三层汇报制、待拍板集中列、永不派 fable 级 agent（一律 ≤sonnet）、用户=唯一终审人。

## 一句话现状
**/overnight 0713 夜挂机进行中（Claude 已从 Codex 手中接回 F3 现场）**：H0-H1 已完（85/85 测试、fault lock 五门落地，commit `adf9eae`/`c31a67b` 已推），F3 红灯中，当前在 **H2 起点**——Factory I/O 已全新启动但未载场景未 preflight。Codex 正在跑轨A批次+H1 对抗审查（用户已启动），回执到达要处置。

## 立即恢复三步
1. 读 `docs/overnight2-report_0712night.md` 三节：①「0713 夜 Claude 接管 F3 · 5 轮 13 问拍板」（最高拍板真相）②「0713 夜挂机相位规划」（开工单）③「四、时间线」最新几行（进度指针）。
2. 现场真相源按需深挖：`l2_factoryio/F3_现场故障与Claude接管_20260713.md`（Codex 交接总账+原始证据索引）、`l2_factoryio/io_map.md`（点表+0713 现场纠错）、`l2_factoryio/ClaudeCode接管提示词_F3_20260713.md`。
3. 从相位规划最新 ⬜ 继续（当前=H2），连续满负荷。

## H2 操作清单（当前相位，按序）
1. computer-use 截图确认 Factory I/O 状态 → GUI Open 载入 debug 场景（My Scenes，文件名畸形 `Automated WarehouAutomated Warehouse_debug_prebaseline_0713se.factoryio`，对话框选择勿拼路径）。
2. EDIT 模式开 console（Backslash）执行 `app.web_server = True`。
3. `python l2_factoryio/webapi_probe.py dump` / `find`（H1.5 全量 tag 枚举，找 X/Z 实体坐标 tag——若找到，"状态失配"假设一测便知，H4 几何 A/B 可能整段跳过）。
4. `webapi_probe.py emitter-off`（治理强制值陷阱）+ GUI 检查 Emitter 配置、调试期固定单一箱型（拍板#5：调试固定+9/9 验收恢复混出）。
5. RUN → `python l2_factoryio/f3_stacker_control.py snapshot` 空场 preflight（全零输出/one-hot Middle/Target=0）。
6. H3 开工前先搭录屏（视觉门=录 10-20s 逐帧判读；候选 ffmpeg gdigrab（`C:\Program Files\kedou\` 有 ffmpeg）或 CopyFromScreen 连拍）。
7. 相机导航：`l2_factoryio/rdrag.ps1 -dx N -dy M`=右键拖转向（Unity 需相对位移，SetCursorPos 无效）；W/S/A/D=hold_key 平移；每次 rdrag 前先 mouse_move 进视图防右键落错窗口。

## 双 AI 协作
- Codex 任务=致Codex.md 顶部【0713夜 ✅轨A批次启动+H1对抗审查+新常态复核机制】；其回执到 `_通信/致Claude.md` → 审+签+双留痕。
- **新常态（拍板#10）**：每完成一个 F 级任务→给 Codex 签发独立复核任务书。
- H1 对抗审查若报致命漏洞→先修锁再进现场。

## 节奏铁律（用户两次批评偷懒后固化）
任务完成立即进下一个；每小时账本时间线写反思行；每相位收口=commit+push（**仅限 `l2_factoryio/`、`docs/`、`_通信/` 三路径，push 已获 0713 本夜批量授权**）；同一子问题三连败→冻结分支→写 Codex 复核任务书→转 F8（全作品优化，不进文档线/报告线）；额度尽 ScheduleWakeup 自动醒（1800s 兜底循环已挂：触发时正常就重设，中断就续跑）。

## 红线速记
fault lock 语义不可绕过（锁下只许 snapshot/stop/mark-fault，解锁唯一路径=完整重启 Factory I/O+`reset-epoch --operator-confirmed-restart`+空场 preflight 实过；**F6 不算重启**）｜恢复后的货物绝不 travel｜I/O 完成≠货物完成，关键门以画面为准（录屏逐帧）｜不碰 1_给你看/2_你要操作/canonical/账号密码｜AB GUI 开着绝不跑 ab_scripting｜素材口径="Python 直控技术预演/54 格代表性物理 I/O 验证"，禁称 400 格孪生/AC500 闭环｜9/9 前 F3 不签绿不录正式素材｜禁 superpowers skills｜`git add` 只点名路径绝不 -A｜7 月内清完全部 AB 在线活（8/2 许可到期）。

## F3 之后的既定队列（13 问拍板）
F3 绿→顺进 F4（AB OPC UA 3b，成败均档）→F5 联调（不通走 CODESYS 兜底，撞登录墙即停）→C5 三任务→**C6 自研 3D（F5 后立即做）**→F6 回补（C5 绿后触发，备份→合入→75/0 复验→commit）。悬置等用户：PPT 重生、quiz、三课重排、录屏讲解词配音vs字幕、L2 科普 docx（全线完工后写）。
