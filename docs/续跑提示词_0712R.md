# 续跑提示词（0712R · compact 后接力用）

> 用法：compact 后新上下文读本文即恢复全部状态。本文=唯一接力入口；深挖按文内指针。
> 用户在场、昼伏夜出。沟通=三层汇报制（三句话人话→你要做的→细节路径）；一次只推一个主线；待拍板集中列；**永不派 fable 级 agent（一律 ≤sonnet）**；用户=唯一终审人。

## 一句话现状
**overnight 第二轮（0712 夜）进行中**：主攻 Factory I/O 快线+正线（L2 层），用户 11 问拍板全在 `docs/overnight2-report_0712night.md` §〇（双线并行/连续满负荷/3b 优先/全自动回补一条龙/Codex 夜班全自动）。第一轮成果（T0-T6+续航 A/B/C）见 `overnight-report_0712pm.md`，勿重做。Codex 基线阻塞已解（plc_evidence.csv 逗号 bug 已修，core_smoke PASS），其轨A 批次运行中（PPT 延缓）。

## 立即动作（compact 接力唯一入口）
1. 读 `docs/overnight2-report_0712night.md`（本轮唯一账本：规格/11 决策/任务队列 F0-F8/时间线）→ 从队列第一个 ⬜ 继续，**连续满负荷**（用户批评过值守太慢——干活循环不是巡检循环，任务池尽按"完美标准"从全作品扩展，不进文档线）。
2. F2-F5 要点：Factory I/O 在 `F:\Factory IO.exe`（kedou 是视频下载器与此无关）；内置 Automated Warehouse 场景免搭建；快线=Engine I/O SDK（github realgamessoftware/factoryio-sdk）或 pymodbus server+Factory I/O Modbus client；正线 3b=AB 仿真 PLC 试开 OPC UA server（Symbol Configuration），成败均落档。
3. 双 AI 夜班：见到 `_通信/致Claude.md` 新回执→审+处置+签，双账本留痕；Codex 明晨对抗审查我的 L2 产出（任务书我写）。
4. ⚠ 硬约束：AB 许可约 8/2 到期（用户已解除试用期"顾虑"但日期事实仍在，录屏终验别拖过）；账号/密码操作=红线；2_你要操作 课纲原件不碰（三课若因 3b 成功而失效→账本记"待重排"）。
5. 关键教训（画面再改时用）：对已存在 visu 页补"交互动作"必须 GUI 手工配；元件本体/文本/布局可脚本（wash_visu_0712.py 模式）。ab_sync 前 AB GUI 必关（守门 exit 3）。

## 修复内容速查（勿重做）
R1-R8 全部已落码（对照表+连锁自查+复验后动作链）= `docs/轨B_R审查修复与复验_0712.md`。07 已重生（CB 2.495106/TOB 2.143856 零漂移）。**不要再改这些文件后忘记重跑复验。**

## 复验绿后的主线队列（用户拍板顺序，一次一个）
1. **画面洗 4+17 处**（用户/computer-use；4 新增清单=`docs/演示预演记录_0712.md §2`：SelObjective 开关/CB/TOB 按钮/"59/59"→75 标签/密集载入可选）。
2. 三页在线长跑（用户在场）。
3. 报告周（≈7/20-8/1）：B1 按 DR-1 重做 / B10 98.5 话术升级（现可写"CB/TOB 已入 PLC+AB 黄金向量+在线预演验证"）/ §6 空当——结构=`docs/报告大纲与草稿映射_0712.md`。
4. 提交包 manifest/requirements/allowlist 草案（Claude）。
5. 补课+quiz（约定=ST 收口后）。

## 关键索引（最小集）
- 指挥舱=桌面 `智储优控_作战面板.html`（每大节点更新 L1-L3 槽位+时间戳；口令「对齐面板」）。
- 文档地图=`docs/README.md`；层级裁决=`docs/真相源与文档层级_0712.md`（git>代码锁>复核回执>canonical>派生）。
- 口径终值=`canonical_assumptions.md`（C11=AB 证据条款）+`Claude_N5复核回执_0712.md` 口径字典。
- 双轨历史：轨A 终审=`Claude_0712合并修复终审回执.md`；轨B 计划=`ST补丁计划_轨B_0712.md`(头部有历史快照注)；对抗审查原文=`_通信/codex_out/轨B_ST对抗审查_0712.md`。

## 红线速记
sim 数不称"AC500 实测"（证据外推禁区：400件/watchdog/长跑/实体机/多客户端）｜ab_sync 前 AB 必关（脚本已守门）｜`git add` 只指定路径绝不 -A（1_给你看/2_你要操作 的 docx 严禁混入；本轮 Codex 重生的 1_给你看 docx **待用户核对后单独 commit**）｜真相源实质修改=提案制｜密钥/PII 不碰。
