# Codex 任务 C · 03 页整页重设计(4-Tab → 大图沉浸展陈风滚动式)

## 0. 项目底座(自包含,codex 必读)

- 仓库根 `F:\abb_wh_work`;目标文件 `F:\abb_wh_work\l4_showcase\src\03_FIO物理证据.html`(**文件名不得改**——00 导览与 Electron 壳按此名链接);现状约 1457 行,自封闭单文件(内联 style+script)。
- **视觉基准 = `docs/全站设计规范_0724.md`(必读全文)。** 本任务全权 03(含顶栏白红化 + 正文重设计)。
- 素材(只读,禁写):`F:\abb_wh_work\l2_factoryio\media\g4_evidence_0713\`(6 张站点证据图)、`media\F3_素材_cell30全链_0713\`(f_001–f_040 共 40 帧 + `F3_cell30_全链素材.mp4`)、`l2_factoryio\img\`(F2 映射图等)、`l2_factoryio\logs\`(F3_diagnose 日志,66 份)。src 出发相对路径 `../../l2_factoryio/...`。
- Electron 壳:03 在 iframe 内加载,壳已装 l2_factoryio 白名单静态路由(`app_shell/main.js`,commit 1ccf5c5),素材壳内可达;**滚动式在壳 iframe 内 OK**(iframe 内容区可滚)。
- Playwright 底座:Chromium `C:/Users/86177/AppData/Local/ms-playwright/chromium-1223/chrome-win64/chrome.exe`,args `['--use-angle=d3d11','--enable-unsafe-swiftshader']`;静态服务器 root=**仓库根**(素材才可达),MIME 含 .html/.js/.css/.json/.png/.jpg/.mp4;就绪判据 `load` + `document.fonts.ready`。
- 红线见规范 §6。

## 1. 背景与拍板

0722 把 03 做成 4-Tab 一屏版(现状)。用户拍板**推翻 Tab,回到「上下滚动依次介绍」**,但要**重新设计得更美观好看**。拍板细节(0724 二轮六问):
- 视觉基调=**大图沉浸展陈风**(实拍图/视频/40 帧带为主角,配精炼说明,展陈/杂志感)。
- 篇幅=**舒展留白**(每节可超一屏,充分留白与视觉呼吸,总长约 5–7 屏)。
- 内容叙事=**沿用四块顺序改滚动**:证据画面 → 证据链 → I/O 矩阵 → 边界与负结果(内容不变,只改版式与美观,不重编叙事线)。
- 顶栏=按规范白红工程风(消除现状深色 #111 顶栏)。

## 2. 目标与改动

把 03 从 4-Tab 改成**单页上下滚动、大图沉浸展陈风**,四块依序:

1. **顶层**:规范 §2 白底 ABB 红线顶栏(品牌区:ABB logo + 「智储优控 · 物理实证 · S3」+ 副标口径)+ 口径带(单行,措辞不改)。**删现状深色顶栏**。
2. **§证据画面**:三视图(总览/垂直观察/交接近景,现有 3 张 g4_evidence 图)做成**大图沉浸**——全宽或近全宽大图,配来源文件/采集阶段/证明什么/不证明什么四项元数据(现有内容)。点击大图 lightbox 放大。
3. **§证据链**:现矩阵 9 行的证据链内容,做成舒展的证据卡流(主张→缩略图→原始文件路径→采集时间→限制条件三色徽章)。
4. **§I/O 矩阵**:66 份 F3_diagnose 日志的 11 组×6 相位矩阵热格(数据管线 `tools/gen_f3_log_matrix.mjs` + `tools/f3_log_matrix.json` 已存在,复用)+ 六站点点表(点站看点表/语义/图证)。**54/66 口径**:「66 含验收前预演与验收后复核,其中 54=H5 验收链(9链×6相位)」——页面文案、矩阵高亮、判定折叠三处一致(现状已落地,迁移保留)。
5. **§边界与负结果**:七条(现 §neg)——**措辞逐字不改(红线)**,版式可升级为舒展卡片,可为有对应截图的条目配图(如 G4_lower_NOSTART)。
6. **底部**:40 帧带(f_001–f_040,懒加载,点击 lightbox)+ cell30 全链 mp4(`<video controls preload="metadata">`)——作为"物理素材库"节,大图带展陈。

**美观要求(大图沉浸展陈风,舒展留白)**:每节标题区留白充分;大图/视频占视觉主导;说明文字精炼(不堆砌);节与节之间有清晰视觉分隔与呼吸;配色/字号严守规范(白红工程风,不花哨);响应式(1280–1945 宽都好看,壳内 1552 宽为主战场)。

## 3. 内容红线(逐字保留,只改版式)

- **负结果七条**:施工前用脚本从现文件精确抽取七条 `.item` 的标题+正文存基线,施工后逐字符 diff 证明一致(现状 QA 有 negResultsVerbatim 断言,沿用)。
- **54/66 口径**四处一致;77/0 引用保留原语义位;命名总表词根。
- 素材真实引用(禁编造),40 帧全引用+懒加载,mp4 真嵌入。

## 4. QA 重写(版式变了,QA 须适配)

现有 `tools/capture_fio54_evidence_qa.mjs`(19 项)中:
- **废弃**(Tab/一屏零滚动相关):`zeroScrollAllTabsOk`/`tabStateAllOk`/tab 切换类、每 Tab 零竖滚——滚动式不再一屏,这些作废。
- **保留并适配滚动式**(内容红线):`noConsoleErrors`、`noFailedRequests`、`allImagesLoaded`、`negResultsVerbatim`(负结果逐字)、`assetPathsExist5`(≥5 素材路径 200)、`matrixNumbers66_54_11`、`videoElementExists`、`frameThumbCount40`、`caliberSentencePresent`、`blindFiveSecond`、`grid2020OnlyInBoundary`、`noForbiddenTerms`、`strayOk`(无 mock 泄漏词如"变体/候选")、`noHorizontalScroll`(横向仍禁,纵向允许滚动)。
- **新增**:四节按序存在且可滚动到、每节标题可见、lightbox 开合(× / 点遮罩 / Esc 三路径,参考现有 03 lightbox 实现 03_FIO物理证据.html:1414-1431 已完备)、双分辨率(1552×900 + 1945×1094)无横向溢出、大图节的图片实际渲染(非 0 尺寸)。
- 截图底座:`{animations:'disabled'}` + `document.fonts.ready`。

## 5. 验收标准(须实测,数字原样贴)

1. 新版 03QA 全绿(重写后的项数与逐项 pass);命令 `node tools/capture_fio54_evidence_qa.mjs`(cwd=l4_showcase),原样输出。
2. 负结果七条逐字符 diff 结论(独立于 QA 再验一遍)。
3. 双分辨率(1552×900 壳内 + 1945×1094)整页滚动截图(每节至少 1 张),证明大图沉浸展陈风、白红顶栏、舒展留白、无横向溢出、素材真实渲染。
4. 壳内验证:起 Electron 壳(`cd l4_showcase/app_shell && npm start` 或 `verify` 脚本),03 在 iframe 内素材可达(png/mp4 200)、滚动正常、控制台零 error。
5. `git status` 证明只动 `03_FIO物理证据.html` 与 `tools/capture_fio54_evidence_qa.mjs`,未碰素材/sim/用户文档。

## 6. 交付格式

`git diff --stat`;四节版式设计说明(大图沉浸怎么落地);QA 废弃/保留/新增清单与实测输出;负结果 diff 结论;双分辨率每节截图路径;壳内验证结果;偏离规范之处与理由。**不 commit**,主会话验收后提交。

## 7. 与任务 B 的边界

本任务全权 03(含顶栏);任务 B 只改 01/02。两者共用 `docs/全站设计规范_0724.md` 保证三页一致,文件不重叠,可独立验收。
